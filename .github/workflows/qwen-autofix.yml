name: 'Qwen Autofix'

# One workflow for the whole autonomous-fix lifecycle:
#
#   issue → locate → fix → open PR         (issue phase)
#   open PR → review → triage → fix → push (review phase)
#
# The long-form design record — trigger routing, the trust model, and the
# reasoning behind every threshold — lives next to this file in
# qwen-autofix.md, reachable from the `qwen-autofix.md#af-NNN` pointer left
# wherever a block used to sit. Full rationale for the lifecycle itself →
# qwen-autofix.md#af-001.
#
# SIZE IS A HARD CONSTRAINT, NOT A STYLE PREFERENCE. GitHub refuses to start
# runs for a workflow file over 500 KB (512,000 bytes), and it fails SILENTLY:
# schedule ticks stop firing, dispatches sit 'queued' forever with zero jobs,
# and only PR-event runs keep working — those resolve the workflow from the
# PR's own branch, so an older, smaller copy of this file runs and the loop
# looks half-alive. That is precisely what happened on 2026-08-19: this file
# crossed the limit at 512,782 bytes and the loop went dark for a day while
# `pull_request_review` runs kept reporting success. Prose belongs in
# qwen-autofix.md and long steps belong in .github/scripts/ — that is what
# keeps this file runnable at all.
on:
  issues:
    types:
      - 'labeled'
      - 'assigned'
  pull_request_review:
    types:
      - 'submitted'
  pull_request:
    types:
      - 'labeled'
      - 'unlabeled'
  issue_comment:
    types:
      - 'created'
  schedule:
    - cron: '*/10 * * * *' # Review first; issue fallback only when no PR needs work
  workflow_dispatch:
    inputs:
      phase:
        description: 'Which phase(s) to run'
        required: false
        default: 'auto'
        type: 'choice'
        options:
          - 'auto' # review always; issue on schedule or ready-for-agent label
          - 'issue' # locate + fix one bug only
          - 'review' # address review on open PRs only
          - 'both' # issue and review
      issue_number:
        description: 'Force a specific issue number (implies the issue phase)'
        required: false
        type: 'string'
      pr_number:
        description: 'Force a specific bot PR number (implies the review phase)'
        required: false
        type: 'string'
      dry_run:
        description: 'Assess/develop/address and verify, but do not claim, push, or comment'
        required: false
        type: 'boolean'
        default: false
      source:
        description: 'Dispatch-origin marker (fork-bridge = the fork-review bridge); routing metadata only — never changes what may be touched, and its one behavioral effect (staying quiet at the round cap) is honored only after the scan verifies a matching fork-bridge run'
        required: false
        type: 'string'

defaults:
  run:
    shell: 'bash'

permissions:
  contents: 'read'

env:
  # Identity of the autofix bot. All open in-repo PRs authored by this bot are
  # eligible for the review phase — not limited to autofix/issue-* branches.
  AUTOFIX_BOT: "${{ vars.AUTOFIX_BOT_LOGIN || 'qwen-code-dev-bot' }}"
  # Branch-name prefix used by the issue phase when creating PRs. Also used
  # for duplicate-PR detection and issue-number extraction from branch names.
  BRANCH_PREFIX: 'autofix/issue-'
  # The automated Qwen PR reviewer posts as this account; its review counts as
  # actionable feedback even though it is not a human collaborator.
  REVIEW_BOT: 'qwen-code-ci-bot'
  # Human reviews/comments only count when the author is a real maintainer. This
  # is the prompt-injection trust gate: feedback from anyone else is ignored so a
  # hostile commenter cannot steer the agent.
  TRUSTED_ASSOC: '["OWNER", "MEMBER", "COLLABORATOR"]'
  # Hard cap on automated review-address rounds per PR. After this the bot stops
  # and leaves the PR for a human. Raised from 5: across the last 40 bot PRs
  # only 3 ever reached the cap and all 3 merged AT it (one having spent two of
  # its five rounds on the verify-gate ENOENT that #7330 fixed), so the ceiling
  # was near enough to bind on a bad day without any headroom for one. The cap
  # exists to stop an unproductive LOOP, not to ration ordinary iteration —
  # a genuinely stuck PR still stops, just later.
  MAX_ROUNDS: '10'
  # Suggestions may improve a PR, but continuing to implement them after five
  # change-producing rounds expands the diff and creates fresh review churn.
  # Full rationale → qwen-autofix.md#af-002
  CRITICAL_ONLY_AFTER_ROUND: '5'
  # Per-author tail budget inside Critical-only mode.
  # Full rationale → qwen-autofix.md#af-074
  CRITICAL_ONLY_HUMAN_BATCHES: '2'
  # Net-diff growth budgets per counting window — the SIZE sibling of the
  # round brake above. CRITICAL_ONLY_AFTER_ROUND counts rounds, but one round
  # Full rationale → qwen-autofix.md#af-003
  GROWTH_BUDGET_SRC_LINES: '${{ vars.QWEN_AUTOFIX_GROWTH_BUDGET_SRC_LINES || 400 }}'
  GROWTH_BUDGET_TEST_LINES: '${{ vars.QWEN_AUTOFIX_GROWTH_BUDGET_TEST_LINES || 400 }}'
  # Growth audit: a budget breach engages Critical-only AND makes the round a
  # growth-audit round.
  # Full rationale → qwen-autofix.md#af-075
  API_AUTH_MAX_ROUNDS: '3'
  # Checks the "wait for checks to settle" gate does NOT wait for. The gate
  # exists so a FAILED check can be read as feedback, which is why build/test/
  # lint are still waited for. `review-pr` is the LLM code review: its output
  # is a REVIEW, delivered by its own real-time pull_request_review trigger and
  # counted by the review path — the check conclusion carries nothing the loop
  # acts on. Blocking on it bought nothing and cost a median 49 minutes per
  # round (p90 123, max 158, over 32 completed runs), during which the PR was
  # invisible to the scan even when it already had unaddressed feedback.
  # Names must match job ids in qwen-code-pr-review.yml; a test pins that.
  NON_BLOCKING_CHECKS: '["review-pr"]'
  # Failed-check annotation patterns that mean the INFRASTRUCTURE died, not the
  # code — a self-hosted runner losing the server, the disk filling, a runner
  # shutdown, or a git fetch/clone dying mid-transfer.
  # Full rationale → qwen-autofix.md#af-076
  INFRA_FAILURE_SIGNATURES: 'lost communication with the server|No space left on device|ENOSPC|received a shutdown signal|The runner has received|Failed to initialize container|runner (was|has been) (lost|terminated)|invalid index-pack output|RPC failed'
  # Upper bound on review targets emitted per scan (fan-out defense-in-depth;
  # excess is logged and deferred to the next scan).
  # Full rationale → qwen-autofix.md#af-077
  MAX_TARGETS_PER_SCAN: '${{ vars.QWEN_AUTOFIX_MAX_TARGETS_PER_SCAN || 30 }}'
  # Upper bound on candidates INSPECTED per scan: idle candidates consume
  # serial API calls even when they emit nothing, and takeover widens the
  # candidate pool.
  # Full rationale → qwen-autofix.md#af-078
  MAX_CANDIDATE_INSPECTIONS: '${{ vars.QWEN_AUTOFIX_MAX_CANDIDATE_INSPECTIONS || 60 }}'
  # Maintainer-facing engagement labels (applying labels requires GitHub
  # triage+, so the permission gate is GitHub's own): TAKEOVER opts a PR —
  # including a human-authored one — into the loop; SKIP opts any PR out
  # everywhere, and wins when both are present.
  TAKEOVER_LABEL: 'autofix/takeover'
  SKIP_LABEL: 'autofix/skip'
  # Commit-status context stamped PENDING on a PR head when a scan dispatches
  # a review-address leg for it, and re-stamped SUCCESS by the leg on
  # checkout.
  # Full rationale → qwen-autofix.md#af-079
  DISPATCH_STATUS_CONTEXT: 'qwen-autofix/dispatch-pending'
  DISPATCH_STATUS_TTL_MINUTES: '30'
  # Comment-command sugar over TAKEOVER_LABEL ('<cmd>' applies it, '<cmd>
  # stop' removes it). Matched EXACTLY against the trimmed comment body —
  # with ONE parameterized form, '<cmd> from N', which applies the label and
  # additionally seeds this window's round counter at N (see
  # CRITICAL_ONLY_AFTER_ROUND). The literal prefix still has to match this
  # constant byte-for-byte; only a bounded 1-2 digit integer is read out of
  # the body, and it reaches nothing but an integer comparison.
  TAKEOVER_COMMAND: '@qwen-code /takeover'
  # Escalation label: applied when the loop STOPS on a PR (round cap,
  # consecutive-failure or time-budget breaker) and a human must act —
  # re-arm, split, merge, or close. It makes paused PRs filterable and lets
  # the fleet shepherd age out unanswered stops. Removed wherever management
  # resumes (engage/re-arm) or a human releases the PR.
  NEEDS_HUMAN_LABEL: 'autofix/needs-human'
  # Re-arm sugar. Recovering a stranded PR previously meant DELETING the
  # bot's autofix-eval marker comment by hand (undiscoverable, destructive,
  # and it erases the audit trail). This command instead posts an
  # 'autofix-rearm' marker that supersedes the earlier evaluation markers:
  # the scan re-reads the feedback from scratch and the round counter resets.
  RETRY_COMMAND: '@qwen-code /retry'
  # Round cap while TAKEOVER_LABEL is present. Large managed PRs routinely
  # need dozens of feedback rounds — that is the point of takeover — so the
  # unattended cap (MAX_ROUNDS) would strangle it. The circuit breaker stays
  # (a bot↔review-bot ping-pong is still bounded), it is just sized for
  # explicitly delegated work; removing the label restores the strict cap,
  # and re-engaging opens a fresh counting window (see REARM_KEY below).
  TAKEOVER_MAX_ROUNDS: '100'
  # Consecutive-failure sub-cap, distinct from the total round cap above. The
  # total cap bounds how many PRODUCTIVE rounds a PR may take; this bounds how
  # many rounds may fail IN A ROW with nothing pushed.
  # Full rationale → qwen-autofix.md#af-080
  CONSECUTIVE_FAILURE_CAP: '5'
  # Cumulative agent-timeout sub-cap, the sibling of the consecutive cap for
  # the failure shape it cannot see: timeouts INTERLEAVED with successful
  # rounds.
  # Full rationale → qwen-autofix.md#af-081
  TIMEOUT_WINDOW_CAP: '3'
  # Convergence-signal circuit breaker — the off-ramp the round and growth
  # brakes cannot provide (#10107): N consecutive review rounds carrying a
  # machine-readable non-convergence code, with no trusted-human response
  # in between, pause the loop. Tunable without a code change; a malformed
  # value falls back to its default at the read site.
  # Full rationale → qwen-autofix.md#af-151
  CONVERGENCE_BREAK_ROUNDS: '${{ vars.QWEN_AUTOFIX_CONVERGENCE_BREAK_ROUNDS || 3 }}'
  # Which codes count as the signal (jq-ready JSON array). The vocabulary
  # is the review CLI's RECOMMENDATION_CODES, published per round as `rec`
  # in the posted ledger marker; a test pins this list inside it.
  # 'land-and-defer' is deliberately absent — see af-151.
  CONVERGENCE_SIGNAL_CODES: '["root-cause-triage","successor-chain","batch-fixes","stem-surface"]'
  # Do not claim more issues when too many existing autofix PRs are still open.
  MAX_OPEN_AUTOFIX_PRS: '5'

jobs:
  # ---------------------------------------------------------------------------
  # Router: fork the run into phases by schedule/dispatch input.
  # ---------------------------------------------------------------------------
  route:
    # The issue_comment clause is a cheap expression-level prefilter: the
    # overwhelming majority of comments never start a job at all. The real
    # gates (exact body match, sender authorization) live in 'Decide phases'.
    # Full rationale → qwen-autofix.md#af-004
    if: |-
      ${{ github.repository == 'QwenLM/qwen-code' && (github.event_name != 'issue_comment' || (github.event.issue.pull_request && (startsWith(github.event.comment.body, '@qwen-code /takeover') || startsWith(github.event.comment.body, '@qwen-code /retry')))) && (github.event_name != 'pull_request' || github.event.label.name == 'autofix/takeover') && (github.event_name != 'pull_request_review' || github.event.pull_request.state == 'open') }}
    # Persistent pool, not hosted: a hosted backlog queued route past the cron
    # period, and af-005's supersede then starved every scan round.
    # Full rationale → qwen-autofix.md#af-148
    runs-on: '${{ (github.repository == ''QwenLM/qwen-code'' && vars.MAINTAINER_ECS_RUNNER_DISABLED != ''true'' && (github.event_name != ''pull_request'' && github.event_name != ''pull_request_review'' || github.event.pull_request.head.repo.full_name == github.repository || contains(fromJSON(''["OWNER","MEMBER","COLLABORATOR"]''), github.event.pull_request.author_association))) && fromJSON(''["self-hosted", "linux", "x64", "ecs-qwen"]'') || fromJSON(''["ubuntu-latest"]'') }}'
    timeout-minutes: 5
    concurrency:
      # Concurrency is keyed by TARGET, not shared and not fully unique:
      # Full rationale → qwen-autofix.md#af-005
      group: >-
        ${{ github.event_name == 'schedule' && 'qwen-autofix-route-cron' || (github.event_name == 'pull_request_review' && (contains(fromJSON('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.review.author_association) || github.event.review.user.login == 'qwen-code-ci-bot') && format('qwen-autofix-route-pr-{0}', github.event.pull_request.number)) || (github.event_name == 'pull_request' && github.event.label.name == 'autofix/takeover' && format('qwen-autofix-route-label-{0}', github.event.pull_request.number)) || (github.event_name == 'issue_comment' && contains(fromJSON('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.comment.author_association) && format('qwen-autofix-route-cmd-{0}', github.event.issue.number)) || (github.event_name == 'issues' && format('qwen-autofix-route-issue-{0}', github.event.issue.number)) || format('qwen-autofix-route-{0}', github.run_id) }}
      cancel-in-progress: |-
        ${{ github.event_name != 'workflow_dispatch' }}
    permissions:
      contents: 'read'
    outputs:
      do_issue: '${{ steps.decide.outputs.do_issue }}'
      do_review: '${{ steps.decide.outputs.do_review }}'
      dry_run: '${{ steps.decide.outputs.dry_run }}'
      issue_number: '${{ steps.decide.outputs.issue_number }}'
      pr_number: '${{ steps.decide.outputs.pr_number }}'
      takeover_ack: '${{ steps.decide.outputs.takeover_ack }}'
      ack_pr: '${{ steps.decide.outputs.ack_pr }}'
      ack_base: '${{ steps.decide.outputs.ack_base }}'
      takeover_cmd: '${{ steps.decide.outputs.takeover_cmd }}'
      takeover_from: '${{ steps.decide.outputs.takeover_from }}'
      retry_pr: '${{ steps.decide.outputs.retry_pr }}'
      cmd_pr: '${{ steps.decide.outputs.cmd_pr }}'
      review_sender: '${{ github.event.review.user.login }}'
    steps:
      - name: 'Decide phases'
        id: 'decide'
        env:
          PHASE: '${{ inputs.phase }}'
          FORCED_ISSUE: '${{ inputs.issue_number }}'
          FORCED_PR: '${{ inputs.pr_number }}'
          DRY_RUN_INPUT: '${{ inputs.dry_run }}'
          EVENT_NAME: '${{ github.event_name }}'
          GITHUB_TOKEN: '${{ github.token }}'
          BUG_LABEL: 'type/bug'
          ISSUE_LABEL: '${{ github.event.label.name }}'
          ISSUE_LABELS_JSON: '${{ toJSON(github.event.issue.labels.*.name) }}'
          ISSUE_NUMBER: '${{ github.event.issue.number }}'
          ISSUE_STATE: '${{ github.event.issue.state }}'
          READY_FOR_AGENT_LABEL: 'status/ready-for-agent'
          AUTOFIX_APPROVED_LABEL: 'autofix/approved'
          REPO: '${{ github.repository }}'
          SENDER_LOGIN: '${{ github.event.sender.login }}'
          ASSIGNEE_LOGIN: '${{ github.event.assignee.login }}'
          SCHEDULE: '${{ github.event.schedule }}'
          PR_AUTHOR: '${{ github.event.pull_request.user.login }}'
          PR_NUMBER_EVENT: '${{ github.event.pull_request.number }}'
          PR_HEAD_REPO: '${{ github.event.pull_request.head.repo.full_name }}'
          PR_BASE_REF: '${{ github.event.pull_request.base.ref }}'
          PR_STATE: '${{ github.event.pull_request.state }}'
          EVENT_ACTION: '${{ github.event.action }}'
          COMMENT_BODY: '${{ github.event.comment.body }}'
          COMMENT_PR_AUTHOR: '${{ github.event.issue.user.login }}'
          HAS_PR_URL: '${{ github.event.issue.pull_request.url }}'
        run: |-
          # gh has its own reroute channels: pin the host, drop planted tokens,
          # and point gh at a fresh empty config dir — on the persistent pool
          # the shared ~/.config/gh is attacker-writable (config.yml can carry
          # http_unix_socket transport reroutes), and a forged collaborator-
          # permission response here would open the /takeover and /retry gates.
          # Mirrors the heavy jobs' preamble. Full rationale → qwen-autofix.md#af-148
          export GH_HOST=github.com
          unset GH_ENTERPRISE_TOKEN GH_TOKEN
          # `export VAR="$(...)"` reports export's status, not the
          # substitution's: a failing mktemp must abort the step, not continue
          # with an empty GH_CONFIG_DIR that gh treats as unset — falling back
          # to the shared ~/.config/gh this preamble closes.
          if ! GH_CONFIG_DIR="$(mktemp -d "${RUNNER_TEMP}/autofix-gh-config.XXXXXX")"; then
            echo "::error::could not create gh config dir; refusing to run gh without reroute hardening"
            exit 1
          fi
          export GH_CONFIG_DIR
          DO_ISSUE=false
          DO_REVIEW=false
          TAKEOVER_ACK=''
          ACK_BASE=''
          TAKEOVER_CMD=''
          # Round-counter seed carried by '<cmd> from N' (see the command
          # parser below). Empty on every other path, which reads as "start
          # this window at round 0" — the pre-existing behaviour.
          TAKEOVER_FROM=''
          CMD_PR=''
          RETRY_PR=''
          DRY_RUN="${DRY_RUN_INPUT:-false}"
          sanitize_number() {
            local value="${1//$'\r'/}"
            value="${value//$'\n'/}"
            if [[ "${value}" =~ ^[0-9]+$ ]]; then
              printf '%s' "${value}"
            elif [[ -n "${value}" ]]; then
              echo "::warning::Rejected non-numeric routing input: '${value}'" >&2
            fi
          }
          # workflow_dispatch inputs are user-controlled; keep GITHUB_OUTPUT
          # routing values single-line numeric before later jobs consume them.
          ROUTE_ISSUE="$(sanitize_number "${FORCED_ISSUE}")"
          ROUTE_PR="$(sanitize_number "${FORCED_PR}")"
          case "${PHASE}" in
            issue) DO_ISSUE=true ;;
            review) DO_REVIEW=true ;;
            both) DO_ISSUE=true; DO_REVIEW=true ;;
            *)
              # auto only runs review from scheduled/manual events. Label events
              # route below after their trust gates pass.
              if [[ "${EVENT_NAME}" == 'schedule' || "${EVENT_NAME}" == 'workflow_dispatch' ]]; then
                DO_REVIEW=true
              fi
              # Scheduled runs scan review PRs first; issue-autofix runs only
              # when review-scan reports no target.
              if [[ "${EVENT_NAME}" == 'schedule' ]]; then
                DO_ISSUE=true
              fi
              # Real-time review triggers: process the SAME managed set the
              # scheduled scan does, so feedback is picked up seconds after the
              # review instead of waiting for a schedule GitHub throttles hard
              # (the */10 cron actually lands every 40-70min on this repo).
              # Full rationale → qwen-autofix.md#af-082
              if [[ "${EVENT_NAME}" == 'pull_request_review' ]]; then
                DO_ISSUE=false
                pr_is_managed=false
                if [[ "${PR_BASE_REF}" != "main" ]]; then
                  echo "🧭 review event ignored: PR targets '${PR_BASE_REF}' not 'main'"
                elif [[ "${PR_HEAD_REPO}" == "${REPO}" ]]; then
                  if [[ "${PR_AUTHOR}" == "${AUTOFIX_BOT}" ]]; then
                    pr_is_managed=true
                  else
                    echo "🧭 review event ignored: PR author '${PR_AUTHOR}' is not ${AUTOFIX_BOT}"
                  fi
                else
                  # Fork PR — decline, and say so. This event carries NO
                  # repository secrets: GitHub withholds them from every run
                  # Full rationale → qwen-autofix.md#af-006
                  echo "🧭 fork review noted for #${PR_NUMBER_EVENT} — this event carries no repository secrets, so nothing here could authenticate; the next scheduled scan engages (author write+ and allow-edits required)"
                fi
                if [[ "${pr_is_managed}" == 'true' ]]; then
                  # Verify the reviewer/commenter is trusted (prompt-injection gate).
                  sender_permission=''
                  sender_is_trusted=false
                  if [[ "${SENDER_LOGIN}" == "${REVIEW_BOT}" ]]; then
                    sender_is_trusted=true
                  elif [[ -n "${SENDER_LOGIN}" ]]; then
                    api_error_file="$(mktemp)"
                    if sender_permission="$(gh api "repos/${REPO}/collaborators/${SENDER_LOGIN}/permission" --jq '.permission // ""' 2>"${api_error_file}")"; then
                      case "${sender_permission}" in
                        admin|maintain|write) sender_is_trusted=true ;;
                      esac
                    else
                      api_error="$(tr '\r\n' ' ' < "${api_error_file}")"
                      echo "::warning::Permission API call failed for ${SENDER_LOGIN}: ${api_error:-unknown error}"
                      sender_permission=''
                    fi
                    rm -f "${api_error_file}"
                  fi
                  if [[ "${sender_is_trusted}" == "true" ]]; then
                    DO_REVIEW=true
                    ROUTE_PR="$(sanitize_number "${PR_NUMBER_EVENT}")"
                    echo "🧭 review event on bot PR #${PR_NUMBER_EVENT} by ${SENDER_LOGIN} (${sender_permission:-review-bot}) → review phase"
                  else
                    echo "🧭 review event ignored: sender '${SENDER_LOGIN}' permission='${sender_permission:-none}' is not trusted"
                  fi
                fi
              fi
              # Comment-command sugar over the labels: TAKEOVER_COMMAND
              # applies TAKEOVER_LABEL, 'TAKEOVER_COMMAND stop' removes it —
              # nothing else.
              # Full rationale → qwen-autofix.md#af-083
              if [[ "${EVENT_NAME}" == 'issue_comment' ]]; then
                DO_ISSUE=false
                DO_REVIEW=false
                BODY_TRIMMED="$(printf '%s' "${COMMENT_BODY}" | tr -d '\r' | sed -e 's/^[[:space:]]*//' -e 's/[[:space:]]*$//')"
                CMD=''
                [[ "${BODY_TRIMMED}" == "${TAKEOVER_COMMAND}" ]] && CMD='add'
                [[ "${BODY_TRIMMED}" == "${TAKEOVER_COMMAND} stop" ]] && CMD='remove'
                # '<cmd> from N' — the ONE parameterized form, and the only
                # place this workflow reads a value out of a comment body.
                # Full rationale → qwen-autofix.md#af-007
                # Operator guide → qwen-autofix-round-seed.md
                if [[ "${BODY_TRIMMED}" =~ ^(.*)\ from\ ([0-9]{1,2})$ ]]; then
                  # Two statements, not one `[[ ... && ... ]]`: BASH_REMATCH is
                  # only guaranteed populated once the =~ test has completed,
                  # and reading it from the right-hand operand of the same
                  # conditional relies on evaluation order this must not bet on.
                  if [[ "${BASH_REMATCH[1]}" == "${TAKEOVER_COMMAND}" ]]; then
                    CMD='add'
                    # 10# canonicalizes the capture to decimal: the value ends
                    # up in bare-context bash arithmetic downstream (the
                    # toggle's remainder, the read-site clamps' -ge), where a
                    # zero-padded spelling is octal — '08'/'09' even error
                    # outright and drop the seed note. '00' lands on '0', the
                    # explicit no-seed spelling.
                    TAKEOVER_FROM="$((10#${BASH_REMATCH[2]}))"
                  fi
                fi
                # Re-arm shares the takeover command's authorization exactly:
                # both summon bot activity on a managed PR, so inventing a
                # second policy would only add surface.
                RETRY_REQ=''
                [[ "${BODY_TRIMMED}" == "${RETRY_COMMAND}" ]] && RETRY_REQ='true'
                if [[ -z "${HAS_PR_URL}" ]]; then
                  echo "🧭 command ignored: not a PR comment"
                elif [[ -z "${CMD}" && -z "${RETRY_REQ}" ]]; then
                  echo "🧭 command ignored: body is not an exact command"
                elif [[ "${ISSUE_STATE}" != 'open' ]]; then
                  echo "🧭 command ignored: PR is not open"
                elif [[ -z "${SENDER_LOGIN}" || "${SENDER_LOGIN}" == "${AUTOFIX_BOT}" ]]; then
                  echo "🧭 command ignored: sender '${SENDER_LOGIN:-n/a}'"
                else
                  sender_is_authorized=false
                  sender_permission=''
                  # Author privilege applies to IN-REPO PRs only: on a fork
                  # PR the author is an arbitrary external account, and
                  # accepting them here would let them drive PAT-authored
                  # writes (even just refusal comments) onto their own PR at
                  # will. Fork authors without write+ are dropped silently.
                  CMD_HEAD_REPO="$(gh api "repos/${REPO}/pulls/${ISSUE_NUMBER}" --jq '.head.repo.full_name // ""' 2> /dev/null || echo '')"
                  if [[ "${SENDER_LOGIN}" == "${COMMENT_PR_AUTHOR}" && "${CMD_HEAD_REPO}" == "${REPO}" ]]; then
                    # Author privilege is LIVE, not durable: an author removed
                    # from the repo keeps their PR/head-repo match forever, so
                    # authorship alone must not keep summoning secret-bearing
                    # runs. Authors qualify at triage+ (the sugar exists for
                    # members below write who cannot apply labels).
                    api_error_file="$(mktemp)"
                    if sender_permission="$(gh api "repos/${REPO}/collaborators/${SENDER_LOGIN}/permission" --jq '.permission // ""' 2>"${api_error_file}")"; then
                      case "${sender_permission}" in
                        admin|maintain|write|triage) sender_is_authorized=true; sender_permission="pr-author/${sender_permission}" ;;
                      esac
                    else
                      api_error="$(tr '\r\n' ' ' < "${api_error_file}")"
                      echo "::warning::Permission API call failed for author ${SENDER_LOGIN}: ${api_error:-unknown error}"
                      sender_permission=''
                    fi
                    rm -f "${api_error_file}"
                  else
                    api_error_file="$(mktemp)"
                    if sender_permission="$(gh api "repos/${REPO}/collaborators/${SENDER_LOGIN}/permission" --jq '.permission // ""' 2>"${api_error_file}")"; then
                      case "${sender_permission}" in
                        admin|maintain|write) sender_is_authorized=true ;;
                      esac
                    else
                      api_error="$(tr '\r\n' ' ' < "${api_error_file}")"
                      echo "::warning::Permission API call failed for ${SENDER_LOGIN}: ${api_error:-unknown error}"
                      sender_permission=''
                    fi
                    rm -f "${api_error_file}"
                  fi
                  if [[ "${sender_is_authorized}" == 'true' ]]; then
                    if [[ -n "${RETRY_REQ}" ]]; then
                      RETRY_PR="$(sanitize_number "${ISSUE_NUMBER}")"
                      echo "🧭 retry command accepted: re-arm PR #${ISSUE_NUMBER} by ${SENDER_LOGIN} (${sender_permission})"
                    else
                      TAKEOVER_CMD="${CMD}"
                      CMD_PR="$(sanitize_number "${ISSUE_NUMBER}")"
                      echo "🧭 takeover command accepted: ${CMD} ${TAKEOVER_LABEL} on PR #${ISSUE_NUMBER} by ${SENDER_LOGIN} (${sender_permission})"
                    fi
                  else
                    echo "🧭 command ignored: sender '${SENDER_LOGIN}' permission='${sender_permission:-none}' is not the PR author or write+"
                  fi
                fi
              fi
              # Label-driven takeover: TAKEOVER_LABEL applied to an eligible
              # PR summons the loop onto it (human-authored included);
              # removing it releases the PR — future engagement stops, and an
              # in-flight round, if any, completes its bounded work (matrix
              # runs are shared across PRs, so cancelling one is not possible
              # without collateral damage). ISSUE_LABEL carries the label
              # name for pull_request events too (same payload field).
              if [[ "${EVENT_NAME}" == 'pull_request' ]]; then
                DO_ISSUE=false
                if [[ "${ISSUE_LABEL}" != "${TAKEOVER_LABEL}" ]]; then
                  echo "🧭 pull_request ${EVENT_ACTION} ignored: label '${ISSUE_LABEL:-n/a}' is not ${TAKEOVER_LABEL}"
                elif [[ "${EVENT_ACTION}" == 'labeled' ]]; then
                  if [[ "${PR_HEAD_REPO}" != "${REPO}" ]]; then
                    # Fork pull_request events carry NO secrets, so neither
                    # the immediate scan nor the ack job can run from this
                    # event. The label still counts: the next scheduled scan
                    # (repo context, ≤10m) admits fork takeover PRs whose
                    # author holds write+ and whose PR allows maintainer
                    # edits, and posts the engage ack on first pickup.
                    echo "🧭 fork takeover noted for #${PR_NUMBER_EVENT} — the next scheduled scan engages (author write+ and allow-edits required)"
                  elif [[ "${PR_STATE}" != 'open' ]]; then
                    echo "🧭 takeover ignored: PR state '${PR_STATE:-unknown}' is not open"
                  elif [[ "${PR_BASE_REF}" != 'main' ]]; then
                    # Refuse OUT LOUD. Staying silent here made a labelled
                    # stacked PR indistinguishable from a managed one: the
                    # label stuck, the route run went green, and the only
                    # trace was this log line — so the PR sat unmanaged for
                    # hours with nobody able to tell without reading the job
                    # log. The ack job posts the explanation instead.
                    TAKEOVER_ACK='base-refused'
                    ACK_BASE="${PR_BASE_REF}"
                    echo "🧭 takeover ignored: PR targets '${PR_BASE_REF}' not 'main'"
                  else
                    DO_REVIEW=true
                    ROUTE_PR="$(sanitize_number "${PR_NUMBER_EVENT}")"
                    if [[ "${SENDER_LOGIN}" == "${AUTOFIX_BOT}" ]]; then
                      # The bot only applies this label from takeover-command,
                      # which posts the engage ack ITSELF: the labeled event
                      # has been observed to simply not fire (#7999, #8002).
                      # Full rationale → qwen-autofix.md#af-084
                      echo "🧭 engage ack skipped: label applied by ${AUTOFIX_BOT} — the command path already acked"
                    else
                      TAKEOVER_ACK='engaged'
                    fi
                    echo "🧭 ${TAKEOVER_LABEL} applied by ${SENDER_LOGIN} on PR #${PR_NUMBER_EVENT} → review phase (takeover)"
                  fi
                elif [[ "${EVENT_ACTION}" == 'unlabeled' ]]; then
                  # Mirror the labeled-path guards: a fork, closed, or
                  # non-main PR was never engaged, so a release ack would
                  # announce a disengagement that never existed.
                  if [[ "${PR_STATE}" != 'open' || "${PR_BASE_REF}" != 'main' ]]; then
                    echo "🧭 takeover release ignored: PR state '${PR_STATE:-unknown}' base '${PR_BASE_REF:-unknown}' was never engaged"
                  elif [[ "${PR_HEAD_REPO}" != "${REPO}" ]]; then
                    # Fork pull_request events carry no secrets: emitting the
                    # ack here would start takeover-ack with an empty PAT and
                    # fail its identity check — a red run for a label that
                    # never engaged anything. Log and stop.
                    echo "🧭 takeover release ignored: PR is a fork (${PR_HEAD_REPO} != ${REPO})"
                  elif [[ "${SENDER_LOGIN}" == "${AUTOFIX_BOT}" ]]; then
                    # Mirror of the labeled-path suppression: the bot only
                    # removes this label from takeover-command, which posts
                    # the release ack itself — acking here too would
                    # double-post on every command-driven stop.
                    echo "🧭 release ack skipped: label removed by ${AUTOFIX_BOT} — the command path already acked"
                  else
                    TAKEOVER_ACK='released'
                    echo "🧭 ${TAKEOVER_LABEL} removed from PR #${PR_NUMBER_EVENT} by ${SENDER_LOGIN} → released"
                  fi
                fi
              fi
              if [[ "${EVENT_NAME}" == 'issues' ]]; then
                DO_REVIEW=false
                label_is_trigger=false
                [[ "${ISSUE_LABEL}" == "${READY_FOR_AGENT_LABEL}" || "${ISSUE_LABEL}" == "${BUG_LABEL}" || "${ISSUE_LABEL}" == "${AUTOFIX_APPROVED_LABEL}" ]] && label_is_trigger=true
                [[ "${ASSIGNEE_LOGIN}" == "${AUTOFIX_BOT}" ]] && label_is_trigger=true
                sender_permission=''
                sender_is_trusted=false
                if [[ -n "${SENDER_LOGIN}" ]]; then
                  if ! sender_permission="$(gh api "repos/${REPO}/collaborators/${SENDER_LOGIN}/permission" --jq '.permission // ""' 2>&1)"; then
                    api_error="${sender_permission}"
                    sender_permission=''
                    api_error="${api_error//$'\r'/ }"
                    api_error="${api_error//$'\n'/ }"
                    echo "::warning::Permission API call failed for ${SENDER_LOGIN}: ${api_error}"
                  fi
                  [[ "${sender_permission}" == 'write' || "${sender_permission}" == 'maintain' || "${sender_permission}" == 'admin' ]] && sender_is_trusted=true
                fi
                if [[ "${label_is_trigger}" != 'true' ]]; then
                  # A non-trigger label (e.g. scope/*, priority/*) may arrive
                  # after the trigger labels and cancel their runs via per-issue
                  # concurrency. If the issue already carries both required
                  # labels and the current sender is trusted, proceed anyway.
                  _late_ready="$(jq -r --arg l "${READY_FOR_AGENT_LABEL}" 'index($l) != null' <<< "${ISSUE_LABELS_JSON:-[]}")"
                  _late_approved="$(jq -r --arg l "${AUTOFIX_APPROVED_LABEL}" 'index($l) != null' <<< "${ISSUE_LABELS_JSON:-[]}")"
                  if [[ "${ISSUE_STATE}" == 'open' && "${_late_ready}" == 'true' && "${_late_approved}" == 'true' && "${sender_is_trusted}" == 'true' ]]; then
                    echo "🧭 non-trigger label '${ISSUE_LABEL:-n/a}' but issue #${ISSUE_NUMBER} already approved+ready → issue phase"
                    DO_ISSUE=true
                  else
                    echo "🧭 issue event ignored: trigger_label=false label='${ISSUE_LABEL:-n/a}' issue='#${ISSUE_NUMBER:-n/a}'"
                  fi
                else
                  issue_is_bug="$(jq -r --arg label "${BUG_LABEL}" 'index($label) != null' <<< "${ISSUE_LABELS_JSON:-[]}")"
                  issue_is_ready="$(jq -r --arg label "${READY_FOR_AGENT_LABEL}" 'index($label) != null' <<< "${ISSUE_LABELS_JSON:-[]}")"
                  issue_is_approved="$(jq -r --arg label "${AUTOFIX_APPROVED_LABEL}" 'index($label) != null' <<< "${ISSUE_LABELS_JSON:-[]}")"
                  if [[ "${ISSUE_STATE}" == 'open' && "${issue_is_ready}" == 'true' && "${issue_is_approved}" == 'true' && "${label_is_trigger}" == 'true' && "${sender_is_trusted}" == 'true' ]]; then
                    DO_ISSUE=true
                  else
                    if [[ "${ISSUE_STATE}" == 'open' && "${label_is_trigger}" == 'true' && "${sender_is_trusted}" == 'true' && "${issue_is_ready}" != "${issue_is_approved}" ]]; then
                      echo "::notice::Issue #${ISSUE_NUMBER:-n/a} needs both ${READY_FOR_AGENT_LABEL} and ${AUTOFIX_APPROVED_LABEL} before autofix can run."
                    fi
                    echo "🧭 issue event ignored: state_open=$([[ "${ISSUE_STATE}" == 'open' ]] && echo true || echo false) bug=${issue_is_bug} ready=${issue_is_ready} approved=${issue_is_approved} trigger_label=${label_is_trigger} sender_permission='${sender_permission:-none}' sender_trusted=${sender_is_trusted} label='${ISSUE_LABEL:-n/a}' issue='#${ISSUE_NUMBER:-n/a}'"
                  fi
                fi
              fi
              ;;
          esac
          # Forcing a specific issue/PR implies running that phase only for
          # explicit manual dispatch. Event payload numbers still flow to the
          # phase jobs after routing, but must not bypass the label/schedule gates.
          # Explicit phases (issue/review/both) take precedence over forced
          # issue/PR overrides — only apply forced routing in auto/default mode.
          if [[ "${EVENT_NAME}" == 'workflow_dispatch' && ( -z "${PHASE}" || "${PHASE}" == 'auto' ) ]]; then
            [[ -n "${ROUTE_ISSUE}" && -z "${ROUTE_PR}" ]] && DO_ISSUE=true && DO_REVIEW=false
            [[ -n "${ROUTE_PR}" && -z "${ROUTE_ISSUE}" ]] && DO_ISSUE=false && DO_REVIEW=true
            [[ -n "${ROUTE_ISSUE}" && -n "${ROUTE_PR}" ]] && DO_ISSUE=true && DO_REVIEW=true
          fi
          echo "do_issue=${DO_ISSUE}" >> "${GITHUB_OUTPUT}"
          echo "do_review=${DO_REVIEW}" >> "${GITHUB_OUTPUT}"
          echo "dry_run=${DRY_RUN}" >> "${GITHUB_OUTPUT}"
          echo "issue_number=${ROUTE_ISSUE}" >> "${GITHUB_OUTPUT}"
          echo "pr_number=${ROUTE_PR}" >> "${GITHUB_OUTPUT}"
          echo "takeover_ack=${TAKEOVER_ACK}" >> "${GITHUB_OUTPUT}"
          echo "ack_pr=$(sanitize_number "${PR_NUMBER_EVENT}")" >> "${GITHUB_OUTPUT}"
          echo "ack_base=${ACK_BASE}" >> "${GITHUB_OUTPUT}"
          echo "takeover_cmd=${TAKEOVER_CMD}" >> "${GITHUB_OUTPUT}"
          # Re-validated on the way out, not just on the way in: this value
          # crosses a job boundary into a printf that writes a PR comment, and
          # the only shape the marker reader accepts is a bare 1-2 digit
          # integer. sanitize_number rejects (and warns about) anything else.
          echo "takeover_from=$(sanitize_number "${TAKEOVER_FROM}")" >> "${GITHUB_OUTPUT}"
          echo "retry_pr=${RETRY_PR}" >> "${GITHUB_OUTPUT}"
          echo "cmd_pr=${CMD_PR}" >> "${GITHUB_OUTPUT}"
          echo "🧭 phase='${PHASE:-auto}' event='${EVENT_NAME}' issue='#${ISSUE_NUMBER:-n/a}' pr='#${PR_NUMBER_EVENT:-n/a}' schedule='${SCHEDULE:-n/a}' dry_run=${DRY_RUN} → issue=${DO_ISSUE} review=${DO_REVIEW}"

  # ===========================================================================
  # ISSUE PHASE — locate one maintainer-ready issue, fix it, open a PR.
  # ===========================================================================
  issue-autofix:
    needs: ['route', 'review-scan']
    if: |-
      ${{
        always() &&
        needs.route.outputs.do_issue == 'true' &&
        (github.event_name != 'schedule' || (needs.review-scan.result == 'success' && needs.review-scan.outputs.has_targets != 'true' && needs.review-scan.outputs.enum_failed != 'true'))
      }}
    # Secret-bearing and executes agent-driven code, but the agent runs inside
    # the docker sandbox image and only ever writes a new branch as the
    # Full rationale → qwen-autofix.md#af-008
    runs-on: '${{ (github.repository == ''QwenLM/qwen-code'' && vars.MAINTAINER_ECS_RUNNER_DISABLED != ''true'' && (github.event_name != ''pull_request'' && github.event_name != ''pull_request_review'' || github.event.pull_request.head.repo.full_name == github.repository || contains(fromJSON(''["OWNER","MEMBER","COLLABORATOR"]''), github.event.pull_request.author_association))) && fromJSON(''["self-hosted", "linux", "x64", "ecs-agent"]'') || fromJSON(''["ubuntu-latest"]'') }}'
    timeout-minutes: 180
    # route.issue_number is only set for forced dispatches; label events carry
    # the issue in the payload, and scan-and-pick runs (cron, unforced
    # Full rationale → qwen-autofix.md#af-009
    concurrency:
      group: >-
        ${{ needs.route.outputs.do_issue == 'true' && needs.route.outputs.dry_run != 'true' && (github.event_name != 'schedule' || (needs.review-scan.result == 'success' && needs.review-scan.outputs.has_targets != 'true' && needs.review-scan.outputs.enum_failed != 'true')) && format('qwen-autofix-issue-{0}', needs.route.outputs.issue_number || github.event.issue.number || 'scheduled') || format('qwen-autofix-issue-run-{0}', github.run_id) }}
      cancel-in-progress: false
    permissions:
      contents: 'read'
    env:
      REPO: '${{ github.repository }}'
      # Per-run private dir: this pool carries many registrations sharing one
      # OS /tmp, and issue-phase runs never serialize against each other, so
      # a fixed path let concurrent runs clobber each other's decision files.
      WORKDIR: '/tmp/autofix-${{ github.run_id }}'
      EVENT_NAME: '${{ github.event_name }}'
      READY_FOR_AGENT_LABEL: 'status/ready-for-agent'
      AUTOFIX_APPROVED_LABEL: 'autofix/approved'
      AUTOFIX_ISSUE_EXCLUDES: 'no:assignee -linked:pr -label:autofix/skip -label:autofix/in-progress -label:status/need-information -label:status/need-retesting sort:created-desc'
    steps:
      # Self-hosted runners reuse the workspace; a prior containerised job
      # can leave root-owned, read-only files anywhere in it. Restore
      # ownership and write permission unconditionally before checkout.
      - name: 'Restore workspace ownership'
        run: |-
          set -uo pipefail
          RUNNER_UID="$(id -u)"
          RUNNER_GID="$(id -g)"
          if [ "$RUNNER_UID" != "0" ]; then
            chown -R "$RUNNER_UID:$RUNNER_GID" "$GITHUB_WORKSPACE" 2>/dev/null || sudo -n chown -R "$RUNNER_UID:$RUNNER_GID" "$GITHUB_WORKSPACE" || echo "::warning::could not restore workspace ownership; checkout may fail on leftover root-owned files"
          fi
          chmod -R u+rwX "$GITHUB_WORKSPACE" 2>/dev/null || sudo -n chmod -R u+rwX "$GITHUB_WORKSPACE" || echo "::warning::could not restore workspace write permissions; checkout may fail on leftover read-only files"

      # Self-hosted runners keep the workspace between runs, and other pool
      # jobs execute human-authored code as the runner user, so a prior job
      # can plant git exec knobs (core.fsmonitor, filter.*.smudge,
      # diff.external, includeIf, hooks) in the local config that would fire
      # inside THIS job's PAT-bearing git steps. It keeps
      # a known-safe allowlist and unsets everything else, hardened against
      # the worktree-config and global-hooksPath bypasses verified in
      # qwen-triage on this pool. No-op on a fresh hosted runner.
      - name: 'Sanitize workspace git config'
        run: |-
          set -uo pipefail
          # The runner USER's global config is the same exec surface as the
          # workspace config below: pool jobs run human-authored code (branch
          # Full rationale → qwen-autofix.md#af-010
          for global_file in "${HOME}/.gitconfig" "${XDG_CONFIG_HOME:-${HOME}/.config}/git/config"; do
            { GIT_CONFIG_GLOBAL="${global_file}" git config --global --name-only --list 2>/dev/null || true; } \
              | { grep -iE '^(core\.(hookspath|fsmonitor|pager|editor|sshcommand|askpass|alternaterefscommand|gitproxy)$|diff\.external$|diff\..+\.(command|textconv)$|merge\..+\.driver$|filter\.|alias\.|pager\.|difftool\.|mergetool\.|interactive\.difffilter$|sequence\.editor$|gpg\.(.+\.)?program$|init\.templatedir$|remote\..+\.(uploadpack|receivepack)$|submodule\..+\.update$|url\..+\.(insteadof|pushinsteadof)$|http\.(.+\.)?(sslverify|sslcainfo)$|include\.|includeif\.|protocol\.(ext\.)?allow$)' || true; } \
              | while IFS= read -r key; do GIT_CONFIG_GLOBAL="${global_file}" git config --global --unset-all "$key" 2>/dev/null || true; done
          done
          # `.git` is a directory in a normal checkout but a gitlink file in
          # a worktree; -e covers both, and a missing .git (first run) too.
          if [ ! -e .git ]; then
            echo "no prior workspace; nothing local to sanitize"
            exit 0
          fi
          # Worktree-scoped config FIRST: `extensions.worktreeConfig=true` is
          # on the allowlist below (it carries no command itself), but it
          # activates `.git/config.worktree` — a second config file that
          # `git config --local` neither lists nor unsets, and that CAN carry
          # core.hooksPath. Verified in qwen-triage: a prior run can set
          # `--worktree core.hooksPath=/`, survive the sweep untouched, and
          # make the hooks deletion below walk /. Delete the file outright,
          # then drop the extension.
          rm -f "$(git rev-parse --git-path config.worktree 2>/dev/null || echo /nonexistent)" 2>/dev/null || true
          git config --local --unset-all extensions.worktreeConfig 2>/dev/null || true
          # Rather than denylist each exec-vector family (which kept missing
          # new ones), KEEP a known-safe allowlist and --unset-all everything
          # else: this closes the whole class, including knobs not yet
          # enumerated.
          # Full rationale → qwen-autofix.md#af-085
          git config --local --name-only --list 2>/dev/null \
            | { grep -ivE '^(core\.(repositoryformatversion|bare|filemode|symlinks|ignorecase|precomposeunicode|logallrefupdates|worktree|hidedotfiles|protecthfs|protectntfs)|remote\..+\.(url|fetch|pushurl)|branch\.|extensions\.|gc\.|pack\.|fetch\.|index\.|safe\.|submodule\..+\.(url|active|branch))' || true; } \
            | while IFS= read -r key; do git config --local --unset-all "$key" 2>/dev/null || true; done
          # Belt and braces after the config scrub: only delete inside the
          # repository's own git dir. A hooks path resolving anywhere else is
          # unlinked, never swept — a recursive delete of a planted path is
          # far worse than a stale hook on a runner the pool re-cleans.
          # Resolve hooks with global/system config OUT of the way. Verified
          # in qwen-triage: with a global core.hooksPath set, `git rev-parse
          # --git-path hooks` returns that path, the guard below sees
          # "outside the git dir", and a planted `.git/hooks` symlink
          # survives untouched. Keep this resolution AFTER the sweep above.
          GIT_DIR_ABS="$(git rev-parse --absolute-git-dir 2>/dev/null || echo '')"
          HOOKS_DIR="$(GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_SYSTEM=/dev/null git rev-parse --git-path hooks 2>/dev/null || echo .git/hooks)"
          HOOKS_ABS="$(cd "$HOOKS_DIR" 2>/dev/null && pwd -P || echo '')"
          if [ -n "$GIT_DIR_ABS" ] && [ -n "$HOOKS_ABS" ] && [ "${HOOKS_ABS#"$GIT_DIR_ABS"/}" != "$HOOKS_ABS" ]; then
            # Match -type f OR -type l: a symlinked hook survives a bare
            # `-type f` sweep and still fires on the next checkout.
            find "$HOOKS_ABS" \( -type f -o -type l \) ! -name '*.sample' -delete 2>/dev/null || true
          else
            # Resolves outside the git dir (or not at all). Warning and
            # walking away would leave a live hook directory that the next
            # git command executes, so unlink the ENTRY without descending
            # into it and put an empty hooks directory back.
            RAW_HOOKS="$(GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_SYSTEM=/dev/null git rev-parse --git-path hooks 2>/dev/null || echo .git/hooks)"
            echo "::warning::hooks path did not resolve inside the git dir (${HOOKS_ABS:-unresolved}); unlinking it."
            rm -f "$RAW_HOOKS" 2>/dev/null || echo "::warning::refusing to recursively delete planted hooks path '$RAW_HOOKS' (a hooksPath resolving to the git dir itself would otherwise wipe .git); leaving it to the pool re-clean."
            mkdir -p "${GIT_DIR_ABS:-.git}/hooks" 2>/dev/null || true
            git config --local --unset-all core.hooksPath 2>/dev/null || true
          fi

      - name: 'Checkout'
        uses: 'actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10' # v6.0.3
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: 'Remove stale sandbox containers'
        run: |-
          # run-agent.mjs's budget kill removes the container it launched,
          # but a JOB timeout still reaps only the HOST-side docker client,
          # Full rationale → qwen-autofix.md#af-011
          command -v docker > /dev/null || exit 0
          STALE="$(timeout 30 docker ps -aq --filter 'name=qwen-code-' --filter 'status=exited' --filter 'status=dead' 2>/dev/null)" || STALE=''
          if [ -n "${STALE}" ]; then
            echo "removing stale sandbox containers:"
            timeout 30 docker ps -a --filter 'name=qwen-code-' --filter 'status=exited' --filter 'status=dead' --format '  {{.Names}} ({{.Status}})' || true
            printf '%s\n' "${STALE}" | xargs -r -I{} timeout 30 docker rm -f {} > /dev/null 2>&1 || true
          fi

      - name: 'Reset autofix workspace'
        run: |-
          rm -rf "${WORKDIR}"
          # 0700: the dir holds agent transcripts and decision files; the
          # sandbox container runs as this same user, so the tighter mode
          # costs the job nothing. umask at creation, not mkdir-then-chmod —
          # the chmod form leaves a world-readable window on this shared /tmp.
          (umask 077; mkdir -p "${WORKDIR}")
          # Age-sweep abandoned run-scoped dirs on this shared /tmp: a hard
          # runner kill skips the always() teardown and run_id never repeats,
          # so nothing else ever reclaims them.
          find /tmp -maxdepth 1 -name 'autofix*' -mmin +1440 -exec rm -rf {} + 2>/dev/null || true
          # The reused workspace's .git accumulates unreferenced objects
          # across fetch runs on this persistent pool; prune them.
          git -c gc.autoDetach=false gc --auto --prune=now --quiet 2>/dev/null || true

      # Self-hosted runners keep the workspace's .git across runs, so a
      # failed earlier attempt's local branch survives here: the agent's
      # branch create then dies "branch already exists", or an adaptation
      # checks out the stale line and pushes the failed attempt's commits
      # into the new PR. Drop them deterministically (refs survive
      # actions/checkout's untracked-file clean).
      - name: 'Drop stale autofix branches'
        run: |-
          # Detach first: `git branch -D` refuses the currently checked-out
          # branch, so a stale autofix branch holding HEAD would otherwise
          # silently survive the sweep (actions/checkout normally leaves
          # HEAD on the default branch; this makes the sweep unconditional).
          git checkout --detach 2>/dev/null || true
          git for-each-ref --format='%(refname:short)' "refs/heads/${BRANCH_PREFIX}*" \
            | xargs -r -n 1 git branch -D 2>/dev/null || true

      # Same staging as the review-address job: the verify gate always runs the
      # trusted checkout's copy of the schema gate, never a working-tree copy.
      - name: 'Stage trusted schema gate'
        id: 'stage'
        run: |-
          cp .github/scripts/check-settings-schema.sh "${RUNNER_TEMP}/check-settings-schema.sh"
          cp .github/scripts/check-autofix-contracts.sh "${RUNNER_TEMP}/check-autofix-contracts.sh"
          cp .github/scripts/resolve-owning-packages.sh "${RUNNER_TEMP}/resolve-owning-packages.sh"
          cp .github/scripts/resanitize-git-config.sh "${RUNNER_TEMP}/resanitize-git-config.sh"
          # The staged copy's trusted-base provenance holds at cp time only:
          # RUNNER_TEMP is writable by the branch/agent code later steps run
          # on this host, so record the digest in GITHUB_OUTPUT — expression
          # context, which a disk write after staging cannot reach — for the
          # PAT-bearing step to verify at invocation time.
          # Full rationale → qwen-autofix.md#af-086
          echo "resanitize_sha256=$(sha256sum "${RUNNER_TEMP}/resanitize-git-config.sh" | cut -d' ' -f1)" >> "${GITHUB_OUTPUT}"
          echo "trusted_path=${PATH}" >> "${GITHUB_OUTPUT}"

      - name: 'Check bot credentials'
        env:
          GITHUB_TOKEN: '${{ secrets.CI_DEV_BOT_PAT }}'
        run: |-
          if [[ -z "${GITHUB_TOKEN}" ]]; then
            echo '::error::CI_DEV_BOT_PAT is required to run the issue autofix job.'
            exit 1
          fi
          api_error_file="$(mktemp)"
          if ! bot_actor="$(GH_TOKEN="${GITHUB_TOKEN}" gh api user --jq '.login' 2>"${api_error_file}")"; then
            api_error="$(tr '\r\n' ' ' < "${api_error_file}")"
            rm -f "${api_error_file}"
            echo "::error::Failed to verify CI_DEV_BOT_PAT identity with gh api user: ${api_error:-unknown error}."
            exit 1
          fi
          rm -f "${api_error_file}"
          echo "CI_DEV_BOT_PAT authenticates as ${bot_actor}"
          if [[ "${bot_actor}" != "${AUTOFIX_BOT}" ]]; then
            echo "::error::CI_DEV_BOT_PAT authenticates as ${bot_actor}; expected ${AUTOFIX_BOT}."
            exit 1
          fi

      - name: 'Check runner environment'
        env:
          RUNNER_ENVIRONMENT: '${{ runner.environment }}'
          RUNNER_NAME: '${{ runner.name }}'
        run: |-
          case "${RUNNER_ENVIRONMENT}" in
            github-hosted|self-hosted) ;;
            *)
              echo "::error::Unsupported runner environment: ${RUNNER_ENVIRONMENT:-unset}."
              exit 1
              ;;
          esac
          # The label routing pins ecs-agent. Existing relabelled hosts retain
          # ecs-qwen names while future dedicated hosts may use ecs-agent, so
          # accept both names and reject any other mis-labelled registration.
          if [[ "${RUNNER_ENVIRONMENT}" == 'self-hosted' ]]; then
            case "${RUNNER_NAME}" in
              ecs-qwen-*|ecs-agent-*) ;;
              *)
                echo "::error::self-hosted runner '${RUNNER_NAME}' is not an approved agent pool member; refusing to run here."
                exit 1
                ;;
            esac
            echo 'QWEN_SKIP_LATENCY_BUDGETS=1' >> "${GITHUB_ENV}"
          fi
          # Capability preflight for the persistent pool: this job's agent
          # runs inside the docker sandbox, and a missing daemon otherwise
          # surfaces only at 'Resolve sandbox image' — after npm ci/build
          # has already burned tens of minutes. Fail in seconds instead.
          # Hosted runners ship docker; the ECS pool's docker is proven by
          # qwen-triage's container jobs on the same labels.
          if ! docker info > /dev/null 2>&1; then
            echo "::error::docker daemon is not reachable on this runner; the sandboxed agent cannot start."
            exit 1
          fi
          # 'Set up Node.js' assumes this pool keeps ~/.npm across jobs;
          # log its size so a move to per-job containers is visible.
          du -sh "${HOME}/.npm" 2>/dev/null || echo '~/.npm is absent on this runner.'

      - name: 'Set up Node.js'
        uses: 'actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e' # v6.4.0
        with:
          node-version: '22.x'
          # No remote npm cache on the persistent pool: one measured
          # review-address leg spent 339s in `Set up Node.js` restoring
          # Full rationale → qwen-autofix.md#af-012
          cache: "${{ runner.environment != 'self-hosted' && 'npm' || '' }}"
          package-manager-cache: false
          cache-dependency-path: 'package-lock.json'

      - name: 'Install tmux'
        run: |-
          if command -v tmux > /dev/null 2>&1; then
            tmux -V
          elif command -v sudo > /dev/null 2>&1 && command -v apt-get > /dev/null 2>&1; then
            # sudo -n: a host without passwordless sudo must fail fast with a
            # clear message, not die on a password prompt (the pr-review pool
            # steps make the same assumption with `sudo -n ... || ::warning`).
            sudo -n apt-get update -qq && sudo -n apt-get install -y -qq tmux || {
              echo '::error::tmux is required on the autofix runner and passwordless install failed.'
              exit 1
            }
          else
            echo '::error::tmux is required on the autofix runner.'
            exit 1
          fi

      # The npm-ci retry recipe and the 'Prepare Qwen Code CLI' shim below
      # are duplicated in build-cli and review-address; the workflow
      # contract tests pin every copy in lockstep — edit them together.
      - name: 'Install dependencies and build'
        env:
          QWEN_SKIP_PREPARE: '1'
        run: |-
          for attempt in 1 2 3; do
            if npm ci --prefer-offline --no-audit --progress=false; then
              break
            fi
            if [[ "${attempt}" == "3" ]]; then
              exit 1
            fi
            sleep $((attempt * 15))
          done
          git config core.hooksPath .husky
          npm run build
          npm run bundle

      - name: 'Prepare Qwen Code CLI'
        run: |-
          qwen_version="$(node -p "require('./package.json').version")"
          echo "Using checked-out Qwen Code bundle ${qwen_version}"
          qwen_bin="${RUNNER_TEMP}/qwen-bin"
          mkdir -p "${qwen_bin}"
          cat > "${qwen_bin}/qwen" <<'EOF'
          #!/usr/bin/env bash
          exec node "${GITHUB_WORKSPACE}/dist/cli.js" "$@"
          EOF
          chmod +x "${qwen_bin}/qwen"
          echo "${qwen_bin}" >> "${GITHUB_PATH}"
          PATH="${qwen_bin}:${PATH}"
          qwen --version

      - name: 'Find candidate issues'
        id: 'scan'
        env:
          GITHUB_TOKEN: '${{ secrets.CI_DEV_BOT_PAT }}'
          # Must resolve to the same issue as this job's concurrency group
          # expression; a test pins the two equal.
          FORCED_ISSUE: '${{ needs.route.outputs.issue_number || github.event.issue.number }}'
        run: |-
          mkdir -p "${WORKDIR}"
          OPEN_AUTOFIX_PR_COUNT=0

          if [[ -n "${FORCED_ISSUE}" ]]; then
            echo "🎯 Forced issue #${FORCED_ISSUE}"
            forced_issue_json="${WORKDIR}/forced-issue.json"
            gh issue view "${FORCED_ISSUE}" --repo "${REPO}" \
              --json number,title,body,labels,createdAt,url,state \
              > "${forced_issue_json}"
            if jq -e \
              '(.labels // []) | map(.name) | any(. == "autofix/skip" or . == "autofix/in-progress")' \
              "${forced_issue_json}" > /dev/null; then
              echo "⏭️ Forced issue #${FORCED_ISSUE} has an autofix exclusion label; skipping."
              jq -n -c '[]' > "${WORKDIR}/candidates.json"
            elif [[ "$(jq -r '.state // ""' "${forced_issue_json}")" != 'OPEN' ]]; then
              echo "⏭️ Forced issue #${FORCED_ISSUE} is not open; skipping."
              jq -n -c '[]' > "${WORKDIR}/candidates.json"
            # workflow_dispatch is a maintainer-initiated escape hatch, so it
            # intentionally bypasses the label gates that protect event/cron
            # paths from issue-content prompt injection.
            elif [[ "${EVENT_NAME}" != 'workflow_dispatch' ]] && ! jq -e --arg ready "${READY_FOR_AGENT_LABEL}" \
              '(.labels // []) | map(.name) as $labels | ($labels | index($ready))' \
              "${forced_issue_json}" > /dev/null; then
              echo "⏭️ Forced issue #${FORCED_ISSUE} is missing ${READY_FOR_AGENT_LABEL}; skipping."
              jq -n -c '[]' > "${WORKDIR}/candidates.json"
            elif [[ "${EVENT_NAME}" != 'workflow_dispatch' ]] && ! jq -e --arg approved "${AUTOFIX_APPROVED_LABEL}" \
              '(.labels // []) | map(.name) as $labels | ($labels | index($approved))' \
              "${forced_issue_json}" > /dev/null; then
              echo "⏭️ Forced issue #${FORCED_ISSUE} is missing ${AUTOFIX_APPROVED_LABEL}; skipping."
              jq -n -c '[]' > "${WORKDIR}/candidates.json"
            else
              if ! jq -c '[. + {autofixTier: 0}]' "${forced_issue_json}" > "${WORKDIR}/candidates.json"; then
                echo "::warning::Forced issue #${FORCED_ISSUE} processing failed; falling back to an empty candidate list."
                jq -n -c '[]' > "${WORKDIR}/candidates.json"
              fi
            fi
          else
            if ! gh pr list --repo "${REPO}" --state open --author "${AUTOFIX_BOT}" \
              --limit 100 --json number,headRefName,isCrossRepository > "${WORKDIR}/open-autofix-prs.json"; then
              echo "::warning::Open autofix PR scan failed; proceeding without WIP-cap enforcement."
            else
              OPEN_AUTOFIX_PR_COUNT="$(jq --arg p "${BRANCH_PREFIX}" \
                '[.[] | select((.isCrossRepository != true) and ((.headRefName // "") | startswith($p)))] | length' \
                "${WORKDIR}/open-autofix-prs.json")"
            fi

            if [[ "${OPEN_AUTOFIX_PR_COUNT}" -ge "${MAX_OPEN_AUTOFIX_PRS}" ]]; then
              echo "⏭️ ${OPEN_AUTOFIX_PR_COUNT} open autofix PR(s) already exist; WIP limit is ${MAX_OPEN_AUTOFIX_PRS}; skipping issue fallback."
              jq -n -c '[]' > "${WORKDIR}/candidates.json"
            else
              echo "🔍 Ready-for-agent issues (newest first)..."
              if ! gh issue list --repo "${REPO}" \
              --search "is:open is:issue label:${READY_FOR_AGENT_LABEL} label:${AUTOFIX_APPROVED_LABEL} ${AUTOFIX_ISSUE_EXCLUDES}" \
              --limit 30 --json number,title,body,labels,createdAt,url \
              > "${WORKDIR}/scan.json"; then
                echo "::warning::Ready-for-agent issue scan failed; falling back to an empty candidate list."
                jq -n -c '[]' > "${WORKDIR}/candidates.json"
              else
                if ! jq -c '.[0:10] | map(. + {autofixTier: 1})' \
                  "${WORKDIR}/scan.json" > "${WORKDIR}/candidates.json"; then
                  echo "::warning::Ready-for-agent result processing failed; falling back to an empty candidate list."
                  jq -n -c '[]' > "${WORKDIR}/candidates.json"
                fi
              fi
            fi
          fi

          COUNT="$(jq length "${WORKDIR}/candidates.json")"
          if [[ "${COUNT}" -gt 0 ]]; then
            if [[ -s "${WORKDIR}/open-autofix-prs.json" ]]; then
              echo "ℹ️ Reusing open autofix PR scan for duplicate-PR annotation."
            elif ! gh pr list --repo "${REPO}" --state open --author "${AUTOFIX_BOT}" \
              --limit 100 --json number,headRefName,isCrossRepository > "${WORKDIR}/open-autofix-prs.json"; then
              echo "::warning::Open autofix PR scan failed; candidates will proceed without duplicate-PR annotation."
            fi
            if [[ -s "${WORKDIR}/open-autofix-prs.json" ]]; then
              if ! jq -c --arg p "${BRANCH_PREFIX}" --slurpfile prs "${WORKDIR}/open-autofix-prs.json" '
                ($prs[0] // []) as $prs
                | map(
                    ($p + (.number | tostring)) as $branch
                    | (
                        first($prs[] | select((.isCrossRepository != true) and ((.headRefName // "") == $branch)) | {
                          number,
                          headRefName
                        }) // null
                      ) as $existing
                    | . + {existingAutofixPr: $existing}
                  )
              ' "${WORKDIR}/candidates.json" > "${WORKDIR}/annotated-candidates.json"; then
                echo "::warning::Open autofix PR annotation failed; candidates will proceed without duplicate-PR annotation."
              else
                mv "${WORKDIR}/annotated-candidates.json" "${WORKDIR}/candidates.json"
                CANDIDATES_WITH_PRS="$(jq '[.[] | select(.existingAutofixPr != null)] | length' "${WORKDIR}/candidates.json")"
                if [[ "${CANDIDATES_WITH_PRS}" -gt 0 ]]; then
                  echo "ℹ️ ${CANDIDATES_WITH_PRS} candidate(s) already have open autofix PRs; the skill must skip them."
                fi
              fi
            fi
          fi

          COUNT="$(jq length "${WORKDIR}/candidates.json")"
          echo "📋 ${COUNT} candidate(s) found"
          if [[ "${COUNT}" -gt 0 ]]; then
            OLDEST_CREATED="$(jq -r 'map(.createdAt) | min' "${WORKDIR}/candidates.json")"
            NEWEST_CREATED="$(jq -r 'map(.createdAt) | max' "${WORKDIR}/candidates.json")"
            echo "🕒 Candidate createdAt range: ${OLDEST_CREATED} .. ${NEWEST_CREATED}"
          fi
          echo "has_candidates=$([[ "${COUNT}" -gt 0 ]] && echo true || echo false)" >> "${GITHUB_OUTPUT}"

      - name: 'Resolve sandbox image'
        id: 'sandbox_image'
        if: |-
          ${{ steps.scan.outputs.has_candidates == 'true' }}
        env:
          # The export is derived from this binary's stdout, so step env
          # pins it the way the DOCKER_HOST pins do: step env outranks an
          # appended $GITHUB_ENV value, and the absolute path cannot be
          # shadowed by a '${RUNNER_TEMP}/qwen-bin' or $GITHUB_PATH plant
          # (#9527 review).
          SANDBOX_COMMAND: '/usr/bin/docker'
        run: |-
          node .github/scripts/resolve-sandbox-image.mjs \
            "$(node -p "require('./package.json').config.sandboxImageUri")"

      - name: 'Fast-track decision'
        id: 'fasttrack'
        if: |-
          ${{ steps.scan.outputs.has_candidates == 'true' }}
        env:
          EVENT_NAME: '${{ github.event_name }}'
          FORCED_ISSUE: '${{ inputs.issue_number }}'
        run: |-
          FAST_TRACK=false
          if [[ "${EVENT_NAME}" == 'workflow_dispatch' && -n "${FORCED_ISSUE}" ]]; then
            FAST_TRACK=true
          fi
          if [[ "${EVENT_NAME}" == 'issues' ]]; then
            FAST_TRACK=true
          fi

          if [[ "${FAST_TRACK}" == 'true' ]]; then
            ISSUE_NUM="$(jq -r '.[0].number' "${WORKDIR}/candidates.json")"
            jq -n -c --argjson num "${ISSUE_NUM}" \
              '{go: $num, reason: "Fast-tracked: trusted trigger bypasses LLM assessment.", skip: []}' \
              > "${WORKDIR}/decision.json"
            echo "⚡ Fast-track decision: issue #${ISSUE_NUM}"
            echo 'fast_tracked=true' >> "${GITHUB_OUTPUT}"
          else
            echo 'fast_tracked=false' >> "${GITHUB_OUTPUT}"
          fi

      - name: 'Assess candidates'
        id: 'assess'
        if: |-
          ${{ steps.scan.outputs.has_candidates == 'true' && steps.fasttrack.outputs.fast_tracked != 'true' }}
        env:
          # Step-level env outranks $GITHUB_ENV: bind the resolver's pinned
          # digest output, not the appendable env value it also writes.
          QWEN_SANDBOX_IMAGE: '${{ steps.sandbox_image.outputs.image }}'
          # Pin the daemon endpoint for the sandbox this step spawns, the
          # same way the resolver pins its own spawns: an empty DOCKER_HOST
          # is skipped by the docker CLI, and step env outranks both the
          # appendable $GITHUB_ENV and the pool-shared currentContext.
          DOCKER_HOST: ''
          DOCKER_CONTEXT: 'default'
          GITHUB_TOKEN: '${{ secrets.CI_DEV_BOT_PAT }}'
          OPENAI_API_KEY: '${{ secrets.AUTOFIX_OPENAI_API_KEY }}'
          OPENAI_BASE_URL: '${{ secrets.AUTOFIX_OPENAI_BASE_URL || secrets.OPENAI_BASE_URL }}'
          OPENAI_MODEL: '${{ vars.QWEN_AUTOFIX_MODEL || vars.QWEN_PR_REVIEW_MODEL }}'
          NO_PROXY: '127.0.0.1,localhost,::1'
          QWEN_HOME: '${{ runner.temp }}/qwen-autofix-home'
          # 50m, the runner's former default, pinned here on purpose: the
          # default moved to 90m for `Develop fix`, but assessment is triage
          # (observed max 41m, p50 7m) and this job has no step-level caps,
          # so its 180-minute cap must cover build (~6m) + assess + develop
          # (90m) + gate + publish. Letting assess grow to 90m too would let
          # a hung assessment push the round into the JOB cap, which cancels
          # the always() reporters (claim withdrawal) — a silent round.
          QWEN_TIMEOUT_MS: '3000000'
          SETTINGS_JSON: |-
            {
              "maxSessionTurns": 60,
              "coreTools": [
                "read_file",
                "read_many_files",
                "glob",
                "search_file_content",
                "write_file",
                "run_shell_command(cat)",
                "run_shell_command(git log)",
                "run_shell_command(git diff)"
              ],
              "tools": {
                "sandbox": "docker"
              }
            }
        run: |-
          rm -rf "${QWEN_HOME}"
          mkdir -p .qwen "${QWEN_HOME}"
          if [[ -z "${OPENAI_API_KEY:-}" ]]; then
            echo '::error::AUTOFIX_OPENAI_API_KEY secret is required for Qwen Autofix.'
            exit 1
          fi
          printf '%s\n' "${SETTINGS_JSON}" > .qwen/settings.json
          rm -f "${WORKDIR}/decision.json" "${WORKDIR}/failure.md" "${WORKDIR}/failure.zh.md"
          node .qwen/skills/autofix/scripts/run-agent.mjs \
            --mode assess-candidates \
            --workdir "${WORKDIR}"

      - name: 'Read decision'
        id: 'decision'
        if: |-
          ${{ steps.scan.outputs.has_candidates == 'true' }}
        env:
          GITHUB_TOKEN: '${{ secrets.CI_DEV_BOT_PAT }}'
          DRY_RUN: '${{ needs.route.outputs.dry_run }}'
          EVENT_NAME: '${{ github.event_name }}'
        run: |-
          if [[ ! -s "${WORKDIR}/decision.json" ]] || ! jq -e . "${WORKDIR}/decision.json" > /dev/null; then
            echo "❌ Assessment produced no valid decision.json"
            echo "go_issue=" >> "${GITHUB_OUTPUT}"
            exit 0
          fi

          GO="$(jq -r '.go // empty' "${WORKDIR}/decision.json")"
          if [[ -n "${GO}" && ! "${GO}" =~ ^[1-9][0-9]*$ ]]; then
            echo "❌ Assessment produced an invalid issue number"
            echo "go_issue=" >> "${GITHUB_OUTPUT}"
            exit 0
          fi

          CANDIDATE_NUMS="$(jq -r '.[].number' "${WORKDIR}/candidates.json")"
          if [[ -n "${GO}" ]] && ! grep -qx "${GO}" <<< "${CANDIDATE_NUMS}"; then
            echo "❌ Assessment selected issue #${GO} which is not in the candidate list"
            echo "go_issue=" >> "${GITHUB_OUTPUT}"
            exit 0
          fi

          if [[ -n "${GO}" ]]; then
            EXISTING_PR="$(jq -r --argjson go "${GO}" '
              first(.[] | select(.number == $go) | .existingAutofixPr.number) // empty
            ' "${WORKDIR}/candidates.json")"
            if [[ -n "${EXISTING_PR}" ]]; then
              echo "⏭️ Selected issue #${GO} already has open autofix PR #${EXISTING_PR}; skipping issue develop."
              echo "go_issue=" >> "${GITHUB_OUTPUT}"
              exit 0
            fi
          fi

          if [[ -n "${GO}" && "${DRY_RUN}" != "true" && "${EVENT_NAME}" != 'workflow_dispatch' ]]; then
            if ! live_issue_json="$(gh issue view "${GO}" --repo "${REPO}" --json labels,state)"; then
              echo "::warning::Failed to re-validate live labels for issue #${GO}; skipping due to API error"
              echo "go_issue=" >> "${GITHUB_OUTPUT}"
              exit 0
            fi
            if [[ "$(jq -r '.state // ""' <<< "${live_issue_json}")" != 'OPEN' ]]; then
              echo "⏭️ Selected issue #${GO} is no longer open; skipping."
              echo "go_issue=" >> "${GITHUB_OUTPUT}"
              exit 0
            fi
            if ! jq -e --arg ready "${READY_FOR_AGENT_LABEL}" --arg approved "${AUTOFIX_APPROVED_LABEL}" \
              '(.labels // []) | map(.name) as $labels | (($labels | index($ready)) and ($labels | index($approved)))' \
              <<< "${live_issue_json}" > /dev/null; then
              echo "⏭️ Selected issue #${GO} no longer has both ${READY_FOR_AGENT_LABEL} and ${AUTOFIX_APPROVED_LABEL}; skipping."
              echo "go_issue=" >> "${GITHUB_OUTPUT}"
              exit 0
            fi
          fi

          echo "go_issue=${GO}" >> "${GITHUB_OUTPUT}"
          echo "🧭 Decision: go=${GO:-none}"
          jq -r '.reason // empty' "${WORKDIR}/decision.json"

          # Label permanently-skipped issues so future scans move past them.
          if [[ "${DRY_RUN}" != "true" ]]; then
            gh label create 'autofix/skip' --repo "${REPO}" \
              --description 'Not eligible for the scheduled autofix agent' \
              --color 'ededed' 2> /dev/null || true
            jq -c '(.skip // [])[] | select(.permanent == true)' "${WORKDIR}/decision.json" \
              | while read -r row; do
                  NUM="$(jq -r '.number' <<< "${row}")"
                  if [[ ! "${NUM}" =~ ^[1-9][0-9]*$ ]]; then
                    echo "⚠️ Invalid skip number: ${NUM}"
                    continue
                  fi
                  if ! grep -qx "${NUM}" <<< "${CANDIDATE_NUMS}"; then
                    echo "⚠️ Skip issue #${NUM} is not in the candidate list"
                    continue
                  fi
                  echo "🏷️ Skipping #${NUM} permanently: $(jq -r '.reason' <<< "${row}")"
                  gh issue edit "${NUM}" --repo "${REPO}" --add-label 'autofix/skip' || true
                done
          fi

      - name: 'Claim issue'
        id: 'claim'
        if: |-
          ${{ steps.decision.outputs.go_issue != '' && needs.route.outputs.dry_run != 'true' }}
        env:
          GITHUB_TOKEN: '${{ secrets.CI_DEV_BOT_PAT }}'
          ISSUE: '${{ steps.decision.outputs.go_issue }}'
        run: |-
          BODY="🤖 The scheduled autofix agent is picking this issue up. It will attempt to establish the current behavior, implement the requested change, run E2E verification, and open a pull request linked to this issue. If the attempt fails, this claim will be withdrawn so a human can take over.

          Maintainers: comment or assign someone to stop future automated attempts, or add the \`autofix/skip\` label."

          # The label, not the comment, is what future scans key off to
          # avoid double-claiming.
          gh label create 'autofix/in-progress' --repo "${REPO}" \
            --description 'The scheduled autofix agent has claimed this issue' \
            --color '1d76db' 2> /dev/null || true
          gh label create "${AUTOFIX_APPROVED_LABEL}" --repo "${REPO}" \
            --description 'Maintainer explicitly approved this issue for autonomous autofix' \
            --color '0e8a16' 2> /dev/null || true
          if ! gh issue edit "${ISSUE}" --repo "${REPO}" \
            --add-label 'autofix/in-progress'; then
            echo "::error::Failed to add autofix/in-progress label on #${ISSUE} before claim comment was posted"
            exit 1
          fi
          gh issue edit "${ISSUE}" --repo "${REPO}" \
            --remove-label "${AUTOFIX_APPROVED_LABEL}" || true

          COMMENT_URL="$(gh issue comment "${ISSUE}" --repo "${REPO}" --body "${BODY}")"
          COMMENT_ID="${COMMENT_URL##*-}"
          echo "comment_id=${COMMENT_ID}" >> "${GITHUB_OUTPUT}"
          echo "📌 Claimed #${ISSUE} (comment ${COMMENT_ID})"

      - name: 'Develop fix'
        id: 'develop'
        if: |-
          ${{ steps.decision.outputs.go_issue != '' }}
        env:
          QWEN_SANDBOX_IMAGE: '${{ steps.sandbox_image.outputs.image }}'
          DOCKER_HOST: ''
          DOCKER_CONTEXT: 'default'
          ISSUE: '${{ steps.decision.outputs.go_issue }}'
          OPENAI_API_KEY: '${{ secrets.AUTOFIX_OPENAI_API_KEY }}'
          OPENAI_BASE_URL: '${{ secrets.AUTOFIX_OPENAI_BASE_URL || secrets.OPENAI_BASE_URL }}'
          OPENAI_MODEL: '${{ vars.QWEN_AUTOFIX_MODEL || vars.QWEN_PR_REVIEW_MODEL }}'
          NO_PROXY: '127.0.0.1,localhost,::1'
          QWEN_HOME: '${{ runner.temp }}/qwen-autofix-home'
          SETTINGS_JSON: |-
            {
              "maxSessionTurns": 400,
              "coreTools": [
                "read_file",
                "read_many_files",
                "glob",
                "search_file_content",
                "write_file",
                "run_shell_command(cat)",
                "run_shell_command(git add)",
                "run_shell_command(git checkout)",
                "run_shell_command(git commit)",
                "run_shell_command(git diff)",
                "run_shell_command(git log)",
                "run_shell_command(git status)",
                "run_shell_command(git switch)",
                "run_shell_command(ls)",
                "run_shell_command(mkdir)",
                "run_shell_command(npm run build)",
                "run_shell_command(npm run typecheck)",
                "run_shell_command(npm run lint)",
                "run_shell_command(npx vitest)",
                "run_shell_command(npm run generate:settings-schema)",
                "run_shell_command(pwd)"
              ],
              "tools": {
                "sandbox": "docker"
              }
            }
        run: |-
          rm -rf "${QWEN_HOME}"
          mkdir -p .qwen "${QWEN_HOME}"
          if [[ -z "${OPENAI_API_KEY:-}" ]]; then
            echo '::error::AUTOFIX_OPENAI_API_KEY secret is required for Qwen Autofix.'
            exit 1
          fi
          printf '%s\n' "${SETTINGS_JSON}" > .qwen/settings.json
          rm -f "${WORKDIR}/failure.md" "${WORKDIR}/failure.zh.md"
          node .qwen/skills/autofix/scripts/run-agent.mjs \
            --mode develop-issue \
            --issue "${ISSUE}" \
            --workdir "${WORKDIR}"

      - name: 'Verification gate'
        id: 'verify'
        if: |-
          ${{ steps.decision.outputs.go_issue != '' }}
        env:
          ISSUE: '${{ steps.decision.outputs.go_issue }}'
        run: |-
          BRANCH="autofix/issue-${ISSUE}"

          # Hermetic git config for the gate and every check it spawns, same
          # rationale and shape as run-autofix-review-verification.sh (a
          # leaked global exec knob on the persistent pool must not fail
          # branch tests, a branch-authored `git config --global` must not
          # outlive the run, and GITHUB_ENV-injected git env channels outrank
          # every file layer) — the contract test pins this env+redirect
          # block equal to the review gate's copy.
          unset GIT_CONFIG_PARAMETERS GIT_ALLOW_PROTOCOL GIT_PROXY_COMMAND \
            GIT_SSL_NO_VERIFY GIT_SSL_CAINFO GIT_EXEC_PATH GIT_DIR \
            GIT_WORK_TREE GIT_COMMON_DIR GIT_OBJECT_DIRECTORY \
            GIT_ALTERNATE_OBJECT_DIRECTORIES GIT_SHALLOW_FILE \
            GIT_ASKPASS GIT_SSH GIT_SSH_COMMAND
          export GIT_CONFIG_COUNT=0
          export GIT_TERMINAL_PROMPT=0
          export GIT_CONFIG_SYSTEM=/dev/null
          export GIT_CONFIG_GLOBAL="${RUNNER_TEMP}/autofix-gate-gitconfig"
          : > "${GIT_CONFIG_GLOBAL}"
          git config --file "${GIT_CONFIG_GLOBAL}" safe.directory "$(pwd)"
          if [ -s /etc/gitconfig ]; then
            echo "::notice::/etc/gitconfig exists but is bypassed by the gate's GIT_CONFIG_SYSTEM redirect — replicate any setting the checks need via per-job env."
          fi

          if [[ -f "${WORKDIR}/failure.md" && -n "$(git status --porcelain)" ]]; then
            echo "❌ Agent wrote failure.md after leaving a dirty workspace:"
            git status --short
            # Agent-written content on step stdout: neutralize both command
            # syntaxes (`##[` parses mid-line too — #9761).
            sed -e 's/::/;;/g' -e 's/##\[/##［/g' "${WORKDIR}/failure.md"
            exit 1
          fi

          if [[ -f "${WORKDIR}/failure.md" ]]; then
            echo "🛑 Agent aborted intentionally:"
            sed -e 's/::/;;/g' -e 's/##\[/##［/g' "${WORKDIR}/failure.md"
            exit 1
          fi

          if ! git rev-parse --verify "${BRANCH}" > /dev/null 2>&1; then
            echo "❌ Expected branch ${BRANCH} does not exist"
            exit 1
          fi
          git config core.hooksPath /dev/null
          git checkout "${BRANCH}"

          if git diff --quiet origin/main..."${BRANCH}"; then
            echo "❌ Branch has no changes against main"
            exit 1
          fi

          for f in pr-title.txt pr-body.md e2e-report.md; do
            if [[ ! -s "${WORKDIR}/${f}" ]]; then
              echo "❌ Missing required output ${f}"
              exit 1
            fi
          done

          echo '🔬 Re-running deterministic checks (independent of the agent)...'
          npm run build
          npm run typecheck
          npm run lint

          # Settings-schema freshness gate, shared with the triage-and-address
          # verify step so the two copies cannot drift (rationale + the
          # generator crash guard live in the script).
          # Full rationale → qwen-autofix.md#af-087
          bash "${RUNNER_TEMP}/check-settings-schema.sh"
          git diff --name-only "origin/main...${BRANCH}" \
            | bash "${RUNNER_TEMP}/check-autofix-contracts.sh"

          # Run changed/related tests for the packages this fix touches.
          # Full rationale → qwen-autofix.md#af-013
          CHANGED_PKGS="$(git diff --name-only "origin/main...${BRANCH}" \
            | bash "${RUNNER_TEMP}/resolve-owning-packages.sh")"
          if [[ -z "${CHANGED_PKGS}" ]]; then
            echo 'No package changes detected; skipping package tests.'
          else
            for p in ${CHANGED_PKGS}; do
              if [[ ! -f "${p}/package.json" ]]; then
                echo "Skipping ${p}: no package.json."
                continue
              fi
              test_script="$(node -e 'const fs = require("node:fs"); const pkg = JSON.parse(fs.readFileSync(process.argv[1], "utf8")); process.stdout.write(pkg.scripts?.test || "");' "${p}/package.json")"
              if [[ "${test_script}" != *vitest* ]]; then
                echo "Skipping ${p}: test script is not Vitest."
                continue
              fi
              echo "🧪 Testing ${p} (changed files only)..."
              npm run test --workspace "${p}" --if-present -- --changed origin/main --passWithNoTests
            done
          fi

      - name: 'Show run artifacts'
        if: |-
          ${{ always() && steps.decision.outputs.go_issue != '' }}
        env:
          ISSUE: '${{ steps.decision.outputs.go_issue }}'
        run: |-
          BRANCH="autofix/issue-${ISSUE}"
          if git rev-parse --verify "${BRANCH}" > /dev/null 2>&1; then
            git diff "origin/main...${BRANCH}" > "${WORKDIR}/fix.diff" || true
          fi
          for f in decision.json pr-title.txt pr-body.md e2e-report.md failure.md failure.zh.md fix.diff; do
            if [[ -f "${WORKDIR}/${f}" ]]; then
              echo "=============== ${f} ==============="
              # Agent-written content on step STDOUT: both workflow-command
              # syntaxes parse here — a line-start `::` (::error::,
              # ::add-mask::) AND `##[` even mid-line (a quoted
              # `##[add-matcher]` fails THIS step; measured on #9761). The
              # same reason the PR-lane dump loop neutralizes both. (The two
              # step-SUMMARY loops write to a file, where commands are not
              # parsed.)
              sed -e 's/::/;;/g' -e 's/##\[/##［/g' "${WORKDIR}/${f}"
              echo
            fi
          done

      - name: 'Upload run artifacts'
        if: |-
          ${{ always() && steps.scan.outputs.has_candidates == 'true' }}
        uses: 'actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a' # v7.0.1
        with:
          name: 'autofix-issue-artifacts'
          path: '${{ env.WORKDIR }}/'
          if-no-files-found: 'ignore'

      - name: 'Publish PR'
        id: 'publish'
        if: |-
          ${{ steps.decision.outputs.go_issue != '' && needs.route.outputs.dry_run != 'true' }}
        env:
          # CI_DEV_BOT_PAT opens the PR as the configured autofix bot. This is
          # required: the default GITHUB_TOKEN is
          # blocked from creating PRs ("GitHub Actions is not permitted to
          # create or approve pull requests"), and PRs it does create do not
          # trigger CI. The bot PAT clears both problems.
          GITHUB_TOKEN: '${{ secrets.CI_DEV_BOT_PAT }}'
          ISSUE: '${{ steps.decision.outputs.go_issue }}'
          MODEL: '${{ vars.QWEN_AUTOFIX_MODEL || vars.QWEN_PR_REVIEW_MODEL }}'
          RESANITIZE_SHA256: '${{ steps.stage.outputs.resanitize_sha256 }}'
          TRUSTED_PATH: '${{ steps.stage.outputs.trusted_path }}'
        run: |-
          # gh has its own $GITHUB_ENV-injectable channels: pin the host and
          # drop any planted token BEFORE the identity check below, so a
          # GH_HOST reroute cannot spoof `gh api user` and a planted GH_TOKEN
          # cannot outrank the inline GITHUB_TOKEN. (git's channels are
          # stripped in the hermetic preamble further down.)
          export GH_HOST=github.com
          unset GH_ENTERPRISE_TOKEN GH_TOKEN
          # Point gh at a fresh empty config dir, not the default
          # ~/.config/gh on the shared attacker-writable HOME — its
          # config.yml can carry http_unix_socket and other transport
          # reroutes no sweep here touches. mktemp -d gives an
          # unpredictable path a watcher cannot pre-seed.
          export GH_CONFIG_DIR="$(mktemp -d "${RUNNER_TEMP}/autofix-gh-config.XXXXXX")"
          MODEL_DISPLAY="${MODEL:-default}"
          if [[ -z "${GITHUB_TOKEN}" ]]; then
            echo '::error::CI_DEV_BOT_PAT is required to publish the PR as the autofix bot.'
            exit 1
          fi
          api_error_file="$(mktemp)"
          if ! publish_actor="$(GH_TOKEN="${GITHUB_TOKEN}" gh api user --jq '.login' 2>"${api_error_file}")"; then
            api_error="$(tr '\r\n' ' ' < "${api_error_file}")"
            rm -f "${api_error_file}"
            echo "::error::Failed to verify CI_DEV_BOT_PAT identity with gh api user: ${api_error:-unknown error}."
            exit 1
          fi
          rm -f "${api_error_file}"
          echo "CI_DEV_BOT_PAT authenticates as ${publish_actor}"
          if [[ "${publish_actor}" != "${AUTOFIX_BOT}" ]]; then
            echo "::error::CI_DEV_BOT_PAT authenticates as ${publish_actor}; expected ${AUTOFIX_BOT}."
            exit 1
          fi
          BRANCH="autofix/issue-${ISSUE}"
          # Take this PAT-bearing step off every mutable host git surface —
          # both the shared config FILES and git's ENV channels — keep this
          # Full rationale → qwen-autofix.md#af-014
          export PATH="${TRUSTED_PATH}"
          unset LD_PRELOAD LD_AUDIT LD_LIBRARY_PATH \
            GIT_CONFIG_PARAMETERS GIT_ALLOW_PROTOCOL GIT_PROXY_COMMAND \
            GIT_SSL_NO_VERIFY GIT_SSL_CAINFO GIT_EXEC_PATH GIT_DIR \
            GIT_WORK_TREE GIT_COMMON_DIR GIT_OBJECT_DIRECTORY \
            GIT_ALTERNATE_OBJECT_DIRECTORIES GIT_SHALLOW_FILE \
            GIT_ASKPASS GIT_SSH GIT_SSH_COMMAND
          export GIT_CONFIG_COUNT=0
          export GIT_TERMINAL_PROMPT=0
          export GIT_CONFIG_SYSTEM=/dev/null
          export GIT_CONFIG_GLOBAL="$(mktemp "${RUNNER_TEMP}/autofix-pat-gitconfig.XXXXXX")"
          git config --file "${GIT_CONFIG_GLOBAL}" safe.directory "$(pwd)"
          # Host hygiene + LOCAL .git/config scrub (the throwaway global above
          # covers only the global scope, not the highest-precedence local
          # file the branch/agent can plant). The staged copy's trusted-base
          # provenance holds at cp time only — RUNNER_TEMP is writable by that
          # same branch code — so verify the digest the staging step recorded
          # in GITHUB_OUTPUT (unreachable from a disk write) before executing.
          # Never run the script from the working tree; it holds the branch.
          echo "${RESANITIZE_SHA256}  ${RUNNER_TEMP}/resanitize-git-config.sh" | sha256sum -c - > /dev/null
          bash "${RUNNER_TEMP}/resanitize-git-config.sh"
          git config --local --unset-all http.https://github.com/.extraheader || true
          git config core.hooksPath /dev/null
          # Authenticate the push with a one-shot, host-scoped credential
          # helper via `git -c`: nothing is written to the reused
          # Full rationale → qwen-autofix.md#af-015
          git -c http.sslVerify=true -c credential.helper= -c credential."https://github.com".helper='!f(){ echo username=x-access-token; echo "password=${GITHUB_TOKEN}"; };f' \
            push --no-verify "https://github.com/${REPO}.git" "${BRANCH}"

          PR_URL="$(gh pr create --repo "${REPO}" \
            --base main --head "${BRANCH}" \
            --title "$(cat "${WORKDIR}/pr-title.txt")" \
            --body-file "${WORKDIR}/pr-body.md")"
          echo "🚀 Opened ${PR_URL}"

          # Per AGENTS.md, post the E2E report as a separate PR comment.
          {
            echo
            echo "---"
            echo "🧠 Handled by **Qwen Code** · model/模型 \`${MODEL_DISPLAY}\`"
          } >> "${WORKDIR}/e2e-report.md"
          gh pr comment "${PR_URL}" --body-file "${WORKDIR}/e2e-report.md"

      - name: 'Report dry-run / failure'
        if: |-
          ${{ always() && (needs.route.outputs.dry_run == 'true' || failure() || cancelled()) }}
        env:
          ISSUE: '${{ steps.decision.outputs.go_issue }}'
          DRY_RUN: '${{ needs.route.outputs.dry_run }}'
          OUTCOME: '${{ steps.verify.outputs.outcome }}'
        run: |-
          SUFFIX=''
          [[ "${DRY_RUN}" == "true" ]] && SUFFIX=' (dry-run, nothing pushed)'
          {
            echo "### Issue autofix${ISSUE:+ #${ISSUE}} — outcome=${OUTCOME:-unknown}${SUFFIX}"
            echo
            for f in decision.json pr-title.txt pr-body.md e2e-report.md failure.md failure.zh.md fix.diff; do
              if [[ -s "${WORKDIR}/${f}" ]]; then
                echo "**${f}:**"
                echo '```'
                cat "${WORKDIR}/${f}"
                echo '```'
                echo
              fi
            done
          } >> "${GITHUB_STEP_SUMMARY}"

      - name: 'Withdraw claim on failure'
        if: |-
          ${{ (failure() || cancelled()) && steps.claim.outcome == 'success' }}
        env:
          GITHUB_TOKEN: '${{ secrets.CI_DEV_BOT_PAT }}'
          ISSUE: '${{ steps.decision.outputs.go_issue }}'
          COMMENT_ID: '${{ steps.claim.outputs.comment_id }}'
          PUBLISH_OUTCOME: '${{ steps.publish.outcome }}'
        run: |-
          # shellcheck disable=SC2016
          if [[ -f "${WORKDIR}/failure.md" ]]; then
            REASON='no further automated attempts will be made on this issue.'
            REASON_ZH='不会对该 issue 再做自动尝试。'
            # Same hygiene as the PR-lane DETAIL_FILE excerpt: -c drops a
            # partial multi-byte sequence the byte-level head -c may have
            # split, and the markup neutralization stops HTML quoted across
            # the 1500-byte cut from swallowing the 中文说明 <details> block.
            # Full rationale → qwen-autofix.md#af-088
            DETAIL="$(head -c 1500 "${WORKDIR}/failure.md" | iconv -f utf-8 -t utf-8 -c | sed -e 's/<!--/<!\\-\\-/g' -e 's/<[dD][eE][tT][aA][iI][lL][sS]/＜details/g' -e 's/<\/[dD][eE][tT][aA][iI][lL][sS]/＜\/details/g' -e 's/<[sS][uU][mM][mM][aA][rR][yY]/＜summary/g' || true)"
            LABEL_ARGS=(--remove-label 'autofix/in-progress' --add-label 'autofix/skip')
          elif [[ "${PUBLISH_OUTCOME}" == 'failure' ]]; then
            REASON='the issue will require the `autofix/approved` label to be re-added before any future automated attempt.'
            REASON_ZH='该 issue 需要重新添加 `autofix/approved` 标签才能再次自动尝试。'
            DETAIL='The agent produced and verified a fix, but publishing the PR failed. Check the Publish PR step logs for the CI_DEV_BOT_PAT actor, git push, PR creation, or PR comment error.'
            LABEL_ARGS=(--remove-label 'autofix/in-progress')
          else
            REASON='the issue will require the `autofix/approved` label to be re-added before any future automated attempt.'
            REASON_ZH='该 issue 需要重新添加 `autofix/approved` 标签才能再次自动尝试。'
            DETAIL='The run failed before producing a verified fix.'
            LABEL_ARGS=(--remove-label 'autofix/in-progress')
          fi
          gh issue edit "${ISSUE}" --repo "${REPO}" "${LABEL_ARGS[@]}" || true
          # failure.zh.md is the agent's translation of failure.md (SKILL:
          # GitHub Actions Rules); the same sanitization as the PR-lane
          # handoff keeps a pathological translation from breaking markup.
          ZH_DETAIL=''
          if [[ -s "${WORKDIR}/failure.zh.md" ]]; then
            ZH_DETAIL="$(head -c 3000 "${WORKDIR}/failure.zh.md" | iconv -f utf-8 -t utf-8 -c | sed -e 's/<!--/<!\\-\\-/g' -e 's/<[dD][eE][tT][aA][iI][lL][sS]/＜details/g' -e 's/<\/[dD][eE][tT][aA][iI][lL][sS]/＜\/details/g' -e 's/<[sS][uU][mM][mM][aA][rR][yY]/＜summary/g' || true)"
          fi
          BODY="🤖 Withdrawing the claim above — the automated fix attempt did not succeed; ${REASON}

          What the agent found, in case it helps a human contributor:

          ${DETAIL}"
          # The 中文说明 block renders UNCONDITIONALLY, mirroring the PR
          # lane's floor: on the crash shapes run-agent.mjs writes failure.md
          # itself with no zh companion, and degrading this lane to zero
          # Chinese while the PR lane still shows HEADLINE_ZH contradicts the
          # design doc §5 floor — so the translated REASON always renders,
          # with the sanitized excerpt inside it only when present.
          ZH_BLOCK="${REASON_ZH}"
          if [[ -n "${ZH_DETAIL}" ]]; then
            ZH_BLOCK="${ZH_BLOCK}

          ${ZH_DETAIL}"
          fi
          BODY="${BODY}

          <details>
          <summary>中文说明</summary>

          ${ZH_BLOCK}

          </details>"
          gh issue comment "${ISSUE}" --repo "${REPO}" --body "${BODY}" || true
          if [[ -n "${COMMENT_ID}" ]]; then
            gh api -X DELETE "/repos/${REPO}/issues/comments/${COMMENT_ID}" || true
          fi

      # Nothing else removes the per-run WORKDIR; on the persistent pool
      # every run would leave its transcripts and decision files behind
      # forever. Last step, after every reader including the artifact
      # upload. always() covers cancellation too; only a hard runner kill
      # abandons the dir, and run_id never repeats — the age sweep in
      # Reset autofix workspace reclaims it.
      - name: 'Clean up autofix workdir'
        if: 'always()'
        run: 'rm -rf "${WORKDIR}"'

  # ===========================================================================
  # TAKEOVER COMMAND — the accepted comment command's side effects are the
  # label toggle plus its own acknowledgements: add on an unlabeled PR
  # toggles TAKEOVER_LABEL (the resulting pull_request event drives
  # engagement exactly like a manual label edit — one mechanism, two entry
  # points); add on an already-managed PR posts the re-arm ack (the round
  # window anchor); skip-labeled and fork PRs get bilingual refusals instead
  # of a toggle.
  # ===========================================================================
  takeover-command:
    needs: 'route'
    if: |-
      ${{ needs.route.outputs.takeover_cmd != '' }}
    # Two quick commands are independent runs; without serialization an older
    # /takeover could land its label write AFTER a newer /takeover stop read
    # the unlabeled state, leaving the PR engaged against the latest intent.
    # Queued (never cancelled) per-PR execution preserves command order.
    concurrency:
      group: 'qwen-autofix-takeover-cmd-${{ needs.route.outputs.cmd_pr }}'
      cancel-in-progress: false
    runs-on: 'ubuntu-latest'
    timeout-minutes: 5
    permissions:
      contents: 'read'
    env:
      REPO: '${{ github.repository }}'
      CMD: '${{ needs.route.outputs.takeover_cmd }}'
      CMD_FROM: '${{ needs.route.outputs.takeover_from }}'
      PR: '${{ needs.route.outputs.cmd_pr }}'
      GITHUB_TOKEN: '${{ secrets.CI_DEV_BOT_PAT }}'
    steps:
      - name: 'Toggle takeover label'
        run: |-
          api_error_file="$(mktemp)"
          if ! bot_actor="$(GH_TOKEN="${GITHUB_TOKEN}" gh api user --jq '.login' 2>"${api_error_file}")"; then
            api_error="$(tr '\r\n' ' ' < "${api_error_file}")"
            rm -f "${api_error_file}"
            echo "::error::Failed to verify CI_DEV_BOT_PAT identity with gh api user: ${api_error:-unknown error}."
            exit 1
          fi
          rm -f "${api_error_file}"
          if [[ "${bot_actor}" != "${AUTOFIX_BOT}" ]]; then
            echo "::error::CI_DEV_BOT_PAT authenticates as '${bot_actor:-unknown}'; expected ${AUTOFIX_BOT}."
            exit 1
          fi
          # Presence-aware toggle: adding a present label / removing an
          # absent one fires no event, so make the no-op explicit instead.
          # The same fetch carries isCrossRepository: fork PRs are refused UP
          # FRONT — unlike the label path this job runs in issue_comment
          # context WITH secrets, so it can explain itself, and refusing here
          # keeps the label from ever sticking to a fork PR via the command.
          if ! PR_INFO="$(gh pr view "${PR}" --repo "${REPO}" --json labels,isCrossRepository,state,baseRefName,author,maintainerCanModify 2> /dev/null)"; then
            echo "::error::could not read PR #${PR}"
            exit 1
          fi
          # State/base can change while this job sits in its per-PR queue —
          # re-verify what the route checked so a stale command cannot label
          # a closed or non-main PR.
          if [[ "$(jq -r '.state // ""' <<< "${PR_INFO}")" != "OPEN" ]]; then
            echo "🧭 takeover command dropped: PR #${PR} is no longer an open PR"
            exit 0
          fi
          CMD_BASE_REF="$(jq -r '.baseRefName // ""' <<< "${PR_INFO}")"
          if [[ "${CMD_BASE_REF}" != "main" ]]; then
            if [[ "${CMD}" == 'add' ]]; then
              # Refuse OUT LOUD — the silent drop made a /takeover on a
              # stacked PR indistinguishable from a lost event. Mirrors the
              # label path's base-refused ack, except no label was applied
              # here, so the ask is to re-run the command after retargeting
              # (not "the label is left in place").
              gh pr comment "${PR}" --repo "${REPO}" --body "$(printf '🚫 Takeover not engaged: the loop only manages PRs that target `main`, and this one targets `%s`. A stacked PR moves whenever its base branch does, so "new feedback since the last round" and base-conflict resolution are not well defined until the base lands. Retarget this PR to `main` once the base PR merges and re-run `%s` — or take over the base PR instead.\n\n<details>\n<summary>中文说明</summary>\n\n🚫 未接管：循环只管理以 `main` 为 base 的 PR，而本 PR 的 base 是 `%s`。堆叠 PR 会随 base 分支移动，因此“自上一轮以来的新反馈”与 base 冲突处理都无法良定义。待 base 的 PR 合入后将本 PR 改为面向 `main` 并重新执行 `%s`；或改为接管 base 那个 PR。\n\n</details>\n\n<!-- takeover-ack base-refused -->' "${CMD_BASE_REF}" "${TAKEOVER_COMMAND}" "${CMD_BASE_REF}" "${TAKEOVER_COMMAND}")"
              echo "🧭 takeover command refused: PR #${PR} targets '${CMD_BASE_REF}' not 'main'"
              exit 0
            fi
            # 'stop' proceeds: removing the label from a non-main PR is
            # harmless and matches the latest intent — dropping it here left
            # a manually-applied label stuck with no command able to remove
            # it (the label path's release ack ignores non-main PRs too).
          fi
          # Skip wins over takeover EVERYWHERE — including here: engaging or
          # re-arming a skip-labeled PR would post an 'engaged' window anchor
          # for management that the scans deliberately refuse to perform.
          if [[ "${CMD}" == 'add' && "$(jq -r --arg t "${SKIP_LABEL}" '[.labels[].name] | index($t) != null' <<< "${PR_INFO}")" == "true" ]]; then
            gh pr comment "${PR}" --repo "${REPO}" --body "$(printf '🚫 Takeover not engaged: this PR carries `%s`, which wins over takeover. Remove `%s` first, then re-run `%s`.\n\n<details>\n<summary>中文说明</summary>\n\n🚫 未接管：本 PR 带有 `%s` 标签，其优先级高于接管。请先移除 `%s`，再执行 `%s`。\n\n</details>\n\n<!-- takeover-ack skip-blocked -->' "${SKIP_LABEL}" "${SKIP_LABEL}" "${TAKEOVER_COMMAND}" "${SKIP_LABEL}" "${SKIP_LABEL}" "${TAKEOVER_COMMAND}")"
            echo "🧭 takeover command refused: ${SKIP_LABEL} present on #${PR}"
            exit 0
          fi
          # Fork PRs are manageable when the bot can actually push: the
          # author must have granted 'Allow edits from maintainers'
          # (org-owned forks cannot — adoption stays the path there), and
          # only write+ senders reach this job for forks (fork authors are
          # silently dropped at route). Without allow-edits, refuse with the
          # actionable ask. Engage-side requirement only: release ('stop')
          # is never blocked.
          if [[ "${CMD}" == 'add' && "$(jq -r 'if has("isCrossRepository") then .isCrossRepository else true end' <<< "${PR_INFO}")" != "false" \
            && "$(jq -r '.maintainerCanModify // false' <<< "${PR_INFO}")" != "true" ]]; then
            gh pr comment "${PR}" --repo "${REPO}" --body "$(printf '🚫 Takeover needs push access to this fork branch: please tick “Allow edits from maintainers” on the PR and re-run `%s`. (Org-owned forks cannot enable it — a maintainer can adopt the PR instead: snapshot the head into an in-repo branch and take that over.)\n\n<details>\n<summary>中文说明</summary>\n\n🚫 托管需要对 fork 分支的推送权限：请在 PR 上勾选 “Allow edits from maintainers” 后重新执行 `%s`。（组织账号的 fork 无法勾选 —— 维护者可改用领养：将 head 快照为本仓库分支后接管。）\n\n</details>\n\n<!-- takeover-ack fork-refused -->' "${TAKEOVER_COMMAND}" "${TAKEOVER_COMMAND}")"
            echo "🧭 takeover command refused: fork PR #${PR} without maintainer-edit access"
            exit 0
          fi
          # The fork AUTHOR must hold write+ too (the scan re-checks this on
          # every pickup and the address job once more live): engaging a
          # below-write fork would stick a label that nothing ever manages —
          # a silent ghost engagement with no ack and no explanation.
          # Engage-side only; release is never blocked.
          if [[ "${CMD}" == 'add' && "$(jq -r 'if has("isCrossRepository") then .isCrossRepository else true end' <<< "${PR_INFO}")" != "false" ]]; then
            FORK_PR_AUTHOR="$(jq -r '.author.login // ""' <<< "${PR_INFO}")"
            FORK_AUTHOR_PERM="$(gh api "repos/${REPO}/collaborators/${FORK_PR_AUTHOR}/permission" --jq '.permission // ""' 2> /dev/null || echo '')"
            case "${FORK_AUTHOR_PERM}" in
              admin|maintain|write) : ;;
              *)
                gh pr comment "${PR}" --repo "${REPO}" --body "$(printf '🚫 Takeover not engaged: fork takeover requires the PR author to hold write access on this repository (author `%s` currently: `%s`). A maintainer can adopt the PR instead: snapshot the head into an in-repo branch, open a new PR (commit authorship is preserved), and take that over.\n\n<details>\n<summary>中文说明</summary>\n\n🚫 未接管：fork 托管要求 PR 作者在本仓库持有 write 及以上权限（作者 `%s` 当前为：`%s`）。维护者可改用领养：将 head 快照为本仓库分支并另开 PR（commit 署名保留），再对新 PR 执行接管。\n\n</details>\n\n<!-- takeover-ack fork-refused -->' "${FORK_PR_AUTHOR}" "${FORK_AUTHOR_PERM:-none}" "${FORK_PR_AUTHOR}" "${FORK_AUTHOR_PERM:-none}")"
                echo "🧭 takeover command refused: fork PR #${PR} author '${FORK_PR_AUTHOR}' permission='${FORK_AUTHOR_PERM:-none}' below write"
                exit 0
                ;;
            esac
          fi
          HAS="$(jq -r --arg t "${TAKEOVER_LABEL}" '[.labels[].name] | index($t) != null' <<< "${PR_INFO}")"
          # The round seed rides as its OWN marker on a separate line, NEVER as
          # a field inside '<!-- takeover-ack engaged -->'. That literal is
          # Full rationale → qwen-autofix.md#af-016
          FROM_MARKER=''
          FROM_NOTE=''
          FROM_NOTE_ZH=''
          FROM_NOTE_REARM=''
          FROM_NOTE_REARM_ZH=''
          # A seeded RE-ARM must not keep the unseeded fresh-window clause:
          # the seed makes the earlier rounds count toward the cap (they ARE
          # the seed), and on a re-arm they were typically already-managed
          # rounds, not pre-takeover review — both wordings flip below.
          REARM_FRESH_CLAUSE=' (previous rounds no longer count toward the cap)'
          REARM_FRESH_CLAUSE_ZH='（此前轮次不再计入上限）'
          if [[ -n "${CMD_FROM}" && "${CMD_FROM}" =~ ^[0-9]{1,2}$ && "${CMD_FROM}" != '0' ]]; then
            FROM_MARKER="$(printf '\n<!-- autofix-round-start %s -->' "${CMD_FROM}")"
            REARM_FRESH_CLAUSE=' (earlier rounds count toward the cap only via this seed)'
            REARM_FRESH_CLAUSE_ZH='（此前轮次仅通过该种子计入上限）'
            FROM_REMAIN="$(( CRITICAL_ONLY_AFTER_ROUND > CMD_FROM ? CRITICAL_ONLY_AFTER_ROUND - CMD_FROM : 0 ))"
            FROM_NOTE="$(printf ' This window'"'"'s round counter starts at %s (the rounds this PR spent in review before takeover), so the Critical-only brake engages after %s more change-producing round(s) instead of a full fresh %s.' "${CMD_FROM}" "${FROM_REMAIN}" "${CRITICAL_ONLY_AFTER_ROUND}")"
            FROM_NOTE_ZH="$(printf '本窗口轮次计数从 %s 起算（即本 PR 托管前已进行的评审轮数），因此再经过 %s 个产生改动的轮次即进入 Critical-only，而非重新计满 %s 轮。' "${CMD_FROM}" "${FROM_REMAIN}" "${CRITICAL_ONLY_AFTER_ROUND}")"
            FROM_NOTE_REARM="$(printf ' This window'"'"'s round counter restarts at %s (rounds already spent on this PR), so the Critical-only brake engages after %s more change-producing round(s) instead of a full fresh %s.' "${CMD_FROM}" "${FROM_REMAIN}" "${CRITICAL_ONLY_AFTER_ROUND}")"
            FROM_NOTE_REARM_ZH="$(printf '本窗口轮次计数从 %s 重启（即本 PR 已消耗的轮次），因此再经过 %s 个产生改动的轮次即进入 Critical-only，而非重新计满 %s 轮。' "${CMD_FROM}" "${FROM_REMAIN}" "${CRITICAL_ONLY_AFTER_ROUND}")"
          fi
          if [[ "${CMD}" == 'add' ]]; then
            if [[ "${HAS}" == 'true' ]]; then
              # Already managed: repeating the command is the ROUND-COUNTER
              # RESET. A fresh engage ack starts a new counting window (only
              # Full rationale → qwen-autofix.md#af-017
              REARM_BODY="$(printf '🔄 Takeover re-armed: the round counter starts a fresh window%s; management continues.%s\n\n<details>\n<summary>中文说明</summary>\n\n🔄 已重新武装：轮次计数开启新窗口%s，托管继续。%s\n\n</details>\n\n<!-- takeover-ack engaged -->%s' "${REARM_FRESH_CLAUSE}" "${FROM_NOTE_REARM}" "${REARM_FRESH_CLAUSE_ZH}" "${FROM_NOTE_REARM_ZH}" "${FROM_MARKER}")"
              gh pr comment "${PR}" --repo "${REPO}" --body "${REARM_BODY}" \
                || { sleep 5; gh pr comment "${PR}" --repo "${REPO}" --body "${REARM_BODY}"; } \
                || { echo "::error::re-arm ack comment failed on #${PR} after one retry — the round window was NOT reset and no seed landed; re-run the command"; exit 1; }
              echo "🔄 re-armed ${TAKEOVER_LABEL} window on #${PR}"
              # Management resumed — the escalation label is stale. 404 is
              # the common case (the PR was never paused).
              if ! NH_ERR="$(gh api -X DELETE "repos/${REPO}/issues/${PR}/labels/$(jq -rn --arg l "${NEEDS_HUMAN_LABEL}" '$l|@uri')" 2>&1)"; then
                [[ "${NH_ERR}" == *"HTTP 404"* ]] || echo "::warning::#${PR}: ${NEEDS_HUMAN_LABEL} removal failed — ${NH_ERR}"
              fi
            else
              # REST for consistency and runner-version independence: `gh pr
              # edit`'s GraphQL lookup requests
              # Full rationale → qwen-autofix.md#af-018
              gh label create "${TAKEOVER_LABEL}" --repo "${REPO}" --color '1D76DB' \
                --description 'Summon the autofix loop to manage this PR (remove to release; needs triage+)' \
                2> /dev/null || true
              gh api -X POST "repos/${REPO}/issues/${PR}/labels" -f "labels[]=${TAKEOVER_LABEL}" > /dev/null
              echo "🏷️ applied ${TAKEOVER_LABEL} to #${PR}"
              # Ack HERE, not via the pull_request:labeled round-trip: that
              # event has been observed to simply not fire (#7999 — the
              # author read the silence as failure and removed the label;
              # #8002 — no ack for hours), and fork label events could never
              # ack at all (they carry no secrets).
              # Full rationale → qwen-autofix.md#af-089
              FORK_NOTE=''
              FORK_NOTE_ZH=''
              if [[ "$(jq -r 'if has("isCrossRepository") then .isCrossRepository else true end' <<< "${PR_INFO}")" != "false" ]]; then
                FORK_NOTE=' This is a fork PR, so the first round comes from the next scheduled scan (usually within minutes).'
                FORK_NOTE_ZH='本 PR 来自 fork，首轮处理将由下一次定时扫描执行（通常几分钟内）。'
              fi
              # Body built ONCE so the retry posts byte-identical text.
              # One retry before the heal-path warning: the seed marker's
              # only copy lives in this body, and the heal ack has no slot
              # to recover it — a transient 5xx must not silently un-seed
              # the window.
              ENGAGE_BODY="$(printf '🤝 Takeover engaged: the autofix loop now manages this PR — it will address new review feedback and resolve base conflicts until the label is removed or the round cap is reached.%s%s Remove the `%s` label (or comment `%s stop`) to release.\n\n<details>\n<summary>中文说明</summary>\n\n🤝 已接管：autofix 循环现在管理此 PR —— 将持续处理新的评审反馈与 base 冲突，直到移除标签或达到轮次上限。%s%s移除 `%s` 标签（或评论 `%s stop`）即可释放。\n\n</details>\n\n<!-- takeover-ack engaged -->%s' "${FORK_NOTE}" "${FROM_NOTE}" "${TAKEOVER_LABEL}" "${TAKEOVER_COMMAND}" "${FORK_NOTE_ZH}" "${FROM_NOTE_ZH}" "${TAKEOVER_LABEL}" "${TAKEOVER_COMMAND}" "${FROM_MARKER}")"
              gh pr comment "${PR}" --repo "${REPO}" --body "${ENGAGE_BODY}" \
                || { sleep 5; gh pr comment "${PR}" --repo "${REPO}" --body "${ENGAGE_BODY}"; } \
                || echo "::warning::engage ack comment failed on #${PR}; the scan's first-pickup ack heals it"
              # Engaged (possibly re-engaging an auto-released PR) — the
              # escalation label is stale. 404 is the common case.
              if ! NH_ERR="$(gh api -X DELETE "repos/${REPO}/issues/${PR}/labels/$(jq -rn --arg l "${NEEDS_HUMAN_LABEL}" '$l|@uri')" 2>&1)"; then
                [[ "${NH_ERR}" == *"HTTP 404"* ]] || echo "::warning::#${PR}: ${NEEDS_HUMAN_LABEL} removal failed — ${NH_ERR}"
              fi
            fi
          else
            if [[ "${HAS}" != 'true' ]]; then
              echo "ℹ️ #${PR} does not carry ${TAKEOVER_LABEL} — nothing to do"
            else
              # REST for the same reason as the add above; the label name is a
              # path segment and contains a slash, so it must be URI-encoded.
              # Full rationale → qwen-autofix.md#af-019
              LBL_DEL_FAILED=false
              if ! REMOVE_ERR="$(gh api -X DELETE "repos/${REPO}/issues/${PR}/labels/$(jq -rn --arg l "${TAKEOVER_LABEL}" '$l|@uri')" 2>&1)"; then
                [[ "${REMOVE_ERR}" == *"HTTP 404"* ]] || { LBL_DEL_FAILED=true; echo "::warning::#${PR}: ${TAKEOVER_LABEL} removal failed — ${REMOVE_ERR}"; }
              fi
              REMOVED_OK=true
              if [[ "${LBL_DEL_FAILED}" == "true" ]]; then
                REMOVED_OK=false
              fi
              if [[ "${REMOVED_OK}" == "true" ]]; then
                echo "🏷️ removed ${TAKEOVER_LABEL} from #${PR}"
              else
                # R7-7: the success claim must not fire when the DELETE did
                # not land — the motivating fix for pr-self-report-label.yml
                # in this very PR is precisely this lying-log shape.
                echo "⚠️ #${PR}: ${TAKEOVER_LABEL} removal did not land — the next /takeover stop retries"
              fi
              # Released means a human is driving — the escalation label is
              # stale. Remove it only when the release landed (REMOVED_OK)
              # AND the PR is not frozen by skip (a frozen PR must keep its
              # only filterable escalation state — R4-3). 404 is the common
              # case (never paused).
              SKIP_STATE="$(jq -r --arg t "${SKIP_LABEL}" '[.labels[].name] | index($t) != null' <<< "${PR_INFO}")"
              if [[ "${REMOVED_OK}" != "true" ]]; then
                echo "::warning::#${PR}: release did not land — keeping ${NEEDS_HUMAN_LABEL}; the next /takeover stop retries both"
              elif [[ "${SKIP_STATE}" == "true" ]]; then
                echo "🧭 ${NEEDS_HUMAN_LABEL} removal skipped: ${SKIP_LABEL} present on #${PR}"
              elif ! NH_ERR="$(gh api -X DELETE "repos/${REPO}/issues/${PR}/labels/$(jq -rn --arg l "${NEEDS_HUMAN_LABEL}" '$l|@uri')" 2>&1)"; then
                [[ "${NH_ERR}" == *"HTTP 404"* ]] || echo "::warning::#${PR}: ${NEEDS_HUMAN_LABEL} removal failed — ${NH_ERR}"
              fi
              # Release ack, direct from the command — the exact mirror of
              # the engage side above, for the same reason: the unlabeled
              # round-trip is the thing we no longer trust, fork unlabeled
              # events can never ack (no secrets), and a non-main release
              # never even reaches the ack job.
              # Full rationale → qwen-autofix.md#af-090
              REL_AUTHOR="$(jq -r '.author.login // ""' <<< "${PR_INFO}")"
              REL_HAS_SKIP="$(jq -r --arg t "${SKIP_LABEL}" '[.labels[].name] | index($t) != null' <<< "${PR_INFO}")"
              if [[ "${REMOVED_OK}" != "true" ]]; then
                # The DELETE did not land — the label is still on and the
                # loop still manages this PR. A "released" ack (and its
                # marker) would record a release that never happened: no
                # unlabeled event fires, nothing retries, and no human
                # re-issues the command. Own the failure and name the retry.
                REL_BODY="$(printf '⚠️ Takeover release did not land: removing the `%s` label failed transiently, so it is still present and the autofix loop keeps managing this PR under the round cap. Comment `%s stop` to retry the release.\n\n<details>\n<summary>中文说明</summary>\n\n⚠️ 释放未生效：移除 `%s` 标签时瞬时失败，标签仍在，autofix 循环仍按轮次上限继续托管此 PR。评论 `%s stop` 可重试释放。\n\n</details>\n\n<!-- takeover-ack release-failed -->' "${TAKEOVER_LABEL}" "${TAKEOVER_COMMAND}" "${TAKEOVER_LABEL}" "${TAKEOVER_COMMAND}")"
              elif [[ "${REL_AUTHOR}" == "${AUTOFIX_BOT}" && "${REL_HAS_SKIP}" == "true" ]]; then
                REL_BODY="$(printf '👋 Takeover mode ended. This bot-authored PR also carries `%s`, which opts it out of standard bot management entirely — nothing will engage it until that label is removed.\n\n<details>\n<summary>中文说明</summary>\n\n👋 接管模式结束。本 bot 创建的 PR 同时带有 `%s`，已完全退出常规 bot 管理 —— 移除该标签前不会有任何介入。\n\n</details>\n\n<!-- takeover-ack released -->' "${SKIP_LABEL}" "${SKIP_LABEL}")"
              elif [[ "${REL_AUTHOR}" == "${AUTOFIX_BOT}" ]]; then
                REL_BODY="$(printf '👋 Takeover mode ended: the raised round cap no longer applies. This is a bot-authored PR, so STANDARD bot management continues under the strict cap (apply `%s` to opt it out entirely). Re-apply `%s` (or comment `%s`) for the raised cap again.\n\n<details>\n<summary>中文说明</summary>\n\n👋 接管模式结束：提升的轮次上限不再适用。这是 bot 创建的 PR，常规 bot 管理仍将继续（严格上限；如需完全退出请打 `%s`）。重新打上 `%s` 标签（或评论 `%s`）可恢复提升上限。\n\n</details>\n\n<!-- takeover-ack released -->' "${SKIP_LABEL}" "${TAKEOVER_LABEL}" "${TAKEOVER_COMMAND}" "${SKIP_LABEL}" "${TAKEOVER_LABEL}" "${TAKEOVER_COMMAND}")"
              else
                REL_BODY="$(printf '👋 Takeover released: the autofix loop will no longer engage this PR (an in-flight round, if any, completes its bounded work). Re-apply `%s` (or comment `%s`) to re-engage.\n\n<details>\n<summary>中文说明</summary>\n\n👋 已释放：autofix 循环不再介入此 PR（在飞的一轮如有，将完成其有界工作）。重新打上 `%s` 标签（或评论 `%s`）即可再次接管。\n\n</details>\n\n<!-- takeover-ack released -->' "${TAKEOVER_LABEL}" "${TAKEOVER_COMMAND}" "${TAKEOVER_LABEL}" "${TAKEOVER_COMMAND}")"
              fi
              gh pr comment "${PR}" --repo "${REPO}" --body "${REL_BODY}" \
                || echo "::warning::release ack comment failed on #${PR}"
            fi
          fi

  # ===========================================================================
  # TAKEOVER ACK — visible confirmation when a maintainer engages or releases
  # a PR via the takeover label. Manual label toggles are explicit user
  # Full rationale → qwen-autofix.md#af-020
  retry-command:
    needs: 'route'
    if: |-
      ${{ needs.route.outputs.retry_pr != '' }}
    # Queued (never cancelled) per PR so two quick /retry comments cannot
    # interleave their marker writes.
    concurrency:
      group: 'qwen-autofix-retry-cmd-${{ needs.route.outputs.retry_pr }}'
      cancel-in-progress: false
    runs-on: 'ubuntu-latest'
    timeout-minutes: 5
    permissions:
      contents: 'read'
    env:
      REPO: '${{ github.repository }}'
      PR: '${{ needs.route.outputs.retry_pr }}'
      GITHUB_TOKEN: '${{ secrets.CI_DEV_BOT_PAT }}'
    steps:
      - name: 'Post re-arm marker'
        run: |-
          # The marker only counts when the AUTOFIX_BOT authored it (both the
          # scan and the address-side recheck filter markers by that login), so
          # a mis-scoped PAT would post a comment that silently does nothing.
          api_error_file="$(mktemp)"
          if ! bot_actor="$(GH_TOKEN="${GITHUB_TOKEN}" gh api user --jq '.login' 2>"${api_error_file}")"; then
            api_error="$(tr '\r\n' ' ' < "${api_error_file}")"
            rm -f "${api_error_file}"
            echo "::error::Failed to verify CI_DEV_BOT_PAT identity with gh api user: ${api_error:-unknown error}."
            exit 1
          fi
          rm -f "${api_error_file}"
          if [[ "${bot_actor}" != "${AUTOFIX_BOT}" ]]; then
            echo "::error::CI_DEV_BOT_PAT authenticates as '${bot_actor:-unknown}'; expected ${AUTOFIX_BOT}."
            exit 1
          fi
          gh pr comment "${PR}" --repo "${REPO}" --body "$(printf '\U0001F504 AutoFix re-armed. The next scan re-reads this PR'"'"'s feedback from the start and the round counter resets. Nothing was deleted — this marker supersedes the evaluation markers above it.\n\n<details>\n<summary>中文说明</summary>\n\n\U0001F504 已重新武装 AutoFix。下一次扫描会从头重新读取本 PR 的反馈，轮次计数也已重置。未删除任何内容 —— 本标记使其上方的评估标记失效。\n\n</details>\n\n<!-- autofix-rearm -->')"
          echo "🔄 re-armed PR #${PR}"
          # Management resumed — the escalation label is stale. 404 is the
          # common case (the PR was never paused).
          # Full rationale → qwen-autofix.md#af-091
          if ! RETRY_INFO="$(gh pr view "${PR}" --repo "${REPO}" --json labels,author 2> /dev/null)"; then
            echo "::warning::#${PR}: label state unreadable — keeping ${NEEDS_HUMAN_LABEL} (fail closed)"
          elif [[ "$(jq -r --arg t "${SKIP_LABEL}" '[.labels[]?.name] | index($t) != null' <<< "${RETRY_INFO}")" == "true" ]]; then
            echo "🧭 ${NEEDS_HUMAN_LABEL} removal skipped: ${SKIP_LABEL} present on #${PR}"
          elif [[ "$(jq -r --arg ab "${AUTOFIX_BOT}" --arg tk "${TAKEOVER_LABEL}" '
                ((.author.login // "") == $ab) or (([.labels[]?.name] | index($tk)) != null)
              ' <<< "${RETRY_INFO}")" != "true" ]]; then
            echo "🧭 keeping ${NEEDS_HUMAN_LABEL} on #${PR}: nothing manages it until re-engaged (no takeover label, not bot-authored)"
          elif ! NH_ERR="$(gh api -X DELETE "repos/${REPO}/issues/${PR}/labels/$(jq -rn --arg l "${NEEDS_HUMAN_LABEL}" '$l|@uri')" 2>&1)"; then
            [[ "${NH_ERR}" == *"HTTP 404"* ]] || echo "::warning::#${PR}: ${NEEDS_HUMAN_LABEL} removal failed — ${NH_ERR}"
          fi

  takeover-ack:
    needs: 'route'
    if: |-
      ${{ needs.route.outputs.takeover_ack != '' }}
    # A delayed or overlapping ack must not race a newer cycle's ack:
    # queued (never cancelled) per-PR execution serializes the runs so
    # the staleness reads below see each predecessor's writes (mirrors
    # takeover-command).
    concurrency:
      group: 'qwen-autofix-takeover-ack-${{ needs.route.outputs.ack_pr }}'
      cancel-in-progress: false
    runs-on: 'ubuntu-latest'
    timeout-minutes: 5
    permissions:
      contents: 'read'
    env:
      REPO: '${{ github.repository }}'
      ACK: '${{ needs.route.outputs.takeover_ack }}'
      PR: '${{ needs.route.outputs.ack_pr }}'
      # The base the ROUTE saw when it refused — naming a live re-read here
      # could report 'main' for a refusal that was decided against something
      # else, so the message stays tied to the decision it explains.
      ACK_BASE: '${{ needs.route.outputs.ack_base }}'
      GITHUB_TOKEN: '${{ secrets.CI_DEV_BOT_PAT }}'
    steps:
      - name: 'Acknowledge takeover state change'
        run: |-
          api_error_file="$(mktemp)"
          if ! bot_actor="$(GH_TOKEN="${GITHUB_TOKEN}" gh api user --jq '.login' 2>"${api_error_file}")"; then
            api_error="$(tr '\r\n' ' ' < "${api_error_file}")"
            rm -f "${api_error_file}"
            echo "::error::Failed to verify CI_DEV_BOT_PAT identity with gh api user: ${api_error:-unknown error}."
            exit 1
          fi
          rm -f "${api_error_file}"
          if [[ "${bot_actor}" != "${AUTOFIX_BOT}" ]]; then
            echo "::error::CI_DEV_BOT_PAT authenticates as '${bot_actor:-unknown}'; expected ${AUTOFIX_BOT}."
            exit 1
          fi
          # Bilingual with COLLAPSED Chinese (project convention), built via
          # printf so no workflow indentation leaks into the markdown (4+
          # leading spaces would render the marker line as a code block).
          # Full rationale → qwen-autofix.md#af-021
          HAS_SKIP=''
          PR_AUTHOR_LIVE=''
          if [[ "${ACK}" != 'base-refused' ]]; then
            if ! PR_STATE_INFO="$(gh pr view "${PR}" --repo "${REPO}" --json labels,author 2> /dev/null)"; then
              echo "::error::could not read PR #${PR} state for takeover ack"
              exit 1
            fi
            HAS_SKIP="$(jq -r --arg t "${SKIP_LABEL}" '[.labels[]?.name] | index($t) != null' <<< "${PR_STATE_INFO}")"
            PR_AUTHOR_LIVE="$(jq -r '.author.login // ""' <<< "${PR_STATE_INFO}")"
          fi
          # R2-4: a delayed release ack can run AFTER takeover was
          # re-applied and the new cycle paused again — a live takeover
          # label means this ack's premise is stale. Post nothing and
          # touch nothing: the new cycle's own events produce their own
          # acks, while a stale run here would delete the new cycle's
          # needs-human and claim a release while takeover is live.
          if [[ "${ACK}" == 'released' && "$(jq -r --arg t "${TAKEOVER_LABEL}" '[.labels[]?.name] | index($t) != null' <<< "${PR_STATE_INFO}")" == "true" ]]; then
            echo "⚠️ released ack skipped on #${PR}: ${TAKEOVER_LABEL} re-applied since the release (stale ack)"
            exit 0
          fi
          # R2-4 mirror for the engaged direction: a delayed engaged ack — a
          # red run re-run later, or overlapping runs from quick label
          # toggles — must not DELETE a fresh cycle's needs-human and must
          # not post a marker that resets the round window (REARM_KEY is
          # the newest engage marker).
          # Full rationale → qwen-autofix.md#af-092
          if [[ "${ACK}" == 'engaged' ]]; then
            if [[ "$(jq -r --arg t "${TAKEOVER_LABEL}" '[.labels[]?.name] | index($t) != null' <<< "${PR_STATE_INFO}")" != "true" ]]; then
              echo "⚠️ engaged ack skipped on #${PR}: ${TAKEOVER_LABEL} removed since the label event (stale ack)"
              exit 0
            fi
            if ! ack_ic="$(gh api "repos/${REPO}/issues/${PR}/comments" --paginate 2> /dev/null)"; then
              echo "::warning::engaged ack skipped on #${PR}: comment history unreadable (fail closed)"
              exit 0
            fi
            if ! ack_ev="$(gh api "repos/${REPO}/issues/${PR}/events" --paginate 2> /dev/null)"; then
              echo "::warning::engaged ack skipped on #${PR}: event history unreadable (fail closed)"
              exit 0
            fi
            ack_aced_ts="$(jq -rs --arg ab "${AUTOFIX_BOT}" '
              add // [] | [ .[] | select((.user.login // "") == $ab)
                | select((.body // "") | contains("<!-- takeover-ack engaged -->"))
                | .created_at ] | max // ""' <<< "${ack_ic}")"
            ack_labeled_ts="$(jq -rs --arg tl "${TAKEOVER_LABEL}" '
              add // [] | [ .[] | select((.event // "") == "labeled")
                | select((.label.name // "") == $tl)
                | .created_at ] | max // ""' <<< "${ack_ev}")"
            if [[ -n "${ack_aced_ts}" && ! "${ack_labeled_ts}" > "${ack_aced_ts}" ]]; then
              echo "⚠️ engaged ack skipped on #${PR}: a bot engage ack already landed after the newest ${TAKEOVER_LABEL} label event (stale ack)"
              exit 0
            fi
          fi
          if [[ "${ACK}" == 'base-refused' ]]; then
            BODY="$(printf '🚫 Takeover not engaged: the loop only manages PRs that target `main`, and this one targets `%s`. A stacked PR moves whenever its base branch does, so "new feedback since the last round" and base-conflict resolution are not well defined until the base lands. Two ways forward: retarget this PR to `main` once the base PR merges — the `%s` label is left in place and the scan lists by label, so the next scan engages it with no re-labelling — or take over the base PR instead.\n\n<details>\n<summary>中文说明</summary>\n\n🚫 未接管：循环只管理以 `main` 为 base 的 PR，而本 PR 的 base 是 `%s`。堆叠 PR 会随 base 分支移动，因此“自上一轮以来的新反馈”与 base 冲突处理都无法良定义。两条路：待 base 的 PR 合入后把本 PR 改为面向 `main` —— `%s` 标签予以保留，扫描按标签枚举，下一次扫描即会自动接管，无需重新打标签；或改为接管 base 那个 PR。\n\n</details>\n\n<!-- takeover-ack base-refused -->' "${ACK_BASE}" "${TAKEOVER_LABEL}" "${ACK_BASE}" "${TAKEOVER_LABEL}")"
          elif [[ "${ACK}" == 'engaged' && "${HAS_SKIP}" == "true" ]]; then
            BODY="$(printf '🚫 Takeover label applied but NOT engaged: this PR carries `%s`, which wins over takeover — the loop will not manage it. Remove `%s` to engage.\n\n<details>\n<summary>中文说明</summary>\n\n🚫 已打接管标签但未生效：本 PR 带有 `%s`，其优先级高于接管，循环不会介入。移除 `%s` 后才会接管。\n\n</details>\n\n<!-- takeover-ack skip-blocked -->' "${SKIP_LABEL}" "${SKIP_LABEL}" "${SKIP_LABEL}" "${SKIP_LABEL}")"
          elif [[ "${ACK}" == 'engaged' ]]; then
            BODY="$(printf '🤝 Takeover engaged: the autofix loop now manages this PR — it will address new review feedback and resolve base conflicts until the label is removed or the round cap is reached. Remove the `%s` label (or comment `%s stop`) to release.\n\n<details>\n<summary>中文说明</summary>\n\n🤝 已接管：autofix 循环现在管理此 PR —— 将持续处理新的评审反馈与 base 冲突，直到移除标签或达到轮次上限。移除 `%s` 标签（或评论 `%s stop`）即可释放。\n\n</details>\n\n<!-- takeover-ack engaged -->' "${TAKEOVER_LABEL}" "${TAKEOVER_COMMAND}" "${TAKEOVER_LABEL}" "${TAKEOVER_COMMAND}")"
          elif [[ "${PR_AUTHOR_LIVE}" == "${AUTOFIX_BOT}" && "${HAS_SKIP}" == "true" ]]; then
            BODY="$(printf '👋 Takeover mode ended. This bot-authored PR also carries `%s`, which opts it out of standard bot management entirely — nothing will engage it until that label is removed.\n\n<details>\n<summary>中文说明</summary>\n\n👋 接管模式结束。本 bot 创建的 PR 同时带有 `%s`，已完全退出常规 bot 管理 —— 移除该标签前不会有任何介入。\n\n</details>\n\n<!-- takeover-ack released -->' "${SKIP_LABEL}" "${SKIP_LABEL}")"
          elif [[ "${PR_AUTHOR_LIVE}" == "${AUTOFIX_BOT}" ]]; then
            BODY="$(printf '👋 Takeover mode ended: the raised round cap no longer applies. This is a bot-authored PR, so STANDARD bot management continues under the strict cap (apply `%s` to opt it out entirely). Re-apply `%s` (or comment `%s`) for the raised cap again.\n\n<details>\n<summary>中文说明</summary>\n\n👋 接管模式结束：提升的轮次上限不再适用。这是 bot 创建的 PR，常规 bot 管理仍将继续（严格上限；如需完全退出请打 `%s`）。重新打上 `%s` 标签（或评论 `%s`）可恢复提升上限。\n\n</details>\n\n<!-- takeover-ack released -->' "${SKIP_LABEL}" "${TAKEOVER_LABEL}" "${TAKEOVER_COMMAND}" "${SKIP_LABEL}" "${TAKEOVER_LABEL}" "${TAKEOVER_COMMAND}")"
          else
            BODY="$(printf '👋 Takeover released: the autofix loop will no longer engage this PR (an in-flight round, if any, completes its bounded work). Re-apply `%s` (or comment `%s`) to re-engage.\n\n<details>\n<summary>中文说明</summary>\n\n👋 已释放：autofix 循环不再介入此 PR（在飞的一轮如有，将完成其有界工作）。重新打上 `%s` 标签（或评论 `%s`）即可再次接管。\n\n</details>\n\n<!-- takeover-ack released -->' "${TAKEOVER_LABEL}" "${TAKEOVER_COMMAND}" "${TAKEOVER_LABEL}" "${TAKEOVER_COMMAND}")"
          fi
          # The escalation label goes stale on a real engage or any release
          # (a human is driving again).
          # Full rationale → qwen-autofix.md#af-093
          if [[ ( "${ACK}" == 'released' || "${ACK}" == 'engaged' ) && "${HAS_SKIP}" != 'true' ]]; then
            if ! NH_ERR="$(gh api -X DELETE "repos/${REPO}/issues/${PR}/labels/$(jq -rn --arg l "${NEEDS_HUMAN_LABEL}" '$l|@uri')" 2>&1)"; then
              [[ "${NH_ERR}" == *"HTTP 404"* ]] || echo "::warning::#${PR}: ${NEEDS_HUMAN_LABEL} removal failed — ${NH_ERR}"
            fi
          fi
          gh pr comment "${PR}" --repo "${REPO}" --body "${BODY}"

  # ===========================================================================
  # REVIEW PHASE (scan) — find one autofix PR with new, unaddressed feedback
  # (or a base conflict) and emit it as a matrix. Cheap: GitHub API only —
  # its single write is the identity-verified, once-per-window cap notice.
  # ===========================================================================
  review-scan:
    needs: 'route'
    if: |-
      ${{ needs.route.outputs.do_review == 'true' }}
    # Same pool decision as route. Full rationale → qwen-autofix.md#af-148
    runs-on: '${{ (github.repository == ''QwenLM/qwen-code'' && vars.MAINTAINER_ECS_RUNNER_DISABLED != ''true'' && (github.event_name != ''pull_request'' && github.event_name != ''pull_request_review'' || github.event.pull_request.head.repo.full_name == github.repository || contains(fromJSON(''["OWNER","MEMBER","COLLABORATOR"]''), github.event.pull_request.author_association))) && fromJSON(''["self-hosted", "linux", "x64", "ecs-qwen"]'') || fromJSON(''["ubuntu-latest"]'') }}'
    timeout-minutes: 15
    # A forced scan can write the same status comment as review-address.
    # Share its per-PR lock so neither writer can erase the other's state.
    # Keep the predicate as narrow as the job's own `if:` — concurrency is
    # evaluated BEFORE it, so without the do_review conjunct a dispatch with
    # `phase: issue` + `pr_number: N` (route emits pr_number unconditionally)
    # would park this skipped job in that PR's shared slot behind a 345-minute
    # address round, stalling the issue phase that `needs` it.
    concurrency:
      group: "qwen-pr-head-write-${{ needs.route.outputs.do_review == 'true' && needs.route.outputs.pr_number || github.run_id }}"
      cancel-in-progress: false
    outputs:
      targets: '${{ steps.scan.outputs.targets }}'
      has_targets: '${{ steps.scan.outputs.has_targets }}'
      enum_failed: '${{ steps.scan.outputs.enum_failed }}'
    env:
      REPO: '${{ github.repository }}'
      # Per-run home for the scan's API dumps. A fixed autofix* name (not
      # mktemp's tmp.*) so this job's age sweep can reclaim it after a hard
      # runner kill; cleaned normally by the always() step.
      # Full rationale → qwen-autofix.md#af-148
      WORKDIR: '/tmp/autofix-scan-${{ github.run_id }}'
    steps:
      - name: 'Scan for PRs with new feedback'
        id: 'scan'
        env:
          GITHUB_TOKEN: '${{ secrets.CI_DEV_BOT_PAT }}'
          FORCED_PR: '${{ needs.route.outputs.pr_number }}'
          DRY_RUN: '${{ needs.route.outputs.dry_run }}'
          EVENT_NAME: '${{ github.event_name }}'
          REVIEW_SENDER: '${{ needs.route.outputs.review_sender }}'
          DISPATCH_SOURCE: "${{ github.event_name == 'workflow_dispatch' && inputs.source || '' }}"
        run: |-
          # gh reroute hardening, mirroring the heavy jobs: pin the host, drop
          # planted tokens, and use a fresh empty config dir — the pool's
          # shared ~/.config/gh is attacker-writable, and a config.yml with an
          # http_unix_socket reroute would swallow this step's gh calls,
          # CI_DEV_BOT_PAT in the Authorization header included.
          # Full rationale → qwen-autofix.md#af-148
          export GH_HOST=github.com
          unset GH_ENTERPRISE_TOKEN GH_TOKEN
          # `export VAR="$(...)"` reports export's status, not the
          # substitution's: a failing mktemp must abort the step, not continue
          # with an empty GH_CONFIG_DIR that gh treats as unset — falling back
          # to the shared ~/.config/gh this preamble closes.
          if ! GH_CONFIG_DIR="$(mktemp -d "${RUNNER_TEMP}/autofix-gh-config.XXXXXX")"; then
            echo "::error::could not create gh config dir; refusing to run gh without reroute hardening"
            exit 1
          fi
          export GH_CONFIG_DIR

          # Every lane that reaches this scan is supposed to hold the PAT:
          # route now declines the one event GitHub is known to run without
          # secrets (a fork PR's own review) before it can set do_review.
          # Full rationale → qwen-autofix.md#af-094
          if [[ -z "${GITHUB_TOKEN}" ]]; then
            echo "::error::CI_DEV_BOT_PAT is empty in this ${EVENT_NAME} scan — every API call would be unauthenticated and the scan would report an empty fleet; check the repository secret"
            exit 1
          fi

          # Reclaim hard-killed scan dirs on this persistent ecs-qwen pool;
          # the agent jobs' sweep now runs on a separate ecs-agent pool.
          find /tmp -maxdepth 1 -name 'autofix*' -mmin +1440 -exec rm -rf {} + 2>/dev/null || true
          # Pre-clean before create, mirroring the heavy jobs: run_id is
          # public and sequential, and mkdir -p alone would succeed over a
          # dir or symlink pre-planted on the shared pool /tmp — the scan's
          # redirects would then land in an attacker-chosen place, and a
          # planted bot-prs.json would feed the forced-PR guard below.
          rm -rf "${WORKDIR}"
          (umask 077; mkdir -p "${WORKDIR}")

          # Fleet visibility: every per-PR decision below also records a row so
          # the run summary shows the WHOLE managed fleet in one table.
          # Reconstructing this by hand (list bot PRs, regex each one's eval
          # markers, cross-check checks and fork state) was the only way to see
          # a stall, so stalls stayed invisible until someone went looking.
          # Inside WORKDIR, not mktemp's /tmp/tmp.*: on a cancelled run the
          # EXIT trap never fires, and only the always() step and the heavy
          # jobs' autofix* age sweep reclaim what this step leaves — neither
          # can reach a tmp.* name.
          FLEET_FILE="${WORKDIR}/fleet.tsv"
          trap 'rm -f "${FLEET_FILE}"; rm -rf "${WORKDIR}"' EXIT
          fleet_row() {
            printf '%s\t%s\t%s\n' "$1" "$2" "$3" >> "${FLEET_FILE}"
          }

          read_forced_pr_meta() {
            local attempt meta
            for attempt in 1 2 3; do
              if meta="$(gh pr view "${FORCED_PR}" --repo "${REPO}" \
                --json number,state,author,headRefName,isCrossRepository,baseRefName,labels,maintainerCanModify 2> /dev/null)" \
                && jq -e 'type == "object"
                  and (.number | type == "number")
                  and (.state | type == "string")
                  and (.author.login | type == "string")
                  and (.headRefName | type == "string")
                  and (.baseRefName | type == "string")
                  and (.isCrossRepository | type == "boolean")
                  and (.labels | type == "array")
                  and (.maintainerCanModify | type == "boolean")' > /dev/null <<< "${meta}"; then
                printf '%s' "${meta}"
                return 0
              fi
              echo "::warning::Forced PR #${FORCED_PR} metadata lookup failed (attempt ${attempt}/3)" >&2
              [[ "${attempt}" -lt 3 ]] && sleep "${attempt}"
            done
            return 1
          }

          # 'none' and HTTP 404 are DEFINITIVE answers, not lookup failures.
          # Full rationale → qwen-autofix.md#af-022
          read_live_permission() {
            local login="$1" attempt permission err result=''
            # An empty login can only 404; skip the call and answer terminally.
            if [[ -z "${login}" ]]; then
              printf 'none'
              return 0
            fi
            err="$(mktemp)"
            for attempt in 1 2 3; do
              if permission="$(gh api "repos/${REPO}/collaborators/${login}/permission" --jq '.permission // ""' 2> "${err}")" \
                && [[ "${permission}" =~ ^(admin|maintain|write|triage|read|none)$ ]]; then
                result="${permission}"
                break
              fi
              if grep -q 'HTTP 404' "${err}"; then
                result='none'
                break
              fi
              # Surface gh's own diagnosis instead of discarding it: a rate
              # limit, an expired PAT and a 5xx all look identical otherwise.
              echo "::warning::Permission lookup failed for ${login} (attempt ${attempt}/3): $(tr '\n' ' ' < "${err}")" >&2
              [[ "${attempt}" -lt 3 ]] && sleep "${attempt}"
            done
            rm -f "${err}"
            [[ -n "${result}" ]] || return 1
            printf '%s' "${result}"
          }

          forced_admission_reason() {
            jq -r --arg ab "${AUTOFIX_BOT}" --arg take "${TAKEOVER_LABEL}" --arg skip "${SKIP_LABEL}" '
              if (.state // "") != "OPEN" then "not_open"
              elif (.baseRefName // "") != "main" then "wrong_base"
              elif ([.labels[]?.name] | index($skip) != null) then "skip_label"
              elif ((((.author.login // "") == $ab) or ([.labels[]?.name] | index($take) != null)) | not) then "unmanaged_author"
              elif (.isCrossRepository == true) and (.maintainerCanModify != true) then "maintainer_edits_disabled"
              elif (((.isCrossRepository == true) or (.isCrossRepository == false)) | not) then "cross_repo_state_missing"
              else "eligible"
              end'
          }

          report_forced_takeover_blocked() {
            local reason="$1" actor status_ids status_id body attempt status_lookup_ok next_en next_zh err
            [[ "${DRY_RUN}" == 'true' ]] && return 0
            [[ "$(jq -r --arg ab "${AUTOFIX_BOT}" --arg take "${TAKEOVER_LABEL}" '
              ((.author.login // "") == $ab) or ([.labels[]?.name] | index($take) != null)
            ' <<< "${META}")" == 'true' ]] || return 0
            case "${reason}" in
              permission_lookup_failed|author_permission_*|maintainer_edits_disabled|cross_repo_state_missing) ;;
              *) return 0 ;;
            esac
            actor=''
            for attempt in 1 2 3; do
              if actor="$(gh api user --jq '.login' 2> /dev/null)" && [[ -n "${actor}" ]]; then
                break
              fi
              echo "::warning::PAT identity lookup failed (attempt ${attempt}/3)" >&2
              [[ "${attempt}" -lt 3 ]] && sleep "${attempt}"
            done
            if [[ "${actor}" != "${AUTOFIX_BOT}" ]]; then
              echo "::warning::Blocked takeover status skipped: PAT authenticates as '${actor:-unknown}'" >&2
              return 1
            fi
            if [[ "${reason}" == 'maintainer_edits_disabled' ]]; then
              next_en='Re-enable maintainer edits on the fork PR to resume takeover.'
              next_zh='请在 fork PR 上重新允许 maintainer edits，以恢复 takeover。'
            elif [[ "${reason}" == author_permission_* ]]; then
              next_en='Grant the fork author write access, or remove the autofix/takeover label, to resume takeover.'
              next_zh='请授予 fork 作者 write 权限，或移除 autofix/takeover 标签，以恢复 takeover。'
            else
              next_en='A later scheduled scan will retry without advancing the feedback watermark.'
              next_zh='后续定时扫描会重试，本次不会推进反馈水位。'
            fi
            # Every other status writer resolves its run link from
            # github.server_url / GITHUB_SERVER_URL. Hardcoding github.com here
            # would make the one link this message exists to surface the only
            # broken one on a GHES or proxied host.
            body="$(printf '<!-- autofix-status -->\n\n⛔ **AutoFix blocked** — takeover admission stopped at `%s`, so no work was started. [View run](%s/%s/actions/runs/%s). %s\n\n<details>\n<summary>中文说明</summary>\n\n⛔ **AutoFix 已阻塞** —— takeover 准入停在 `%s`，因此本轮未开始处理。[查看运行](%s/%s/actions/runs/%s)。%s\n\n</details>' \
              "${reason}" "${GITHUB_SERVER_URL}" "${REPO}" "${GITHUB_RUN_ID}" "${next_en}" \
              "${reason}" "${GITHUB_SERVER_URL}" "${REPO}" "${GITHUB_RUN_ID}" "${next_zh}")"
            status_ids=''
            status_lookup_ok=false
            err="$(mktemp)"
            # Same filter as the sibling upsert in 'Post autofix status comment',
            # including its two guards: `// ""` so a single comment with a null
            # Full rationale → qwen-autofix.md#af-023
            for attempt in 1 2 3; do
              if status_ids="$(set -o pipefail; gh api "repos/${REPO}/issues/${FORCED_PR}/comments" --paginate 2> "${err}" |
                jq -rs --arg ab "${AUTOFIX_BOT}" --arg m '<!-- autofix-status -->' \
                  '.[][] | select((.user.login // "") == $ab) | select((.body // "") | contains($m)) | .id')"; then
                status_lookup_ok=true
                break
              fi
              # Surface gh's own diagnosis instead of discarding it, exactly as
              # read_live_permission does: a rate limit, an expired PAT and a
              # 5xx are indistinguishable from 'attempt 3/3' alone.
              echo "::warning::Takeover status lookup failed for #${FORCED_PR} (attempt ${attempt}/3): $(tr '\n' ' ' < "${err}")" >&2
              [[ "${attempt}" -lt 3 ]] && sleep "${attempt}"
            done
            rm -f "${err}"
            if [[ "${status_lookup_ok}" != 'true' ]]; then
              echo "::warning::Failed to read takeover status comments for #${FORCED_PR}" >&2
              return 1
            fi
            status_id="$(tail -1 <<< "${status_ids}")"
            if [[ -n "${status_id}" ]]; then
              for attempt in 1 2 3; do
                if gh api --method PATCH "repos/${REPO}/issues/comments/${status_id}" -f body="${body}" > /dev/null; then
                  return 0
                fi
                echo "::warning::Failed to update blocked takeover status for #${FORCED_PR} (attempt ${attempt}/3)" >&2
                [[ "${attempt}" -lt 3 ]] && sleep "${attempt}"
              done
            else
              for attempt in 1 2 3; do
                if gh pr comment "${FORCED_PR}" --repo "${REPO}" --body "${body}" > /dev/null; then
                  return 0
                fi
                echo "::warning::Failed to post blocked takeover status for #${FORCED_PR} (attempt ${attempt}/3)" >&2
                [[ "${attempt}" -lt 3 ]] && sleep "${attempt}"
              done
            fi
            return 1
          }

          # Candidate PRs: open, same-repo, targeting main, and either
          # authored by the dev-bot or opted in via TAKEOVER_LABEL. A PR
          # carrying SKIP_LABEL is excluded everywhere — skip wins over
          # takeover when both are present.
          # Full rationale → qwen-autofix.md#af-095
          if [[ -n "${FORCED_PR}" ]]; then
            if ! META="$(read_forced_pr_meta)"; then
              echo "::error::Forced PR #${FORCED_PR} admission blocked: metadata_fetch_failed"
              echo "targets=[]" >> "${GITHUB_OUTPUT}"
              echo "has_targets=false" >> "${GITHUB_OUTPUT}"
              exit 1
            fi
            # Same admission as the scheduled scan below. In-repo PRs fail
            # CLOSED on a missing isCrossRepository field (`.isCrossRepository
            # == false`, never a `// true | not` default — jq's // treats false
            # as empty, so that form is false for EVERY input and silently
            # green-no-op'd all forced dispatches).
            # Full rationale → qwen-autofix.md#af-096
            ADMISSION_REASON="$(forced_admission_reason <<< "${META}")"
            # Fork only: the author must hold write+ RIGHT NOW (the same
            # live-privilege rule the scan applies per candidate and
            # review-address re-checks before pushing). In-repo PRs are gated
            # by author/label alone.
            if [[ "${ADMISSION_REASON}" == 'eligible' && "$(jq -r '.isCrossRepository == true' <<< "${META}")" == 'true' ]]; then
              FORK_AUTHOR="$(jq -r '.author.login // ""' <<< "${META}")"
              if ! FPERM="$(read_live_permission "${FORK_AUTHOR}")"; then
                ADMISSION_REASON='permission_lookup_failed'
                report_forced_takeover_blocked "${ADMISSION_REASON}" \
                  || echo "::error::Forced PR #${FORCED_PR} blocked status update failed"
                echo "::error::Forced PR #${FORCED_PR} admission blocked: ${ADMISSION_REASON}"
                echo "targets=[]" >> "${GITHUB_OUTPUT}"
                echo "has_targets=false" >> "${GITHUB_OUTPUT}"
                exit 1
              fi
              case "${FPERM}" in
                admin|maintain|write)
                  echo "🌿 forced fork PR #${FORCED_PR} admitted (author ${FORK_AUTHOR}=${FPERM})"
                  ;;
                *)
                  ADMISSION_REASON="author_permission_${FPERM:-none}"
                  echo "🧭 forced fork PR #${FORCED_PR} rejected: ${ADMISSION_REASON}"
                  ;;
              esac
            fi
            if [[ "${ADMISSION_REASON}" != 'eligible' ]]; then
              if ! report_forced_takeover_blocked "${ADMISSION_REASON}"; then
                echo "::error::Forced PR #${FORCED_PR} blocked status update failed"
                echo "targets=[]" >> "${GITHUB_OUTPUT}"
                echo "has_targets=false" >> "${GITHUB_OUTPUT}"
                exit 1
              fi
              echo "❌ Forced PR #${FORCED_PR} rejected: ${ADMISSION_REASON}"
              echo "targets=[]" >> "${GITHUB_OUTPUT}"
              echo "has_targets=false" >> "${GITHUB_OUTPUT}"
              exit 0
            fi
            CANDIDATES="${FORCED_PR}"
          else
            gh pr list --repo "${REPO}" --state open --author "${AUTOFIX_BOT}" \
              --base main \
              --limit 100 --json number,headRefName,isCrossRepository,labels,author,maintainerCanModify,updatedAt > "${WORKDIR}/bot-prs.json"
            gh pr list --repo "${REPO}" --state open --label "${TAKEOVER_LABEL}" \
              --base main \
              --limit 100 --json number,headRefName,isCrossRepository,labels,author,maintainerCanModify,updatedAt > "${WORKDIR}/takeover-prs.json"
            # Skip-labeled PRs are excluded HERE, not only at the address
            # gate: that gate discards without writing a marker, so the
            # watermark never advances and an unfiltered scan would re-emit
            # the PR (checkout, npm ci, build) every tick forever.
            # Full rationale → qwen-autofix.md#af-097
            ROT_OFF="$(( ($(date -u +%s) / 600) % 97 ))"
            CANDIDATES="$(jq -rs --arg skip "${SKIP_LABEL}" --argjson off "${ROT_OFF}" \
              'add
               | unique_by(.number)
               | sort_by(-.number)
               | map(select(.isCrossRepository == false))
               | map(select([.labels[]?.name] | index($skip) | not))
               | (if length == 0 then . else (($off % length) as $o | .[$o:] + .[:$o]) end)
               | .[]
               | .number' \
              "${WORKDIR}/bot-prs.json" "${WORKDIR}/takeover-prs.json")"
            # FORK PRs are admitted per candidate: the author must hold write+
            # RIGHT NOW (the same live-privilege rule as the comment command)
            # and the PR must allow maintainer edits (or the bot cannot push).
            # Full rationale → qwen-autofix.md#af-024
            while IFS=$'\t' read -r FPR FAUTHOR; do
              [[ -z "${FPR}" ]] && continue
              if ! FPERM="$(read_live_permission "${FAUTHOR}")"; then
                echo "::warning::Fork takeover candidate #${FPR} blocked: permission_lookup_failed"
                fleet_row "${FPR}" 'blocked' 'permission_lookup_failed'
                continue
              fi
              case "${FPERM}" in
                admin|maintain|write)
                  echo "🌿 fork takeover candidate #${FPR} admitted (author ${FAUTHOR}=${FPERM})"
                  CANDIDATES="${CANDIDATES} ${FPR}"
                  ;;
                *)
                  echo "🧭 fork takeover candidate #${FPR} skipped: author ${FAUTHOR} permission='${FPERM:-none}' below write"
                  fleet_row "${FPR}" 'blocked' "author_permission_${FPERM:-none}"
                  ;;
              esac
            done < <(jq -rs --arg skip "${SKIP_LABEL}" '
              add | unique_by(.number)
              | .[] | select(.isCrossRepository == true)
                    | select(.maintainerCanModify == true)
                    | select([.labels[]?.name] | index($skip) | not)
                    | [(.number | tostring), (.author.login // "")] | @tsv' \
              "${WORKDIR}/bot-prs.json" "${WORKDIR}/takeover-prs.json")
          fi

          # Pending-check staleness bound (invariant across candidate PRs, computed
          # once): ignore a check stuck far past any legitimate runtime. The bound
          # must sit ABOVE real check durations here — review-pr can take ~50m and
          # a review-address JOB runs up to its 345-minute cap — so an active run
          # keeps blocking and is never aged out mid-flight (which would enqueue
          # the PR against a live check and double-process the feedback). 375 holds
          # a 30-minute margin over that cap.
          PENDING_STALE_MIN=375
          PENDING_CUTOFF="$(date -u -d "${PENDING_STALE_MIN} minutes ago" +%Y-%m-%dT%H:%M:%SZ)"

          # Repetition-guard cutoff for the stale-base update marker (invariant
          # across candidate PRs, computed once — same reasoning as
          # PENDING_CUTOFF above). A marker newer than this bounds re-updates
          # to once per 2 hours (CI takes ~40 min; main moves ~13 min).
          BASE_UPDATE_CUTOFF="$(date -u -d '120 minutes ago' +%Y-%m-%dT%H:%M:%SZ)"

          # Base of the auto-update-stale-base decision below. A PR can be red
          # purely because it merged a main that was BROKEN at the time and has
          # Full rationale → qwen-autofix.md#af-025
          MAIN_HEAD="$(gh api "repos/${REPO}/commits/main" --jq '.sha' 2> /dev/null || echo '')"
          MAIN_GREEN_CHECKS='[]'
          if [[ -n "${MAIN_HEAD}" ]]; then
            MAIN_PR_HEAD="$(gh api "repos/${REPO}/commits/${MAIN_HEAD}/pulls" \
              --jq '.[0].head.sha // ""' 2> /dev/null || echo '')"
            if [[ -n "${MAIN_PR_HEAD}" ]]; then
              MAIN_GREEN_CHECKS="$(gh api --paginate "repos/${REPO}/commits/${MAIN_PR_HEAD}/check-runs" \
                --jq '[.check_runs[] | select(.conclusion == "success") | .name]' 2> /dev/null \
                | jq -c -s 'add // [] | unique')" || MAIN_GREEN_CHECKS='[]'
            fi
          fi

          # Review-workflow id, resolved ONCE per scan for the review-in-flight
          # gate below (#8888): during qwen-code-pr-review.yml's 10-minute
          # delay-automatic-review wait the review-pr JOB (and thus its
          # check-run in statusCheckRollup) does not exist yet, so the rollup
          # alone misses a just-triggered review; the runs API sees the run
          # by head SHA before its job starts. Empty on lookup failure — the
          # gate then degrades to the rollup check only (fail-open here,
          # though the BUSY_PRS enumeration below is fail-closed).
          REVIEW_WF_ID="$(gh api "repos/${REPO}/actions/workflows/qwen-code-pr-review.yml" --jq '.id' 2> /dev/null || echo '')"
          REVIEW_RUNS_JSON='{"workflow_runs":[]}'
          if [[ -n "${REVIEW_WF_ID}" ]] \
            && ! REVIEW_RUNS_JSON="$(gh api "repos/${REPO}/actions/workflows/${REVIEW_WF_ID}/runs?per_page=100" 2> /dev/null)"; then
            REVIEW_RUNS_JSON='{"workflow_runs":[]}'
          fi

          # PRs whose review-address is already RUNNING OR QUEUED in any live
          # autofix run must not be re-targeted. Schedule/dispatch runs execute
          # Full rationale → qwen-autofix.md#af-026
          BUSY_PRS=' '
          BUSY_ENUM_OK=1
          LIVE_RUNS=''
          BUSY_ENUM_ERR="$(mktemp)"
          for LIVE_STATUS in in_progress queued pending; do
            # Runs API, not `gh run list --status`: gh validates --status
            # against a client-side allow-list that rejects 'pending' before
            # 2.65.0; the API filter is server-side on every gh version.
            # The REST envelope's field is `id`, NOT the gh-CLI `databaseId`.
            if ! PART="$(gh api "repos/${REPO}/actions/workflows/qwen-autofix.yml/runs?status=${LIVE_STATUS}&per_page=50" \
              --jq '.workflow_runs[].id' 2>> "${BUSY_ENUM_ERR}")"; then
              BUSY_ENUM_OK=0
              break
            fi
            LIVE_RUNS="${LIVE_RUNS}${PART}"$'\n'
          done
          if [[ "${BUSY_ENUM_OK}" == '1' ]]; then
            while IFS= read -r LIVE_RUN; do
              [[ -z "${LIVE_RUN}" ]] && continue
              if ! BUSY_OUT="$(gh run view "${LIVE_RUN}" --repo "${REPO}" --json jobs \
                --jq '.jobs[] | select(.status != "completed") | .name | capture("^review-address \\((?<pr>[0-9]+),") | .pr' 2>> "${BUSY_ENUM_ERR}")"; then
                BUSY_ENUM_OK=0
                break
              fi
              while IFS= read -r BUSY; do
                [[ -n "${BUSY}" ]] && BUSY_PRS="${BUSY_PRS}${BUSY} "
              done <<< "${BUSY_OUT}"
            done <<< "$(sort -u <<< "${LIVE_RUNS}")"
          fi
          if [[ "${BUSY_ENUM_OK}" != '1' ]]; then
            BUSY_ENUM_ERR_TAIL="$(tail -c 200 "${BUSY_ENUM_ERR}" 2> /dev/null | tr '\r\n' '  ')"
            echo "::warning::busy-PR enumeration failed (run list or jobs view unreadable) — failing closed: no scan targets dispatched this pass${BUSY_ENUM_ERR_TAIL:+ — last error: ${BUSY_ENUM_ERR_TAIL}}"
            fleet_row '-' 'fail-closed' "busy enumeration unreadable; scan dispatch skipped this pass (next tick retries)${BUSY_ENUM_ERR_TAIL:+ — last error: ${BUSY_ENUM_ERR_TAIL}}"
            echo "enum_failed=true" >> "${GITHUB_OUTPUT}"
            if [[ -z "${FORCED_PR}" || "${EVENT_NAME}" != 'workflow_dispatch' ]]; then
              CANDIDATES=''
            fi
          fi
          rm -f "${BUSY_ENUM_ERR}"
          [[ "${BUSY_PRS}" != ' ' ]] && echo "🚧 address in flight/queued for PR(s):${BUSY_PRS}"

          # Cutoff for the dispatch-pending marker check in the loop below.
          DISPATCH_CUTOFF="$(date -u -d "${DISPATCH_STATUS_TTL_MINUTES} minutes ago" +%Y-%m-%dT%H:%M:%SZ)"

          # Idle backoff, from the list's own updatedAt (no API call): a
          # candidate with no activity for >24h is inspected on about one
          # Full rationale → qwen-autofix.md#af-027
          IDLE_PRS=' '
          if [[ -f "${WORKDIR}/bot-prs.json" && -f "${WORKDIR}/takeover-prs.json" ]]; then
            IDLE_CUTOFF="$(date -u -d '24 hours ago' +%Y-%m-%dT%H:%M:%SZ)"
            IDLE_PRS=" $(jq -rs --arg cut "${IDLE_CUTOFF}" 'add | unique_by(.number)
              | map(select((.updatedAt // "") != "" and .updatedAt < $cut) | .number | tostring)
              | join(" ")' \
              "${WORKDIR}/bot-prs.json" "${WORKDIR}/takeover-prs.json" 2> /dev/null || echo '') "
          fi
          IDLE_SLOT_NOW="$(( ($(date -u +%s) / 600) % 4 ))"

          TARGETS='[]'
          INSPECTED=0
          for PR in ${CANDIDATES}; do
            # The busy skip is free (in-memory set) — it must not consume
            # inspection budget.
            if [[ "${BUSY_PRS}" == *" ${PR} "* ]]; then
              echo "⏳ #${PR}: review-address already in flight or queued — skipping"
              fleet_row "${PR}" 'busy' 'address run in flight'
              continue
            fi
            # The idle-backoff skip is free too (a bash substring test on
            # the precomputed set, same idiom as the busy skip) — it must
            # not consume inspection budget either.
            if [[ "${IDLE_PRS}" == *" ${PR} "* && "$(( PR % 4 ))" != "${IDLE_SLOT_NOW}" ]]; then
              echo "😴 #${PR}: idle >24h — deferring to its rotation slot (slot $(( PR % 4 )), current ${IDLE_SLOT_NOW}; inspected ~1 scan in 4)"
              fleet_row "${PR}" 'idle-backoff' 'idle >24h; inspected ~1 scan in 4 (median ~2h, p90 ~6h)'
              continue
            fi
            INSPECTED=$(( INSPECTED + 1 ))
            if [[ "${INSPECTED}" -gt "${MAX_CANDIDATE_INSPECTIONS}" ]]; then
              echo "🧮 candidate-inspection budget (${MAX_CANDIDATE_INSPECTIONS}) reached — deferring the rest to the next scan"
              fleet_row '-' 'deferred' "candidate-inspection budget (${MAX_CANDIDATE_INSPECTIONS}) reached — remaining candidates deferred to next scan"
              break
            fi
            # One PR fetch for the branch name, check rollup, creation time
            # (the watermark floor below), labels (the effective round
            # cap), and author (the cap branch's bot-author exemption) —
            # avoids extra round-trips per candidate PR.
            PR_META="$(gh pr view "${PR}" --repo "${REPO}" \
              --json headRefName,headRefOid,statusCheckRollup,createdAt,labels,isCrossRepository,headRepositoryOwner,headRepository,author 2> /dev/null || echo '{}')"
            HEAD_REPO_FULL="${REPO}"
            if [[ "$(jq -r '.isCrossRepository // false' <<< "${PR_META}")" == "true" ]]; then
              HR_OWNER="$(jq -r '.headRepositoryOwner.login // ""' <<< "${PR_META}")"
              HR_NAME="$(jq -r '.headRepository.name // ""' <<< "${PR_META}")"
              # Component-wise: a deleted fork yields owner XOR name empty,
              # which a joined '/' test would wave through into a red fetch.
              if [[ -z "${HR_OWNER}" || -z "${HR_NAME}" ]]; then
                echo "⚠️ #${PR}: fork head repository unresolved (owner='${HR_OWNER}' name='${HR_NAME}') — skipping"
                fleet_row "${PR}" 'skipped' "fork head unresolved (owner='${HR_OWNER}' name='${HR_NAME}')"
                continue
              fi
              HEAD_REPO_FULL="${HR_OWNER}/${HR_NAME}"
            fi
            BRANCH="$(jq -r '.headRefName // ""' <<< "${PR_META}")"
            HAS_TAKEOVER="$(jq -r --arg t "${TAKEOVER_LABEL}" '[.labels[]?.name] | index($t) != null' <<< "${PR_META}")"
            # The candidate snapshot filtered skip once, but this per-PR fetch
            # is FRESHER — a skip label applied mid-scan must still win (the
            # address job re-checks it live once more before any mutation).
            if [[ "$(jq -r --arg t "${SKIP_LABEL}" '[.labels[]?.name] | index($t) != null' <<< "${PR_META}")" == "true" ]]; then
              echo "⏭️ #${PR}: ${SKIP_LABEL} label present (applied mid-scan) — skipping"
              fleet_row "${PR}" 'skipped' "${SKIP_LABEL} label present"
              continue
            fi
            # Dispatch-pending marker: a scan that dispatched this PR within
            # DISPATCH_STATUS_TTL_MINUTES may still be building its CLI
            # bundle — its matrix leg does not exist yet, so the live-run
            # busy enumeration above cannot see it.
            # Full rationale → qwen-autofix.md#af-098
            if jq -e --arg ctx "${DISPATCH_STATUS_CONTEXT}" --arg cut "${DISPATCH_CUTOFF}" '
                [.statusCheckRollup[]? | select(.__typename == "StatusContext")
                 | select(.context == $ctx) | select(.state == "PENDING")
                 | select((.startedAt // "") > $cut)] | length > 0' <<< "${PR_META}" > /dev/null; then
              echo "⏳ #${PR}: dispatch pending (marker fresher than ${DISPATCH_STATUS_TTL_MINUTES}m, leg not materialized yet) — skipping"
              fleet_row "${PR}" 'busy' "dispatch-pending marker live (<${DISPATCH_STATUS_TTL_MINUTES}m)"
              continue
            fi
            EFF_MAX_ROUNDS="${MAX_ROUNDS}"
            [[ "${HAS_TAKEOVER}" == "true" ]] && EFF_MAX_ROUNDS="${TAKEOVER_MAX_ROUNDS}"
            if [[ -z "${BRANCH}" ]]; then
              # Metadata fetch failed (transient API error / rate limit). Skip rather
              # than fall through with an empty branch, which would make the address
              # job fail at `git checkout -B "" origin/` and post a misleading "could
              # not start evaluation" handoff. Retried on the next scan. (This also
              # means CREATED_WM below is only reached with a populated PR_META.)
              echo "⚠️ #${PR}: could not fetch PR metadata (API error); skipping until next scan"
              fleet_row "${PR}" 'unknown' 'PR metadata unreadable (API error)'
              continue
            fi
            # Extract issue number: autofix/issue-<N> → N; otherwise use PR number.
            if [[ "${BRANCH}" == "${BRANCH_PREFIX}"* ]]; then
              ISSUE="${BRANCH#"${BRANCH_PREFIX}"}"
            else
              ISSUE="${PR}"
            fi
            CHECKS_JSON="$(jq -c '.statusCheckRollup // []' <<< "${PR_META}")"
            PR_HEAD_OID="$(jq -r '.headRefOid // ""' <<< "${PR_META}")"

            # Review-in-flight gate (#8888): NON_BLOCKING_CHECKS keeps an
            # in-flight review-pr from blocking the FEEDBACK gate (its
            # Full rationale → qwen-autofix.md#af-028
            REVIEW_PR_LIVE="$(jq -r '
              [ .[]
                | select((((.status // .state // "") | IN("QUEUED", "IN_PROGRESS", "PENDING", "WAITING", "REQUESTED")) and ((.name // "") == "review-pr") and ((.workflowName // "") == "🧐 Qwen Pull Request Review"))) ]
              | length > 0
            ' <<< "${CHECKS_JSON}")"
            REVIEW_RUN_STARTED_AT=""
            if [[ "${REVIEW_PR_LIVE}" != "true" && -n "${REVIEW_WF_ID}" && -n "${PR_HEAD_OID}" ]]; then
              # Delay-window fallback: a review run parked BEFORE its job
              # starts (the 10-minute environment wait) has no review-pr
              # check-run yet, but a push now would still supersede it
              # (#10110: a parked or pre-threshold run yields to the push —
              # its work is discarded exactly as a cancel did).
              # Full rationale → qwen-autofix.md#af-099
              REVIEW_RUN_STARTED_AT="$(jq -r --arg wf "${REVIEW_WF_ID}" --arg pr "${PR}" --arg head "${PR_HEAD_OID}" '
                [ .workflow_runs[]?
                  | select((.workflow_id | tostring) == $wf)
                  | select((.event // "") == "pull_request_target")
                  | select((.status // "") | IN("queued", "waiting", "pending", "requested", "in_progress"))
                  | select(((.head_sha // "") == $head) or any(.pull_requests[]?; (.number | tostring) == $pr))
                  | (.run_started_at // .created_at // "") ]
                | map(select(. != "")) | sort | last // ""
              ' <<< "${REVIEW_RUNS_JSON}")"
              if [[ -n "${REVIEW_RUN_STARTED_AT}" ]]; then
                REVIEW_PR_LIVE="true"
              fi
            fi

            # Auto-rerun a check that died on INFRASTRUCTURE, not the code (see
            # INFRA_FAILURE_SIGNATURES).
            # Full rationale → qwen-autofix.md#af-100
            if [[ -n "${PR_HEAD_OID}" && "${REVIEW_PR_LIVE}" != "true" ]] && jq -e 'any(.[]; ((.conclusion // .state // "") | IN("FAILURE","FAILED","ERROR","TIMED_OUT","ACTION_REQUIRED")) and (((.workflowName // "") != "Qwen Autofix") or ((.name // "") | startswith("review-address"))))' <<< "${CHECKS_JSON}" > /dev/null 2>&1; then
              RERAN_INFRA=false
              # Failed check-runs on this head, with their run id and annotation
              # count — fetched once. External statuses (no check-run) are absent
              # here, which is fine: only Actions runs can be reran.
              FAILED_CRS="$(gh api --paginate "repos/${REPO}/commits/${PR_HEAD_OID}/check-runs" \
                --jq '.check_runs[] | select(.conclusion == "failure")
                  | select(((.output.annotations_count // 0) > 0))
                  | [(.id | tostring), (.details_url // ""), (.name // "")] | @tsv' 2> /dev/null | sort -u || true)"
              while IFS=$'\t' read -r CR_ID DETAILS_URL CR_NAME; do
                [[ -n "${CR_ID}" && "${DETAILS_URL}" == *"/actions/runs/"* ]] || continue
                RUN_ID="${DETAILS_URL#*/actions/runs/}"
                RUN_ID="${RUN_ID%%/*}"
                [[ "${RUN_ID}" =~ ^[0-9]+$ ]] || continue
                ANNS="$(gh api --paginate "repos/${REPO}/check-runs/${CR_ID}/annotations" \
                  --jq '[.[].message] | join("\n")' 2> /dev/null || true)"
                grep -qiE "${INFRA_FAILURE_SIGNATURES}" <<< "${ANNS}" || continue
                RUN_META="$(gh api "repos/${REPO}/actions/runs/${RUN_ID}" --jq '[(.run_attempt // 0), (.name // "")] | @tsv' 2> /dev/null || printf '0\t')"
                ATTEMPT="${RUN_META%%$'\t'*}"
                WF_NAME="${RUN_META#*$'\t'}"
                # Mirror the gate's self-trigger guard: skip Qwen Autofix runs
                # unless the check is a review-address job.
                if [[ "${WF_NAME}" == "Qwen Autofix" && "${CR_NAME}" != review-address* ]]; then
                  continue
                fi
                if [[ "${ATTEMPT}" == '1' ]]; then
                  if gh api -X POST "repos/${REPO}/actions/runs/${RUN_ID}/rerun-failed-jobs" > /dev/null 2>&1; then
                    echo "♻️ #${PR}: a check died on infrastructure (run ${RUN_ID}, attempt 1) — reran its failed jobs; not the PR's code"
                    fleet_row "${PR}" 'infra-reran' "infra failure (run ${RUN_ID}) reran once"
                    RERAN_INFRA=true
                    break
                  fi
                else
                  echo "⚠️ #${PR}: infra failure persisted after a rerun (run ${RUN_ID}, attempt ${ATTEMPT}) — leaving for a human"
                fi
              done <<< "${FAILED_CRS}"
              [[ "${RERAN_INFRA}" == 'true' ]] && continue
            fi

            # startedAt is the only staleness clock: a check blocks only if it
            # started within the bound; one with no startedAt (queued, not yet
            # running) is not blocking (the next scan re-checks once it starts).
            # Full rationale → qwen-autofix.md#af-101
            HAS_PENDING_CHECKS="$(jq -r --arg cut "${PENDING_CUTOFF}" \
              --argjson nonblocking "${NON_BLOCKING_CHECKS}" \
              --arg ctx "${DISPATCH_STATUS_CONTEXT}" '
              [ .[]
                | select((.status // .state // "") | IN("QUEUED", "IN_PROGRESS", "PENDING", "WAITING", "REQUESTED"))
                | select(((.workflowName // "") != "Qwen Autofix") or (((.name // "") | startswith("review-address"))))
                | select((.name // "") as $n | ($nonblocking | index($n)) == null)
                | select((.context // "") != $ctx)
                | select((.startedAt // $cut) > $cut) ]
              | length > 0
            ' <<< "${CHECKS_JSON}")"
            if [[ "${HAS_PENDING_CHECKS}" == "true" ]]; then
              echo "⏳ #${PR}: active checks in flight; skipping until they finish (only checks stuck >${PENDING_STALE_MIN}m past their start are treated as dead and ignored)"
              fleet_row "${PR}" 'waiting' 'active checks in flight'
              continue
            fi
            if [[ "${REVIEW_PR_LIVE}" == "true" ]]; then
              echo "🔍 #${PR}: review-pr in flight on this head — holding this round so the push cannot cancel it (#8888)"
              fleet_row "${PR}" 'review-in-flight' 'review-pr live on head; round deferred'
              # Ack-on-defer (#8888): a real-time human review routed this
              # scan straight here, but the gate holds every mutation — from
              # the human's seat the bot read their review and then did
              # nothing.
              # Full rationale → qwen-autofix.md#af-102
              if [[ "${EVENT_NAME}" == 'pull_request_review' && "${DRY_RUN}" != "true" && "${REVIEW_SENDER}" != "${REVIEW_BOT}" ]]; then
                REVIEW_STARTED_AT="$(jq -r '
                  [ .[]
                    | select((((.status // .state // "") | IN("QUEUED", "IN_PROGRESS", "PENDING", "WAITING", "REQUESTED")) and ((.name // "") == "review-pr") and ((.workflowName // "") == "🧐 Qwen Pull Request Review")))
                    | (.startedAt // "")
                    | select(. != "") ] | first // ""' <<< "${CHECKS_JSON}")"
                [[ -z "${REVIEW_STARTED_AT}" ]] && REVIEW_STARTED_AT="${REVIEW_RUN_STARTED_AT}"
                # An empty key (a queued check with no startedAt yet) would
                # make the marker match EVERY future deferral — skip the ack
                # this scan rather than arm a permanently-dead dedup.
                if [[ -z "${REVIEW_STARTED_AT}" ]]; then
                  echo "🕐 #${PR}: deferred-review ack skipped: live review-pr check has no startedAt yet (queued); a later scan acks once it starts"
                else
                  DEFER_ACKS="$(gh api "repos/${REPO}/issues/${PR}/comments" --paginate \
                    | jq -r --arg ab "${AUTOFIX_BOT}" '.[] | select((.user.login // "") == $ab) | .body // ""' 2> /dev/null || true)"
                  if grep -qF "<!-- autofix-review-deferred ${REVIEW_STARTED_AT} -->" <<< "${DEFER_ACKS}"; then
                    echo "🕐 #${PR}: deferred-review ack already posted for this review run"
                  else
                    if [[ -z "${SCAN_BOT_ACTOR:-}" ]]; then
                      SCAN_BOT_ACTOR="$(gh api user --jq '.login' 2> /dev/null || echo 'unknown')"
                    fi
                    if [[ "${SCAN_BOT_ACTOR}" != "${AUTOFIX_BOT}" ]]; then
                      echo "::warning::#${PR}: deferred-review ack skipped: PAT authenticates as '${SCAN_BOT_ACTOR}', expected ${AUTOFIX_BOT}"
                    else
                      gh pr comment "${PR}" --repo "${REPO}" --body "$(printf '🕐 Review received — an automatic review of the current head is still running, so this round is held until it lands (a push now would cancel it and discard its work, #8888). Your feedback stays queued for the next eligible round.\n\n<details>\n<summary>中文说明</summary>\n\n🕐 已收到评审 —— 当前 head 上仍有一轮自动 review 在运行，本轮暂缓（现在推送会取消该 review 并丢弃其工作，#8888）。反馈保持排队，等待下一次可运行的轮次处理。\n\n</details>\n\n<!-- autofix-review-deferred %s -->' "${REVIEW_STARTED_AT}")" > /dev/null 2>&1 \
                        || echo "::warning::#${PR}: deferred-review ack failed — the dedup marker is NOT posted (a later scan may ack again)"
                    fi
                  fi
                fi
              fi
            fi
            # Pre-first-eval floor: the PR's IMMUTABLE creation time. Feedback
            # cannot predate the PR, and unlike the head commit date this never
            # advances when the branch is synced with main ("Update branch"/base
            # merge), so an early base-sync merge cannot bury a comment made before
            # the first eval.
            # Full rationale → qwen-autofix.md#af-103
            CREATED_WM="$(jq -r '.createdAt // ""' <<< "${PR_META}")"

            # PAGINATION NOTE: gh >= v2.31.0 merges all pages of a REST array
            # endpoint into ONE flat JSON array (cli/cli#7190), so the WORKDIR
            # files below are single arrays on every hosted runner. The jq
            # normalizer pipes are retained as cheap defense-in-depth:
            # idempotent over a flat array, and they would also normalize the
            # per-page "[…][…]" shape that older gh versions emitted.
            gh api "repos/${REPO}/issues/${PR}/comments" --paginate \
              | jq -s 'add // []' > "${WORKDIR}/ic.json"
            # First-pickup engage ack: fork label events carry no secrets and
            # manual labels may race the ack job, so a takeover PR with NO
            # Full rationale → qwen-autofix.md#af-029
            NEED_ENGAGE_ACK='false'
            if [[ "${HAS_TAKEOVER}" == "true" ]]; then
              LAST_ENGAGE_ACK_TS="$(jq -rs --arg ab "${AUTOFIX_BOT}" '
                add | [.[] | select((.user.login // "") == $ab)
                     | select(.body // "" | contains("<!-- takeover-ack engaged -->"))
                     | .created_at] | sort | last // ""' "${WORKDIR}/ic.json")"
              PR_EVENTS_OK=''
              if gh api "repos/${REPO}/issues/${PR}/events" --paginate 2> /dev/null \
                | jq -s 'add // []' > "${WORKDIR}/pr-events.json"; then
                PR_EVENTS_OK=true
              else
                echo '[]' > "${WORKDIR}/pr-events.json"
              fi
              LAST_LABELED_TS="$(jq -rs --arg lb "${TAKEOVER_LABEL}" '
                add | [.[] | select(.event == "labeled")
                     | select((.label.name // "") == $lb)
                     | .created_at] | sort | last // ""' "${WORKDIR}/pr-events.json")"
              if [[ -z "${LAST_ENGAGE_ACK_TS}" ]]; then
                NEED_ENGAGE_ACK='true'
                # Grace windows keyed by WHO owns the missing ack, read from
                # the label event's actor (pr-events.json is already here).
                # Full rationale → qwen-autofix.md#af-030
                LAST_LABELED_BY="$(jq -rs --arg lb "${TAKEOVER_LABEL}" '
                  add | [.[] | select(.event == "labeled")
                       | select((.label.name // "") == $lb)]
                  | sort_by(.created_at) | last | .actor.login // ""' "${WORKDIR}/pr-events.json")"
                if [[ "${LAST_LABELED_BY}" == "${AUTOFIX_BOT}" ]]; then
                  if [[ -n "${LAST_LABELED_TS}" && "${LAST_LABELED_TS}" > "$(date -u -d '45 seconds ago' +%Y-%m-%dT%H:%M:%SZ)" ]]; then
                    echo "🧭 engage ack deferred for #${PR}: command-applied label <45s ago — the command's own ack is in flight"
                    NEED_ENGAGE_ACK='false'
                  fi
                elif [[ "$(jq -r '.isCrossRepository // false' <<< "${PR_META}")" != "true" ]] \
                  && [[ -n "${LAST_LABELED_TS}" && "${LAST_LABELED_TS}" > "$(date -u -d '3 minutes ago' +%Y-%m-%dT%H:%M:%SZ)" ]]; then
                  echo "🧭 engage ack deferred for #${PR}: in-repo label applied <3m ago — the ack job owns it"
                  NEED_ENGAGE_ACK='false'
                fi
              else
                if [[ -n "${LAST_LABELED_TS}" && "${LAST_LABELED_TS}" > "${LAST_ENGAGE_ACK_TS}" ]]; then
                  NEED_ENGAGE_ACK='true'
                fi
              fi
            fi
            if [[ "${NEED_ENGAGE_ACK}" == "true" && "${DRY_RUN}" == "true" ]]; then
              echo "🧪 DRY-RUN: would post engage ack on #${PR} (window key untouched)"
              NEED_ENGAGE_ACK='false'
            fi
            if [[ "${NEED_ENGAGE_ACK}" == "true" ]]; then
              if [[ -z "${SCAN_BOT_ACTOR:-}" ]]; then
                SCAN_BOT_ACTOR="$(gh api user --jq '.login' 2> /dev/null || echo 'unknown')"
              fi
              if [[ "${SCAN_BOT_ACTOR}" == "${AUTOFIX_BOT}" ]]; then
                if gh pr comment "${PR}" --repo "${REPO}" --body "$(printf '🤝 Takeover engaged: the autofix loop now manages this PR — it will address new review feedback and resolve base conflicts until the label is removed or the round cap is reached. Remove the `%s` label (or comment `%s stop`) to release.\n\n<details>\n<summary>中文说明</summary>\n\n🤝 已接管：autofix 循环现在管理此 PR —— 将持续处理新的评审反馈与 base 冲突，直到移除标签或达到轮次上限。移除 `%s` 标签（或评论 `%s stop`）即可释放。\n\n</details>\n\n<!-- takeover-ack engaged -->' "${TAKEOVER_LABEL}" "${TAKEOVER_COMMAND}" "${TAKEOVER_LABEL}" "${TAKEOVER_COMMAND}")"; then
                  # Engaged — the escalation label is stale. 404 is the
                  # common case (the PR was never paused).
                  if ! NH_ERR="$(gh api -X DELETE "repos/${REPO}/issues/${PR}/labels/$(jq -rn --arg l "${NEEDS_HUMAN_LABEL}" '$l|@uri')" 2>&1)"; then
                    [[ "${NH_ERR}" == *"HTTP 404"* ]] || echo "::warning::#${PR}: ${NEEDS_HUMAN_LABEL} removal failed — ${NH_ERR}"
                  fi
                  # Atomic re-fetch: ic.json already holds a successful full
                  # fetch from above; a truncated stream must not leave it 0
                  # bytes (jq -s fails only AFTER the redirect truncates) —
                  # MARKERS on an empty file exits 0 with '' and the round
                  # cap silently resets. Empty history must still never
                  # masquerade as "no markers", so swap in the fresh copy
                  # only when the whole pipeline succeeded.
                  if gh api "repos/${REPO}/issues/${PR}/comments" --paginate \
                    | jq -s 'add // []' > "${WORKDIR}/ic.next.json"; then
                    mv "${WORKDIR}/ic.next.json" "${WORKDIR}/ic.json"
                  else
                    echo "::warning::ic re-fetch after engage ack failed for #${PR}; window anchors on a later scan"
                  fi
                else
                  echo "::warning::first-pickup engage ack failed for #${PR}; window anchors on a later scan"
                fi
              else
                echo "::warning::engage ack skipped: PAT authenticates as '${SCAN_BOT_ACTOR}'"
              fi
            fi
            # Eval markers the bot left after a previous evaluation carry the
            # newest feedback timestamp it already considered, plus the round.
            # Only our own comments are trusted, so a spoofed marker is ignored.
            # NOTE: this regex is POSITIONAL — group .[0]=ts, .[2]=round — and must
            # match the marker string emitted at every write site verbatim (search
            # `autofix-eval ts=`: the push/report success, noop, and handoff steps).
            # Inserting or reordering a field here or at any write site silently
            # corrupts round tracking; keep the `ts= acted= round=` order in lockstep.
            MARKERS="$(jq -c --arg ab "${AUTOFIX_BOT}" '
              [ .[] | select((.user.login // "") == $ab) | . as $c | ($c.body // "")
                | [ scan("<!-- autofix-eval ts=([^ ]+) acted=([^ ]+) round=([0-9]+)(?: win=([^ ]+))? -->") ] | .[]
                | {ts: .[0], round: (.[2] | tonumber), win: (.[3] // "none"), at: ($c.created_at // "")} ]' "${WORKDIR}/ic.json")"
            # The watermark is GLOBAL across all markers — feedback already
            # evaluated stays evaluated, no matter how counting windows move.
            # The terminal-handoff SENTINEL ts is excluded: it is a flag, not
            # an evaluation time, and letting it into the watermark would
            # filter all future feedback forever — making a re-arm after a
            # terminal handoff dead on arrival (terminal skipping itself is
            # round-based and window-scoped, so re-arm properly clears it).
            REARM_AT="$(jq -r --arg ab "${AUTOFIX_BOT}" '
              [ .[] | select((.user.login // "") == $ab)
                | select((.body // "") | contains("<!-- autofix-rearm -->"))
                | .created_at ] | max // ""' "${WORKDIR}/ic.json")"
            # The head this PR's red checks were last reported against. Carried
            # as its OWN marker inside the eval comment rather than a new field
            # on autofix-eval, so none of the ts/acted/round parsers change —
            # and the comment still matches the eval filter, so the agent never
            # sees it as feedback.
            RED_HEAD="$(jq -r --arg ab "${AUTOFIX_BOT}" '
              [ .[] | select((.user.login // "") == $ab) | . as $c | ($c.body // "")
                | [ scan("<!-- autofix-redcheck head=([0-9a-f]+) -->") ] | .[]
                | {sha: .[0], at: ($c.created_at // "")} ]
              | sort_by(.at) | last | .sha // ""' "${WORKDIR}/ic.json")"
            # A maintainer '@qwen-code /retry' posts that marker to say
            # "evaluate this feedback again": markers written BEFORE it stop
            # holding the watermark down. This is the sanctioned exception to
            # the global rule above, and it replaces the old recovery -
            # deleting the bot's marker comment by hand, which erased the
            # audit trail and needed raw API access to do at all.
            EVAL_WM="$(jq -r --arg rearm "${REARM_AT}" '
              map(select($rearm == "" or (.at > $rearm)))
              | map(.ts) | map(select(. != "9999-12-31T23:59:59Z")) | max // ""' <<< "${MARKERS}")"
            # ROUND counting is windowed by KEY EQUALITY, not timestamps: the
            # current window key is the created_at of the latest
            # '<!-- takeover-ack engaged -->' comment ('none' before any takeover).
            # Full rationale → qwen-autofix.md#af-104
            REARM_KEY="$(jq -r --arg ab "${AUTOFIX_BOT}" '
              [ .[] | select((.user.login // "") == $ab)
                | select(((.body // "") | contains("<!-- takeover-ack engaged -->"))
                    or ((.body // "") | contains("<!-- autofix-rearm -->")))
                | .created_at ] | max // "none"' "${WORKDIR}/ic.json")"
            # Seed for THIS window, from the '<cmd> from N' marker carried by
            # the comment that IS the window key — so it is window-scoped for
            # free, exactly like the key itself: a later /retry or a bare
            # /takeover opens a window whose anchor has no marker and the seed
            # returns to 0.
            # Full rationale → qwen-autofix.md#af-105
            ROUND_START="$(jq -r --arg ab "${AUTOFIX_BOT}" --arg key "${REARM_KEY}" '
              [ .[] | select((.user.login // "") == $ab)
                | select((.created_at // "") == $key)
                | ((.body // "") | [ scan("<!-- autofix-round-start ([0-9]{1,2}) -->") ] | .[] | .[0]) ]
              | last // "0"' "${WORKDIR}/ic.json" 2> /dev/null || echo '0')"
            [[ "${ROUND_START}" =~ ^[0-9]{1,2}$ ]] || ROUND_START=0
            # Clamp strictly below the effective cap. The seed must be able to
            # bring the Critical-only brake forward; it must NEVER be able to
            # park a PR at its round cap on the very round it is taken over
            # (which would stop the loop instead of starting it), and a seed
            # is honoured on any PR whose window anchor carries the marker —
            # including one whose takeover label was later removed, dropping
            # EFF_MAX_ROUNDS back to the strict 10.
            if [[ "${EFF_MAX_ROUNDS:-0}" -gt 0 && "${ROUND_START}" -ge "${EFF_MAX_ROUNDS}" ]]; then
              echo "🔢 #${PR}: round seed ${ROUND_START} clamped to $(( EFF_MAX_ROUNDS - 1 )) (effective cap ${EFF_MAX_ROUNDS})"
              ROUND_START=$(( EFF_MAX_ROUNDS - 1 ))
            fi
            ROUND="$(jq -r --arg key "${REARM_KEY}" --argjson start "${ROUND_START}" 'map(select(.win == $key)) | map(.round) | max // $start' <<< "${MARKERS}")"
            # No mention of the Critical-only threshold here, deliberately: the
            # scan must stay ignorant of that brake. It keeps SELECTING fresh
            # suggestions so a no-op report can still advance the watermark;
            # only prepare hides them from the agent. A test pins the scan
            # against the string.
            [[ "${ROUND_START}" != '0' ]] && echo "🔢 #${PR}: window seeded at round ${ROUND_START} → effective round ${ROUND}/${EFF_MAX_ROUNDS}"

            # Effective watermark = what the agent has actually evaluated (its last
            # eval marker's newest-feedback timestamp), NOT the last push. A bot
            # fix always writes a marker, so a real fix advances this; a base-sync
            # "Merge branch 'main'" push (or any commit that did not evaluate
            # feedback) does NOT, so it can never bury unaddressed maintainer
            # comments under the watermark. Before the first evaluation there is no
            # marker, so fall back to the PR creation floor.
            if [[ -n "${EVAL_WM}" ]]; then
              EFF_WM="${EVAL_WM}"
            else
              EFF_WM="${CREATED_WM}"
            fi

            if [[ "${ROUND}" -ge "${EFF_MAX_ROUNDS}" ]]; then
              echo "🚧 #${PR}: hit the round cap (${ROUND}/${EFF_MAX_ROUNDS}) — leaving for a human"
              fleet_row "${PR}" 'round-capped' "round ${ROUND}/${EFF_MAX_ROUNDS} - needs a human or @qwen-code /retry"
              # A FORCED dispatch refused here answers OUT LOUD. Observed on
              # #7836: the fleet shepherd detected a merge conflict, posted
              # Full rationale → qwen-autofix.md#af-031
              FORK_BRIDGE_VERIFIED=false
              if [[ "${DISPATCH_SOURCE}" == 'fork-bridge' ]]; then
                BRIDGE_CUTOFF="$(date -u -d '360 minutes ago' +%Y-%m-%dT%H:%M:%SZ)"
                if BRIDGE_RUNS="$(gh run list --repo "${REPO}" --workflow qwen-autofix-fork-bridge.yml --limit 20 --json conclusion,createdAt,displayTitle 2> /dev/null)" \
                  && jq -e --arg pr "${PR}" --arg cutoff "${BRIDGE_CUTOFF}" '
                    [ .[]
                      | select(.conclusion == "success" and ((.createdAt // "") >= $cutoff))
                      | select((.displayTitle // "") | startswith("fork-bridge: fork-signal: PR \($pr) reviewed by ")) ]
                    | length > 0' <<< "${BRIDGE_RUNS}" > /dev/null; then
                  FORK_BRIDGE_VERIFIED=true
                else
                  echo "::warning::fork-bridge provenance unverified for #${PR} — answering this dispatch like an explicit one"
                fi
              fi
              if [[ -n "${FORCED_PR}" && "${FORCED_PR}" == "${PR}" && "${EVENT_NAME}" == 'workflow_dispatch' && "${FORK_BRIDGE_VERIFIED}" != 'true' ]]; then
                if [[ "${DRY_RUN}" == "true" ]]; then
                  echo "🧪 DRY-RUN: would post cap-refused notice on #${PR}"
                else
                  if [[ -z "${SCAN_BOT_ACTOR:-}" ]]; then
                    SCAN_BOT_ACTOR="$(gh api user --jq '.login' 2> /dev/null || echo 'unknown')"
                  fi
                  if [[ "${SCAN_BOT_ACTOR}" != "${AUTOFIX_BOT}" ]]; then
                    echo "::warning::cap-refused notice skipped: PAT authenticates as '${SCAN_BOT_ACTOR}', expected ${AUTOFIX_BOT}"
                  elif ! gh pr comment "${PR}" --repo "${REPO}" --body "$(printf '⏸️ Dispatch refused: this PR has exhausted its automatic round cap (%s/%s), so the loop will not touch it — whatever triggered this dispatch (a merge conflict, new feedback) stays unhandled. Comment `%s` to re-arm a fresh window, or `%s` for the raised takeover cap; the next scheduled scan then picks it up.\n\n<details>\n<summary>中文说明</summary>\n\n⏸️ 已拒绝本次调度：本 PR 的自动轮次上限已用完（%s/%s），循环不会介入——触发本次调度的事项（合并冲突、新反馈）仍未处理。评论 `%s` 可重置计数窗口，或 `%s` 获得更高的接管上限；随后下一次定时扫描会接手。\n\n</details>\n\n<!-- takeover-cap-refused -->' "${ROUND}" "${EFF_MAX_ROUNDS}" "${RETRY_COMMAND}" "${TAKEOVER_COMMAND}" "${ROUND}" "${EFF_MAX_ROUNDS}" "${RETRY_COMMAND}" "${TAKEOVER_COMMAND}")"; then
                    echo "::warning::cap-refused notice failed for #${PR}"
                  fi
                fi
              fi
              # A MANAGED PR pausing at its cap deserves a visible reminder —
              # maintainers otherwise learn about it only from workflow logs.
              # Full rationale → qwen-autofix.md#af-032
              NOTICE_RT="${REARM_KEY}"
              [[ "${NOTICE_RT}" == "none" ]] && NOTICE_RT=''
              CAP_NOTICED="$(jq -r --arg ab "${AUTOFIX_BOT}" --arg rt "${NOTICE_RT}" '
                [ .[] | select((.user.login // "") == $ab)
                  | select((.body // "") | contains("<!-- takeover-cap-reached -->"))
                  | select((.created_at // "") > $rt) ] | length' "${WORKDIR}/ic.json")"
              # Release evidence = a takeover unlabeled EVENT at-or-newer
              # than the window key. GitHub records it whenever the label
              # Full rationale → qwen-autofix.md#af-033
              RELEASE_ACKED=''
              if [[ "${HAS_TAKEOVER}" == "true" && "${PR_EVENTS_OK}" == "true" ]]; then
                if ! cp "${WORKDIR}/pr-events.json" "${WORKDIR}/ev.json"; then
                  RELEASE_ACKED='unreadable'
                fi
              elif ! gh api "repos/${REPO}/issues/${PR}/events" --paginate 2> /dev/null | jq -s 'add // []' > "${WORKDIR}/ev.json"; then
                RELEASE_ACKED='unreadable'
              fi
              if [[ "${RELEASE_ACKED}" != 'unreadable' ]]; then
                RELEASE_ACKED="$(jq -r --arg tl "${TAKEOVER_LABEL}" --arg rt "${NOTICE_RT}" '
                  [ .[] | select((.event // "") == "unlabeled")
                    | select((.label.name // "") == $tl)
                    | select((.created_at // "") >= $rt) ] | length' "${WORKDIR}/ev.json")"
              fi
              # A release only suppresses re-labeling for HUMAN-authored PRs.
              # A bot-authored PR released from takeover returns to STANDARD
              # management (its ack says so) — it is still managed, so at the
              # strict cap it deserves the same notice + escalation label as
              # any managed PR (R4-5).
              IS_BOT_AUTHOR="$(jq -r --arg ab "${AUTOFIX_BOT}" '((.author.login // "") == $ab)' <<< "${PR_META}")"
              if [[ "${DRY_RUN}" == "true" ]]; then
                echo "🧪 DRY-RUN: would post cap-paused notice and apply ${NEEDS_HUMAN_LABEL} on #${PR}"
              else
                # Consent may have moved since PR_META: skip wins everywhere,
                # and a takeover notice additionally requires the label to
                # still be present — a label removed (or skip added) moments
                # ago must not receive a stale 'paused' notice.
                # Full rationale → qwen-autofix.md#af-106
                LIVE_LABELS_JSON="$(gh pr view "${PR}" --repo "${REPO}" --json labels 2> /dev/null || echo '')"
                LIVE_LABELS="$(jq -r '[.labels[]?.name] | join(" ")' <<< "${LIVE_LABELS_JSON}" 2> /dev/null || echo '')"
                if [[ -z "${LIVE_LABELS_JSON}" ]]; then
                  echo "🧭 cap notice skipped: label state unreadable (fail closed) on #${PR}"
                  continue
                fi
                if [[ " ${LIVE_LABELS} " == *" ${SKIP_LABEL} "* ]] \
                  || [[ "${HAS_TAKEOVER}" == "true" && " ${LIVE_LABELS} " != *" ${TAKEOVER_LABEL} "* ]]; then
                  echo "🧭 cap notice skipped: consent changed since the snapshot (labels: ${LIVE_LABELS:-unreadable})"
                  continue
                fi
                # Convention: verify the PAT identity before ANY write. A
                # rotated PAT would post under a foreign login the dedup
                # (which counts AUTOFIX_BOT comments only) can never see —
                # reposting the notice every scan. Memoized per scan run.
                if [[ -z "${SCAN_BOT_ACTOR:-}" ]]; then
                  SCAN_BOT_ACTOR="$(gh api user --jq '.login' 2> /dev/null || echo 'unknown')"
                fi
                if [[ "${SCAN_BOT_ACTOR}" != "${AUTOFIX_BOT}" ]]; then
                  echo "::warning::cap-paused notice skipped: PAT authenticates as '${SCAN_BOT_ACTOR}', expected ${AUTOFIX_BOT}"
                elif [[ "${RELEASE_ACKED}" != "0" && "${IS_BOT_AUTHOR}" != "true" ]]; then
                  # R6-4: an events-API outage is NOT a release — report it
                  # as what it is, or the log lies during an incident.
                  if [[ "${RELEASE_ACKED}" == "unreadable" ]]; then
                    echo "🧭 cap label/notice skipped: release history unreadable (fail closed) on #${PR}"
                  else
                    echo "🧭 cap label/notice skipped: PR was released after its last re-arm (#${PR})"
                  fi
                else
                  # The escalation label rides EVERY cap detection, noticed
                  # or not: the once-per-window dedup suppresses repeat
                  # comments, but the label is what makes a paused PR
                  # filterable (the shepherd's auto-release ages from the
                  # cap notice itself).
                  # Full rationale → qwen-autofix.md#af-107
                  gh label create "${NEEDS_HUMAN_LABEL}" --repo "${REPO}" --color 'D93F0B' \
                    --description 'The autofix loop stopped on this PR — a human must re-arm, split, merge, or close it' \
                    2> /dev/null || true
                  if ! gh api -X POST "repos/${REPO}/issues/${PR}/labels" -f "labels[]=${NEEDS_HUMAN_LABEL}" > /dev/null; then
                    echo "::warning::${NEEDS_HUMAN_LABEL} add failed for #${PR}; will retry next scan"
                  fi
                  if [[ "${CAP_NOTICED}" == "0" ]]; then
                    if [[ "${HAS_TAKEOVER}" == "true" ]]; then
                      CAP_BODY="$(printf '⏸️ Takeover paused: this PR reached its round cap (%s/%s). Comment `%s` to re-arm a fresh window and continue management, or `%s stop` to release.\n\n<details>\n<summary>中文说明</summary>\n\n⏸️ 托管已暂停：本 PR 达到轮次上限（%s/%s）。评论 `%s` 可重新武装、开启新窗口继续托管；或评论 `%s stop` 释放。\n\n</details>\n\n<!-- takeover-cap-reached -->' "${ROUND}" "${EFF_MAX_ROUNDS}" "${TAKEOVER_COMMAND}" "${TAKEOVER_COMMAND}" "${ROUND}" "${EFF_MAX_ROUNDS}" "${TAKEOVER_COMMAND}" "${TAKEOVER_COMMAND}")"
                    else
                      CAP_BODY="$(printf '⏸️ AutoFix paused: this PR reached its automatic round cap (%s/%s) and the loop will not manage it further — new feedback and base conflicts stay unhandled. Comment `%s` to re-arm a fresh window under the same cap, or `%s` to take it over with the raised cap.\n\n<details>\n<summary>中文说明</summary>\n\n⏸️ AutoFix 已暂停：本 PR 达到自动轮次上限（%s/%s），循环不再管理——新反馈与 base 冲突将无人处理。评论 `%s` 可在同一上限下重置计数窗口，或评论 `%s` 以更高上限接管。\n\n</details>\n\n<!-- takeover-cap-reached -->' "${ROUND}" "${EFF_MAX_ROUNDS}" "${RETRY_COMMAND}" "${TAKEOVER_COMMAND}" "${ROUND}" "${EFF_MAX_ROUNDS}" "${RETRY_COMMAND}" "${TAKEOVER_COMMAND}")"
                    fi
                    if ! gh pr comment "${PR}" --repo "${REPO}" --body "${CAP_BODY}"; then
                      echo "::warning::cap-paused notice failed for #${PR}; will retry next scan"
                    fi
                  fi
                fi
              fi
              continue
            fi
            if [[ "${REVIEW_PR_LIVE}" == "true" ]]; then
              continue
            fi
            # Conflict-park gate for the loop's OWN head move: while a
            # conflict handoff pends in the live window, an update-branch
            # merge re-fires every synchronize-triggered workflow on the new
            # head, and those loop-generated checks complete after both
            # park clocks — lifting the park with zero human activity.
            # Full rationale → qwen-autofix.md#af-108
            CONFLICT_PARKED='false'
            CONFLICT_SINCE_SCAN="$(jq -r --arg ab "${AUTOFIX_BOT}" --arg key "${REARM_KEY}" '
              [ .[] | select((.user.login // "") == $ab) | . as $c | ($c.body // "")
                | [ scan("<!-- autofix-growth-audit verdict=conflict win=([^ ]+) -->") ] | .[]
                | select(.[0] == $key) | ($c.created_at // "") ]
              | max // ""' "${WORKDIR}/ic.json" 2> /dev/null || echo "")"
            if [[ -n "${CONFLICT_SINCE_SCAN}" ]]; then
              gh api "repos/${REPO}/pulls/${PR}/reviews" --paginate 2> /dev/null \
                | jq -s 'add // []' > "${WORKDIR}/rv.scan.json" || echo '[]' > "${WORKDIR}/rv.scan.json"
              gh api "repos/${REPO}/pulls/${PR}/comments" --paginate 2> /dev/null \
                | jq -s 'add // []' > "${WORKDIR}/rc.scan.json" || echo '[]' > "${WORKDIR}/rc.scan.json"
              printf '%s' "${CHECKS_JSON}" > "${WORKDIR}/checks.scan.json"
              BASE_UPD_AT_SCAN="$(jq -r --arg ab "${AUTOFIX_BOT}" '
                [ .[] | select((.user.login // "") == $ab)
                  | select((.body // "") | contains("<!-- autofix-base-updated"))
                  | (.created_at // "") ] | max // ""' "${WORKDIR}/ic.json" 2> /dev/null || echo "")"
              CONFLICT_WAKE_SCAN="$(jq -rs \
                --arg since "${CONFLICT_SINCE_SCAN}" --arg baseupd "${BASE_UPD_AT_SCAN}" \
                --arg rb "${REVIEW_BOT}" --arg ab "${AUTOFIX_BOT}" \
                --argjson trust "${TRUSTED_ASSOC}" '
                (.[0] | map(select((.submitted_at // "") > $since)
                  | select((.user.login // "") != $ab and (.user.login // "") != $rb)
                  | select(((.author_association // "") | IN($trust[])))
                  | select((.state // "") | IN("CHANGES_REQUESTED", "COMMENTED"))) | length)
                + (.[1] | map(select((.created_at // "") > $since)
                  | select((.user.login // "") != $ab and (.user.login // "") != $rb)
                  | select(((.author_association // "") | IN($trust[])))) | length)
                + (.[2] | map(select((.created_at // "") > $since)
                  | select((.user.login // "") != $ab and (.user.login // "") != $rb)
                  | select(((.author_association // "") | IN($trust[])))
                  | select((.body // "") | test("<!-- (autofix-eval|autofix-rearm|qwen-triage|qwen-review-suggestion-summary|pr-force-push|qwen-review-ack) ") | not)
                  | select((.body // "") | test("^\\s*@qwen-code /") | not)) | length)
                + (.[3] | map(select((.conclusion // .state // "") | IN("FAILURE", "FAILED", "ERROR", "TIMED_OUT", "ACTION_REQUIRED"))
                  | select(((.workflowName // "") | IN("Qwen Autofix", "🧐 Qwen Pull Request Review", "Qwen CI Failure Patrol", "Qwen Autofix Fork Bridge", "Qwen Autofix Fork Signal")) | not)
                  | select((.completedAt // .updatedAt // "") > $since)
                  | select((.completedAt // .updatedAt // "") > $baseupd)) | length)' \
                "${WORKDIR}/rv.scan.json" "${WORKDIR}/rc.scan.json" "${WORKDIR}/ic.json" "${WORKDIR}/checks.scan.json" 2> /dev/null || echo 0)"
              [[ "${CONFLICT_WAKE_SCAN}" =~ ^[0-9]+$ ]] || CONFLICT_WAKE_SCAN=0
              if [[ "${CONFLICT_WAKE_SCAN}" == "0" ]]; then
                CONFLICT_PARKED='true'
              fi
              rm -f "${WORKDIR}/rv.scan.json" "${WORKDIR}/rc.scan.json" "${WORKDIR}/checks.scan.json"
            fi
            if [[ "${CONFLICT_PARKED}" == 'true' ]]; then
              echo "🫥 #${PR}: conflict handoff pending in the live window — skipping the stale-base update (the loop does not move the head while parked)"
              fleet_row "${PR}" 'conflict-parked' 'conflict handoff pending — no stale-base head move while parked'
            fi
            # Convergence-signal circuit breaker (#10107): the verdict is
            # derived HERE, above the stale-base update, and the update's
            # gate refuses a parked PR beside the conflict park: a base
            # merge into a parked PR re-fires every synchronize-triggered
            # workflow on the new head, and the base-merge round reviews
            # an unchanged diff — its clean marker resets the streak,
            # silently lifting the park with zero human activity (af-108's
            # exact hazard, the one the conflict park above was added
            # for). The notice's promise that base conflicts stay
            # unhandled while paused only holds with this guard. The
            # verdict and the park action BOTH sit above the idle
            # fast-path (an idle parked PR must still reach its notice),
            # and BOTH stale-base gates reuse this one derivation, so the
            # reads cannot drift. Mirrored live in prepare; keep in
            # lockstep.
            # Full rationale → qwen-autofix.md#af-152
            gh api "repos/${REPO}/pulls/${PR}/reviews" --paginate \
              | jq -s 'add // []' > "${WORKDIR}/rv.json"
            gh api "repos/${REPO}/pulls/${PR}/comments" --paginate \
              | jq -s 'add // []' > "${WORKDIR}/rc.json"
            [[ "${CONVERGENCE_BREAK_ROUNDS}" =~ ^[1-9][0-9]{0,2}$ ]] || CONVERGENCE_BREAK_ROUNDS=3
            CONV_SINCE="${REARM_KEY}"
            [[ "${CONV_SINCE}" == 'none' ]] && CONV_SINCE=''
            CONV_HUMAN_AT="$(jq -rs \
              --arg rb "${REVIEW_BOT}" --arg ab "${AUTOFIX_BOT}" \
              --argjson trust "${TRUSTED_ASSOC}" '
              (.[0] | map(select((.user.login // "") != $ab and (.user.login // "") != $rb)
                | select(((.author_association // "") | IN($trust[])))
                | select((.state // "") | IN("CHANGES_REQUESTED", "COMMENTED", "APPROVED", "DISMISSED")) | (.submitted_at // "")))
              + (.[1] | map(select((.user.login // "") != $ab and (.user.login // "") != $rb)
                | select(((.author_association // "") | IN($trust[]))) | (.created_at // "")))
              + (.[2] | map(select((.user.login // "") != $ab and (.user.login // "") != $rb)
                | select(((.author_association // "") | IN($trust[])))
                | select((.body // "") | test("<!-- (autofix-eval|autofix-rearm|qwen-triage|qwen-review-suggestion-summary|pr-force-push|qwen-review-ack) ") | not)
                | select((.body // "") | test("^\\s*@qwen-code /") | not) | (.created_at // "")))
              | max // ""' "${WORKDIR}/rv.json" "${WORKDIR}/rc.json" "${WORKDIR}/ic.json" 2> /dev/null || echo "")"
            [[ "${CONV_HUMAN_AT}" > "${CONV_SINCE}" ]] && CONV_SINCE="${CONV_HUMAN_AT}"
            CONV_STREAK="$(jq -r --arg rb "${REVIEW_BOT}" --arg since "${CONV_SINCE}" \
              --argjson codes "${CONVERGENCE_SIGNAL_CODES}" '
              [ .[] | select((.user.login // "") == $rb)
                | select((.submitted_at // "") > $since)
                | {at: (.submitted_at // ""),
                   marker: ((.body // "") | [ scan("<!-- qwen-review-ledger (.*?) -->") ] | last | .[0] // "")}
                | select(.marker != "")
                | .led = (.marker | try fromjson catch null)
                | select(.led | type == "object")
                | {at, round: (.led.round // 0),
                   rec: (.led.rec | if type == "array" then . else [] end)} ]
              | sort_by(.at) | group_by(.round) | map(last) | sort_by(.at)
              | reduce .[] as $r (0; if ($r.rec | any(. as $c | $codes | index($c))) then . + 1 else 0 end)' \
              "${WORKDIR}/rv.json" 2> /dev/null || echo 0)"
            [[ "${CONV_STREAK}" =~ ^[0-9]+$ ]] || CONV_STREAK=0
            CONV_PARKED='false'
            if [[ "${CONV_STREAK}" -ge "${CONVERGENCE_BREAK_ROUNDS}" ]]; then
              CONV_PARKED='true'
            else
              # Release census = the park's own enumeration: a posted notice
              # HOLDS the park until the boundary itself moves (a trusted-
              # human response or a re-arm). Any clean round resets the
              # streak — including the review a maintainer push triggers via
              # synchronize — but must not silently release: the dedup
              # counts notices newer than the boundary, so a release-then-
              # re-trip would re-park under the stale notice with no fresh
              # one. If the signal resumes, the streak census above re-parks
              # loudly; this hold only keeps the quiet middle honest.
              # Mirrored live in prepare and in the report guard; keep in
              # lockstep. Full rationale → qwen-autofix.md#af-152
              CONV_NOTICES="$(jq -r --arg ab "${AUTOFIX_BOT}" --arg since "${CONV_SINCE}" '
                [ .[] | select((.user.login // "") == $ab)
                  | select((.body // "") | contains("<!-- autofix-convergence-break -->"))
                  | select((.created_at // "") > $since) ] | length' "${WORKDIR}/ic.json" 2> /dev/null || echo 0)"
              [[ "${CONV_NOTICES}" =~ ^[0-9]+$ ]] || CONV_NOTICES=0
              [[ "${CONV_NOTICES}" != "0" ]] && CONV_PARKED='true'
            fi
            # Auto-update a PR that is red ONLY because of a stale base (see the
            # MAIN_GREEN_CHECKS rationale above).
            # Full rationale → qwen-autofix.md#af-109
            if [[ -n "${MAIN_HEAD}" && -n "${PR_HEAD_OID}" && "${CONFLICT_PARKED}" != 'true' && "${CONV_PARKED}" != 'true' ]]; then
              # STALE_BASE_REDS is pure jq over data already in memory
              # (CHECKS_JSON, MAIN_GREEN_CHECKS) — free, and far more selective
              # than the compare round-trip.
              # Full rationale → qwen-autofix.md#af-110
              STALE_BASE_REDS="$(jq -c -n \
                --argjson checks "${CHECKS_JSON}" --argjson green "${MAIN_GREEN_CHECKS}" '
                [ $checks[]
                  | select((.conclusion // .state // "") | IN("FAILURE", "FAILED", "ERROR", "TIMED_OUT", "ACTION_REQUIRED"))
                  | select((.workflowName // "") != "Qwen Autofix")
                  | (.name // .workflowName // "")
                  | select(. != "" and (. as $n | $green | index($n))) ]')" || STALE_BASE_REDS='[]'
              if [[ "${STALE_BASE_REDS}" != '[]' ]]; then
                # --jq '.status': the compare document is ~60KB; only the
                # behind/diverged/ahead status is needed.
                CMP_STATUS="$(gh api "repos/${REPO}/compare/${MAIN_HEAD}...${PR_HEAD_OID}" --jq '.status // ""' 2> /dev/null || echo '')"
                if [[ "${CMP_STATUS}" == 'behind' || "${CMP_STATUS}" == 'diverged' ]]; then
                  # Repetition guard: a marker comment bounds re-updates to
                  # once per 2 hours (see BASE_UPDATE_CUTOFF, hoisted above the
                  # loop). Without this, a still-red PR would be re-updated on
                  # every scan after main advances.
                  BASE_UPDATE_RECENT="$(jq -r --arg ab "${AUTOFIX_BOT}" \
                    --arg cutoff "${BASE_UPDATE_CUTOFF}" '
                    [ .[] | select((.user.login // "") == $ab)
                      | select((.body // "") | contains("<!-- autofix-base-updated -->"))
                      | select((.created_at // "") > $cutoff) ] | length > 0' "${WORKDIR}/ic.json")"
                  if [[ "${BASE_UPDATE_RECENT}" != "true" ]]; then
                    RED_NAMES="$(jq -r 'join(", ")' <<< "${STALE_BASE_REDS}")"
                    if [[ "${DRY_RUN}" == "true" ]]; then
                      echo "🧪 DRY-RUN: would update stale base on #${PR} (red [${RED_NAMES}] green on main ${MAIN_HEAD:0:9})"
                      fleet_row "${PR}" 'dry-run-base' "would merge main (stale-base red [${RED_NAMES}])"
                      continue
                    fi
                    # Convention: verify the PAT identity before ANY write (same
                    # as the engage ack and cap notice above). update-branch AND
                    # its marker are writes; a rotated PAT would do both under a
                    # foreign login the dedup (which counts AUTOFIX_BOT comments
                    # only) can never see — re-updating every scan. Memoized.
                    if [[ -z "${SCAN_BOT_ACTOR:-}" ]]; then
                      SCAN_BOT_ACTOR="$(gh api user --jq '.login' 2> /dev/null || echo 'unknown')"
                    fi
                    if [[ "${SCAN_BOT_ACTOR}" != "${AUTOFIX_BOT}" ]]; then
                      echo "::warning::#${PR}: stale-base update skipped: PAT authenticates as '${SCAN_BOT_ACTOR}', expected ${AUTOFIX_BOT}"
                      fleet_row "${PR}" 'base-update-skipped' "stale-base red [${RED_NAMES}] but PAT identity '${SCAN_BOT_ACTOR}' != ${AUTOFIX_BOT}"
                    # expected_head_sha makes this a compare-and-swap: if the
                    # author pushed between our compare read and this call,
                    # GitHub rejects it rather than merging main into an
                    # unverified head.
                    elif UPDATE_ERR="$(gh api -X PUT "repos/${REPO}/pulls/${PR}/update-branch" -f expected_head_sha="${PR_HEAD_OID}" 2>&1 >/dev/null)"; then
                      echo "🔀 #${PR}: red check(s) [${RED_NAMES}] pass on current main ${MAIN_HEAD:0:9} — merged main in via update-branch; CI will re-run"
                      fleet_row "${PR}" 'base-updated' "stale-base red [${RED_NAMES}] — merged current main, CI re-running"
                      # The marker is the ONLY repetition guard for this mutating
                      # action; a failed post must be loud, not swallowed, so the
                      # dedup gap is visible (else the next scan re-updates).
                      gh pr comment "${PR}" --repo "${REPO}" --body "$(printf '🔀 Base updated: red check(s) [%s] pass on current main — merged current main via update-branch; CI will re-run.\n\n<details>\n<summary>中文说明</summary>\n\n🔀 已更新 base：红色检查 [%s] 在当前 main 上通过 —— 已通过 update-branch 合入当前 main，CI 将重新运行。\n\n</details>\n\n<!-- autofix-base-updated -->' "${RED_NAMES}" "${RED_NAMES}")" > /dev/null 2>&1 \
                        || echo "::warning::#${PR}: base-updated marker post failed — the 2h repetition guard is NOT armed for this update"
                      continue
                    else
                      echo "⚠️ #${PR}: wanted to update the stale base (red [${RED_NAMES}] green on main) but update-branch failed: ${UPDATE_ERR:-unknown error}"
                      fleet_row "${PR}" 'base-update-failed' "stale-base red [${RED_NAMES}] but update-branch failed"
                      # A failed update (merge conflict, CAS rejection, or
                      # missing allow-edits) is a human problem, but the bot's
                      # review comments need not be deferred forever — fall
                      # through to feedback processing.
                    fi
                  fi
                fi
              fi
            fi

            # Convergence-signal circuit breaker (#10107): the park action on
            # the verdict derived above the stale-base update — a base merge
            # into a parked PR would self-lift it (see the read site's
            # comment). ABOVE the idle fast-path: a parked PR whose signal
            # rounds all sit at or below the eval watermark IS the idle case,
            # and the notice and fleet row this action writes are the park's
            # only visible escalation — below the fast-path they would never
            # be reached. A parked PR spends no dispatch, no runner, and no
            # round either way: the `continue` precedes target emission. Both
            # gates reuse the SAME derivation — re-reading here could drift.
            # Full rationale → qwen-autofix.md#af-152
            if [[ "${CONV_PARKED}" == 'true' ]]; then
              echo "🛑 #${PR}: convergence park active (streak ${CONV_STREAK}/${CONVERGENCE_BREAK_ROUNDS}) — paused until a human weighs in"
              fleet_row "${PR}" 'convergence-parked' "park active (streak ${CONV_STREAK}/${CONVERGENCE_BREAK_ROUNDS}) — a maintainer response or ${RETRY_COMMAND} resumes"
              # The UNION of signal codes observed since the boundary, for
              # the notice — wider than the trailing streak the breaker
              # acted on (a round that reset the streak still contributes).
              # The notice's wording claims exactly this union.
              CONV_CODES="$(jq -r --arg rb "${REVIEW_BOT}" --arg since "${CONV_SINCE}" \
                --argjson codes "${CONVERGENCE_SIGNAL_CODES}" '
                [ .[] | select((.user.login // "") == $rb)
                  | select((.submitted_at // "") > $since)
                  | ((.body // "") | [ scan("<!-- qwen-review-ledger (.*?) -->") ] | last | .[0] // "")
                  | select(. != "") | (try fromjson catch null) | select(type == "object")
                  | (.rec | if type == "array" then . else [] end) | .[] ]
                | map(select(. as $c | $codes | index($c))) | unique | join(", ")' \
                "${WORKDIR}/rv.json" 2> /dev/null || echo '')"
              [[ -z "${CONV_CODES}" ]] && CONV_CODES='non-convergence'
              # Once per boundary, not per window: a resumed-then-re-parked
              # loop earned a fresh notice — the earlier one is older than
              # the human activity that resumed it.
              CONV_NOTICED="$(jq -r --arg ab "${AUTOFIX_BOT}" --arg since "${CONV_SINCE}" '
                [ .[] | select((.user.login // "") == $ab)
                  | select((.body // "") | contains("<!-- autofix-convergence-break -->"))
                  | select((.created_at // "") > $since) ] | length' "${WORKDIR}/ic.json")"
              if [[ "${DRY_RUN}" == "true" ]]; then
                echo "🧪 DRY-RUN: would post convergence-break notice on #${PR} (codes: ${CONV_CODES})"
              elif [[ "${CONV_NOTICED}" == "0" ]]; then
                # Consent may have moved since PR_META: skip wins everywhere,
                # and a label added moments ago must not receive a notice.
                LIVE_LABELS_JSON="$(gh pr view "${PR}" --repo "${REPO}" --json labels 2> /dev/null || echo '')"
                LIVE_LABELS="$(jq -r '[.labels[]?.name] | join(" ")' <<< "${LIVE_LABELS_JSON}" 2> /dev/null || echo '')"
                if [[ -z "${LIVE_LABELS_JSON}" ]] || [[ " ${LIVE_LABELS} " == *" ${SKIP_LABEL} "* ]]; then
                  echo "🧭 convergence-break notice skipped: label state '${LIVE_LABELS:-unreadable}' (fail closed) on #${PR}"
                else
                  # Convention: verify the PAT identity before ANY write. A
                  # rotated PAT would post under a foreign login the dedup
                  # (which counts AUTOFIX_BOT comments only) can never see —
                  # reposting the notice every scan. Memoized per scan run.
                  if [[ -z "${SCAN_BOT_ACTOR:-}" ]]; then
                    SCAN_BOT_ACTOR="$(gh api user --jq '.login' 2> /dev/null || echo 'unknown')"
                  fi
                  if [[ "${SCAN_BOT_ACTOR}" != "${AUTOFIX_BOT}" ]]; then
                    echo "::warning::convergence-break notice skipped: PAT authenticates as '${SCAN_BOT_ACTOR}', expected ${AUTOFIX_BOT}"
                  else
                    # The release lever only exists under the takeover label: on
                    # any other bot PR `/takeover stop` hits the remove branch's
                    # logged no-op, so branch the wording like the cap notices
                    # and offer takeover itself.
                    if [[ "${HAS_TAKEOVER}" == "true" ]]; then
                      CONV_BODY="$(printf '⏸️ AutoFix paused by a review convergence signal: %s consecutive automated review round(s) signalled machine-readable non-convergence (codes observed since the last maintainer response, or the window start if none: %s) — the reviewer keeps finding new defects at a rate that is not falling while the loop keeps widening the diff, so another automatic round is unlikely to converge this PR. The loop resumes once a maintainer responds on this PR (a review or comment counts, and steers the next rounds), and pauses again if the signal persists for %s more round(s). Alternatives: split the recurring cluster into its own PR, batch the remaining fixes into one push, comment `%s` to re-arm a fresh window, or `%s stop` to release takeover. While paused, new feedback and base conflicts stay unhandled.\n\n<details>\n<summary>中文说明</summary>\n\n⏸️ AutoFix 已因评审收敛信号暂停：%s 轮自动评审连续发出机器可读的不收敛信号（自上次维护者响应以来观察到的信号码；若无响应则自窗口开始：%s）——评审仍在以不降的速率发现新缺陷，而循环在继续扩大 diff，再跑一轮自动修复难以收敛本 PR。维护者在本 PR 上作出回应后循环自动恢复（评论或评审均可，并将作为后续轮次的指引）；若信号再持续 %s 轮会再次暂停。可选做法：把反复出问题的簇拆成独立 PR、把剩余修复攒成一批一次推送、评论 `%s` 重开计数窗口、或评论 `%s stop` 释放接管。暂停期间，新反馈与 base 冲突不会被处理。\n\n</details>\n\n<!-- autofix-convergence-break -->' "${CONV_STREAK}" "${CONV_CODES}" "${CONVERGENCE_BREAK_ROUNDS}" "${RETRY_COMMAND}" "${TAKEOVER_COMMAND}" "${CONV_STREAK}" "${CONV_CODES}" "${CONVERGENCE_BREAK_ROUNDS}" "${RETRY_COMMAND}" "${TAKEOVER_COMMAND}")"
                    else
                      CONV_BODY="$(printf '⏸️ AutoFix paused by a review convergence signal: %s consecutive automated review round(s) signalled machine-readable non-convergence (codes observed since the last maintainer response, or the window start if none: %s) — the reviewer keeps finding new defects at a rate that is not falling while the loop keeps widening the diff, so another automatic round is unlikely to converge this PR. The loop resumes once a maintainer responds on this PR (a review or comment counts, and steers the next rounds), and pauses again if the signal persists for %s more round(s). Alternatives: split the recurring cluster into its own PR, batch the remaining fixes into one push, comment `%s` to re-arm a fresh window, or `%s` to take it over with the raised cap. While paused, new feedback and base conflicts stay unhandled.\n\n<details>\n<summary>中文说明</summary>\n\n⏸️ AutoFix 已因评审收敛信号暂停：%s 轮自动评审连续发出机器可读的不收敛信号（自上次维护者响应以来观察到的信号码；若无响应则自窗口开始：%s）——评审仍在以不降的速率发现新缺陷，而循环在继续扩大 diff，再跑一轮自动修复难以收敛本 PR。维护者在本 PR 上作出回应后循环自动恢复（评论或评审均可，并将作为后续轮次的指引）；若信号再持续 %s 轮会再次暂停。可选做法：把反复出问题的簇拆成独立 PR、把剩余修复攒成一批一次推送、评论 `%s` 重开计数窗口、或评论 `%s` 以更高轮次上限接管。暂停期间，新反馈与 base 冲突不会被处理。\n\n</details>\n\n<!-- autofix-convergence-break -->' "${CONV_STREAK}" "${CONV_CODES}" "${CONVERGENCE_BREAK_ROUNDS}" "${RETRY_COMMAND}" "${TAKEOVER_COMMAND}" "${CONV_STREAK}" "${CONV_CODES}" "${CONVERGENCE_BREAK_ROUNDS}" "${RETRY_COMMAND}" "${TAKEOVER_COMMAND}")"
                    fi
                    if ! gh pr comment "${PR}" --repo "${REPO}" --body "${CONV_BODY}"; then
                      echo "::warning::convergence-break notice failed for #${PR}; will retry next scan"
                    fi
                  fi
                fi
              fi
              continue
            fi

            N_FAILED_CHECKS="$(jq --arg wm "${EFF_WM}" '
              [ .[]
                | select((.conclusion // .state // "") | IN("FAILURE", "FAILED", "ERROR", "TIMED_OUT", "ACTION_REQUIRED", "CANCELLED"))
                | select(((.workflowName // "") != "Qwen Autofix") or (((.name // "") | startswith("review-address"))))
                | select((.completedAt // .updatedAt // "") > $wm) ]
              | length
            ' <<< "${CHECKS_JSON}")"
            # A red check is a persistent STATE, not the instant it turned red.
            # Full rationale → qwen-autofix.md#af-034
            LIVE_HEAD="$(jq -r '.headRefOid // ""' <<< "${PR_META}")"
            N_RED_NOW=0
            if [[ -n "${LIVE_HEAD}" && "${RED_HEAD}" != "${LIVE_HEAD}" ]]; then
              N_RED_NOW="$(jq '
                [ .[]
                  | select((.conclusion // .state // "") | IN("FAILURE", "FAILED", "ERROR", "TIMED_OUT", "ACTION_REQUIRED"))
                  | select(((.workflowName // "") != "Qwen Autofix") or (((.name // "") | startswith("review-address")))) ]
                | length
              ' <<< "${CHECKS_JSON}")"
            fi

            N_REVIEWS="$(jq --arg wm "${EFF_WM}" --arg rb "${REVIEW_BOT}" --arg ab "${AUTOFIX_BOT}" \
              --argjson trust "${TRUSTED_ASSOC}" '
              [ .[]
                | select((.submitted_at // "") > $wm)
                | select((.user.login // "") != $ab)
                | select(((.author_association // "") | IN($trust[])) or (.user.login // "") == $rb)
                | select((.state // "") | IN("CHANGES_REQUESTED", "COMMENTED")) ] | length' \
              "${WORKDIR}/rv.json")"
            # Per AGENTS.md's review policy, Suggestion-level findings are
            # actionable during the initial change-producing rounds; takeover
            # extends that window. The scan still selects later feedback so
            # prepare can record and watermark its deferral; only the address
            # job filters what may drive code changes.
            N_COMMENTS="$(jq --arg wm "${EFF_WM}" --arg rb "${REVIEW_BOT}" --arg ab "${AUTOFIX_BOT}" \
              --argjson trust "${TRUSTED_ASSOC}" '
              [ .[]
                | select((.created_at // "") > $wm)
                | select((.user.login // "") != $ab)
                | select(((.author_association // "") | IN($trust[])) or (.user.login // "") == $rb) ] | length' \
              "${WORKDIR}/rc.json")"
            # Issue-level PR comments are also actionable feedback. Exclude the
            # bot's own eval markers, and known non-actionable bot comments
            # (triage stages, coverage reports, legacy suggestion summaries,
            # force-push reminders).
            BOT_COMMENT_FILTER='<!-- (autofix-eval|autofix-rearm|autofix-base-updated|autofix-milestone|qwen-triage|qwen-review-suggestion-summary|pr-force-push|qwen-review-ack|qwen-review-salvaged) '
            # Command-style comments (@qwen-code /takeover, /triage, /review …)
            # are INSTRUCTIONS to tooling, not review feedback on the diff:
            # counting them as actionable burns a full agent cycle to post a
            # no-action report about the command itself (and the takeover
            # command would self-trigger such a round on every engagement).
            COMMAND_FILTER='^\s*@qwen-code /'
            N_ISSUE_COMMENTS="$(jq --arg wm "${EFF_WM}" --arg rb "${REVIEW_BOT}" --arg ab "${AUTOFIX_BOT}" \
              --argjson trust "${TRUSTED_ASSOC}" --arg bf "${BOT_COMMENT_FILTER}" --arg cf "${COMMAND_FILTER}" '
              [ .[]
                | select((.created_at // "") > $wm)
                | select((.user.login // "") != $ab)
                | select(((.author_association // "") | IN($trust[])) or (.user.login // "") == $rb)
                | select((.body // "") | test($bf) | not)
                | select(((((.user.login // "") == $rb)) and ((.body // "") | test("<!-- qwen-review docs-only-medium "))) | not)
                | select((.body // "") | test($cf) | not) ] | length' \
              "${WORKDIR}/ic.json")"

            # mergeable: GitHub may report UNKNOWN until it recomputes; treat only
            # an explicit CONFLICTING as a conflict so we never block on UNKNOWN.
            MERGEABLE="$(gh pr view "${PR}" --repo "${REPO}" --json mergeable --jq '.mergeable' 2> /dev/null || echo 'UNKNOWN')"
            HAS_CONFLICT='false'
            if [[ "${MERGEABLE}" == "CONFLICTING" ]]; then HAS_CONFLICT='true'; fi

            if [[ "${N_REVIEWS}" -eq 0 && "${N_COMMENTS}" -eq 0 && "${N_ISSUE_COMMENTS}" -eq 0 && "${N_FAILED_CHECKS}" -eq 0 && "${N_RED_NOW}" -eq 0 && "${HAS_CONFLICT}" != "true" ]]; then
              echo "✅ #${PR}: nothing new since ${EFF_WM} (conflict=${HAS_CONFLICT})"
              fleet_row "${PR}" 'idle' "nothing new since ${EFF_WM} (round ${ROUND}/${EFF_MAX_ROUNDS}, conflict=${HAS_CONFLICT})"
              continue
            fi

            echo "🔎 #${PR}: ${N_REVIEWS} review(s) + ${N_COMMENTS} inline + ${N_ISSUE_COMMENTS} issue comment(s) + ${N_FAILED_CHECKS} failed check(s) new, ${N_RED_NOW} still-red on ${LIVE_HEAD:0:9}, conflict=${HAS_CONFLICT}, round=${ROUND}"
            fleet_row "${PR}" 'SELECTED' "${N_REVIEWS} review + ${N_COMMENTS} inline + ${N_ISSUE_COMMENTS} comment + ${N_FAILED_CHECKS} failed-check new, ${N_RED_NOW} still-red (round ${ROUND}/${EFF_MAX_ROUNDS}, conflict=${HAS_CONFLICT})"
            TARGETS="$(jq -c \
              --arg pr "${PR}" --arg branch "${BRANCH}" --arg issue "${ISSUE}" \
              --arg round "${ROUND}" --arg wm "${EFF_WM}" --arg mr "${EFF_MAX_ROUNDS}" \
              --arg win "${REARM_KEY}" --arg hr "${HEAD_REPO_FULL}" \
              '. + [{pr: $pr, branch: $branch, issue: $issue, round: $round, watermark: $wm, max_rounds: $mr, win: $win, head_repo: $hr}]' \
              <<< "${TARGETS}")"
            # Stamp the dispatch-pending marker now, while this scan still
            # owns the decision: an overlapping scan inspecting this PR while
            # Full rationale → qwen-autofix.md#af-035
            if [[ "${DRY_RUN}" != "true" && "${HEAD_REPO_FULL}" == "${REPO}" ]]; then
              gh api "repos/${REPO}/statuses/${LIVE_HEAD}" -X POST \
                -f state="pending" -f context="${DISPATCH_STATUS_CONTEXT}" \
                -f description="review-address dispatched (run ${GITHUB_RUN_ID}); the leg starts once the CLI bundle builds" \
                > /dev/null 2>&1 \
                || echo "⚠️ #${PR}: dispatch-pending status stamp failed (overlapping-scan skip degrades to live-run enumeration)"
            fi
            # Fan out: emit EVERY eligible PR up to the per-scan budget. The
            # address matrix bounds simultaneity (max-parallel) and the per-PR
            # Full rationale → qwen-autofix.md#af-036
            if [[ "$(jq 'length' <<< "${TARGETS}")" -ge "${MAX_TARGETS_PER_SCAN}" ]]; then
              echo "⚠️ target budget (${MAX_TARGETS_PER_SCAN}) reached; deferring the remaining candidates to the next scan"
              fleet_row '-' 'deferred' "target budget (${MAX_TARGETS_PER_SCAN}) reached — remaining candidates deferred to next scan"
              break
            fi
          done

          COUNT="$(jq 'length' <<< "${TARGETS}")"
          echo "📋 ${COUNT} PR(s) to process"
          {
            echo "### 🤖 AutoFix fleet (${COUNT} selected this scan)"
            echo
            echo '| PR | State | Detail |'
            echo '| --- | --- | --- |'
            if [[ -s "${FLEET_FILE}" ]]; then
              while IFS=$'\t' read -r f_pr f_state f_detail; do
                f_detail="${f_detail//|/\\|}"
                if [[ "${f_pr}" == '-' ]]; then
                  printf '| — | %s | %s |\n' "${f_state}" "${f_detail}"
                else
                  printf '| #%s | %s | %s |\n' "${f_pr}" "${f_state}" "${f_detail}"
                fi
              done < "${FLEET_FILE}"
            else
              echo '| - | no managed PRs inspected | - |'
            fi
          } >> "${GITHUB_STEP_SUMMARY}"
          echo "targets=${TARGETS}" >> "${GITHUB_OUTPUT}"
          echo "has_targets=$([[ "${COUNT}" -gt 0 ]] && echo true || echo false)" >> "${GITHUB_OUTPUT}"

      # Nothing else removes the per-run WORKDIR; on the persistent pool
      # every scan would leave its API dumps behind forever. always() covers
      # cancellation (the in-step EXIT trap does not fire on a killed run);
      # only a hard runner kill abandons the dir, and the autofix* age sweep
      # in the heavy jobs reclaims it. Mirrors issue-autofix's cleanup step.
      # Full rationale → qwen-autofix.md#af-148
      - name: 'Clean up scan workdir'
        if: 'always()'
        run: 'rm -rf "${WORKDIR}"'

  # ===========================================================================
  # REVIEW PHASE (build) — compile the trusted-base CLI bundle ONCE per scan
  # and fan it out to the address legs as an artifact. Each leg otherwise
  # repeated the same base build: measured 3.5-5 minutes of npm ci + build +
  # bundle per leg (6 legs ≈ 25 runner-minutes on one 2026-08 scan) before
  # the agent could start. Gated on has_targets so an idle tick builds
  # nothing — the issue phase runs only when there are no review targets, so
  # gating on do_issue too would rebuild on every quiet scheduled tick.
  # fetch-depth 1 suffices: nothing here needs history, and the legs (full
  # fetch) always contain this commit as an ancestor of their base fetch.
  # ===========================================================================
  build-cli:
    needs: ['route', 'review-scan']
    if: |-
      ${{ needs.review-scan.outputs.has_targets == 'true' }}
    runs-on: '${{ (github.repository == ''QwenLM/qwen-code'' && vars.MAINTAINER_ECS_RUNNER_DISABLED != ''true'' && (github.event_name != ''pull_request'' && github.event_name != ''pull_request_review'' || github.event.pull_request.head.repo.full_name == github.repository || contains(fromJSON(''["OWNER","MEMBER","COLLABORATOR"]''), github.event.pull_request.author_association))) && fromJSON(''["self-hosted", "linux", "x64", "ecs-qwen"]'') || fromJSON(''["ubuntu-latest"]'') }}'
    timeout-minutes: 30
    permissions:
      contents: 'read'
    outputs:
      base_sha: '${{ steps.meta.outputs.base_sha }}'
    steps:
      # Self-hosted runners reuse the workspace; a prior containerised job
      # can leave root-owned, read-only files anywhere in it. Restore
      # ownership and write permission unconditionally before checkout.
      - name: 'Restore workspace ownership'
        run: |-
          set -uo pipefail
          RUNNER_UID="$(id -u)"
          RUNNER_GID="$(id -g)"
          if [ "$RUNNER_UID" != "0" ]; then
            chown -R "$RUNNER_UID:$RUNNER_GID" "$GITHUB_WORKSPACE" 2>/dev/null || sudo -n chown -R "$RUNNER_UID:$RUNNER_GID" "$GITHUB_WORKSPACE" || echo "::warning::could not restore workspace ownership; checkout may fail on leftover root-owned files"
          fi
          chmod -R u+rwX "$GITHUB_WORKSPACE" 2>/dev/null || sudo -n chmod -R u+rwX "$GITHUB_WORKSPACE" || echo "::warning::could not restore workspace write permissions; checkout may fail on leftover read-only files"

      # Same sweep as the agent jobs: this checkout runs in a workspace
      # other pool jobs have written, and a surviving filter.*.smudge or
      # hook fires during it — in the job that builds the CLI bundle every
      # review-address leg then executes with a PAT in env.
      - name: 'Sanitize workspace git config'
        run: |-
          set -uo pipefail
          # The runner USER's global config is the same exec surface as the
          # workspace config below: pool jobs run human-authored code (branch
          # Full rationale → qwen-autofix.md#af-010
          for global_file in "${HOME}/.gitconfig" "${XDG_CONFIG_HOME:-${HOME}/.config}/git/config"; do
            { GIT_CONFIG_GLOBAL="${global_file}" git config --global --name-only --list 2>/dev/null || true; } \
              | { grep -iE '^(core\.(hookspath|fsmonitor|pager|editor|sshcommand|askpass|alternaterefscommand|gitproxy)$|diff\.external$|diff\..+\.(command|textconv)$|merge\..+\.driver$|filter\.|alias\.|pager\.|difftool\.|mergetool\.|interactive\.difffilter$|sequence\.editor$|gpg\.(.+\.)?program$|init\.templatedir$|remote\..+\.(uploadpack|receivepack)$|submodule\..+\.update$|url\..+\.(insteadof|pushinsteadof)$|http\.(.+\.)?(sslverify|sslcainfo)$|include\.|includeif\.|protocol\.(ext\.)?allow$)' || true; } \
              | while IFS= read -r key; do GIT_CONFIG_GLOBAL="${global_file}" git config --global --unset-all "$key" 2>/dev/null || true; done
          done
          # `.git` is a directory in a normal checkout but a gitlink file in
          # a worktree; -e covers both, and a missing .git (first run) too.
          if [ ! -e .git ]; then
            echo "no prior workspace; nothing local to sanitize"
            exit 0
          fi
          # Worktree-scoped config FIRST: `extensions.worktreeConfig=true` is
          # on the allowlist below (it carries no command itself), but it
          # activates `.git/config.worktree` — a second config file that
          # `git config --local` neither lists nor unsets, and that CAN carry
          # core.hooksPath. Verified in qwen-triage: a prior run can set
          # `--worktree core.hooksPath=/`, survive the sweep untouched, and
          # make the hooks deletion below walk /. Delete the file outright,
          # then drop the extension.
          rm -f "$(git rev-parse --git-path config.worktree 2>/dev/null || echo /nonexistent)" 2>/dev/null || true
          git config --local --unset-all extensions.worktreeConfig 2>/dev/null || true
          # Rather than denylist each exec-vector family (which kept missing
          # new ones), KEEP a known-safe allowlist and --unset-all everything
          # else: this closes the whole class, including knobs not yet
          # enumerated.
          # Full rationale → qwen-autofix.md#af-085
          git config --local --name-only --list 2>/dev/null \
            | { grep -ivE '^(core\.(repositoryformatversion|bare|filemode|symlinks|ignorecase|precomposeunicode|logallrefupdates|worktree|hidedotfiles|protecthfs|protectntfs)|remote\..+\.(url|fetch|pushurl)|branch\.|extensions\.|gc\.|pack\.|fetch\.|index\.|safe\.|submodule\..+\.(url|active|branch))' || true; } \
            | while IFS= read -r key; do git config --local --unset-all "$key" 2>/dev/null || true; done
          # Belt and braces after the config scrub: only delete inside the
          # repository's own git dir. A hooks path resolving anywhere else is
          # unlinked, never swept — a recursive delete of a planted path is
          # far worse than a stale hook on a runner the pool re-cleans.
          # Resolve hooks with global/system config OUT of the way. Verified
          # in qwen-triage: with a global core.hooksPath set, `git rev-parse
          # --git-path hooks` returns that path, the guard below sees
          # "outside the git dir", and a planted `.git/hooks` symlink
          # survives untouched. Keep this resolution AFTER the sweep above.
          GIT_DIR_ABS="$(git rev-parse --absolute-git-dir 2>/dev/null || echo '')"
          HOOKS_DIR="$(GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_SYSTEM=/dev/null git rev-parse --git-path hooks 2>/dev/null || echo .git/hooks)"
          HOOKS_ABS="$(cd "$HOOKS_DIR" 2>/dev/null && pwd -P || echo '')"
          if [ -n "$GIT_DIR_ABS" ] && [ -n "$HOOKS_ABS" ] && [ "${HOOKS_ABS#"$GIT_DIR_ABS"/}" != "$HOOKS_ABS" ]; then
            # Match -type f OR -type l: a symlinked hook survives a bare
            # `-type f` sweep and still fires on the next checkout.
            find "$HOOKS_ABS" \( -type f -o -type l \) ! -name '*.sample' -delete 2>/dev/null || true
          else
            # Resolves outside the git dir (or not at all). Warning and
            # walking away would leave a live hook directory that the next
            # git command executes, so unlink the ENTRY without descending
            # into it and put an empty hooks directory back.
            RAW_HOOKS="$(GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_SYSTEM=/dev/null git rev-parse --git-path hooks 2>/dev/null || echo .git/hooks)"
            echo "::warning::hooks path did not resolve inside the git dir (${HOOKS_ABS:-unresolved}); unlinking it."
            rm -f "$RAW_HOOKS" 2>/dev/null || echo "::warning::refusing to recursively delete planted hooks path '$RAW_HOOKS' (a hooksPath resolving to the git dir itself would otherwise wipe .git); leaving it to the pool re-clean."
            mkdir -p "${GIT_DIR_ABS:-.git}/hooks" 2>/dev/null || true
            git config --local --unset-all core.hooksPath 2>/dev/null || true
          fi

      - name: 'Checkout trusted base'
        uses: 'actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10' # v6.0.3
        with:
          ref: '${{ github.event.repository.default_branch }}'
          fetch-depth: 1
          persist-credentials: false

      - name: 'Set up Node.js'
        uses: 'actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e' # v6.4.0
        with:
          node-version: '22.x'
          # No remote npm cache on the persistent pool — the rationale and
          # the ternary's operand-order trap live in issue-autofix's 'Set up
          # Node.js' step; the contract tests pin all three copies.
          cache: "${{ runner.environment != 'self-hosted' && 'npm' || '' }}"
          package-manager-cache: false
          cache-dependency-path: 'package-lock.json'

      # The npm-ci retry recipe and the 'Prepare Qwen Code CLI' shim below
      # are duplicated in issue-autofix and review-address; the workflow
      # contract tests pin every copy in lockstep — edit them together.
      - name: 'Install dependencies and build'
        env:
          QWEN_SKIP_PREPARE: '1'
        run: |-
          for attempt in 1 2 3; do
            if npm ci --prefer-offline --no-audit --progress=false; then
              break
            fi
            if [[ "${attempt}" == "3" ]]; then
              exit 1
            fi
            sleep $((attempt * 15))
          done
          npm run build
          npm run bundle

      # Producer-side sanity: a bundle that cannot even answer --version
      # fails THIS job once, instead of every leg failing its restore or
      # first qwen invocation minutes later and N times over.
      - name: 'Prepare Qwen Code CLI'
        run: |-
          qwen_version="$(node -p "require('./package.json').version")"
          echo "Using checked-out Qwen Code bundle ${qwen_version}"
          qwen_bin="${RUNNER_TEMP}/qwen-bin"
          mkdir -p "${qwen_bin}"
          cat > "${qwen_bin}/qwen" <<'EOF'
          #!/usr/bin/env bash
          exec node "${GITHUB_WORKSPACE}/dist/cli.js" "$@"
          EOF
          chmod +x "${qwen_bin}/qwen"
          echo "${qwen_bin}" >> "${GITHUB_PATH}"
          PATH="${qwen_bin}:${PATH}"
          qwen --version

      # The repo-root dist/ plus packages/core/dist are shipped:
      # Full rationale → qwen-autofix.md#af-037
      - name: 'Upload CLI bundle'
        id: 'meta'
        run: |-
          echo "base_sha=$(git rev-parse HEAD)" >> "${GITHUB_OUTPUT}"
          tar -czf "${RUNNER_TEMP}/qwen-cli-dist.tar.gz" dist packages/core/dist

      - name: 'Publish CLI bundle artifact'
        uses: 'actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a' # v7.0.1
        with:
          name: 'qwen-autofix-cli-dist'
          path: '${{ runner.temp }}/qwen-cli-dist.tar.gz'
          retention-days: 1
          if-no-files-found: 'error'

  # ===========================================================================
  # REVIEW PHASE (address) — one job per PR: triage, address (incl. conflict
  # resolution), verify, push, and report.
  # ===========================================================================
  review-address:
    needs: ['route', 'review-scan', 'build-cli']
    if: |-
      ${{ needs.review-scan.outputs.has_targets == 'true' }}
    # Secret-bearing and executes PR code, but every target is live-gated to
    # write+ (internal) authors at scan AND address time. That is an
    # Full rationale → qwen-autofix.md#af-038
    runs-on: '${{ (github.repository == ''QwenLM/qwen-code'' && vars.MAINTAINER_ECS_RUNNER_DISABLED != ''true'' && (github.event_name != ''pull_request'' && github.event_name != ''pull_request_review'' || github.event.pull_request.head.repo.full_name == github.repository || contains(fromJSON(''["OWNER","MEMBER","COLLABORATOR"]''), github.event.pull_request.author_association))) && fromJSON(''["self-hosted", "linux", "x64", "ecs-agent"]'') || fromJSON(''["ubuntu-latest"]'') }}'
    timeout-minutes: 345
    permissions:
      contents: 'read'
    strategy:
      fail-fast: false
      # Simultaneity bound for the whole fleet — the ONLY place different PRs
      # wait on each other (the per-scan target budget and the inspection
      # budget are both far from binding at the current pool size).
      # Full rationale → qwen-autofix.md#af-039
      max-parallel: '${{ fromJSON(vars.QWEN_AUTOFIX_MAX_PARALLEL || 20) }}'
      matrix:
        target: '${{ fromJSON(needs.review-scan.outputs.targets) }}'
    # Serialises every writer of THIS PR's head branch, across workflows.
    # Full rationale → qwen-autofix.md#af-040
    concurrency:
      group: 'qwen-pr-head-write-${{ matrix.target.pr }}'
      cancel-in-progress: false
    env:
      REPO: '${{ github.repository }}'
      WORKDIR: '/tmp/autofix-review-${{ matrix.target.pr }}'
      PR: '${{ matrix.target.pr }}'
      BRANCH: '${{ matrix.target.branch }}'
      ISSUE: '${{ matrix.target.issue }}'
      ROUND: '${{ matrix.target.round }}'
      WATERMARK: '${{ matrix.target.watermark }}'
      # The scan computed the effective cap from the takeover label
      # (TAKEOVER_MAX_ROUNDS for managed PRs, the strict default otherwise);
      # shadowing the workflow-level MAX_ROUNDS
      # makes every round message, marker, and cap gate in this job use it.
      MAX_ROUNDS: '${{ matrix.target.max_rounds }}'
      # The round-counting window key this target was selected under; every
      # marker this job writes records it, and prepare discards the job if a
      # re-arm superseded the key while it sat queued.
      WINDOW: '${{ matrix.target.win }}'
      # owner/name of the PR's HEAD repository — equals REPO for in-repo
      # branches; a fork for maintainer-fork takeover targets (pushable via
      # 'Allow edits from maintainers').
      HEAD_REPO: '${{ matrix.target.head_repo }}'
    steps:
      # SECURITY: checkout trusted base code first. The PR branch is checked
      # out later in "Prepare branch and feedback" after the trusted CLI
      # Full rationale → qwen-autofix.md#af-041
      - name: 'Validate bundle SHA'
        env:
          BASE_SHA: '${{ needs.build-cli.outputs.base_sha }}'
        run: |-
          if [[ ! "${BASE_SHA}" =~ ^[0-9a-f]{40}$ ]]; then
            echo "::error::build-cli published no usable base_sha ('${BASE_SHA:-<empty>}') — refusing to fall back to the event-default ref."
            exit 1
          fi

      # Self-hosted runners reuse the workspace; a prior containerised job
      # can leave root-owned, read-only files anywhere in it. Restore
      # ownership and write permission unconditionally before checkout.
      - name: 'Restore workspace ownership'
        run: |-
          set -uo pipefail
          RUNNER_UID="$(id -u)"
          RUNNER_GID="$(id -g)"
          if [ "$RUNNER_UID" != "0" ]; then
            chown -R "$RUNNER_UID:$RUNNER_GID" "$GITHUB_WORKSPACE" 2>/dev/null || sudo -n chown -R "$RUNNER_UID:$RUNNER_GID" "$GITHUB_WORKSPACE" || echo "::warning::could not restore workspace ownership; checkout may fail on leftover root-owned files"
          fi
          chmod -R u+rwX "$GITHUB_WORKSPACE" 2>/dev/null || sudo -n chmod -R u+rwX "$GITHUB_WORKSPACE" || echo "::warning::could not restore workspace write permissions; checkout may fail on leftover read-only files"

      # Self-hosted runners keep the workspace between runs, and other pool
      # jobs execute human-authored code as the runner user, so a prior job
      # can plant git exec knobs (core.fsmonitor, filter.*.smudge,
      # diff.external, includeIf, hooks) in the local config that would fire
      # inside THIS job's PAT-bearing git steps. It keeps
      # a known-safe allowlist and unsets everything else, hardened against
      # the worktree-config and global-hooksPath bypasses verified in
      # qwen-triage on this pool. No-op on a fresh hosted runner.
      - name: 'Sanitize workspace git config'
        run: |-
          set -uo pipefail
          # The runner USER's global config is the same exec surface as the
          # workspace config below: pool jobs run human-authored code (branch
          # Full rationale → qwen-autofix.md#af-010
          for global_file in "${HOME}/.gitconfig" "${XDG_CONFIG_HOME:-${HOME}/.config}/git/config"; do
            { GIT_CONFIG_GLOBAL="${global_file}" git config --global --name-only --list 2>/dev/null || true; } \
              | { grep -iE '^(core\.(hookspath|fsmonitor|pager|editor|sshcommand|askpass|alternaterefscommand|gitproxy)$|diff\.external$|diff\..+\.(command|textconv)$|merge\..+\.driver$|filter\.|alias\.|pager\.|difftool\.|mergetool\.|interactive\.difffilter$|sequence\.editor$|gpg\.(.+\.)?program$|init\.templatedir$|remote\..+\.(uploadpack|receivepack)$|submodule\..+\.update$|url\..+\.(insteadof|pushinsteadof)$|http\.(.+\.)?(sslverify|sslcainfo)$|include\.|includeif\.|protocol\.(ext\.)?allow$)' || true; } \
              | while IFS= read -r key; do GIT_CONFIG_GLOBAL="${global_file}" git config --global --unset-all "$key" 2>/dev/null || true; done
          done
          # `.git` is a directory in a normal checkout but a gitlink file in
          # a worktree; -e covers both, and a missing .git (first run) too.
          if [ ! -e .git ]; then
            echo "no prior workspace; nothing local to sanitize"
            exit 0
          fi
          # Worktree-scoped config FIRST: `extensions.worktreeConfig=true` is
          # on the allowlist below (it carries no command itself), but it
          # activates `.git/config.worktree` — a second config file that
          # `git config --local` neither lists nor unsets, and that CAN carry
          # core.hooksPath. Verified in qwen-triage: a prior run can set
          # `--worktree core.hooksPath=/`, survive the sweep untouched, and
          # make the hooks deletion below walk /. Delete the file outright,
          # then drop the extension.
          rm -f "$(git rev-parse --git-path config.worktree 2>/dev/null || echo /nonexistent)" 2>/dev/null || true
          git config --local --unset-all extensions.worktreeConfig 2>/dev/null || true
          # Rather than denylist each exec-vector family (which kept missing
          # new ones), KEEP a known-safe allowlist and --unset-all everything
          # else: this closes the whole class, including knobs not yet
          # enumerated.
          # Full rationale → qwen-autofix.md#af-085
          git config --local --name-only --list 2>/dev/null \
            | { grep -ivE '^(core\.(repositoryformatversion|bare|filemode|symlinks|ignorecase|precomposeunicode|logallrefupdates|worktree|hidedotfiles|protecthfs|protectntfs)|remote\..+\.(url|fetch|pushurl)|branch\.|extensions\.|gc\.|pack\.|fetch\.|index\.|safe\.|submodule\..+\.(url|active|branch))' || true; } \
            | while IFS= read -r key; do git config --local --unset-all "$key" 2>/dev/null || true; done
          # Belt and braces after the config scrub: only delete inside the
          # repository's own git dir. A hooks path resolving anywhere else is
          # unlinked, never swept — a recursive delete of a planted path is
          # far worse than a stale hook on a runner the pool re-cleans.
          # Resolve hooks with global/system config OUT of the way. Verified
          # in qwen-triage: with a global core.hooksPath set, `git rev-parse
          # --git-path hooks` returns that path, the guard below sees
          # "outside the git dir", and a planted `.git/hooks` symlink
          # survives untouched. Keep this resolution AFTER the sweep above.
          GIT_DIR_ABS="$(git rev-parse --absolute-git-dir 2>/dev/null || echo '')"
          HOOKS_DIR="$(GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_SYSTEM=/dev/null git rev-parse --git-path hooks 2>/dev/null || echo .git/hooks)"
          HOOKS_ABS="$(cd "$HOOKS_DIR" 2>/dev/null && pwd -P || echo '')"
          if [ -n "$GIT_DIR_ABS" ] && [ -n "$HOOKS_ABS" ] && [ "${HOOKS_ABS#"$GIT_DIR_ABS"/}" != "$HOOKS_ABS" ]; then
            # Match -type f OR -type l: a symlinked hook survives a bare
            # `-type f` sweep and still fires on the next checkout.
            find "$HOOKS_ABS" \( -type f -o -type l \) ! -name '*.sample' -delete 2>/dev/null || true
          else
            # Resolves outside the git dir (or not at all). Warning and
            # walking away would leave a live hook directory that the next
            # git command executes, so unlink the ENTRY without descending
            # into it and put an empty hooks directory back.
            RAW_HOOKS="$(GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_SYSTEM=/dev/null git rev-parse --git-path hooks 2>/dev/null || echo .git/hooks)"
            echo "::warning::hooks path did not resolve inside the git dir (${HOOKS_ABS:-unresolved}); unlinking it."
            rm -f "$RAW_HOOKS" 2>/dev/null || echo "::warning::refusing to recursively delete planted hooks path '$RAW_HOOKS' (a hooksPath resolving to the git dir itself would otherwise wipe .git); leaving it to the pool re-clean."
            mkdir -p "${GIT_DIR_ABS:-.git}/hooks" 2>/dev/null || true
            git config --local --unset-all core.hooksPath 2>/dev/null || true
          fi

      - name: 'Checkout trusted base'
        uses: 'actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10' # v6.0.3
        with:
          ref: '${{ needs.build-cli.outputs.base_sha }}'
          fetch-depth: 0
          persist-credentials: false

      - name: 'Remove stale sandbox containers'
        run: |-
          # run-agent.mjs's budget kill removes the container it launched,
          # but a JOB timeout still reaps only the HOST-side docker client,
          # Full rationale → qwen-autofix.md#af-011
          command -v docker > /dev/null || exit 0
          STALE="$(timeout 30 docker ps -aq --filter 'name=qwen-code-' --filter 'status=exited' --filter 'status=dead' 2>/dev/null)" || STALE=''
          if [ -n "${STALE}" ]; then
            echo "removing stale sandbox containers:"
            timeout 30 docker ps -a --filter 'name=qwen-code-' --filter 'status=exited' --filter 'status=dead' --format '  {{.Names}} ({{.Status}})' || true
            printf '%s\n' "${STALE}" | xargs -r -I{} timeout 30 docker rm -f {} > /dev/null 2>&1 || true
          fi

      - name: 'Reset autofix workspace'
        run: |-
          rm -rf "${WORKDIR}"
          # 0700: the dir holds agent transcripts and decision files; the
          # sandbox container runs as this same user, so the tighter mode
          # costs the job nothing. umask at creation, not mkdir-then-chmod —
          # the chmod form leaves a world-readable window on this shared /tmp.
          (umask 077; mkdir -p "${WORKDIR}")
          # Age-sweep abandoned run-scoped dirs on this shared /tmp: a hard
          # runner kill skips the always() teardown and run_id never repeats,
          # so nothing else ever reclaims them. WHEN a crashed prior round
          # ran on THIS host, the rm above also removes its heartbeat.pid
          # and that loop ends itself at the next identity self-check;
          # cross-host the orphan keeps its own pid file and pulses until
          # its age cap (accepted residual risk). No cross-run kill here,
          # because a pid read from a WORKDIR file is an untrusted kill
          # target (WORKDIR is sandbox-writable).
          # Full rationale → qwen-autofix.md#af-149
          find /tmp -maxdepth 1 -name 'autofix*' -mmin +1440 -exec rm -rf {} + 2>/dev/null || true
          # The reused workspace's .git accumulates unreferenced objects
          # across fetch runs on this persistent pool; prune them.
          git -c gc.autoDetach=false gc --auto --prune=now --quiet 2>/dev/null || true

      # Stage the schema gate script from the TRUSTED BASE checkout before
      # "Prepare branch and feedback" switches the working tree to the PR
      # branch. The verify gate must run the trusted copy: a PR branch that
      # predates this script does not contain it (bash exits 127 and the gate
      # dies without an outcome), and an in-branch copy would let branch code
      # define its own gate.
      - name: 'Stage trusted schema gate and agent runner'
        id: 'stage'
        run: |-
          cp .github/scripts/check-settings-schema.sh "${RUNNER_TEMP}/check-settings-schema.sh"
          cp .github/scripts/check-autofix-contracts.sh "${RUNNER_TEMP}/check-autofix-contracts.sh"
          cp .github/scripts/resolve-owning-packages.sh "${RUNNER_TEMP}/resolve-owning-packages.sh"
          cp .github/scripts/run-autofix-review-verification.sh "${RUNNER_TEMP}/run-autofix-review-verification.sh"
          cp .github/scripts/resanitize-git-config.sh "${RUNNER_TEMP}/resanitize-git-config.sh"
          # Absent from the trusted base until this PR merges, and this step
          # runs under -e: a hard failure here would kill every pre-merge
          # round — same rule as the upsert capture below. `|| true`, and the
          # digest records only when the copy exists; an empty output reaches
          # the consumer's own empty guard, which degrades to the pre-PR
          # inline body.
          # Absent from base must imply absent on disk: this cp fails every
          # pre-merge round, so a planted leftover in RUNNER_TEMP would
          # otherwise be digested below and executed as trusted (af-149).
          # -rf, not -f: the planted leftover can be a DIRECTORY, which rm -f
          # cannot remove — the non-zero exit aborts this step under its
          # ambient -eo pipefail before the tolerant cp, and nothing else
          # reclaims RUNNER_TEMP on this persistent pool, so every later
          # round on the host dies at staging until a human clears it.
          rm -rf "${RUNNER_TEMP}/autofix-status-heartbeat.sh"
          cp .github/scripts/autofix-status-heartbeat.sh "${RUNNER_TEMP}/autofix-status-heartbeat.sh" 2> /dev/null || true
          # The staged copies' trusted-base provenance holds at cp time only:
          # RUNNER_TEMP is writable by the branch/agent code later steps run
          # on this host, so record each digest in GITHUB_OUTPUT — expression
          # context, which a disk write after staging cannot reach — for the
          # invoking step to verify before execution.
          # Full rationale → qwen-autofix.md#af-111
          echo "resanitize_sha256=$(sha256sum "${RUNNER_TEMP}/resanitize-git-config.sh" | cut -d' ' -f1)" >> "${GITHUB_OUTPUT}"
          echo "verify_runner_sha256=$(sha256sum "${RUNNER_TEMP}/run-autofix-review-verification.sh" | cut -d' ' -f1)" >> "${GITHUB_OUTPUT}"
          if [[ -f "${RUNNER_TEMP}/autofix-status-heartbeat.sh" ]]; then
            echo "heartbeat_sha256=$(sha256sum "${RUNNER_TEMP}/autofix-status-heartbeat.sh" | cut -d' ' -f1)" >> "${GITHUB_OUTPUT}"
          fi
          # The upsert script travels as CONTENT, not as a staged copy: it
          # runs in a clean child that reads it from this expression-context
          # output, so there is no agent-writable copy to protect and no
          # digest to verify. The delimiter is random per run, so script text
          # cannot close the heredoc.
          _upsert_delim="EOF_$(head -c 16 /dev/urandom | od -An -tx1 | tr -d ' \n')"
          {
            echo "upsert_src<<${_upsert_delim}"
            # Absent from the trusted base until this PR merges, and this
            # step runs under -e: a hard failure here would kill every
            # pre-merge round. An empty value reaches the consumers' own
            # `-z` guard, which skips with "stage step never ran".
            cat .github/scripts/upsert-deferred-issue.sh 2> /dev/null || true
            echo "${_upsert_delim}"
          } >> "${GITHUB_OUTPUT}"
          # 'Push and report' travels the same way and for the same reason: it
          # is the PAT-bearing step, it runs after the agent and the gate have
          # executed branch code on this host, and a copy staged on disk would
          # be theirs to swap. As content there is nothing to stage, digest,
          # type-check or re-open, and no check→use window between those steps.
          _push_report_delim="EOF_$(head -c 16 /dev/urandom | od -An -tx1 | tr -d ' \n')"
          {
            echo "push_report_src<<${_push_report_delim}"
            # Absent from the trusted base until this PR merges; same rule as
            # the upsert capture above — an empty value reaches the consumer's
            # own `-z` guard, which fails closed with a message.
            cat .github/scripts/autofix-push-and-report.sh 2> /dev/null || true
            echo "${_push_report_delim}"
          } >> "${GITHUB_OUTPUT}"
          echo "trusted_path=${PATH}" >> "${GITHUB_OUTPUT}"
          # HOME is captured on the same doctrine: the verification gates
          # pin it at step level, so a $GITHUB_ENV plant after staging
          # cannot reach npm's userconfig inside the gate child — a planted
          # HOME's .npmrc script-shell wraps every verdict-determining
          # `npm run` in an attacker shell (R8-3).
          echo "trusted_home=${HOME}" >> "${GITHUB_OUTPUT}"
          # The agent step runs AFTER prepare checks out the PR branch, so
          # invoking the runner from the working tree would execute
          # branch-controlled code on the host with the model key in env
          # (takeover targets human-authored branches). Stage the runner AND
          # its SKILL from the trusted base, MIRRORING the skill's on-disk
          # layout (autofix/{SKILL.md,scripts/run-agent.mjs}) — run-agent.mjs
          # resolves the skill as `<its dir>/../SKILL.md`, so a flat stage
          # (RUNNER_TEMP/run-agent.mjs) points ../SKILL.md at RUNNER_TEMP/..
          # and the agent crashes with ENOENT before reading any feedback.
          # The mirror also means the model's instructions come from the
          # trusted base, never the PR branch.
          mkdir -p "${RUNNER_TEMP}/autofix-skill/scripts"
          cp .qwen/skills/autofix/SKILL.md "${RUNNER_TEMP}/autofix-skill/SKILL.md"
          cp .qwen/skills/autofix/scripts/run-agent.mjs "${RUNNER_TEMP}/autofix-skill/scripts/run-agent.mjs"

      - name: 'Check runner environment'
        env:
          RUNNER_ENVIRONMENT: '${{ runner.environment }}'
          RUNNER_NAME: '${{ runner.name }}'
        run: |-
          case "${RUNNER_ENVIRONMENT}" in
            github-hosted|self-hosted) ;;
            *)
              echo "::error::Unsupported runner environment: ${RUNNER_ENVIRONMENT:-unset}."
              exit 1
              ;;
          esac
          # The label routing pins ecs-agent. Existing relabelled hosts retain
          # ecs-qwen names while future dedicated hosts may use ecs-agent, so
          # accept both names and reject any other mis-labelled registration.
          if [[ "${RUNNER_ENVIRONMENT}" == 'self-hosted' ]]; then
            case "${RUNNER_NAME}" in
              ecs-qwen-*|ecs-agent-*) ;;
              *)
                echo "::error::self-hosted runner '${RUNNER_NAME}' is not an approved agent pool member; refusing to run here."
                exit 1
                ;;
            esac
            echo 'QWEN_SKIP_LATENCY_BUDGETS=1' >> "${GITHUB_ENV}"
          fi
          # Capability preflight for the persistent pool: this job's agent
          # runs inside the docker sandbox, and a missing daemon otherwise
          # surfaces only at 'Resolve sandbox image' — after npm ci/build
          # has already burned tens of minutes. Fail in seconds instead.
          # Hosted runners ship docker; the ECS pool's docker is proven by
          # qwen-triage's container jobs on the same labels.
          if ! docker info > /dev/null 2>&1; then
            echo "::error::docker daemon is not reachable on this runner; the sandboxed agent cannot start."
            exit 1
          fi
          # 'Set up Node.js' assumes this pool keeps ~/.npm across jobs;
          # log its size so a move to per-job containers is visible.
          du -sh "${HOME}/.npm" 2>/dev/null || echo '~/.npm is absent on this runner.'

      - name: 'Set up Node.js'
        uses: 'actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e' # v6.4.0
        with:
          node-version: '22.x'
          # No remote npm cache on the persistent pool — the rationale and
          # the ternary's operand-order trap live in issue-autofix's 'Set up
          # Node.js' step; the contract tests pin all three copies.
          cache: "${{ runner.environment != 'self-hosted' && 'npm' || '' }}"
          package-manager-cache: false
          cache-dependency-path: 'package-lock.json'

      - name: 'Install tmux'
        run: |-
          if command -v tmux > /dev/null 2>&1; then
            tmux -V
          elif command -v sudo > /dev/null 2>&1 && command -v apt-get > /dev/null 2>&1; then
            # sudo -n: a host without passwordless sudo must fail fast with a
            # clear message, not die on a password prompt (the pr-review pool
            # steps make the same assumption with `sudo -n ... || ::warning`).
            sudo -n apt-get update -qq && sudo -n apt-get install -y -qq tmux || {
              echo '::error::tmux is required on the autofix runner and passwordless install failed.'
              exit 1
            }
          else
            echo '::error::tmux is required on the autofix runner.'
            exit 1
          fi

      # npm ci only — the build itself ran ONCE in build-cli. The legs still
      # need node_modules: the agent builds/tests the PR branch, and the
      # verify gate rebuilds from branch sources (wiping the restored
      # packages/core/dist via build_package.js, so no base staleness leaks
      # into branch verification). The retry recipe (and the shim below) is
      # duplicated in issue-autofix and build-cli; the workflow contract
      # tests pin every copy in lockstep — edit them together.
      - name: 'Install dependencies'
        env:
          QWEN_SKIP_PREPARE: '1'
        run: |-
          for attempt in 1 2 3; do
            if npm ci --prefer-offline --no-audit --progress=false; then
              break
            fi
            if [[ "${attempt}" == "3" ]]; then
              exit 1
            fi
            sleep $((attempt * 15))
          done
          git config core.hooksPath .husky

      - name: 'Download CLI bundle'
        uses: 'actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c' # v8.0.1
        with:
          name: 'qwen-autofix-cli-dist'
          path: '${{ runner.temp }}/cli-dist'

      - name: 'Restore CLI bundle'
        run: |-
          tar -xzf "${RUNNER_TEMP}/cli-dist/qwen-cli-dist.tar.gz"
          test -f dist/cli.js
          # The verify gate's pre-build settings-schema check resolves
          # '@qwen-code/qwen-code-core' through the workspace symlink to
          # this entry point (the root tsconfig has no `paths`; the i18n
          # check instead resolves core to sources via the packages/cli
          # `paths` map); a bundle without it makes the schema generator
          # crash with ERR_MODULE_NOT_FOUND.
          test -f packages/core/dist/index.js

      - name: 'Prepare Qwen Code CLI'
        run: |-
          qwen_version="$(node -p "require('./package.json').version")"
          echo "Using checked-out Qwen Code bundle ${qwen_version}"
          qwen_bin="${RUNNER_TEMP}/qwen-bin"
          mkdir -p "${qwen_bin}"
          cat > "${qwen_bin}/qwen" <<'EOF'
          #!/usr/bin/env bash
          exec node "${GITHUB_WORKSPACE}/dist/cli.js" "$@"
          EOF
          chmod +x "${qwen_bin}/qwen"
          echo "${qwen_bin}" >> "${GITHUB_PATH}"
          PATH="${qwen_bin}:${PATH}"
          qwen --version

      - name: 'Resolve sandbox image'
        id: 'sandbox_image'
        env:
          # The export is derived from this binary's stdout, so step env
          # pins it the way the DOCKER_HOST pins do: step env outranks an
          # appended $GITHUB_ENV value, and the absolute path cannot be
          # shadowed by a '${RUNNER_TEMP}/qwen-bin' or $GITHUB_PATH plant
          # (#9527 review).
          SANDBOX_COMMAND: '/usr/bin/docker'
        run: |-
          node .github/scripts/resolve-sandbox-image.mjs \
            "$(node -p "require('./package.json').config.sandboxImageUri")"

      - name: 'Prepare branch and feedback'
        id: 'prepare'
        env:
          GITHUB_TOKEN: '${{ secrets.CI_DEV_BOT_PAT }}'
          TRUSTED_PATH: '${{ steps.stage.outputs.trusted_path }}'
          DRY_RUN: '${{ needs.route.outputs.dry_run }}'
        run: |-
          mkdir -p "${WORKDIR}"
          # gh has its own $GITHUB_ENV-injectable channels: pin the host and
          # drop any planted token BEFORE the gh calls below, so a GH_HOST
          # reroute cannot spoof the eligibility re-check and a planted
          # GH_TOKEN cannot outrank the inline GITHUB_TOKEN.
          export GH_HOST=github.com
          unset GH_ENTERPRISE_TOKEN GH_TOKEN
          # Point gh at a fresh empty config dir, not the default
          # ~/.config/gh on the shared attacker-writable HOME — its
          # config.yml can carry http_unix_socket and other transport
          # reroutes no sweep here touches. mktemp -d gives an
          # unpredictable path a watcher cannot pre-seed.
          export GH_CONFIG_DIR="$(mktemp -d "${RUNNER_TEMP}/autofix-gh-config.XXXXXX")"
          # This PAT-bearing step runs git (status/restore/fetch/checkout and a
          # push preflight) on the shared host BEFORE the agent/gate, so it
          # takes the same hermetic preamble the push steps do — the contract
          # test pins the executable lines equal across all three.
          # Full rationale → qwen-autofix.md#af-112
          export PATH="${TRUSTED_PATH}"
          unset LD_PRELOAD LD_AUDIT LD_LIBRARY_PATH \
            GIT_CONFIG_PARAMETERS GIT_ALLOW_PROTOCOL GIT_PROXY_COMMAND \
            GIT_SSL_NO_VERIFY GIT_SSL_CAINFO GIT_EXEC_PATH GIT_DIR \
            GIT_WORK_TREE GIT_COMMON_DIR GIT_OBJECT_DIRECTORY \
            GIT_ALTERNATE_OBJECT_DIRECTORIES GIT_SHALLOW_FILE \
            GIT_ASKPASS GIT_SSH GIT_SSH_COMMAND
          export GIT_CONFIG_COUNT=0
          export GIT_TERMINAL_PROMPT=0
          export GIT_CONFIG_SYSTEM=/dev/null
          export GIT_CONFIG_GLOBAL="$(mktemp "${RUNNER_TEMP}/autofix-pat-gitconfig.XXXXXX")"
          git config --file "${GIT_CONFIG_GLOBAL}" safe.directory "$(pwd)"

          # ---- address-time eligibility recheck ---------------------------
          # Fan-out can hold this job queued for hours behind max-parallel,
          # and the matrix snapshot cannot see lifecycle changes: a PR
          # closed or merged while queued must not get a secret-bearing
          # agent run, a branch push, or a comment; an author/base/head
          # change must not be processed against stale assumptions.
          # Full rationale → qwen-autofix.md#af-113
          PR_LIVE="$(gh pr view "${PR}" --repo "${REPO}" \
            --json state,author,isCrossRepository,baseRefName,headRefName,labels,maintainerCanModify,headRepositoryOwner,headRepository,headRefOid 2> /dev/null || echo '{}')"
          LIVE_STATE="$(jq -r '.state // ""' <<< "${PR_LIVE}")"
          LIVE_AUTHOR="$(jq -r '.author.login // ""' <<< "${PR_LIVE}")"
          LIVE_XREPO="$(jq -r 'if has("isCrossRepository") then .isCrossRepository else true end' <<< "${PR_LIVE}")"
          LIVE_CAN_MODIFY="$(jq -r '.maintainerCanModify // false' <<< "${PR_LIVE}")"
          LIVE_BASE="$(jq -r '.baseRefName // ""' <<< "${PR_LIVE}")"
          LIVE_BRANCH="$(jq -r '.headRefName // ""' <<< "${PR_LIVE}")"
          LIVE_HEAD_OID="$(jq -r '.headRefOid // ""' <<< "${PR_LIVE}")"
          # Engagement labels are re-read LIVE too: a takeover label grants a
          # human-authored PR the author exemption, and a skip label applied
          # while this job sat queued withdraws consent — both must hold at
          # the moment the secret-bearing run starts, not at scan time.
          LIVE_TAKEOVER="$(jq -r --arg t "${TAKEOVER_LABEL}" '[.labels[]?.name] | index($t) != null' <<< "${PR_LIVE}")"
          LIVE_SKIP="$(jq -r --arg t "${SKIP_LABEL}" '[.labels[]?.name] | index($t) != null' <<< "${PR_LIVE}")"
          INELIGIBLE=''
          if [[ "${PR_LIVE}" == '{}' ]]; then INELIGIBLE='metadata fetch failed (API error) — fail-closed'
          elif [[ "${LIVE_STATE}" != "OPEN" ]]; then INELIGIBLE="state='${LIVE_STATE:-unknown}'"
          elif [[ "${LIVE_SKIP}" == "true" ]]; then INELIGIBLE="${SKIP_LABEL} label present"
          elif [[ "${LIVE_AUTHOR}" != "${AUTOFIX_BOT}" && "${LIVE_TAKEOVER}" != "true" ]]; then INELIGIBLE="author='${LIVE_AUTHOR:-unknown}' without ${TAKEOVER_LABEL}"
          elif [[ "${LIVE_BASE}" != "main" ]]; then INELIGIBLE="base='${LIVE_BASE:-unknown}'"
          elif [[ "${LIVE_BRANCH}" != "${BRANCH}" ]]; then INELIGIBLE="head branch is now '${LIVE_BRANCH:-unknown}'"
          # Base/branch invariants sit ABOVE the fork chain: the last fork
          # elif ends the ladder for eligible forks, so anything below it
          # would be unreachable for exactly the PR class we fetch and push.
          elif [[ "${LIVE_XREPO}" != "false" && "${LIVE_TAKEOVER}" != "true" && "${LIVE_AUTHOR}" != "${AUTOFIX_BOT}" ]]; then INELIGIBLE='fork head without takeover (a non-bot fork needs the label)'
          elif [[ "${LIVE_XREPO}" != "false" && "${LIVE_CAN_MODIFY}" != "true" ]]; then INELIGIBLE='fork head without maintainer-edit access'
          elif [[ "${LIVE_XREPO}" != "false" ]]; then
            # Live author-privilege re-check for fork targets: an author who
            # lost write+ since the scan must not get a secret-bearing run.
            FORK_AUTHOR_PERM="$(gh api "repos/${REPO}/collaborators/${LIVE_AUTHOR}/permission" --jq '.permission // ""' 2> /dev/null || echo '')"
            case "${FORK_AUTHOR_PERM}" in
              admin|maintain|write) : ;;
              *) INELIGIBLE="fork author '${LIVE_AUTHOR}' permission='${FORK_AUTHOR_PERM:-none}' below write" ;;
            esac
            # The fetch source and token-push target must still be the LIVE
            # head repository — a fork renamed or transferred since the scan
            # would route both to a stale path. Fail-closed: moved or
            # unresolved discards; the next scan re-admits the fresh path.
            if [[ -z "${INELIGIBLE}" ]]; then
              LIVE_HR_OWNER="$(jq -r '.headRepositoryOwner.login // ""' <<< "${PR_LIVE}")"
              LIVE_HR_NAME="$(jq -r '.headRepository.name // ""' <<< "${PR_LIVE}")"
              if [[ -z "${LIVE_HR_OWNER}" || -z "${LIVE_HR_NAME}" || "${LIVE_HR_OWNER}/${LIVE_HR_NAME}" != "${HEAD_REPO:-${REPO}}" ]]; then
                INELIGIBLE="fork head repository moved or unresolved (live='${LIVE_HR_OWNER}/${LIVE_HR_NAME}' target='${HEAD_REPO:-${REPO}}')"
              fi
            fi
          fi
          if [[ -n "${INELIGIBLE}" ]]; then
            echo "🫥 target no longer eligible (${INELIGIBLE}) — discarding without action or marker"
            # Release the dispatch-pending marker on the way out: this discard
            # exits before checkout, so the checkout-path release never runs
            # and a same-repo head would keep being skipped as dispatch-
            # pending for the full TTL with no leg coming — against the
            # re-emit promise above. Same-repo heads only (fork heads were
            # never stamped) and never in a dry run (nothing was stamped).
            if [[ "${DRY_RUN}" != "true" && "${LIVE_XREPO}" == "false" && -n "${LIVE_HEAD_OID}" ]]; then
              gh api "repos/${REPO}/statuses/${LIVE_HEAD_OID}" -X POST \
                -f state="success" -f context="${DISPATCH_STATUS_CONTEXT}" \
                -f description="address leg discarded (${INELIGIBLE})" \
                > /dev/null 2>&1 || true
            fi
            {
              echo "stale=true"
              echo "conflict=false"
              echo "newest=${WATERMARK}"
              echo "effective_round=${ROUND}"
            } >> "${GITHUB_OUTPUT}"
            exit 0
          fi

          echo 'Restoring tracked build output before switching to the PR branch.'
          git status --short
          git restore --source=HEAD --staged --worktree .
          # This step's env carries the PAT, and checkout fires post-checkout
          # hooks — sever hooks here exactly like the push steps do; the
          # agent step (no PAT, sandboxed tools) re-points hooksPath at
          # .husky itself so ITS commits still get checked.
          git config core.hooksPath /dev/null
          if [[ "${HEAD_REPO:-${REPO}}" != "${REPO}" ]]; then
            # Maintainer-fork target: the branch does not exist on origin —
            # fetch it (data only; hooks are severed) from the fork.
            # Full rationale → qwen-autofix.md#af-114
            if ! git -c http.sslVerify=true -c credential.helper= fetch "https://github.com/${HEAD_REPO}.git" "refs/heads/${BRANCH}"; then
              echo "🫥 fork fetch failed for ${HEAD_REPO} (${BRANCH}) — discarding without action or marker"
              {
                echo "stale=true"
                echo "conflict=false"
                echo "newest=${WATERMARK}"
                echo "effective_round=${ROUND}"
              } >> "${GITHUB_OUTPUT}"
              exit 0
            fi
            git checkout -B "${BRANCH}" FETCH_HEAD
            # Create the remote-tracking ref so the verification gate's
            # `git diff --quiet "origin/${BRANCH}...${BRANCH}"` works for
            # fork PRs the same way it does for same-repo branches.
            git update-ref "refs/remotes/origin/${BRANCH}" "${BRANCH}"
            # Allow-edits pushes ride the classic-PAT grant — GITHUB_TOKEN
            # and fine-grained PATs are documented as NOT receiving it.
            # Full rationale → qwen-autofix.md#af-115
            if ! git -c http.sslVerify=true -c credential.helper= -c credential."https://github.com".helper='!f(){ echo username=x-access-token; echo "password=${GITHUB_TOKEN}"; };f' \
              push --no-verify --dry-run "https://github.com/${HEAD_REPO}.git" HEAD:"${BRANCH}" > /dev/null 2>&1; then
              echo "🫥 fork push preflight failed for ${HEAD_REPO} (allow-edits grant or PAT type) — discarding without action or marker"
              {
                echo "stale=true"
                echo "conflict=false"
                echo "newest=${WATERMARK}"
                echo "effective_round=${ROUND}"
              } >> "${GITHUB_OUTPUT}"
              exit 0
            fi
          else
            git checkout -B "${BRANCH}" "origin/${BRANCH}"
          fi

          # The exact SHA the agent will evaluate. Captured HERE, before any
          # agent mutation, so the marker records what was actually looked at —
          # not the report-time remote head, which can move during the run.
          CHECKED_OUT_HEAD="$(git rev-parse HEAD)"
          echo "checked_out_head=${CHECKED_OUT_HEAD}" >> "${GITHUB_OUTPUT}"

          # Release the dispatch-pending marker the emitting scan stamped on
          # this head: the leg has materialized, so from here the live-run
          # busy enumeration sees it and a PENDING status must not keep
          # overlapping scans away.
          # Full rationale → qwen-autofix.md#af-116
          if [[ "${DRY_RUN}" != "true" && "${HEAD_REPO:-${REPO}}" == "${REPO}" ]]; then
            gh api "repos/${REPO}/statuses/${CHECKED_OUT_HEAD}" -X POST \
              -f state="success" -f context="${DISPATCH_STATUS_CONTEXT}" \
              -f description="address leg started (run ${GITHUB_RUN_ID})" \
              > /dev/null 2>&1 || true
          fi

          # Does the branch conflict with base? merge-tree computes the merge
          # without touching the tree; exit 1 means conflicts. UNKNOWN/errors are
          # treated as no-conflict so we never block on a transient state.
          CONFLICT='false'
          if git merge-tree --write-tree origin/main HEAD > /dev/null 2>&1; then
            CONFLICT='false'
          elif [[ "$?" == "1" ]]; then
            CONFLICT='true'
          fi
          echo "conflict=${CONFLICT}" >> "${GITHUB_OUTPUT}"
          echo "🔀 Conflict with base: ${CONFLICT}"

          # Same flat-array normalization as the scan step (see its PAGINATION
          # NOTE). NEWEST / LIVE_NEW below bind these files POSITIONALLY
          # (.[0]..[3]), so each must remain a single well-formed array.
          gh api "repos/${REPO}/pulls/${PR}/reviews" --paginate \
            | jq -s 'add // []' > "${WORKDIR}/rv.json"
          gh api "repos/${REPO}/pulls/${PR}/comments" --paginate \
            | jq -s 'add // []' > "${WORKDIR}/rc.json"
          gh api "repos/${REPO}/issues/${PR}/comments" --paginate \
            | jq -s 'add // []' > "${WORKDIR}/ic.json"
          gh pr view "${PR}" --repo "${REPO}" \
            --json statusCheckRollup --jq '.statusCheckRollup // []' > "${WORKDIR}/checks.json" \
            2> /dev/null || echo '[]' > "${WORKDIR}/checks.json"

          # Newest actionable feedback timestamp — stamped into the eval marker so
          # the next scan knows everything up to here has been considered.
          # Includes reviews, inline review comments, and issue-level PR comments
          # (excluding the bot's own eval markers).
          NEWEST="$(jq -rs \
            --arg wm "${WATERMARK}" --arg rb "${REVIEW_BOT}" --arg ab "${AUTOFIX_BOT}" \
            --argjson trust "${TRUSTED_ASSOC}" '
            (.[0] | map(select((.submitted_at // "") > $wm)
              | select((.user.login // "") != $ab)
              | select(((.author_association // "") | IN($trust[])) or (.user.login // "") == $rb)
              | select((.state // "") | IN("CHANGES_REQUESTED", "COMMENTED")) | .submitted_at))
            + (.[1] | map(select((.created_at // "") > $wm)
              | select((.user.login // "") != $ab)
              | select(((.author_association // "") | IN($trust[])) or (.user.login // "") == $rb) | .created_at))
            + (.[2] | map(select((.created_at // "") > $wm)
              | select((.user.login // "") != $ab)
              | select(((.author_association // "") | IN($trust[])) or (.user.login // "") == $rb)
              | select((.body // "") | test("<!-- (autofix-eval|autofix-rearm|qwen-triage|qwen-review-suggestion-summary|pr-force-push|qwen-review-ack|qwen-review-salvaged) ") | not)
              | select(((((.user.login // "") == $rb)) and ((.body // "") | test("<!-- qwen-review docs-only-medium "))) | not)
                | select((.body // "") | test("^\\s*@qwen-code /") | not) | .created_at))
            + (.[3] | map(select((.conclusion // .state // "") | IN("FAILURE", "FAILED", "ERROR", "TIMED_OUT", "ACTION_REQUIRED", "CANCELLED"))
              | select(((.workflowName // "") != "Qwen Autofix") or (((.name // "") | startswith("review-address"))))
              | select((.completedAt // .updatedAt // "") > $wm)
              | (.completedAt // .updatedAt // "")))
            | max // ""' "${WORKDIR}/rv.json" "${WORKDIR}/rc.json" "${WORKDIR}/ic.json" "${WORKDIR}/checks.json")"
          [[ -z "${NEWEST}" ]] && NEWEST="${WATERMARK}"
          echo "newest=${NEWEST}" >> "${GITHUB_OUTPUT}"

          # Live-watermark revalidation: two near-simultaneous triggers for the
          # SAME PR can both pass their (per-target, route-level) gates and both
          # Full rationale → qwen-autofix.md#af-042
          STALE='false'
          LIVE_MARKS="$(jq -r --arg ab "${AUTOFIX_BOT}" '
            [ .[] | select((.user.login // "") == $ab) | . as $c | ($c.body // "")
              | [ scan("<!-- autofix-eval ts=([^ ]+) acted=([^ ]+) round=([0-9]+)(?: win=([^ ]+))? -->") ] | .[]
              | {ts: .[0], round: (.[2] | tonumber), win: (.[3] // "none"), at: ($c.created_at // "")} ]' "${WORKDIR}/ic.json")"
          LIVE_REARM_AT="$(jq -r --arg ab "${AUTOFIX_BOT}" '
            [ .[] | select((.user.login // "") == $ab)
              | select((.body // "") | contains("<!-- autofix-rearm -->"))
              | .created_at ] | max // ""' "${WORKDIR}/ic.json")"
          # Mirrors the scan: a /retry marker releases the watermark it holds.
          LIVE_EVAL_WM="$(jq -r --arg rearm "${LIVE_REARM_AT}" '
            map(select($rearm == "" or (.at > $rearm)))
            | map(.ts) | map(select(. != "9999-12-31T23:59:59Z")) | max // ""' <<< "${LIVE_MARKS}")"
          # Round counting mirrors the scan: keyed to the CURRENT window (the
          # latest engage ack's created_at, 'none' before any takeover), so
          # pre-re-arm markers can neither trip the cap nor inflate the live
          # round here.
          LIVE_REARM_KEY="$(jq -r --arg ab "${AUTOFIX_BOT}" '
            [ .[] | select((.user.login // "") == $ab)
              | select(((.body // "") | contains("<!-- takeover-ack engaged -->"))
                    or ((.body // "") | contains("<!-- autofix-rearm -->")))
              | .created_at ] | max // "none"' "${WORKDIR}/ic.json")"
          # …and so does the round seed: same marker, same created_at-equality
          # read against the live window key, same clamp. MAX_ROUNDS is the
          # matrix-shadowed EFFECTIVE cap here (see the address job's env), so
          # the clamp is against the same ceiling the scan used.
          LIVE_ROUND_START="$(jq -r --arg ab "${AUTOFIX_BOT}" --arg key "${LIVE_REARM_KEY}" '
            [ .[] | select((.user.login // "") == $ab)
              | select((.created_at // "") == $key)
              | ((.body // "") | [ scan("<!-- autofix-round-start ([0-9]{1,2}) -->") ] | .[] | .[0]) ]
            | last // "0"' "${WORKDIR}/ic.json" 2> /dev/null || echo '0')"
          [[ "${LIVE_ROUND_START}" =~ ^[0-9]{1,2}$ ]] || LIVE_ROUND_START=0
          # The PRE-clamp value is what the maintainer typed; the Critical-only
          # audit clause cites it so a clamped seed never renders a command
          # nobody sent while the engage ack still shows the original number.
          LIVE_ROUND_START_RAW="${LIVE_ROUND_START}"
          if [[ "${MAX_ROUNDS:-0}" -gt 0 && "${LIVE_ROUND_START}" -ge "${MAX_ROUNDS}" ]]; then
            LIVE_ROUND_START=$(( MAX_ROUNDS - 1 ))
          fi
          LIVE_MAX_ROUND="$(jq -r --arg key "${LIVE_REARM_KEY}" --argjson start "${LIVE_ROUND_START}" 'map(select(.win == $key)) | map(.round) | max // $start' <<< "${LIVE_MARKS}")"
          # The head a sibling last judged, mirrored from the scan's RED_HEAD
          # parse. A no-op sibling records this marker while leaving BOTH ts and
          # round UNCHANGED — so the watermark/round triggers below never fire,
          # yet a second same-watermark target for the same head would otherwise
          # run the agent again and post a duplicate report for that head.
          LIVE_RED_HEAD="$(jq -r --arg ab "${AUTOFIX_BOT}" '
            [ .[] | select((.user.login // "") == $ab) | . as $c | ($c.body // "")
              | [ scan("<!-- autofix-redcheck head=([0-9a-f]+) -->") ] | .[]
              | {sha: .[0], at: ($c.created_at // "")} ]
            | sort_by(.at) | last | .sha // ""' "${WORKDIR}/ic.json")"
          # A re-arm SUPERSEDES every job selected under the old window key:
          # the maintainer asked for a fresh start, so a queued old-window job
          # must discard instead of finishing and stamping an old-sequence
          # marker into the new window. Conflicts stay actionable.
          if [[ "${STALE}" != "true" && "${WINDOW:-none}" != "${LIVE_REARM_KEY}" && "${CONFLICT}" != "true" ]]; then
            STALE='true'
            echo "🫥 window superseded: selected under key '${WINDOW:-none}' but the live window is '${LIVE_REARM_KEY}' (re-armed while queued) — discarding without action or marker"
          fi
          if [[ -n "${LIVE_EVAL_WM}" && "${CONFLICT}" != "true" ]] \
            && { [[ "${LIVE_EVAL_WM}" > "${WATERMARK}" ]] || [[ "${LIVE_MAX_ROUND}" -gt "${ROUND}" ]] \
              || { [[ -n "${LIVE_RED_HEAD}" ]] && [[ "${LIVE_RED_HEAD}" == "${CHECKED_OUT_HEAD}" ]]; }; }; then
            LIVE_NEW="$(jq -rs \
              --arg wm "${LIVE_EVAL_WM}" --arg rb "${REVIEW_BOT}" --arg ab "${AUTOFIX_BOT}" \
              --argjson trust "${TRUSTED_ASSOC}" '
              (.[0] | map(select((.submitted_at // "") > $wm)
                | select((.user.login // "") != $ab)
                | select(((.author_association // "") | IN($trust[])) or (.user.login // "") == $rb)
                | select((.state // "") | IN("CHANGES_REQUESTED", "COMMENTED"))) | length)
              + (.[1] | map(select((.created_at // "") > $wm)
                | select((.user.login // "") != $ab)
                | select(((.author_association // "") | IN($trust[])) or (.user.login // "") == $rb)) | length)
              + (.[2] | map(select((.created_at // "") > $wm)
                | select((.user.login // "") != $ab)
                | select(((.author_association // "") | IN($trust[])) or (.user.login // "") == $rb)
                | select((.body // "") | test("<!-- (autofix-eval|autofix-rearm|qwen-triage|qwen-review-suggestion-summary|pr-force-push|qwen-review-ack|qwen-review-salvaged) ") | not)
                | select(((((.user.login // "") == $rb)) and ((.body // "") | test("<!-- qwen-review docs-only-medium "))) | not)
                | select((.body // "") | test("^\\s*@qwen-code /") | not)) | length)
              + (.[3] | map(select((.conclusion // .state // "") | IN("FAILURE", "FAILED", "ERROR", "TIMED_OUT", "ACTION_REQUIRED", "CANCELLED"))
                | select(((.workflowName // "") != "Qwen Autofix") or (((.name // "") | startswith("review-address"))))
                | select((.completedAt // .updatedAt // "") > $wm)) | length)' \
              "${WORKDIR}/rv.json" "${WORKDIR}/rc.json" "${WORKDIR}/ic.json" "${WORKDIR}/checks.json")"
            if [[ "${LIVE_NEW}" == "0" ]]; then
              STALE='true'
              echo "🫥 stale duplicate target: a sibling run already evaluated through ${LIVE_EVAL_WM} (round ${LIVE_MAX_ROUND}) and nothing is newer — discarding without action or marker"
            fi
          fi
          # A NON-stale run must still not replay a sibling's work: if live
          # markers advanced past the matrix snapshot, adopt the live
          # watermark (so the renderers below list only feedback the fleet
          # has not yet evaluated) and the live round (so the next marker
          # continues the live sequence instead of double-writing a round).
          if [[ "${STALE}" != "true" && -n "${LIVE_EVAL_WM}" && "${LIVE_EVAL_WM}" > "${WATERMARK}" && "${LIVE_EVAL_WM}" != "9999-12-31T23:59:59Z" ]]; then
            echo "⏩ adopting live watermark ${LIVE_EVAL_WM} (matrix had ${WATERMARK})"
            WATERMARK="${LIVE_EVAL_WM}"
          fi
          if [[ "${STALE}" != "true" && "${LIVE_MAX_ROUND}" -gt "${ROUND}" ]]; then
            echo "⏩ adopting live round ${LIVE_MAX_ROUND} (matrix had ${ROUND})"
            ROUND="${LIVE_MAX_ROUND}"
          fi
          # The live round may already sit at the hard cap (a sibling consumed
          # the final round after this target was emitted). Running would do
          # round MAX+1 work and write a second capped marker, concealing the
          # cap — the scan itself skips capped PRs before even looking at
          # conflicts, so mirror that and discard.
          if [[ "${STALE}" != "true" && "${ROUND}" -ge "${MAX_ROUNDS}" ]]; then
            STALE='true'
            echo "⛔ live round ${ROUND} already at MAX_ROUNDS (${MAX_ROUNDS}) — discarding without action or marker"
          fi
          # Growth brake: measure the PR's net size (insertions minus
          # deletions vs the merge base), split into test lines and source
          # Full rationale → qwen-autofix.md#af-043
          if [[ ! "${GROWTH_BUDGET_SRC_LINES}" =~ ^(0|[1-9][0-9]{0,6})$ ]]; then
            echo "::warning::GROWTH_BUDGET_SRC_LINES='${GROWTH_BUDGET_SRC_LINES}' is not a plain line count; using 400"
            GROWTH_BUDGET_SRC_LINES=400
          fi
          if [[ ! "${GROWTH_BUDGET_TEST_LINES}" =~ ^(0|[1-9][0-9]{0,6})$ ]]; then
            echo "::warning::GROWTH_BUDGET_TEST_LINES='${GROWTH_BUDGET_TEST_LINES}' is not a plain line count; using 400"
            GROWTH_BUDGET_TEST_LINES=400
          fi
          # Binary files report "-" in numstat; count them as 0 lines.
          sum_numstat() { awk '{ if ($1 != "-") a += $1; if ($2 != "-") d += $2 } END { print a - d + 0 }'; }
          # __tests__/ is part of the repo's existing test-file definition
          # (AGENTS.md's triage line-counting rule, repo-hygiene's
          # PROD_EXCLUDE): helpers under it without a .test./.spec. suffix
          # are still test code, not source.
          TEST_PATHSPEC=(':(glob)**/*.test.*' ':(glob)**/*.spec.*' ':(glob)**/__snapshots__/**' ':(glob)**/__tests__/**' ':(glob)**/test-utils/**' ':(glob)integration-tests/**')
          # Mechanical churn must not burn the budget: one dependency bump
          # rewrites hundreds of package-lock.json lines and one
          # `generate:settings-schema` run regenerates the committed schema —
          # skimmed, not reviewed, so they measure no review burden.
          # Full rationale → qwen-autofix.md#af-117
          GENERATED_EXCLUDES=(':(exclude,glob)**/package-lock.json' ':(exclude,glob)**/npm-shrinkwrap.json' ':(exclude)packages/vscode-ide-companion/schemas/settings.schema.json')
          # An orphan-history branch (fork takeover / adoption admits one —
          # nothing on this job's fetch requires a common ancestor) has no
          # Full rationale → qwen-autofix.md#af-044
          NET_MEASURED='true'
          NET_TOTAL=0
          NET_TEST=0
          if [[ "${BRANCH}" == 'main' || "${BRANCH}" == 'origin/main' ]]; then
            # 'origin/main' as a LOCAL branch name shadows the remote ref in
            # rev disambiguation — the diff would silently self-compare.
            echo "📏 growth measurement skipped: head branch name '${BRANCH}' shadows the measurement base"
            NET_MEASURED='false'
          else
            NET_TOTAL="$(git diff --numstat origin/main...HEAD -- "${GENERATED_EXCLUDES[@]}" 2> /dev/null | sum_numstat)" || NET_MEASURED='false'
            NET_TEST="$(git diff --numstat origin/main...HEAD -- "${TEST_PATHSPEC[@]}" "${GENERATED_EXCLUDES[@]}" 2> /dev/null | sum_numstat)" || NET_MEASURED='false'
          fi
          NET_SRC=$(( NET_TOTAL - NET_TEST ))
          [[ "${NET_MEASURED}" != 'true' ]] &&
            echo "📏 growth measurement UNAVAILABLE this round (no merge base or shadowed base) — brake skipped, no anchor written"
          # The marker's window field is spelled `key=`, NOT `win=`: this
          # marker can legitimately carry a different window key than its
          # Full rationale → qwen-autofix.md#af-045
          BASE_UPD_AT="$(jq -r --arg ab "${AUTOFIX_BOT}" '
            [ .[] | select((.user.login // "") == $ab)
              | select((.body // "") | contains("<!-- autofix-base-updated"))
              | .created_at ] | max // ""' "${WORKDIR}/ic.json")"
          # The instant THIS round's net was measured. Stamped into the
          # growth-now marker so the census filters on measurement time,
          # not the marker comment's created_at.
          # Full rationale → qwen-autofix.md#af-118
          MEASURED_AT="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          [[ "${NET_MEASURED}" == 'true' ]] && echo "measured_at=${MEASURED_AT}" >> "${GITHUB_OUTPUT}"
          # NOTE (#9114 R2-8/R6-3): re-anchoring on an EXTERNAL head move (an
          # author push) is deliberately NOT done here.
          # Full rationale → qwen-autofix.md#af-119
          GROWTH_NOW_CUTOFF="${BASE_UPD_AT}"
          # KNOWN RESIDUAL (#9114): this sibling read still filters on the
          # comment's created_at, not a prepare-time measured= like the
          # growth-now read below.
          # Full rationale → qwen-autofix.md#af-120
          GROWTH_BASELINE="$(jq -r --arg ab "${AUTOFIX_BOT}" --arg key "${LIVE_REARM_KEY}" --arg baseupd "${BASE_UPD_AT}" '
            [ .[] | select((.user.login // "") == $ab) | . as $c | ($c.body // "")
              | [ scan("<!-- autofix-growth-base src=(-?[0-9]+) test=(-?[0-9]+) key=([^ ]+) -->") ] | .[]
              | {src: (.[0] | tonumber), test: (.[1] | tonumber), win: .[2], at: ($c.created_at // "")} ]
            | map(select(.win == $key))
            | map(select($baseupd == "" or (.at > $baseupd))) | sort_by(.at)
            | .[0] // empty | "\(.src) \(.test)"' "${WORKDIR}/ic.json")"
          GROWTH_BASE_NEW='false'
          if [[ "${NET_MEASURED}" != 'true' ]]; then
            BASE_SRC=0
            BASE_TEST=0
          elif [[ "${GROWTH_BASELINE}" =~ ^(-?[0-9]+)\ (-?[0-9]+)$ ]]; then
            BASE_SRC="${BASH_REMATCH[1]}"
            BASE_TEST="${BASH_REMATCH[2]}"
          else
            BASE_SRC="${NET_SRC}"
            BASE_TEST="${NET_TEST}"
            GROWTH_BASE_NEW='true'
          fi
          GROWTH_SRC=$(( NET_SRC - BASE_SRC ))
          GROWTH_TEST=$(( NET_TEST - BASE_TEST ))
          [[ "${NET_MEASURED}" != 'true' ]] && { GROWTH_SRC=0; GROWTH_TEST=0; }
          {
            echo "growth_base_new=${GROWTH_BASE_NEW}"
            echo "growth_base_src=${BASE_SRC}"
            echo "growth_base_test=${BASE_TEST}"
            # The key the baseline was READ under. The report must write the
            # marker under this same key, not the matrix WINDOW: a conflict
            # round is exempt from the supersede discard, so it can run with
            # a stale WINDOW after a re-arm — a marker written under that
            # dead key would be invisible to every later read and the
            # round's pushed growth would escape the budget for the rest of
            # the live window.
            echo "growth_base_win=${LIVE_REARM_KEY}"
            # The round's own growth + over-budget flag, so the report step can
            # write this round's autofix-growth-now marker (the per-round
            # history the census above consumes).
            echo "growth_src=${GROWTH_SRC}"
            echo "growth_test=${GROWTH_TEST}"
          } >> "${GITHUB_OUTPUT}"
          echo "📏 net diff src ${NET_SRC} / test ${NET_TEST} lines (window baseline ${BASE_SRC}/${BASE_TEST}, growth ${GROWTH_SRC}/${GROWTH_TEST}, budgets ${GROWTH_BUDGET_SRC_LINES}/${GROWTH_BUDGET_TEST_LINES})"

          CRITICAL_ONLY='false'
          CRITICAL_ONLY_ROUNDS='false'
          CRITICAL_ONLY_GROWTH='false'
          if [[ "${ROUND}" -ge "${CRITICAL_ONLY_AFTER_ROUND}" ]]; then
            CRITICAL_ONLY='true'
            CRITICAL_ONLY_ROUNDS='true'
          fi
          if [[ "${GROWTH_SRC}" -gt "${GROWTH_BUDGET_SRC_LINES}" || "${GROWTH_TEST}" -gt "${GROWTH_BUDGET_TEST_LINES}" ]]; then
            CRITICAL_ONLY='true'
            CRITICAL_ONLY_GROWTH='true'
          fi
          # Growth audit: a budget breach engages Critical-only AND makes the
          # round a growth-audit round — a size signal triggers a JUDGMENT,
          # never a stop.
          # Full rationale → qwen-autofix.md#af-121
          OVER_ROUNDS_PRIOR=0
          KISS_AUDIT='false'
          if [[ "${NET_MEASURED}" == 'true' ]]; then
            OVER_ROUNDS_PRIOR="$(jq -r --arg ab "${AUTOFIX_BOT}" --arg key "${LIVE_REARM_KEY}" --arg cutoff "${GROWTH_NOW_CUTOFF}" --arg curr "${GITHUB_RUN_ID}" '
              [ .[] | select((.user.login // "") == $ab) | . as $c | ($c.body // "")
                | [ scan("<!-- autofix-growth-now src=(-?[0-9]+) test=(-?[0-9]+) over=([a-z]+) round=([0-9]+) run=([0-9]+) (?:measured=([^ ]+) )?key=([^ ]+) -->") ] | .[]
                | {over: .[2], run: (.[4] | tonumber), measured: (.[5] // ($c.created_at // "")), explicit: (.[5] != null), win: .[6]} ]
              | group_by(.run) | map(max_by([.explicit, .measured]))
              | map(select(.win == $key and .over == "true"))
              | map(select(.run != ($curr | tonumber)))
              | map(select($cutoff == "" or (.measured > $cutoff)))
              | length' "${WORKDIR}/ic.json" 2> /dev/null || echo 0)"
            [[ "${OVER_ROUNDS_PRIOR}" =~ ^[0-9]+$ ]] || OVER_ROUNDS_PRIOR=0
          fi
          # Audit on the FIRST breach, not after spending more rounds proving
          # non-convergence: the judgment is what a budget breach means now.
          if [[ "${CRITICAL_ONLY_GROWTH}" == 'true' ]]; then
            KISS_AUDIT='true'
          fi
          # The over-budget flag feeds the report's per-round marker; the
          # audit itself is enforced by the feedback.md section below plus
          # the verification gate's verdict requirement.
          echo "critical_only_growth=${CRITICAL_ONLY_GROWTH}" >> "${GITHUB_OUTPUT}"
          echo "kiss_audit=${KISS_AUDIT}" >> "${GITHUB_OUTPUT}"
          [[ "${KISS_AUDIT}" == 'true' ]] &&
            echo "🔍 growth budget breached (source ${GROWTH_SRC} / test ${GROWTH_TEST} vs budgets ${GROWTH_BUDGET_SRC_LINES}/${GROWTH_BUDGET_TEST_LINES}; ${OVER_ROUNDS_PRIOR} prior over-budget round(s) this window) — this round is a growth-audit round."
          # Conflict-handoff idempotence: a conflict verdict parks the PR at
          # a genuinely human call.
          # Full rationale → qwen-autofix.md#af-122
          CONFLICT_SINCE="$(jq -r --arg ab "${AUTOFIX_BOT}" --arg key "${LIVE_REARM_KEY}" '
            [ .[] | select((.user.login // "") == $ab) | . as $c | ($c.body // "")
              | [ scan("<!-- autofix-growth-audit verdict=conflict win=([^ ]+) -->") ] | .[]
              | select(.[0] == $key) | ($c.created_at // "") ]
            | max // ""' "${WORKDIR}/ic.json" 2> /dev/null || echo "")"
          if [[ -n "${CONFLICT_SINCE}" && "${STALE}" != 'true' ]]; then
            CONFLICT_WAKE="$(jq -rs \
              --arg since "${CONFLICT_SINCE}" --arg baseupd "${BASE_UPD_AT}" \
              --arg rb "${REVIEW_BOT}" --arg ab "${AUTOFIX_BOT}" \
              --argjson trust "${TRUSTED_ASSOC}" '
              (.[0] | map(select((.submitted_at // "") > $since)
                | select((.user.login // "") != $ab and (.user.login // "") != $rb)
                | select(((.author_association // "") | IN($trust[])))
                | select((.state // "") | IN("CHANGES_REQUESTED", "COMMENTED"))) | length)
              + (.[1] | map(select((.created_at // "") > $since)
                | select((.user.login // "") != $ab and (.user.login // "") != $rb)
                | select(((.author_association // "") | IN($trust[])))) | length)
              + (.[2] | map(select((.created_at // "") > $since)
                | select((.user.login // "") != $ab and (.user.login // "") != $rb)
                | select(((.author_association // "") | IN($trust[])))
                | select((.body // "") | test("<!-- (autofix-eval|autofix-rearm|qwen-triage|qwen-review-suggestion-summary|pr-force-push|qwen-review-ack) ") | not)
                | select((.body // "") | test("^\\s*@qwen-code /") | not)) | length)
              + (.[3] | map(select((.conclusion // .state // "") | IN("FAILURE", "FAILED", "ERROR", "TIMED_OUT", "ACTION_REQUIRED"))
                | select(((.workflowName // "") | IN("Qwen Autofix", "🧐 Qwen Pull Request Review", "Qwen CI Failure Patrol", "Qwen Autofix Fork Bridge", "Qwen Autofix Fork Signal")) | not)
                | select((.completedAt // .updatedAt // "") > $since)
                | select((.completedAt // .updatedAt // "") > $baseupd)) | length)' \
              "${WORKDIR}/rv.json" "${WORKDIR}/rc.json" "${WORKDIR}/ic.json" "${WORKDIR}/checks.json" 2> /dev/null || echo 0)"
            [[ "${CONFLICT_WAKE}" =~ ^[0-9]+$ ]] || CONFLICT_WAKE=0
            if [[ "${CONFLICT_WAKE}" == "0" ]]; then
              STALE='true'
              echo "🫥 conflict handoff pending: a growth audit returned conflict in this window and no trusted human has responded since — idling without an agent run or comment"
            fi
          fi
          # Convergence-break mirror (#10107): a target can be emitted
          # moments before the tripping review lands, or forced past the
          # scan, so the leg re-derives the scan gate's reading live and
          # idles. Keep the two in lockstep; the notice stays the scan's
          # job — this leg discards without comment, like the park above.
          # Full rationale → qwen-autofix.md#af-153
          if [[ "${STALE}" != 'true' ]]; then
            [[ "${CONVERGENCE_BREAK_ROUNDS}" =~ ^[1-9][0-9]{0,2}$ ]] || CONVERGENCE_BREAK_ROUNDS=3
            LIVE_CONV_SINCE="${LIVE_REARM_KEY}"
            [[ "${LIVE_CONV_SINCE}" == 'none' ]] && LIVE_CONV_SINCE=''
            LIVE_CONV_HUMAN_AT="$(jq -rs \
              --arg rb "${REVIEW_BOT}" --arg ab "${AUTOFIX_BOT}" \
              --argjson trust "${TRUSTED_ASSOC}" '
              (.[0] | map(select((.user.login // "") != $ab and (.user.login // "") != $rb)
                | select(((.author_association // "") | IN($trust[])))
                | select((.state // "") | IN("CHANGES_REQUESTED", "COMMENTED", "APPROVED", "DISMISSED")) | (.submitted_at // "")))
              + (.[1] | map(select((.user.login // "") != $ab and (.user.login // "") != $rb)
                | select(((.author_association // "") | IN($trust[]))) | (.created_at // "")))
              + (.[2] | map(select((.user.login // "") != $ab and (.user.login // "") != $rb)
                | select(((.author_association // "") | IN($trust[])))
                | select((.body // "") | test("<!-- (autofix-eval|autofix-rearm|qwen-triage|qwen-review-suggestion-summary|pr-force-push|qwen-review-ack) ") | not)
                | select((.body // "") | test("^\\s*@qwen-code /") | not) | (.created_at // "")))
              | max // ""' "${WORKDIR}/rv.json" "${WORKDIR}/rc.json" "${WORKDIR}/ic.json" 2> /dev/null || echo "")"
            [[ "${LIVE_CONV_HUMAN_AT}" > "${LIVE_CONV_SINCE}" ]] && LIVE_CONV_SINCE="${LIVE_CONV_HUMAN_AT}"
            LIVE_CONV_STREAK="$(jq -r --arg rb "${REVIEW_BOT}" --arg since "${LIVE_CONV_SINCE}" \
              --argjson codes "${CONVERGENCE_SIGNAL_CODES}" '
              [ .[] | select((.user.login // "") == $rb)
                | select((.submitted_at // "") > $since)
                | {at: (.submitted_at // ""),
                   marker: ((.body // "") | [ scan("<!-- qwen-review-ledger (.*?) -->") ] | last | .[0] // "")}
                | select(.marker != "")
                | .led = (.marker | try fromjson catch null)
                | select(.led | type == "object")
                | {at, round: (.led.round // 0),
                   rec: (.led.rec | if type == "array" then . else [] end)} ]
              | sort_by(.at) | group_by(.round) | map(last) | sort_by(.at)
              | reduce .[] as $r (0; if ($r.rec | any(. as $c | $codes | index($c))) then . + 1 else 0 end)' \
              "${WORKDIR}/rv.json" 2> /dev/null || echo 0)"
            [[ "${LIVE_CONV_STREAK}" =~ ^[0-9]+$ ]] || LIVE_CONV_STREAK=0
            if [[ "${LIVE_CONV_STREAK}" -ge "${CONVERGENCE_BREAK_ROUNDS}" ]]; then
              STALE='true'
              echo "🫥 convergence break pending: the last ${LIVE_CONV_STREAK} review round(s) carried a non-convergence signal and no trusted human has responded since — idling without an agent run or comment"
            else
              # The scan gate's park HOLD, kept in lockstep: a posted notice
              # keeps the leg STALE until the boundary itself moves, so an
              # in-flight round cannot land on a PR the scan still holds
              # parked after a clean round reset the streak.
              LIVE_CONV_NOTICES="$(jq -r --arg ab "${AUTOFIX_BOT}" --arg since "${LIVE_CONV_SINCE}" '
                [ .[] | select((.user.login // "") == $ab)
                  | select((.body // "") | contains("<!-- autofix-convergence-break -->"))
                  | select((.created_at // "") > $since) ] | length' "${WORKDIR}/ic.json" 2> /dev/null || echo 0)"
              [[ "${LIVE_CONV_NOTICES}" =~ ^[0-9]+$ ]] || LIVE_CONV_NOTICES=0
              if [[ "${LIVE_CONV_NOTICES}" != "0" ]]; then
                STALE='true'
                echo "🫥 convergence park holds: a convergence-break notice is newer than any maintainer response — idling without an agent run or comment"
              fi
            fi
          fi
          # Which trusted humans have exhausted their per-window regular
          # feedback budget (see CRITICAL_ONLY_HUMAN_BATCHES). A batch is
          # COUNTED only when a Critical-only round actually consumed it:
          # Full rationale → qwen-autofix.md#af-048
          OVER_BUDGET_AUTHORS='[]'
          if [[ "${CRITICAL_ONLY}" == "true" ]]; then
            OVER_BUDGET_AUTHORS="$(jq -n \
              --arg key "${LIVE_REARM_KEY}" --arg ab "${AUTOFIX_BOT}" --arg rb "${REVIEW_BOT}" \
              --argjson trust "${TRUSTED_ASSOC}" \
              --argjson critical_after "${CRITICAL_ONLY_AFTER_ROUND}" \
              --argjson k "${CRITICAL_ONLY_HUMAN_BATCHES}" \
              --slurpfile rv "${WORKDIR}/rv.json" --slurpfile rc "${WORKDIR}/rc.json" --slurpfile ic "${WORKDIR}/ic.json" '
              def leading_critical:
                gsub("^(?:(?:\\s|<!--[\\s\\S]*?(?:-->|$)|\\p{Cf}))+"; ""; "s")
                | startswith("**[Critical]**");
              ([ ($ic | add)[] | select((.user.login // "") == $ab) | . as $c | ($c.body // "")
                 | [ scan("<!-- autofix-eval ts=([^ ]+) acted=([^ ]+) round=([0-9]+)(?: win=([^ ]+))? -->") ] | .[]
                 | {ts: .[0], acted: .[1], round: (.[2] | tonumber), win: (.[3] // "none"), at: ($c.created_at // "")} ]
               | map(select(.win == $key) | select(.ts != "9999-12-31T23:59:59Z"))
               | sort_by(.at)) as $ms
              | ([ range(0; ($ms | length)) as $i
                   | ($ms[$i]
                      | select((.acted == "true" and .round > $critical_after) or (.acted == "false" and .round >= $critical_after))
                      | {lo: (if $i == 0 then "" else ($ms[$i - 1].ts) end), hi: .ts}) ]) as $spans
              | ($rv | add) as $reviews
              | ($rc | add) as $comments
              | ([ $reviews[]
                   | select((.state // "") == "COMMENTED")
                   | select(((.body // "") | leading_critical) | not)
                   | {at: (.submitted_at // ""), login: (.user.login // ""), assoc: (.author_association // "")} ]
                 + [ $comments[]
                   | select((
                       ((.body // "") | leading_critical)
                       or ((.in_reply_to_id // null) as $root
                         | $root != null
                         and any($comments[]; .id == $root and ((.body // "") | leading_critical)))
                       or ((.pull_request_review_id // null) as $review
                         | $review != null
                         and any($reviews[]; .id == $review and ((.state // "") == "CHANGES_REQUESTED")))
                     ) | not)
                   | {at: (.created_at // ""), login: (.user.login // ""), assoc: (.author_association // "")} ]
                 + [ ($ic | add)[]
                   | select((.body // "") | test("<!-- (autofix-eval|autofix-rearm|qwen-triage|qwen-review-suggestion-summary|pr-force-push|qwen-review-ack) ") | not)
                   | select(((((.user.login // "") == $rb)) and ((.body // "") | test("<!-- qwen-review docs-only-medium "))) | not)
                   | select((.body // "") | test("^\\s*@qwen-code /") | not)
                   | select(((.body // "") | leading_critical) | not)
                   | {at: (.created_at // ""), login: (.user.login // ""), assoc: (.author_association // "")} ])
              | map(select(.login != "" and .login != $ab and .login != $rb) | select(.assoc | IN($trust[])))
              | [ .[] | . as $c
                  | ([ $spans[] | select($c.at > .lo and $c.at <= .hi) ] | .[0] // empty)
                  | {login: $c.login, span: .hi} ]
              | group_by(.login)
              | map(select((map(.span) | unique | length) >= $k) | .[0].login)
              ' || echo '[]')"
            [[ -z "${OVER_BUDGET_AUTHORS}" ]] && OVER_BUDGET_AUTHORS='[]'
            [[ "${OVER_BUDGET_AUTHORS}" != '[]' ]] && echo "🚦 regular-feedback budget exhausted this window for: $(jq -r 'join(", ")' <<< "${OVER_BUDGET_AUTHORS}")"
          fi
          echo "stale=${STALE}" >> "${GITHUB_OUTPUT}"
          echo "effective_round=${ROUND}" >> "${GITHUB_OUTPUT}"
          echo "round_start=${LIVE_ROUND_START}" >> "${GITHUB_OUTPUT}"

          rm -f "${WORKDIR}/deferred-feedback.md"
          if [[ "${CRITICAL_ONLY}" == "true" ]]; then
            PR_URL="https://github.com/${REPO}/pull/${PR}"
            # Name the cause(s) precisely: a maintainer reading "after five
            # rounds" on a round-2 PR that tripped the GROWTH budget would
            # reasonably conclude the brake misfired.
            # No literal '+' prefix: the values are signed (either dimension
            # can shrink while the other trips the brake), and '+-120' in
            # the cause line reads like a misfire to exactly its audience.
            GROWTH_CLAUSE_EN="the PR's diff grew src ${GROWTH_SRC} / test ${GROWTH_TEST} net lines beyond this counting window's baseline (budgets: ${GROWTH_BUDGET_SRC_LINES}/${GROWTH_BUDGET_TEST_LINES})"
            GROWTH_CLAUSE_ZH="本计数窗口内 diff 净增长已达 源码 ${GROWTH_SRC} / 测试 ${GROWTH_TEST} 行（预算 ${GROWTH_BUDGET_SRC_LINES}/${GROWTH_BUDGET_TEST_LINES}）"
            # Name the seed when there is one. Without this the audit record
            # claims "5 change-producing rounds are complete" on a PR the loop
            # has run twice — true of the counter, visibly false of the PR, and
            # unfalsifiable for the maintainer reading it.
            ROUNDS_CLAUSE_EN="${CRITICAL_ONLY_AFTER_ROUND} change-producing rounds are complete"
            ROUNDS_CLAUSE_ZH="已完成 ${CRITICAL_ONLY_AFTER_ROUND} 个产生改动的轮次"
            # :-0 is load-bearing, not defensive habit: an UNSET seed is not
            # '0' under string comparison, so a bare `!= '0'` renders the
            # seeded wording on every ordinary PR.
            if [[ "${LIVE_ROUND_START:-0}" != '0' ]]; then
              # Cite the seed as TYPED, not as clamped: when the read-site
              # clamp fired, quoting the clamped value renders a command
              # nobody sent while the engage ack still shows the original.
              SEED_AS_TYPED="${LIVE_ROUND_START_RAW:-${LIVE_ROUND_START}}"
              CLAMP_NOTE_EN=''
              CLAMP_NOTE_ZH=''
              if [[ "${SEED_AS_TYPED}" != "${LIVE_ROUND_START}" ]]; then
                CLAMP_NOTE_EN=", clamped to ${LIVE_ROUND_START} under the effective cap ${MAX_ROUNDS}"
                CLAMP_NOTE_ZH="，已按有效上限 ${MAX_ROUNDS} 收敛为 ${LIVE_ROUND_START}"
              fi
              ROUNDS_CLAUSE_EN="the round counter reached ${CRITICAL_ONLY_AFTER_ROUND} (this window was seeded at round ${SEED_AS_TYPED} by \`${TAKEOVER_COMMAND} from ${SEED_AS_TYPED}\`${CLAMP_NOTE_EN}, plus $(( ROUND - LIVE_ROUND_START )) change-producing round(s) since)"
              ROUNDS_CLAUSE_ZH="轮次计数已达 ${CRITICAL_ONLY_AFTER_ROUND}（本窗口由 \`${TAKEOVER_COMMAND} from ${SEED_AS_TYPED}\` 从第 ${SEED_AS_TYPED} 轮起算${CLAMP_NOTE_ZH}，此后又完成 $(( ROUND - LIVE_ROUND_START )) 个产生改动的轮次）"
            fi
            if [[ "${CRITICAL_ONLY_ROUNDS}" == 'true' && "${CRITICAL_ONLY_GROWTH}" == 'true' ]]; then
              CAUSE_EN="${ROUNDS_CLAUSE_EN} and ${GROWTH_CLAUSE_EN}"
              CAUSE_ZH="${ROUNDS_CLAUSE_ZH}，且${GROWTH_CLAUSE_ZH}"
            elif [[ "${CRITICAL_ONLY_GROWTH}" == 'true' ]]; then
              CAUSE_EN="${GROWTH_CLAUSE_EN}"
              CAUSE_ZH="${GROWTH_CLAUSE_ZH}"
            else
              CAUSE_EN="${ROUNDS_CLAUSE_EN}"
              CAUSE_ZH="${ROUNDS_CLAUSE_ZH}"
            fi
            # The maintainer batch budget is enforced by the OVER_BUDGET
            # census, whose spans exist only in round-brake territory
            # (rounds past CRITICAL_ONLY_AFTER_ROUND). A growth-only
            # engagement below the threshold has no enforceable budget, and
            # the audit record must describe the policy actually in force —
            # not promise accounting the census cannot produce.
            if [[ "${CRITICAL_ONLY_ROUNDS}" == 'true' ]]; then
              BUDGET_EN="Maintainer feedback is deferred only after its author has used ${CRITICAL_ONLY_HUMAN_BATCHES} regular feedback batches in this window's Critical-only tail; authors at that budget, if any, are named below."
              BUDGET_ZH="维护者反馈仅在其本人于本窗口 Critical-only 阶段已使用 ${CRITICAL_ONLY_HUMAN_BATCHES} 批常规反馈预算后才会延后；达到预算的作者（如有）在下方点名。"
            else
              BUDGET_EN="Maintainer feedback continues to flow unaffected during a growth-only engagement (the per-author batch budget applies only after ${CRITICAL_ONLY_AFTER_ROUND} change-producing rounds)."
              BUDGET_ZH="纯增长触发期间维护者反馈照常流动（按作者的批次预算仅在完成 ${CRITICAL_ONLY_AFTER_ROUND} 个产生改动的轮次后生效）。"
            fi
            {
              echo '## Deferred non-Critical feedback'
              echo
              echo "Critical-only mode is active: ${CAUSE_EN}. The workflow excluded the non-Critical feedback below from this round's actionable sections; the items remain open for human follow-up. ${BUDGET_EN} (\`@qwen-code /retry\` starts a fresh counting window.)"
              echo
              jq -r --arg wm "${WATERMARK}" --arg rb "${REVIEW_BOT}" --arg ab "${AUTOFIX_BOT}" \
                --arg pr_url "${PR_URL}" --argjson over "${OVER_BUDGET_AUTHORS}" '
                def leading_critical:
                  gsub("^(?:(?:\\s|<!--[\\s\\S]*?(?:-->|$)|\\p{Cf}))+"; ""; "s")
                  | startswith("**[Critical]**");
                .[]
                | select((.submitted_at // "") > $wm)
                | select((.user.login // "") != $ab)
                | select(((.user.login // "") == $rb) or ((.user.login // "") | IN($over[])))
                | select((.state // "") == "COMMENTED")
                | select(((.body // "") | leading_critical) | not)
                | "- Review by @\(.user.login): \(.html_url // $pr_url)"' \
                "${WORKDIR}/rv.json"
              jq -rs --arg wm "${WATERMARK}" --arg rb "${REVIEW_BOT}" --arg ab "${AUTOFIX_BOT}" \
                --arg pr_url "${PR_URL}" --argjson over "${OVER_BUDGET_AUTHORS}" \
                --slurpfile reviews "${WORKDIR}/rv.json" '
                def leading_critical:
                  gsub("^(?:(?:\\s|<!--[\\s\\S]*?(?:-->|$)|\\p{Cf}))+"; ""; "s")
                  | startswith("**[Critical]**");
                add as $comments
                | ($reviews | add) as $reviews
                | $comments[]
                | select((.created_at // "") > $wm)
                | select((.user.login // "") != $ab)
                | select(((.user.login // "") == $rb) or ((.user.login // "") | IN($over[])))
                | select((
                    ((.body // "") | leading_critical)
                    or ((.in_reply_to_id // null) as $root
                      | $root != null
                      and any($comments[];
                        .id == $root
                        and ((.body // "") | leading_critical)))
                    or ((.pull_request_review_id // null) as $review
                      | $review != null
                      and any($reviews[];
                        .id == $review
                        and ((.state // "") == "CHANGES_REQUESTED")))
                  ) | not)
                | "- Inline rc:\(.id) \(.path // "?"):\(.line // "?"): \(.html_url // $pr_url)"' \
                "${WORKDIR}/rc.json"
              jq -r --arg wm "${WATERMARK}" --arg rb "${REVIEW_BOT}" --arg ab "${AUTOFIX_BOT}" \
                --arg pr_url "${PR_URL}" --argjson over "${OVER_BUDGET_AUTHORS}" '
                def leading_critical:
                  gsub("^(?:(?:\\s|<!--[\\s\\S]*?(?:-->|$)|\\p{Cf}))+"; ""; "s")
                  | startswith("**[Critical]**");
                .[]
                | select((.created_at // "") > $wm)
                | select((.user.login // "") != $ab)
                | select(((.user.login // "") == $rb) or ((.user.login // "") | IN($over[])))
                | select((.body // "") | test("<!-- (autofix-eval|autofix-rearm|qwen-triage|qwen-review-suggestion-summary|pr-force-push|qwen-review-ack|qwen-review-salvaged) ") | not)
                | select(((((.user.login // "") == $rb)) and ((.body // "") | test("<!-- qwen-review docs-only-medium "))) | not)
                | select((.body // "") | test("^\\s*@qwen-code /") | not)
                | select(((.body // "") | leading_critical) | not)
                | "- PR comment by @\(.user.login): \(.html_url // $pr_url)"' \
                "${WORKDIR}/ic.json"
              if [[ "${OVER_BUDGET_AUTHORS}" != '[]' ]]; then
                echo
                jq -r '.[] | "- @" + . + " is at this window'"'"'s regular-feedback budget — to continue: start your comment with **[Critical]**, submit a Request changes review, or comment `@qwen-code /retry` for a fresh window. / @" + . + " 本窗口常规反馈预算已用完——继续请以 **[Critical]** 开头评论、提交 Request changes、或评论 `@qwen-code /retry` 开新窗口。"' <<< "${OVER_BUDGET_AUTHORS}"
              fi
              echo
              echo '<details>'
              echo '<summary>中文说明</summary>'
              echo
              echo "已进入仅处理 Critical 的模式：${CAUSE_ZH}。本轮可执行区域已排除下方非 Critical 反馈；这些条目保持开放，留待人工跟进。${BUDGET_ZH}（评论 \`@qwen-code /retry\` 可开启新的计数窗口。）"
              echo
              echo '</details>'
            } > "${WORKDIR}/deferred-feedback.md"
          fi

          # Render the actionable feedback into one prompt-ready file.
          {
            ISSUE_REF=""
            [[ "${ISSUE}" != "${PR}" ]] && ISSUE_REF=" (issue #${ISSUE})"
            echo "# Review feedback to triage on PR #${PR}${ISSUE_REF}"
            echo
            echo "Only feedback newer than the last evaluation (${WATERMARK}) from"
            echo "trusted maintainers or the automated reviewer is listed."
            echo
            # Diff-growth trajectory: the agent sees how big this PR has grown
            # so it can prefer minimal/subtractive fixes and recognise a
            # non-converging spiral, rather than reflexively adding code for
            # every finding.
            if [[ "${NET_MEASURED}" == 'true' ]]; then
              echo "## Diff growth this window"
              echo
              echo "Net diff vs this counting window's baseline: source ${GROWTH_SRC} / test ${GROWTH_TEST} lines (budgets ${GROWTH_BUDGET_SRC_LINES}/${GROWTH_BUDGET_TEST_LINES}; ${OVER_ROUNDS_PRIOR} prior round(s) already over budget). Prefer minimal, root-cause, subtractive fixes. If closing a finding would grow the diff materially AND the same class of gap keeps reappearing on code earlier rounds added, consolidate or subtract — do not keep adding guards."
              echo
            fi
            # Growth audit (a size signal triggers a JUDGMENT, never a stop):
            # the window is over its growth budget, so before any other work
            # the round audits the approach — two axes, burden of proof
            # inverted — and records a machine-readable verdict the
            # verification gate requires.
            # Full rationale → qwen-autofix.md#af-123
            if [[ "${KISS_AUDIT}" == 'true' ]]; then
              echo "## Growth audit required — this window is over its growth budget"
              echo
              echo "Window growth: source ${GROWTH_SRC} / test ${GROWTH_TEST} net lines vs budgets ${GROWTH_BUDGET_SRC_LINES}/${GROWTH_BUDGET_TEST_LINES} (${OVER_ROUNDS_PRIOR} prior round(s) already over budget this window). Before any other work this round, run the growth audit (SKILL: growth-audit mode) and write \`${WORKDIR}/growth-audit.json\` — the round cannot push without a valid verdict. A size signal triggers a judgment, never a stop: the audit decides whether to continue as-is (sound), simplify first (drift), or hand a genuinely contested choice to a human (conflict)."
              echo
              PRIOR_AUDITS="$(jq -r --arg ab "${AUTOFIX_BOT}" --arg key "${LIVE_REARM_KEY}" '
                [ .[] | select((.user.login // "") == $ab) | . as $c | ($c.body // "")
                  | [ scan("<!-- autofix-growth-audit verdict=([a-z]+) win=([^ ]+) -->") ] | .[]
                  | select(.[1] == $key) | "- \($c.created_at // "?"): verdict=\(.[0])" ]
                | .[]' "${WORKDIR}/ic.json" 2> /dev/null || true)"
              if [[ -n "${PRIOR_AUDITS}" ]]; then
                echo "Prior growth audits this window — a repeated verdict needs new evidence:"
                echo
                printf '%s\n' "${PRIOR_AUDITS}"
              else
                echo "No prior growth audit this window."
              fi
              echo
            fi
            echo "## Reviews"
            jq -r --arg wm "${WATERMARK}" --arg rb "${REVIEW_BOT}" --arg ab "${AUTOFIX_BOT}" \
              --argjson critical_only "${CRITICAL_ONLY}" --argjson trust "${TRUSTED_ASSOC}" \
              --argjson over "${OVER_BUDGET_AUTHORS}" '
              def leading_critical:
                gsub("^(?:(?:\\s|<!--[\\s\\S]*?(?:-->|$)|\\p{Cf}))+"; ""; "s")
                | startswith("**[Critical]**");
              .[]
              | select((.submitted_at // "") > $wm)
              | select((.user.login // "") != $ab)
              | select(((.author_association // "") | IN($trust[])) or (.user.login // "") == $rb)
              | select((.state // "") | IN("CHANGES_REQUESTED", "COMMENTED"))
              | select(($critical_only | not)
                  or (((.user.login // "") != $rb) and (((.user.login // "") | IN($over[])) | not))
                  or (.state // "") == "CHANGES_REQUESTED"
                  or ((.body // "") | leading_critical))
              | "- [rv:\(.id)] [\(.state)] @\(.user.login): \(.body // "" | gsub("\r"; ""))"' \
              "${WORKDIR}/rv.json"
            echo
            echo "## Inline comments"
            jq -rs --arg wm "${WATERMARK}" --arg rb "${REVIEW_BOT}" --arg ab "${AUTOFIX_BOT}" \
              --argjson critical_only "${CRITICAL_ONLY}" --argjson trust "${TRUSTED_ASSOC}" \
              --argjson over "${OVER_BUDGET_AUTHORS}" \
              --slurpfile reviews "${WORKDIR}/rv.json" '
              def leading_critical:
                gsub("^(?:(?:\\s|<!--[\\s\\S]*?(?:-->|$)|\\p{Cf}))+"; ""; "s")
                | startswith("**[Critical]**");
              add as $comments
              | ($reviews | add) as $reviews
              | $comments[]
              | select((.created_at // "") > $wm)
              | select((.user.login // "") != $ab)
              | select(((.author_association // "") | IN($trust[])) or (.user.login // "") == $rb)
              | select(($critical_only | not)
                  or (((.user.login // "") != $rb) and (((.user.login // "") | IN($over[])) | not))
                  or ((.body // "") | leading_critical)
                  or ((.in_reply_to_id // null) as $root
                    | $root != null
                    and any($comments[];
                      .id == $root
                      and ((.body // "") | leading_critical)))
                  or ((.pull_request_review_id // null) as $review
                    | $review != null
                    and any($reviews[];
                      .id == $review
                      and ((.state // "") == "CHANGES_REQUESTED"))))
              | "- [rc:\(.id)] \(.path // "?"):\(.line // "?") @\(.user.login): \(.body // "" | gsub("\r"; ""))"' \
              "${WORKDIR}/rc.json"
            echo
            echo "## Issue-level comments"
            jq -r --arg wm "${WATERMARK}" --arg rb "${REVIEW_BOT}" --arg ab "${AUTOFIX_BOT}" \
              --argjson critical_only "${CRITICAL_ONLY}" --argjson trust "${TRUSTED_ASSOC}" \
              --argjson over "${OVER_BUDGET_AUTHORS}" '
              def leading_critical:
                gsub("^(?:(?:\\s|<!--[\\s\\S]*?(?:-->|$)|\\p{Cf}))+"; ""; "s")
                | startswith("**[Critical]**");
              .[]
              | select((.created_at // "") > $wm)
              | select((.user.login // "") != $ab)
              | select(((.author_association // "") | IN($trust[])) or (.user.login // "") == $rb)
              | select((.body // "") | test("<!-- (autofix-eval|autofix-rearm|qwen-triage|qwen-review-suggestion-summary|pr-force-push|qwen-review-ack|qwen-review-salvaged) ") | not)
              | select(((((.user.login // "") == $rb)) and ((.body // "") | test("<!-- qwen-review docs-only-medium "))) | not)
                | select((.body // "") | test("^\\s*@qwen-code /") | not)
              | select(($critical_only | not)
                  or (((.user.login // "") != $rb) and (((.user.login // "") | IN($over[])) | not))
                  or ((.body // "") | leading_critical))
              | "- [ic:\(.id)] @\(.user.login): \(.body // "" | gsub("\r"; ""))"' \
              "${WORKDIR}/ic.json"
            if [[ -s "${WORKDIR}/deferred-feedback.md" ]]; then
              echo
              cat "${WORKDIR}/deferred-feedback.md"
            fi
            echo
            echo "## Failed checks"
            jq -r --arg wm "${WATERMARK}" '
              .[]
              | select((.conclusion // .state // "") | IN("FAILURE", "FAILED", "ERROR", "TIMED_OUT", "ACTION_REQUIRED", "CANCELLED"))
              | select(((.workflowName // "") != "Qwen Autofix") or (((.name // "") | startswith("review-address"))))
              | select((.completedAt // .updatedAt // "") > $wm)
              | "- \(((.name // .workflowName) // "external check") | gsub("[^A-Za-z0-9 _./()-]"; "") | .[0:80]): \(.conclusion // .state // "?")"' \
              "${WORKDIR}/checks.json"
            # Checks that turned red BEFORE the watermark and are STILL red.
            # The section above only shows checks that failed AFTER the
            # watermark, so a persistent red (the exact case the scan's
            # N_RED_NOW gate selects) would leave the agent with an empty
            # "Failed checks" section and no check name to reproduce.
            echo
            echo "## Still-red checks (persisting from before the last evaluation)"
            jq -r --arg wm "${WATERMARK}" '
              .[]
              | select((.conclusion // .state // "") | IN("FAILURE", "FAILED", "ERROR", "TIMED_OUT", "ACTION_REQUIRED"))
              | select(((.workflowName // "") != "Qwen Autofix") or (((.name // "") | startswith("review-address"))))
              | select((.completedAt // .updatedAt // "") <= $wm)
              | "- \(((.name // .workflowName) // "external check") | gsub("[^A-Za-z0-9 _./()-]"; "") | .[0:80]): \(.conclusion // .state // "?")"' \
              "${WORKDIR}/checks.json"
            # If the LAST round ended in a gate rejection, show the agent WHY.
            # It is otherwise invisible on the retry: the reason lives in the
            # bot's own handoff comment, which the feedback filter above
            # (correctly) excludes, so the agent would re-read only the original
            # review points and re-make the same mistake - #7208 was handed to a
            # human over a two-character TS4111 fix its own compiler output had
            # already spelled out.
            LAST_REJECTION="$(jq -r --arg ab "${AUTOFIX_BOT}" '
              [ .[] | select((.user.login // "") == $ab)
                | select((.body // "") | contains("<!-- autofix-eval ts=")) ]
              | sort_by(.created_at) | last | .body // ""' "${WORKDIR}/ic.json" \
              | sed -n '/<!-- autofix-gate-rejection-start -->/,/<!-- autofix-gate-rejection-end -->/p' \
              | sed '1d;$d')"
            if [[ -n "${LAST_REJECTION}" ]]; then
              echo
              echo '## Your previous attempt was REJECTED by the verification gate'
              echo
              printf '%s\n' "${LAST_REJECTION}"
            fi
            # Time-budget exhaustions SINCE THE LAST SUCCESSFUL ROUND mean
            # the standard address-everything prompt is not converging at
            # Full rationale → qwen-autofix.md#af-049
            # Idle (silent-sandbox) timeouts are excluded like in the cap
            # census: the narrowing advice targets budget exhaustion, and an
            # infra-killed round never had any budget to exhaust (af-073).
            PRIOR_TIMEOUTS="$(jq -r --arg ab "${AUTOFIX_BOT}" --arg key "${LIVE_REARM_KEY}" '
              [ .[] | select((.user.login // "") == $ab)
                | select((.body // "") | contains("<!-- autofix-eval "))
                | select(([ ((.body // "") | scan("<!-- autofix-eval ts=[^ ]+ acted=[^ ]+ round=[0-9]+(?: win=([^ ]+))? -->")) ] | map(.[0] // "none")) as $wins
                    | ($wins | length) > 0 and (($wins | last) == $key))
              ] | sort_by(.created_at)
              | map((.body | gsub("\r"; "") | split("\n")[0]))
              | (map(test("Addressed the latest review feedback|no changes needed")) | rindex(true) // -1) as $lastok
              | [ .[($lastok + 1):][] | select(contains("AutoFix ran out of time before finishing") and (contains("AutoFix ran out of time before finishing (idle-timeout") | not)) ] | length' "${WORKDIR}/ic.json" 2> /dev/null || true)"
            if [[ "${PRIOR_TIMEOUTS}" -ge 1 ]]; then
              echo
              echo '## Budget warning: previous round(s) ran out of time'
              echo
              echo "${PRIOR_TIMEOUTS} round(s) since the last successful round exhausted the agent time budget before finishing anything."
            fi
          } > "${WORKDIR}/feedback.md"
          echo '--- feedback.md ---'
          # Reviewer/bot comment bodies ride this file VERBATIM and this echo
          # puts them on step stdout, which the runner scans for workflow
          # commands in BOTH syntaxes: `::name::` AND the legacy `##[name]`,
          # the latter parsed MID-line too — a quoted `##[add-matcher]` makes
          # the runner load the rest of the line as a matcher file and fail
          # THIS step before the agent runs. Measured on #9761: a review
          # finding about that injection channel carried the payload strings
          # as its example text, and five consecutive pre-agent crashes
          # burned the takeover window in ~70 minutes. Neutralize both
          # prefixes like every other untrusted echo.
          cat "${WORKDIR}/feedback.md" | sed -e 's/::/;;/g' -e 's/##\[/##［/g'

      # The agent below runs for up to 130 minutes and the verification gate adds
      # more, but nothing reaches the PR thread until "Push and report" at the
      # very end: a maintainer who just engaged takeover sees silence and cannot
      # tell a working round from a stuck one.
      # Full rationale → qwen-autofix.md#af-124
      - name: 'Post autofix status comment'
        id: 'post_status'
        if: |-
          ${{ steps.prepare.outputs.stale != 'true' && needs.route.outputs.dry_run != 'true' }}
        env:
          GITHUB_TOKEN: '${{ secrets.CI_DEV_BOT_PAT }}'
          EFFECTIVE_ROUND: '${{ steps.prepare.outputs.effective_round }}'
          RUN_URL: '${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}'
          SERVER_URL: '${{ github.server_url }}'
          HEARTBEAT_SHA256: '${{ steps.stage.outputs.heartbeat_sha256 }}'
          TRUSTED_PATH: '${{ steps.stage.outputs.trusted_path }}'
        run: |-
          set -uo pipefail
          # Binary resolution: this step and the heartbeat loop it launches
          # carry the bot PAT, and the job's own $GITHUB_PATH append keeps
          # ${RUNNER_TEMP}/qwen-bin ahead of /usr/bin here — pin PATH from
          # the stage-time capture BEFORE the first command word resolves
          # (the R6-3 doctrine the sibling PAT steps apply; the loop
          # re-pins from the same capture). Full rationale → af-149
          export PATH="${TRUSTED_PATH}"
          # The gh calls in this step (deep-link lookup, comment upsert) and
          # the heartbeat loop launched here all carry the bot PAT: take the
          # af-112 hermetic pins BEFORE the first call — pinned host, planted
          # tokens dropped, and a fresh empty GH_CONFIG_DIR per call instead
          # of the default ~/.config/gh on the shared attacker-writable HOME
          # (its config.yml http_unix_socket would deliver the Authorization
          # header — the PAT — to a planted same-UID listener).
          # Full rationale → qwen-autofix.md#af-112
          export GH_HOST=github.com
          unset GH_ENTERPRISE_TOKEN GH_TOKEN
          # The config dir is minted PER CALL (the R11-1 shape): every gh
          # call below rides this wrapper, minting a fresh empty dir
          # milliseconds before the call and removing it right after. A dir
          # minted once and reused across the step's calls is plantable —
          # RUNNER_TEMP is same-UID-writable, and a config.yml carrying
          # http_unix_socket written into it between calls would capture
          # the next call's Authorization header (the PAT). Fail CLOSED:
          # a failed mint refuses the call — gh never runs with the PAT
          # against the shared ~/.config/gh this pin exists to close off.
          # (The loop mints per tick the same way; finalize mints adjacent
          # to its single call.)
          hermetic_gh() {
            local cfg rc
            if ! cfg="$(mktemp -d "${RUNNER_TEMP}/autofix-gh-config.XXXXXX")"; then
              echo "::error::autofix heartbeat: could not create hermetic gh config dir; refusing gh calls with the PAT" >&2
              return 1
            fi
            GH_CONFIG_DIR="${cfg}" gh "$@"
            rc=$?
            rm -rf "${cfg}"
            return "${rc}"
          }
          MARKER='<!-- autofix-status -->'
          ROUND_DISPLAY="${EFFECTIVE_ROUND:-${ROUND}}"
          # ROUND counts rounds already DONE; every other message numbers the
          # round being performed (the report posts ROUND + 1). Match it, or
          # the same round carries two different numbers in one thread.
          if [[ "${ROUND_DISPLAY}" =~ ^[0-9]+$ ]]; then
            ROUND_DISPLAY="$((ROUND_DISPLAY + 1))"
          fi
          # Deep-link "Watch live progress" to THIS matrix leg's live log
          # instead of the run page. Best-effort: any lookup failure keeps
          # the run URL, so the link is never worse than before.
          # Full rationale → qwen-autofix.md#af-150
          JOB_URL="${RUN_URL}"
          JOB_ID="$(hermetic_gh api "repos/${REPO}/actions/runs/${GITHUB_RUN_ID}/attempts/${GITHUB_RUN_ATTEMPT}/jobs" --paginate |
            jq -rs --arg pr "${PR}" \
              '[ .[] | .jobs[]? | select((.name // "") | startswith("review-address (\($pr),")) | .id ] | last // empty')" ||
            JOB_ID=''
          if [[ "${JOB_ID}" =~ ^[0-9]+$ ]]; then
            JOB_URL="${SERVER_URL}/${REPO}/actions/runs/${GITHUB_RUN_ID}/job/${JOB_ID}"
          fi
          # The working-comment text lives in the heartbeat script: the
          # initial post and every later tick render through the SAME 'body'
          # subcommand, so they cannot drift. The working tree is PR-branch
          # code by this point — run the staged copy, digest-verified from
          # expression context (same doctrine as resanitize-git-config.sh).
          # An EMPTY digest means the script was absent from the trusted
          # base at staging (pre-merge rounds of a PR whose workflow
          # resolves from its own ref): degrade to the pre-PR inline body
          # and skip the heartbeat — never fail the round.
          # Full rationale → qwen-autofix.md#af-149
          START_EPOCH="$(date +%s)"
          if [[ -n "${HEARTBEAT_SHA256}" ]]; then
            /usr/bin/echo "${HEARTBEAT_SHA256}  ${RUNNER_TEMP}/autofix-status-heartbeat.sh" | /usr/bin/sha256sum -c - > /dev/null
            BODY="$(HB_ROUND="${ROUND_DISPLAY}" HB_CAP="${MAX_ROUNDS}" HB_URL="${JOB_URL}" \
              HB_WORKDIR="${WORKDIR}" HB_START_EPOCH="${START_EPOCH}" \
              bash --norc "${RUNNER_TEMP}/autofix-status-heartbeat.sh" body)"
          else
            BODY="$(printf '%s\n\n🔄 **AutoFix is working on this PR** — round %s/%s. [Watch live progress](%s); this round posts its report here when it finishes.\n\n<details>\n<summary>中文说明</summary>\n\n🔄 **AutoFix 正在处理此 PR** —— 第 %s/%s 轮。[查看实时进度](%s)；本轮结束后会在此发布报告。\n\n</details>' \
              "${MARKER}" "${ROUND_DISPLAY}" "${MAX_ROUNDS}" "${RUN_URL}" \
              "${ROUND_DISPLAY}" "${MAX_ROUNDS}" "${RUN_URL}")"
          fi
          STATUS_ID="$(hermetic_gh api "repos/${REPO}/issues/${PR}/comments" --paginate |
            jq -rs --arg m "${MARKER}" --arg ab "${AUTOFIX_BOT}" \
              '[ .[][] | select((.user.login // "") == $ab)
                 | select((.body // "") | contains($m)) ] | last | .id // empty')" ||
            STATUS_ID=''
          if [[ -n "${STATUS_ID}" ]]; then
            hermetic_gh api --method PATCH "repos/${REPO}/issues/comments/${STATUS_ID}" \
              -f body="${BODY}" > /dev/null ||
              echo "::warning::Failed to update the autofix status comment on PR #${PR}; continuing."
          else
            STATUS_ID="$(hermetic_gh api "repos/${REPO}/issues/${PR}/comments" \
              -f body="${BODY}" --jq '.id')" ||
              {
                STATUS_ID=''
                echo "::warning::Failed to post the autofix status comment on PR #${PR}; continuing."
              }
          fi
          # Round heartbeat: while the agent works, a detached loop
          # re-PATCHes this comment every ~10 min with elapsed time and
          # last agent activity, so the PR can tell a long round from a
          # dead one. It holds the PAT in env — a deliberate trade bounded
          # to the sandboxed agent phase: the verification gate kills the
          # loop BEFORE the first step that runs branch code on the host.
          # The pid travels to the killers through EXPRESSION CONTEXT —
          # WORKDIR is sandbox-writable, so no WORKDIR file is ever read
          # as a kill target. Full rationale → qwen-autofix.md#af-149
          HEARTBEAT_PID=''
          HEARTBEAT_START_TICKS=''
          # Re-verify immediately before THIS launch (R8-1): the check above
          # is separated from this second, PAT-holding execution by the two
          # gh round-trips of the comment upsert — a live swap window in the
          # attacker-writable RUNNER_TEMP. A mismatch fails the round closed;
          # the explicit `|| exit 1` does not rely on the step's ambient
          # shell options (af-023).
          if [[ -n "${STATUS_ID}" && -n "${HEARTBEAT_SHA256}" ]]; then
            /usr/bin/echo "${HEARTBEAT_SHA256}  ${RUNNER_TEMP}/autofix-status-heartbeat.sh" | /usr/bin/sha256sum -c - > /dev/null || exit 1
            HB_REPO="${REPO}" HB_COMMENT_ID="${STATUS_ID}" \
            HB_ROUND="${ROUND_DISPLAY}" HB_CAP="${MAX_ROUNDS}" HB_URL="${JOB_URL}" \
            HB_WORKDIR="${WORKDIR}" HB_START_EPOCH="${START_EPOCH}" \
              setsid bash --norc "${RUNNER_TEMP}/autofix-status-heartbeat.sh" loop &
            HEARTBEAT_PID=$!
            disown 2> /dev/null || true
            # Lifecycle pin for the killers: between this launch and
            # the later kills the pid above can be REUSED by the
            # runner, so every killer confirms the pid's start time
            # (field 22 of /proc/<pid>/stat — index 19 after stripping
            # through the LAST ')', since comm can carry spaces)
            # against this capture before signaling. The read is
            # /usr/bin/cat, the gate twin's shadowing doctrine:
            # a bare cat is shadowed by a $GITHUB_ENV-planted
            # BASH_FUNC_cat%% function, imported at bash STARTUP
            # even under --norc, ahead of builtins and PATH, and
            # a forged start time travels to every killer (R15-3).
            # Full rationale → qwen-autofix.md#af-149
            HB_STAT="$(/usr/bin/cat "/proc/${HEARTBEAT_PID}/stat" 2>/dev/null)" || HB_STAT=''
            HB_REST="${HB_STAT##*) }"
            HB_FIELDS=(${HB_REST})
            HEARTBEAT_START_TICKS="${HB_FIELDS[19]:-}"
          fi
          # Defense in depth on the producer: the ticks come from
          # the /proc parse above in this PAT-holding shell, and a
          # non-numeric value must never reach $GITHUB_OUTPUT —
          # the consumers route them through step env (the
          # STATUS_ID shape), where the runner sets them as data.
          [[ "${HEARTBEAT_START_TICKS}" =~ ^[0-9]+$ ]] || HEARTBEAT_START_TICKS=''
          # Hand the id to the finalize step so it does not repeat this scan.
          echo "comment_id=${STATUS_ID}" >> "${GITHUB_OUTPUT}"
          echo "heartbeat_pid=${HEARTBEAT_PID}" >> "${GITHUB_OUTPUT}"
          echo "heartbeat_start_ticks=${HEARTBEAT_START_TICKS}" >> "${GITHUB_OUTPUT}"

      - name: 'Triage and address'
        id: 'address'
        # Skipped entirely for a stale duplicate target (see the live-watermark
        # revalidation in prepare) — no agent run, no marker, no comment.
        if: |-
          ${{ steps.prepare.outputs.stale != 'true' }}
        # Bound the agent below the job timeout so a runaway agent fails THIS
        # step (not the whole job), leaving the always() verify and report
        # Full rationale → qwen-autofix.md#af-050
        timeout-minutes: 130
        env:
          QWEN_SANDBOX_IMAGE: '${{ steps.sandbox_image.outputs.image }}'
          DOCKER_HOST: ''
          DOCKER_CONTEXT: 'default'
          PR: '${{ env.PR }}'
          ISSUE: '${{ env.ISSUE }}'
          OPENAI_API_KEY: '${{ secrets.AUTOFIX_OPENAI_API_KEY }}'
          OPENAI_BASE_URL: '${{ secrets.AUTOFIX_OPENAI_BASE_URL || secrets.OPENAI_BASE_URL }}'
          OPENAI_MODEL: '${{ vars.QWEN_AUTOFIX_MODEL || vars.QWEN_PR_REVIEW_MODEL }}'
          NO_PROXY: '127.0.0.1,localhost,::1'
          QWEN_HOME: '${{ runner.temp }}/qwen-autofix-review-home'
          # The primary attempt's real budget: 120m, with a 10-minute margin
          # under the 130-minute step backstop above.
          # Full rationale → qwen-autofix.md#af-125
          QWEN_TIMEOUT_MS: '${{ vars.QWEN_AUTOFIX_TIMEOUT_MS || 7200000 }}'
          CONFLICT: '${{ steps.prepare.outputs.conflict }}'
          BASE: 'main'
          SETTINGS_JSON: |-
            {
              "maxSessionTurns": 400,
              "coreTools": [
                "read_file",
                "read_many_files",
                "glob",
                "search_file_content",
                "write_file",
                "run_shell_command(cat)",
                "run_shell_command(git add)",
                "run_shell_command(git checkout)",
                "run_shell_command(git commit)",
                "run_shell_command(git diff)",
                "run_shell_command(git log)",
                "run_shell_command(git merge)",
                "run_shell_command(git status)",
                "run_shell_command(ls)",
                "run_shell_command(mkdir)",
                "run_shell_command(npm run build)",
                "run_shell_command(npm run typecheck)",
                "run_shell_command(npm run lint)",
                "run_shell_command(npx vitest)",
                "run_shell_command(npm run generate:settings-schema)",
                "run_shell_command(pwd)"
              ],
              "tools": {
                "sandbox": "docker"
              }
            }
        run: |-
          rm -rf "${QWEN_HOME}"
          mkdir -p .qwen "${QWEN_HOME}"
          if [[ -z "${OPENAI_API_KEY:-}" ]]; then
            echo '::error::AUTOFIX_OPENAI_API_KEY secret is required for Qwen Autofix.'
            exit 1
          fi
          printf '%s\n' "${SETTINGS_JSON}" > .qwen/settings.json
          rm -f "${WORKDIR}/failure.md" "${WORKDIR}/failure.zh.md"
          # Prepare severed hooks for its PAT-bearing git ops; THIS step
          # holds no PAT, so the branch's own hooks may check the agent's
          # commits again.
          # Full rationale → qwen-autofix.md#af-126
          git config core.hooksPath .husky
          # Clamp the override to the budget ceiling: a repo variable past
          # 7,200,000 ms (120m) would arm the timer past the 130-minute step
          # Full rationale → qwen-autofix.md#af-051
          BUDGET_CAP_MS=7200000
          BUDGET_FLOOR_MS=60000
          if [[ ! "${QWEN_TIMEOUT_MS}" =~ ^[0-9]{1,8}$ ]] ||
             (( 10#${QWEN_TIMEOUT_MS} < BUDGET_FLOOR_MS )) ||
             (( 10#${QWEN_TIMEOUT_MS} > BUDGET_CAP_MS )); then
            echo "::warning::QWEN_TIMEOUT_MS=${QWEN_TIMEOUT_MS} is not an integer of MILLISECONDS in [${BUDGET_FLOOR_MS}, ${BUDGET_CAP_MS}] (120 means 120ms, not 120 minutes); clamping to ${BUDGET_CAP_MS}"
            QWEN_TIMEOUT_MS="${BUDGET_CAP_MS}"
          fi
          export QWEN_TIMEOUT_MS
          # Trusted staged copy in the mirrored layout — resolves
          # ../SKILL.md to the trusted staged SKILL, never the PR branch's.
          node "${RUNNER_TEMP}/autofix-skill/scripts/run-agent.mjs" \
            --mode address-review \
            --pr "${PR}" \
            --issue "${ISSUE}" \
            --workdir "${WORKDIR}" \
            --conflict "${CONFLICT}" \
            --base "${BASE}"

      - name: 'Verification gate'
        id: 'verify'
        if: |-
          ${{ always() && steps.prepare.outputs.stale != 'true' }}
        continue-on-error: true
        # Unbounded until now, and the largest consumer in the job (22m48s
        # measured on #8211). Left unbounded it eats the job timeout, and a
        # JOB timeout cancels the always() reporters — the silent round this
        # design exists to prevent. Bounded here it degrades to the ordinary
        # verification-failure path instead: continue-on-error keeps the job
        # alive, 'Finalize verification' sees an empty outcome, falls through
        # its case to exit 1, and the always() report step posts.
        timeout-minutes: 60
        env:
          # BASH_ENV is sourced by bash at process STARTUP, before line 1 of
          # the body below — a body-side unset is one hop late. Pinning it
          # empty at step level outranks any $GITHUB_ENV plant (same
          # doctrine as FOOTPRINT_ENFORCE below); SHELLOPTS is the sibling
          # option-import channel. The gate then launches through an env -i
          # clean child, so its bash inherits nothing at all.
          BASH_ENV: ''
          SHELLOPTS: ''
          # LD_* are likewise mapped by ld.so at process STARTUP, before the
          # body's unset can run: the unset clears them for children but
          # cannot unload a library already mapped into THIS step's bash,
          # whose execve hooks would forge the pre-launch digest check
          # below; ld.so ignores empty values (R6-2).
          LD_PRELOAD: ''
          LD_AUDIT: ''
          LD_LIBRARY_PATH: ''
          TRUSTED_PATH: '${{ steps.stage.outputs.trusted_path }}'
          VERIFY_RUNNER_SHA256: '${{ steps.stage.outputs.verify_runner_sha256 }}'
          # RUNNER_TEMP, WORKDIR, and BRANCH re-enter the digest check and
          # the allowlisted child below from the step environment: a
          # $GITHUB_ENV plant points the digest oracle at a decoy runner
          # (the hash is expression-context, the checked path is not) and
          # swaps the tree the gate builds/tests. Pin them from trusted
          # expression context, the TRUSTED_PATH doctrine above (R6-3).
          RUNNER_TEMP: '${{ runner.temp }}'
          WORKDIR: '/tmp/autofix-review-${{ matrix.target.pr }}'
          BRANCH: '${{ matrix.target.branch }}'
          # CI re-enters the allowlisted child through the step
          # environment: the child's `:-true` default only covers an UNSET
          # CI, so a $GITHUB_ENV plant of CI=false survives the expansion
          # and inverts the gate's CI semantics. Pin it at step level, the
          # FOOTPRINT_ENFORCE doctrine below (R1-1).
          CI: 'true'
          # HOME re-enters the allowlisted child below from the step
          # environment: a $GITHUB_ENV-planted HOME points npm's userconfig
          # at a .npmrc whose script-shell wraps every verdict-determining
          # `npm run` in an attacker shell — a red branch reports green.
          # Pin it from the stage-time capture, the TRUSTED_PATH doctrine
          # above (R8-3).
          HOME: '${{ steps.stage.outputs.trusted_home }}'
          # Step-level env outranks $GITHUB_ENV: an earlier shell-capable
          # step (the agent runs branch code on the host) must not be able
          # to downgrade a repo-variable 'reject' back to 'advisory'.
          FOOTPRINT_ENFORCE: "${{ vars.QWEN_AUTOFIX_FOOTPRINT_ENFORCE || 'advisory' }}"
          # Growth-audit rounds must carry a valid growth-audit.json verdict;
          # the gate enforces presence + shape before any push decision.
          KISS_AUDIT: '${{ steps.prepare.outputs.kiss_audit }}'
          # The heartbeat kill targets travel through step env,
          # the STATUS_ID shape: an expression interpolation
          # inside the run body substitutes the value BEFORE the
          # shell parses it, so a forged step output (post_status'
          # shell is pollutable) would arrive as shell syntax and
          # execute in this shell; step env is set by the runner
          # as data (R16-1).
          HB_PID: '${{ steps.post_status.outputs.heartbeat_pid }}'
          HB_START_TICKS: '${{ steps.post_status.outputs.heartbeat_start_ticks }}'
        run: |-
          # The round heartbeat ends HERE: this is the first step that runs
          # branch code ON THE HOST (the agent phase sandboxes it in
          # docker), and the loop holds the bot PAT in its env — the
          # overlap is bounded to the sandboxed agent phase, never the
          # host-side gate. Kill from the pid the launch recorded in
          # expression context; WORKDIR files are sandbox-writable and are
          # never read as kill targets. The session kill covers a kill
          # landing MID-TICK: each tick's `timeout 60 gh` subtree sits in
          # its OWN process group (coreutils timeout default), which the
          # group+pid kills miss — but the loop owns its session via
          # setsid, so -s <pid> targets exactly this round's tree.
          # ABSOLUTE-PATH-ONLY command words here, NOT builtin: this block
          # runs in the gate's OUTER shell, which inherits every
          # $GITHUB_ENV plant — a BASH_FUNC_builtin%% import shadows the
          # `builtin` keyword itself (the R6-4 doctrine the gate body
          # below documents). `builtin kill` is sound only inside the
          # env -i clean child (finalize); here kill is /usr/bin/kill,
          # the same procps already relied on for pkill.
          # Full rationale → qwen-autofix.md#af-149
          /usr/bin/touch "${WORKDIR}/heartbeat-stop" 2> /dev/null || true
          # Lifecycle-confirmed shutdown: this kill can land the whole
          # agent phase after the launch recorded HB_PID, and a pid
          # that old can already be REUSED — TERMing it would kill an
          # unrelated process, its group and session. Signal only
          # after /proc/<pid>/stat still shows the start time this
          # round's launch captured: a reused pid carries a different
          # start time, a dead pid has no stat — a failed check means
          # the loop is gone and killing nothing is right. The parse
          # is syntax-only plus /usr/bin/cat, per this shell's
          # shadowing doctrine. Full rationale → qwen-autofix.md#af-149
          HB_FIELDS=()
          if [[ "${HB_PID:-}" =~ ^[0-9]+$ ]]; then
            HB_STAT="$(/usr/bin/cat "/proc/${HB_PID}/stat" 2>/dev/null)" || HB_STAT=''
            HB_REST="${HB_STAT##*) }"
            HB_FIELDS=(${HB_REST})
          fi
          if [[ "${#HB_FIELDS[@]}" -gt 19 && -n "${HB_START_TICKS}" && "${HB_FIELDS[19]}" == "${HB_START_TICKS}" ]]; then
            /usr/bin/kill -- -"${HB_PID}" 2> /dev/null || true
            /usr/bin/kill "${HB_PID}" 2> /dev/null || true
            /usr/bin/pkill -TERM -s "${HB_PID}" 2> /dev/null || true
          fi
          # The gate decides whether the PAT push runs, and the first pass
          # executes the branch's own build/test on the host before the
          # second — so verify the staged runner's digest (recorded in
          # GITHUB_OUTPUT, unreachable from a disk write) before executing,
          # or a mid-run overwrite lets the branch define its own verdict.
          # Every command word below is called by absolute path: bare names
          # — even builtins like echo, export, or unset — are shadowed by
          # $GITHUB_ENV-planted BASH_FUNC_<name>%% functions, imported at
          # bash STARTUP even under --norc, ahead of builtins and PATH
          # (R6-4; a shadowed echo prints any digest line, blinding the
          # check to a mid-run overwrite of the staged runner, and a
          # shadowed export/unset arms a DEBUG trap that swaps the staged
          # runner AFTER the digest passes and BEFORE the launch executes
          # it, R8-1). The body therefore carries no in-shell pin of its
          # own: PATH reaches the child through the allowlist below, and
          # the preload channels are closed by the step-level LD_* pins
          # above, the env execve prefix, and env -i.
          /usr/bin/echo "${VERIFY_RUNNER_SHA256}  ${RUNNER_TEMP}/run-autofix-review-verification.sh" | /usr/bin/sha256sum -c - > /dev/null
          # Launch the gate through the workflow's env -i clean-child
          # pattern: the step environment inherits every $GITHUB_ENV plant
          # earlier steps left (verdict-variable plants, BITE_RUNNER
          # overrides, whatever is next), and enumeration is the failure
          # mode this design keeps hitting — an allowlisted child drops the
          # whole class. The gate re-declares the variables it needs.
          LD_PRELOAD= LD_AUDIT= LD_LIBRARY_PATH= \
            /usr/bin/env -i \
            PATH="${TRUSTED_PATH}" \
            HOME="${HOME}" \
            RUNNER_TEMP="${RUNNER_TEMP}" \
            WORKDIR="${WORKDIR}" \
            BRANCH="${BRANCH}" \
            GITHUB_OUTPUT="${GITHUB_OUTPUT}" \
            CI="${CI:-true}" \
            KISS_AUDIT="${KISS_AUDIT:-false}" \
            FOOTPRINT_ENFORCE="${FOOTPRINT_ENFORCE:-advisory}" \
            bash --norc "${RUNNER_TEMP}/run-autofix-review-verification.sh"

      - name: 'Repair deterministic rejection'
        id: 'repair'
        # The outcome check fails closed: a FAILED resolver leaves the image
        # binding below empty, and an empty QWEN_SANDBOX_IMAGE relaunches
        # the CLI without any sandbox (#9527 review).
        if: |-
          ${{ always() && steps.verify.outputs.retryable == 'true' && steps.sandbox_image.outcome == 'success' }}
        # 70m: the 60-minute budget below plus the same 10-minute margin the
        # primary attempt keeps under its own backstop, so the internal kill
        # path still writes `agent-timeout` before the step cap fires.
        timeout-minutes: 70
        env:
          QWEN_SANDBOX_IMAGE: '${{ steps.sandbox_image.outputs.image }}'
          DOCKER_HOST: ''
          DOCKER_CONTEXT: 'default'
          PR: '${{ env.PR }}'
          ISSUE: '${{ env.ISSUE }}'
          OPENAI_API_KEY: '${{ secrets.AUTOFIX_OPENAI_API_KEY }}'
          OPENAI_BASE_URL: '${{ secrets.AUTOFIX_OPENAI_BASE_URL || secrets.OPENAI_BASE_URL }}'
          OPENAI_MODEL: '${{ vars.QWEN_AUTOFIX_MODEL || vars.QWEN_PR_REVIEW_MODEL }}'
          NO_PROXY: '127.0.0.1,localhost,::1'
          QWEN_HOME: '${{ runner.temp }}/qwen-autofix-review-home'
          # 60m. The repair attempt starts from a deterministic rejection —
          # an opaque check failure, not the structured review feedback the
          # primary attempt is handed — so it must re-derive which change
          # caused it before it can amend anything. At the original 18m it
          # ran out mid-diagnosis on every observed rejection while the
          # primary attempt (120m) had already succeeded, and the round's
          # work was discarded. At 45m (2026-08-23..26) it still timed out
          # on 48 of 87 repair passes while the 28 that landed took p50 31m
          # / p90 41m — the successes were brushing the cap, not the
          # failures alone. Still a fraction of the primary budget: a
          # repair that cannot land in 60m is a handoff, not a longer retry.
          QWEN_TIMEOUT_MS: '3600000'
          CONFLICT: '${{ steps.prepare.outputs.conflict }}'
          BASE: 'main'
          SETTINGS_JSON: |-
            {
              "maxSessionTurns": 400,
              "coreTools": [
                "read_file",
                "read_many_files",
                "glob",
                "search_file_content",
                "write_file",
                "run_shell_command(cat)",
                "run_shell_command(git add)",
                "run_shell_command(git checkout)",
                "run_shell_command(git commit)",
                "run_shell_command(git diff)",
                "run_shell_command(git log)",
                "run_shell_command(git merge)",
                "run_shell_command(git status)",
                "run_shell_command(ls)",
                "run_shell_command(mkdir)",
                "run_shell_command(npm run build)",
                "run_shell_command(npm run typecheck)",
                "run_shell_command(npm run lint)",
                "run_shell_command(npx vitest)",
                "run_shell_command(npm run generate:settings-schema)",
                "run_shell_command(pwd)"
              ],
              "tools": {
                "sandbox": "docker"
              }
            }
        run: |-
          echo "attempted=true" >> "${GITHUB_OUTPUT}"
          if [[ -z "${OPENAI_API_KEY:-}" ]]; then
            echo '::error::AUTOFIX_OPENAI_API_KEY secret is required for Qwen Autofix.'
            exit 1
          fi
          if [[ ! -s "${WORKDIR}/gate-rejection.md" ]]; then
            echo '::error::Retryable verification rejection has no gate-rejection.md.'
            exit 1
          fi
          {
            cat "${WORKDIR}/feedback.md"
            if [[ -s "${WORKDIR}/address-summary.md" ]]; then
              echo
              echo '## Previous attempt summary'
              cat "${WORKDIR}/address-summary.md"
            fi
            echo
            echo '## Same-run verification repair'
            echo
            echo 'The previous commit was rejected by deterministic verification.'
            echo
            cat "${WORKDIR}/gate-rejection.md"
          } > "${WORKDIR}/feedback.retry.md"
          mv "${WORKDIR}/feedback.retry.md" "${WORKDIR}/feedback.md"
          rm -f \
            "${WORKDIR}/address-summary.md" \
            "${WORKDIR}/no-action.md" \
            "${WORKDIR}/failure.md" \
            "${WORKDIR}/failure.zh.md" \
            "${WORKDIR}/handoff.md" \
            "${WORKDIR}/gate-output.log" \
            "${WORKDIR}/gate-output.log.check" \
            "${WORKDIR}/gate-output.log.baseline" \
            "${WORKDIR}/gate-rejection.md" \
            "${WORKDIR}/agent-api-error" \
            "${WORKDIR}/agent-api-error-kind" \
            "${WORKDIR}/agent-timeout" \
            "${WORKDIR}/resolved-comments.txt" \
            "${WORKDIR}/comment-replies.json"
          # NOT deleted with its siblings: run 2 writes its own
          # deferred-findings.json, and a deferral dropped here is gone for
          # good (the eval watermark filters this round's feedback out of
          # every later round). Carry run 1's into a sidecar the upsert
          # unions in; merge if a previous repair already left one.
          if [[ -s "${WORKDIR}/deferred-findings.json" ]]; then
            if [[ -s "${WORKDIR}/deferred-findings.carry.json" ]]; then
              # This round FIRST: unique_by keeps first-of-group in original
              # order, so a finding re-emitted with fresher text wins — the
              # same precedence the upsert script documents for its own union.
              if jq -s 'add' "${WORKDIR}/deferred-findings.json" \
                "${WORKDIR}/deferred-findings.carry.json" \
                > "${WORKDIR}/deferred-findings.carry.next" 2> /dev/null; then
                mv "${WORKDIR}/deferred-findings.carry.next" \
                  "${WORKDIR}/deferred-findings.carry.json"
                # Merged: this round's copy lives on inside the sidecar.
                rm -f "${WORKDIR}/deferred-findings.json"
              else
                # Which side is corrupt is NOT known here — jq -s fails if
                # EITHER input is unparseable, and today's topology cannot
                # even produce a pre-existing carry (WORKDIR is wiped at run
                # start and there is exactly one repair step), so this branch
                # is defensive.
                # Full rationale → qwen-autofix.md#af-127
                _dfsize="$(wc -c < "${WORKDIR}/deferred-findings.json" 2> /dev/null | tr -d ' ')"
                if [[ -n "${_dfsize}" ]] && (( _dfsize > 4000 )); then
                  echo "::warning::could not merge carried deferrals across the repair (one of the two sets is unparseable); keeping the carried set and preserving this round's as deferred-findings.unmerged.json. Raw content follows, TRUNCATED at 4000 of ${_dfsize} bytes — the full file rides this run's artifact dump:"
                else
                  echo "::warning::could not merge carried deferrals across the repair (one of the two sets is unparseable); keeping the carried set and preserving this round's as deferred-findings.unmerged.json. Raw content follows:"
                fi
                # Both command syntaxes, like every other untrusted echo
                # (`##[` parses mid-line too — #9761).
                head -c 4000 "${WORKDIR}/deferred-findings.json" | sed -e 's/::/;;/g' -e 's/##\[/##［/g'
                echo
                rm -f "${WORKDIR}/deferred-findings.carry.next"
                # Keep the discarded set ON DISK so the warning's pointer at
                # the artifact dump is true past the 4000-byte clip. Renamed,
                # not left in place: the upsert would otherwise union it back
                # in as if it had merged.
                mv "${WORKDIR}/deferred-findings.json" \
                  "${WORKDIR}/deferred-findings.unmerged.json"
              fi
            else
              mv "${WORKDIR}/deferred-findings.json" \
                "${WORKDIR}/deferred-findings.carry.json"
            fi
          fi
          rm -rf "${QWEN_HOME}"
          mkdir -p .qwen "${QWEN_HOME}"
          printf '%s\n' "${SETTINGS_JSON}" > .qwen/settings.json
          git config core.hooksPath .husky
          node "${RUNNER_TEMP}/autofix-skill/scripts/run-agent.mjs" \
            --mode address-review \
            --pr "${PR}" \
            --issue "${ISSUE}" \
            --workdir "${WORKDIR}" \
            --conflict "${CONFLICT}" \
            --base "${BASE}"

      - name: 'Repair verification gate'
        id: 'verify_repair'
        if: |-
          ${{ always() && steps.repair.outputs.attempted == 'true' }}
        continue-on-error: true
        # Same bound as the first pass, for the same reason.
        timeout-minutes: 60
        env:
          # BASH_ENV is sourced by bash at process STARTUP, before line 1 of
          # the body below — a body-side unset is one hop late. Pinning it
          # empty at step level outranks any $GITHUB_ENV plant (same
          # doctrine as FOOTPRINT_ENFORCE below); SHELLOPTS is the sibling
          # option-import channel. The gate then launches through an env -i
          # clean child, so its bash inherits nothing at all.
          BASH_ENV: ''
          SHELLOPTS: ''
          # LD_* are likewise mapped by ld.so at process STARTUP, before the
          # body's unset can run: the unset clears them for children but
          # cannot unload a library already mapped into THIS step's bash,
          # whose execve hooks would forge the pre-launch digest check
          # below; ld.so ignores empty values (R6-2).
          LD_PRELOAD: ''
          LD_AUDIT: ''
          LD_LIBRARY_PATH: ''
          TRUSTED_PATH: '${{ steps.stage.outputs.trusted_path }}'
          VERIFY_RUNNER_SHA256: '${{ steps.stage.outputs.verify_runner_sha256 }}'
          # RUNNER_TEMP, WORKDIR, and BRANCH re-enter the digest check and
          # the allowlisted child below from the step environment: a
          # $GITHUB_ENV plant points the digest oracle at a decoy runner
          # (the hash is expression-context, the checked path is not) and
          # swaps the tree the gate builds/tests. Pin them from trusted
          # expression context, the TRUSTED_PATH doctrine above (R6-3).
          RUNNER_TEMP: '${{ runner.temp }}'
          WORKDIR: '/tmp/autofix-review-${{ matrix.target.pr }}'
          BRANCH: '${{ matrix.target.branch }}'
          # CI re-enters the allowlisted child through the step
          # environment: the child's `:-true` default only covers an UNSET
          # CI, so a $GITHUB_ENV plant of CI=false survives the expansion
          # and inverts the gate's CI semantics. Pin it at step level, the
          # FOOTPRINT_ENFORCE doctrine below (R1-1).
          CI: 'true'
          # HOME re-enters the allowlisted child below from the step
          # environment: a $GITHUB_ENV-planted HOME points npm's userconfig
          # at a .npmrc whose script-shell wraps every verdict-determining
          # `npm run` in an attacker shell — a red branch reports green.
          # Pin it from the stage-time capture, the TRUSTED_PATH doctrine
          # above (R8-3).
          HOME: '${{ steps.stage.outputs.trusted_home }}'
          # Step-level env outranks $GITHUB_ENV: an earlier shell-capable
          # step (the agent runs branch code on the host) must not be able
          # to downgrade a repo-variable 'reject' back to 'advisory'.
          FOOTPRINT_ENFORCE: "${{ vars.QWEN_AUTOFIX_FOOTPRINT_ENFORCE || 'advisory' }}"
          # The control bit arrives through the FIRST gate's defended
          # output (recorded before any check ran, re-appended at every
          # exit); the prepare copy is only the fallback for a first pass
          # that died before recording it.
          KISS_AUDIT: '${{ steps.verify.outputs.kiss_audit || steps.prepare.outputs.kiss_audit }}'
        run: |-
          # The gate decides whether the PAT push runs, and the first pass
          # executes the branch's own build/test on the host before the
          # second — so verify the staged runner's digest (recorded in
          # GITHUB_OUTPUT, unreachable from a disk write) before executing,
          # or a mid-run overwrite lets the branch define its own verdict.
          # Every command word below is called by absolute path: bare names
          # — even builtins like echo, export, or unset — are shadowed by
          # $GITHUB_ENV-planted BASH_FUNC_<name>%% functions, imported at
          # bash STARTUP even under --norc, ahead of builtins and PATH
          # (R6-4; a shadowed echo prints any digest line, blinding the
          # check to a mid-run overwrite of the staged runner, and a
          # shadowed export/unset arms a DEBUG trap that swaps the staged
          # runner AFTER the digest passes and BEFORE the launch executes
          # it, R8-1). The body therefore carries no in-shell pin of its
          # own: PATH reaches the child through the allowlist below, and
          # the preload channels are closed by the step-level LD_* pins
          # above, the env execve prefix, and env -i.
          /usr/bin/echo "${VERIFY_RUNNER_SHA256}  ${RUNNER_TEMP}/run-autofix-review-verification.sh" | /usr/bin/sha256sum -c - > /dev/null
          # Launch the gate through the workflow's env -i clean-child
          # pattern: the step environment inherits every $GITHUB_ENV plant
          # earlier steps left (verdict-variable plants, BITE_RUNNER
          # overrides, whatever is next), and enumeration is the failure
          # mode this design keeps hitting — an allowlisted child drops the
          # whole class. The gate re-declares the variables it needs.
          LD_PRELOAD= LD_AUDIT= LD_LIBRARY_PATH= \
            /usr/bin/env -i \
            PATH="${TRUSTED_PATH}" \
            HOME="${HOME}" \
            RUNNER_TEMP="${RUNNER_TEMP}" \
            WORKDIR="${WORKDIR}" \
            BRANCH="${BRANCH}" \
            GITHUB_OUTPUT="${GITHUB_OUTPUT}" \
            CI="${CI:-true}" \
            KISS_AUDIT="${KISS_AUDIT:-false}" \
            FOOTPRINT_ENFORCE="${FOOTPRINT_ENFORCE:-advisory}" \
            bash --norc "${RUNNER_TEMP}/run-autofix-review-verification.sh"

      - name: 'Finalize verification'
        id: 'final_verify'
        if: |-
          ${{ always() && steps.prepare.outputs.stale != 'true' }}
        env:
          FIRST_OUTCOME: '${{ steps.verify.outputs.outcome }}'
          FIRST_COMMITTED: '${{ steps.verify.outputs.committed }}'
          FIRST_VERIFIED_HEAD: '${{ steps.verify.outputs.verified_head }}'
          REPAIR_ATTEMPTED: '${{ steps.repair.outputs.attempted }}'
          REPAIR_OUTCOME: '${{ steps.verify_repair.outputs.outcome }}'
          REPAIR_COMMITTED: '${{ steps.verify_repair.outputs.committed }}'
          REPAIR_VERIFIED_HEAD: '${{ steps.verify_repair.outputs.verified_head }}'
          FIRST_PREEXISTING: '${{ steps.verify.outputs.preexisting }}'
          REPAIR_PREEXISTING: '${{ steps.verify_repair.outputs.preexisting }}'
          FIRST_AUDIT_VERDICT: '${{ steps.verify.outputs.audit_verdict }}'
          REPAIR_AUDIT_VERDICT: '${{ steps.verify_repair.outputs.audit_verdict }}'
          # The pass conclusions are the tamper-evident seal on the output
          # claims below: a gate that reached fixed/noop EXITED 0, and a
          # step killed mid-check (whose output file stays discoverable and
          # appendable under $RUNNER_TEMP) never concludes success.
          FIRST_CONCLUSION: '${{ steps.verify.conclusion }}'
          REPAIR_CONCLUSION: '${{ steps.verify_repair.conclusion }}'
          FIRST_KISS_AUDIT: '${{ steps.verify.outputs.kiss_audit || steps.prepare.outputs.kiss_audit }}'
          REPAIR_KISS_AUDIT: '${{ steps.verify_repair.outputs.kiss_audit }}'
        run: |-
          OUTCOME="${FIRST_OUTCOME}"
          COMMITTED="${FIRST_COMMITTED}"
          VERIFIED_HEAD="${FIRST_VERIFIED_HEAD}"
          AUDIT_VERDICT="${FIRST_AUDIT_VERDICT}"
          KISS_AUDIT="${FIRST_KISS_AUDIT}"
          PASS_CONCLUSION="${FIRST_CONCLUSION}"
          # The flag travels WITH the attempt whose outcome is selected: the
          # first pass can fail a round-caused check, the repair fixes it, and
          # the repair verification can then hit a pre-existing failure — that
          # final classification is the one the report must render. Forwarded
          # so the failure report can say "base update needed" instead of the
          # generic gate-rejection clause.
          PREEXISTING="${FIRST_PREEXISTING}"
          if [[ "${REPAIR_ATTEMPTED}" == 'true' ]]; then
            PREEXISTING="${REPAIR_PREEXISTING}"
          fi
          if [[ "${PREEXISTING}" == 'true' ]]; then
            echo "preexisting=true" >> "${GITHUB_OUTPUT}"
          fi
          if [[ "${REPAIR_ATTEMPTED}" == 'true' ]]; then
            OUTCOME="${REPAIR_OUTCOME}"
            COMMITTED="${REPAIR_COMMITTED:-${FIRST_COMMITTED}}"
            VERIFIED_HEAD="${REPAIR_VERIFIED_HEAD}"
            # The verdict travels WITH the attempt whose outcome is
            # selected: a repair pass legitimately re-audits (its feedback
            # rebuild keeps the audit section; the SKILL mandates
            # audit-first), and the verdict its gate validated is the one
            # the round's code was judged by — binding the first pass
            # unconditionally dropped it.
            # Full rationale → qwen-autofix.md#af-128
            AUDIT_VERDICT="${REPAIR_AUDIT_VERDICT:-${FIRST_AUDIT_VERDICT}}"
            KISS_AUDIT="${REPAIR_KISS_AUDIT:-${FIRST_KISS_AUDIT}}"
            PASS_CONCLUSION="${REPAIR_CONCLUSION}"
          fi
          # Conclusion gate: fixed/noop are the ONLY outcomes that release
          # the PAT push.
          # Full rationale → qwen-autofix.md#af-129
          if [[ "${OUTCOME}" == 'fixed' || "${OUTCOME}" == 'noop' ]] &&
            [[ "${PASS_CONCLUSION}" != 'success' ]]; then
            echo "::error::verify pass claims outcome=${OUTCOME} but concluded '${PASS_CONCLUSION:-<none>}' — discarding the claim (forged or crashed-gate outputs); NOT pushing"
            OUTCOME=''
            COMMITTED=''
            VERIFIED_HEAD=''
            AUDIT_VERDICT=''
            KISS_AUDIT=''
          fi
          echo "outcome=${OUTCOME}" >> "${GITHUB_OUTPUT}"
          if [[ -n "${COMMITTED}" ]]; then
            echo "committed=${COMMITTED}" >> "${GITHUB_OUTPUT}"
          fi
          if [[ -n "${VERIFIED_HEAD}" ]]; then
            echo "verified_head=${VERIFIED_HEAD}" >> "${GITHUB_OUTPUT}"
          fi
          if [[ -n "${AUDIT_VERDICT}" ]]; then
            echo "audit_verdict=${AUDIT_VERDICT}" >> "${GITHUB_OUTPUT}"
          fi
          if [[ -n "${KISS_AUDIT}" ]]; then
            echo "kiss_audit=${KISS_AUDIT}" >> "${GITHUB_OUTPUT}"
          fi
          case "${OUTCOME}" in
            # handoff and the two brake-violation rejections are
            # deliberate, PUBLISHED verdicts, not failures: the agent
            # stopped under instruction (the growth-brake BLOCKED stop)
            # and the 'Report dry-run / failure' step posts the honest
            # headline, the handoff note, and the eval marker for all
            # three.
            # Full rationale → qwen-autofix.md#af-130
            fixed|noop|handoff|dirty_handoff|committed_handoff) ;;
            *) exit 1 ;;
          esac

      - name: 'Show run artifacts'
        if: |-
          ${{ always() }}
        run: |-
          if git rev-parse --verify "${BRANCH}" > /dev/null 2>&1; then
            git diff "origin/main...${BRANCH}" > "${WORKDIR}/pr.diff" || true
          fi
          for f in feedback.md address-summary.md no-action.md failure.md failure.zh.md handoff.md gate-rejection.md gate-advisories.md growth-audit.json agent-api-error agent-api-error-kind agent-timeout resolved-comments.txt comment-replies.json deferred-findings.json deferred-findings.carry.json deferred-findings.unmerged.json pr.diff heartbeat.log; do
            if [[ -f "${WORKDIR}/${f}" ]]; then
              echo "=============== ${f} ==============="
              # Agent/reviewer-written content: both workflow-command syntaxes
              # parse here — a line-start `::` (::error::, ::add-mask::) AND
              # `##[` even mid-line (a quoted `##[add-matcher]` fails THIS
              # step; measured on #9761 via the prepare echo of this same
              # file). Neutralize both, like every other echo of these files.
              sed -e 's/::/;;/g' -e 's/##\[/##［/g' "${WORKDIR}/${f}"
              echo
            fi
          done

      - name: 'Upload run artifacts'
        if: |-
          ${{ always() }}
        uses: 'actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a' # v7.0.1
        with:
          name: 'autofix-review-pr-${{ matrix.target.pr }}'
          path: '${{ env.WORKDIR }}/'
          if-no-files-found: 'ignore'

      - name: 'Push and report'
        id: 'push_report'
        if: |-
          ${{ always() && needs.route.outputs.dry_run != 'true' && (steps.final_verify.outputs.outcome == 'fixed' || steps.final_verify.outputs.outcome == 'noop') }}
        env:
          # CI_DEV_BOT_PAT (the qwen-code-dev-bot PAT) pushes the branch and
          # posts the report as qwen-code-dev-bot, the same identity that opened
          # the PR. The default GITHUB_TOKEN cannot do either on a bot-owned PR
          # in a way that re-triggers CI.
          GITHUB_TOKEN: '${{ secrets.CI_DEV_BOT_PAT }}'
          OUTCOME: '${{ steps.final_verify.outputs.outcome }}'
          CONFLICT: '${{ steps.prepare.outputs.conflict }}'
          NEWEST: '${{ steps.prepare.outputs.newest }}'
          EFFECTIVE_ROUND: '${{ steps.prepare.outputs.effective_round }}'
          # The seed the window opened at (prepare's clamped read; 0 when
          # unseeded): the milestone crossing trigger counts rounds
          # accumulated in the window, not seed-inflated absolute ones.
          ROUND_START: '${{ steps.prepare.outputs.round_start }}'
          # Surfaced in the report footer for diagnosis + attribution; a repo
          # variable (not a secret), already the agent's OPENAI_MODEL.
          MODEL: '${{ vars.QWEN_AUTOFIX_MODEL || vars.QWEN_PR_REVIEW_MODEL }}'
          CHECKED_OUT_HEAD: '${{ steps.prepare.outputs.checked_out_head }}'
          VERIFIED_HEAD: '${{ steps.final_verify.outputs.verified_head }}'
          RESANITIZE_SHA256: '${{ steps.stage.outputs.resanitize_sha256 }}'
          UPSERT_SRC: '${{ steps.stage.outputs.upsert_src }}'
          TRUSTED_PATH: '${{ steps.stage.outputs.trusted_path }}'
          # Growth-brake baseline: written into this window's FIRST report
          # comment only (growth_base_new), so later rounds' first-wins parse
          # keeps the anchor. Empty when prepare exited early — no marker.
          GROWTH_BASE_NEW: '${{ steps.prepare.outputs.growth_base_new }}'
          GROWTH_BASE_SRC: '${{ steps.prepare.outputs.growth_base_src }}'
          GROWTH_BASE_TEST: '${{ steps.prepare.outputs.growth_base_test }}'
          # The window key prepare READ the baseline under (LIVE_REARM_KEY),
          # not the matrix WINDOW: conflict rounds are supersede-exempt and
          # can report under a stale WINDOW after a re-arm; the marker must
          # land under the key later reads will use.
          GROWTH_BASE_WIN: '${{ steps.prepare.outputs.growth_base_win }}'
          # This round's own growth + over-budget flag, written as the
          # per-round autofix-growth-now marker so later rounds can measure
          # the growth trajectory (the audit's context reads the prior
          # over-budget count).
          GROWTH_SRC: '${{ steps.prepare.outputs.growth_src }}'
          GROWTH_TEST: '${{ steps.prepare.outputs.growth_test }}'
          CRITICAL_ONLY_GROWTH: '${{ steps.prepare.outputs.critical_only_growth }}'
          # Whether this round was a growth-audit round; when it was, the
          # report carries the audit's verdict marker (and a sound verdict
          # additionally re-arms the window at the current size). The bit
          # rides Finalize verification's copy — the gates' defended output
          # chain, never steps.prepare's raw copy.
          KISS_AUDIT: '${{ steps.final_verify.outputs.kiss_audit }}'
          # The verdict Finalize verification selected WITH the outcome —
          # never a re-read of the branch-writable growth-audit.json. The
          # pass whose outcome was selected wins (a repair pass legitimately
          # re-audits, and its gate-validated verdict is the one the round's
          # code was judged by); a repair that validated nothing falls back
          # to the first pass's validated verdict. Empty when no gate
          # validated a verdict; the marker then stays absent.
          AUDIT_VERDICT: '${{ steps.final_verify.outputs.audit_verdict }}'
          MEASURED_AT: '${{ steps.prepare.outputs.measured_at }}'
          PUSH_REPORT_SRC: '${{ steps.stage.outputs.push_report_src }}'
        run: |-
          # The body travels as CONTENT, not as a file on disk: the stage step
          # captured it from the trusted-base checkout before any branch code
          # ran, and it arrives here through step output — expression context,
          # which a disk write on this shared host cannot reach. That is the
          # same delivery the inline block this replaced had, and the same one
          # upsert-deferred-issue.sh uses. With no agent-writable copy there is
          # nothing to stage, digest, type-check or re-open, and no check→use
          # window for a co-resident watcher to race.
          if [[ -z "${PUSH_REPORT_SRC}" ]]; then
            echo '::error::the stage step captured no push-and-report body — refusing to push or report.'
            exit 1
          fi
          bash --norc -c "${PUSH_REPORT_SRC}"
          # Written last and only on success. 'Finalize autofix status comment'
          # reads it to tell "this round published its report" from "the step
          # no-oped": a loader plant that kills this shell at execve exits 0
          # with nothing written, and the finalize step must not then claim a
          # report that never posted. The step shell runs under -eo pipefail,
          # so a failing body never reaches this line.
          echo 'round_reported=true' >> "${GITHUB_OUTPUT}"

      - name: 'Report dry-run / failure'
        if: |-
          ${{ always() && (needs.route.outputs.dry_run == 'true' || failure() || cancelled() || steps.final_verify.outputs.outcome == 'handoff' || steps.final_verify.outputs.outcome == 'dirty_handoff' || steps.final_verify.outputs.outcome == 'committed_handoff') }}
        env:
          OUTCOME: '${{ steps.final_verify.outputs.outcome }}'
          COMMITTED: '${{ steps.final_verify.outputs.committed }}'
          PREEXISTING: '${{ steps.final_verify.outputs.preexisting }}'
          CONFLICT: '${{ steps.prepare.outputs.conflict }}'
          DRY_RUN: '${{ needs.route.outputs.dry_run }}'
          GITHUB_TOKEN: '${{ secrets.CI_DEV_BOT_PAT }}'
          NEWEST: '${{ steps.prepare.outputs.newest }}'
          JOB_STATUS: '${{ job.status }}'
          # 'skipped' when an EARLIER step failed before Prepare ran (base
          # install/build) — an infra/base failure, not the agent.
          # 'success'/'failure' when Prepare itself ran. Distinguishes a
          # transient pre-agent crash (retry) from a genuine agent crash.
          PREPARE_OUTCOME: '${{ steps.prepare.outcome }}'
          STALE: '${{ steps.prepare.outputs.stale }}'
          EFFECTIVE_ROUND: '${{ steps.prepare.outputs.effective_round }}'
          MODEL: '${{ vars.QWEN_AUTOFIX_MODEL || vars.QWEN_PR_REVIEW_MODEL }}'
          CHECKED_OUT_HEAD: '${{ steps.prepare.outputs.checked_out_head }}'
          # This step also posts a round report (timeout / gate-rejection /
          # abort), so it writes the per-round growth-now marker too — else an
          # over-budget round that never reaches 'Push and report' leaves a
          # history gap and the census under-reports.
          # Full rationale → qwen-autofix.md#af-137
          GROWTH_SRC: '${{ steps.prepare.outputs.growth_src }}'
          GROWTH_TEST: '${{ steps.prepare.outputs.growth_test }}'
          CRITICAL_ONLY_GROWTH: '${{ steps.prepare.outputs.critical_only_growth }}'
          MEASURED_AT: '${{ steps.prepare.outputs.measured_at }}'
          GROWTH_BASE_WIN: '${{ steps.prepare.outputs.growth_base_win }}'
          # Same selection chain as AUDIT_VERDICT below — the gates'
          # defended output, never steps.prepare's raw copy.
          KISS_AUDIT: '${{ steps.final_verify.outputs.kiss_audit }}'
          # The verdict Finalize verification selected WITH the outcome —
          # see 'Push and report' for the selection rule. Never a re-read of
          # the branch-writable file.
          AUDIT_VERDICT: '${{ steps.final_verify.outputs.audit_verdict }}'
          UPSERT_SRC: '${{ steps.stage.outputs.upsert_src }}'
          TRUSTED_PATH: '${{ steps.stage.outputs.trusted_path }}'
        run: |-
          # NOTE: the deferred-findings upsert below runs its PAT identity
          # check and the script itself in a sound /usr/bin/env -i child (see
          # Full rationale → qwen-autofix.md#af-060
          REPORT_HEAD="${CHECKED_OUT_HEAD}"
          ROUND="${EFFECTIVE_ROUND:-${ROUND}}"
          MODEL_DISPLAY="${MODEL:-default}"
          # Same helper as 'Push and report' (each step is its own shell, so
          # the definition does not carry over). The verdict is the one the
          # verification GATE validated (AUDIT_VERDICT step output), never a
          # re-read of the branch-writable file. Failure rounds record the
          # verdict for a complete trail but never re-arm the window.
          emit_growth_audit_marker() {
            local allow_rearm="${1:-false}"
            [[ "${KISS_AUDIT}" == 'true' ]] || return 0
            case "${AUDIT_VERDICT:-}" in
              sound | drift | conflict) ;;
              *) return 0 ;;
            esac
            echo "<!-- autofix-growth-audit verdict=${AUDIT_VERDICT} win=${GROWTH_BASE_WIN:-${WINDOW:-none}} -->"
            if [[ "${AUDIT_VERDICT}" == 'sound' && "${allow_rearm}" == 'true' ]]; then
              echo "<!-- autofix-rearm -->"
            fi
          }
          SUFFIX=''
          [[ "${DRY_RUN}" == "true" ]] && SUFFIX=' (dry-run, nothing pushed)'
          {
            ISSUE_REF=""
            [[ "${ISSUE}" != "${PR}" ]] && ISSUE_REF=" (issue #${ISSUE})"
            echo "### PR #${PR}${ISSUE_REF} — outcome=${OUTCOME:-unknown}${SUFFIX}"
            echo "- Base conflict: ${CONFLICT:-unknown}"
            echo
            for f in address-summary.md no-action.md failure.md failure.zh.md handoff.md; do
              if [[ -s "${WORKDIR}/${f}" ]]; then
                echo "**${f}:**"
                cat "${WORKDIR}/${f}"
                echo
              fi
            done
          } >> "${GITHUB_STEP_SUMMARY}"

          # Leave a visible handoff + eval marker when the address did NOT publish a
          # result — a verify failure, or an agent/infra crash or timeout before the
          # Full rationale → qwen-autofix.md#af-061
          POST_HANDOFF=false
          if [[ "${DRY_RUN}" != "true" && "${STALE:-}" != "true" && -n "${GITHUB_TOKEN:-}" && "${OUTCOME:-unknown}" != "fixed" && "${OUTCOME:-unknown}" != "noop" ]]; then
            # handoff rounds end with a SUCCESS job status (a deliberate
            # verdict), so they must trigger on the outcome itself — without
            # this clause nothing would post and the loop would go silent on
            # exactly the rounds that most need a visible human handoff.
            # Full rationale → qwen-autofix.md#af-138
            if [[ "${OUTCOME:-unknown}" == "failed" || "${OUTCOME:-unknown}" == "handoff" || "${OUTCOME:-unknown}" == "dirty_handoff" || "${OUTCOME:-unknown}" == "committed_handoff" || "${JOB_STATUS:-}" != "success" ]]; then
              POST_HANDOFF=true
            fi
          fi
          if [[ "${POST_HANDOFF}" == "true" ]]; then
            api_error_file="$(mktemp)"
            if ! bot_actor="$(GH_TOKEN="${GITHUB_TOKEN}" gh api user --jq '.login' 2>"${api_error_file}")"; then
              api_error="$(tr '\r\n' ' ' < "${api_error_file}")"
              rm -f "${api_error_file}"
              echo "::error::Failed to verify CI_DEV_BOT_PAT identity with gh api user: ${api_error:-unknown error}."
              exit 1
            fi
            rm -f "${api_error_file}"
            echo "CI_DEV_BOT_PAT authenticates as ${bot_actor}"
            if [[ "${bot_actor}" != "${AUTOFIX_BOT}" ]]; then
              echo "::error::CI_DEV_BOT_PAT authenticates as ${bot_actor}; expected ${AUTOFIX_BOT}."
              exit 1
            fi
            # Attach the most actionable agent output. failure.md first (its
            # diagnosis; run-agent.mjs wraps it in a generic handoff.md, so prefer
            # failure.md). Then the agent's SUCCESS outputs: on the OUTCOME=failed
            # path where the agent committed a fix but a post-agent verify gate then
            # failed (most notably the schema-freshness gate), only
            # address-summary.md/no-action.md exist and "Push and report" is
            # skipped, so this handoff is their only route to the PR — otherwise the
            # comment would wrongly say "crashed or timed out". Generic notice only
            # if none exist.
            DETAIL_FILE=''
            for f in failure.md handoff.md address-summary.md no-action.md; do
              if [[ -s "${WORKDIR}/${f}" ]]; then DETAIL_FILE="${WORKDIR}/${f}"; break; fi
            done
            # run-agent.mjs drops this marker when qwen died on a model-side
            # [API Error: 4xx/5xx] (access denied, quota, a 5xx). The agent never
            # evaluated the feedback, so this is treated as a retry (sentinel ts,
            # no watermark advance) below — a model access/quota blip must not
            # strand the PR the way a real evaluated handoff would.
            API_ERROR_DETAIL=''
            API_ERROR_KIND=''
            if [[ -s "${WORKDIR}/agent-api-error" ]]; then
              # First line only, markup neutralized (agent stdout can echo
              # external PR-comment text and the marker regex spans '<!-- ... -->'
              # happily), and capped so a long span can't bloat the headline.
              # Full rationale → qwen-autofix.md#af-062
              API_ERROR_DETAIL="$(head -n 1 "${WORKDIR}/agent-api-error" | sed -e 's/<!--/<!\\-\\-/g' -e 's/<[dD][eE][tT][aA][iI][lL][sS]/＜details/g' -e 's/<\/[dD][eE][tT][aA][iI][lL][sS]/＜\/details/g' -e 's/<[sS][uU][mM][mM][aA][rR][yY]/＜summary/g' | cut -c1-200 | iconv -f utf-8 -t utf-8 -c || true)"
            fi
            if [[ -s "${WORKDIR}/agent-api-error-kind" ]]; then
              API_ERROR_KIND="$(head -n 1 "${WORKDIR}/agent-api-error-kind" | tr -cd 'a-z')"
            fi
            # A timeout means the agent evaluated NOTHING before its budget ran
            # out (run-agent.mjs writes this signal). Routed to retry, not an
            # evaluated advance — same as a pre-verdict crash. Bounded by the
            # round cap and the consecutive-failure cap, so a PR that keeps
            # timing out still stops; a one-off (usually followed by a good
            # round) recovers on the next scan instead of stranding its feedback.
            AGENT_TIMEOUT=''
            if [[ -s "${WORKDIR}/agent-timeout" ]]; then
              AGENT_TIMEOUT="$(head -n 1 "${WORKDIR}/agent-timeout" | cut -c1-120 | iconv -f utf-8 -t utf-8 -c || true)"
            fi
            # If feedback was actually read (prepare ran), stamp its newest ts so
            # the watermark advances and the same feedback is not re-selected next
            # Full rationale → qwen-autofix.md#af-063
            GATE_CRASHED=false
            if [[ -z "${OUTCOME}" && "${JOB_STATUS:-}" != 'success' ]]; then
              GATE_CRASHED=true
            fi
            MARK_TS="${NEWEST:-${WATERMARK:-9999-12-31T23:59:59Z}}"
            if [[ -n "${NEWEST:-}" ]]; then
              MARK_ROUND="$(( ROUND + 1 ))"
              if [[ -z "${DETAIL_FILE}" || -n "${API_ERROR_DETAIL}" || -n "${AGENT_TIMEOUT}" || "${GATE_CRASHED}" == 'true' ]]; then
                # Prepare ran (NEWEST is set) but no verdict was reached. Ways
                # that happens, and in ALL of them the agent evaluated NOTHING:
                # Full rationale → qwen-autofix.md#af-064
                MARK_TS='9999-12-31T23:59:59Z'
                # Cause-aware wording, most specific first — a model error and a
                # gate crash each name the operator fix, while a bare no-output
                # crash points at a human.
                # Full rationale → qwen-autofix.md#af-139
                if [[ -n "${API_ERROR_DETAIL}" ]]; then
                  CAUSE="could not reach the model — ${API_ERROR_DETAIL}"
                  CAUSE_ZH="无法连接模型 —— ${API_ERROR_DETAIL}"
                  LAST_FIX="a maintainer should check the autofix model key/access, then re-arm"
                  LAST_FIX_ZH="维护者应检查 autofix 模型密钥/访问权限，然后重新武装"
                elif [[ -n "${AGENT_TIMEOUT}" ]]; then
                  # A timeout evaluated nothing, so the feedback is unaddressed
                  # and stays live for the retry. On a big / heavily-reviewed PR
                  # this is usually a one-off; the last automatic attempt names
                  # the real fix (split the PR or raise the budget). An IDLE
                  # timeout is a different failure class: the sandbox went
                  # silent — more budget cannot cure it, so the terminal advice
                  # points at the sandbox, not the budget knobs.
                  CAUSE="ran out of time before finishing (${AGENT_TIMEOUT})"
                  CAUSE_ZH="在完成前耗尽了时间（${AGENT_TIMEOUT}）"
                  if [[ "${AGENT_TIMEOUT}" == 'idle-timeout'* ]]; then
                    LAST_FIX="a maintainer should check the sandbox image and runner docker daemon, then re-arm — raising the time budget cannot cure a silent sandbox"
                    LAST_FIX_ZH="维护者应检查 sandbox 镜像与 runner 的 docker daemon，然后重新武装 —— 提高时间预算治不了静默的 sandbox"
                  else
                    LAST_FIX="a human should split the PR or raise the agent time budget and its step backstop, then re-arm"
                    LAST_FIX_ZH="应由人工拆分 PR，或同时提高 agent 时间预算与其步骤兜底，然后重新武装"
                  fi
                elif [[ -z "${DETAIL_FILE}" ]]; then
                  CAUSE="crashed before it could evaluate the feedback"
                  CAUSE_ZH="在评估反馈之前崩溃"
                  LAST_FIX="a human should take over this PR"
                  LAST_FIX_ZH="应由人工接管此 PR"
                else
                  # A gate crash points the maintainer at the gate logs: the
                  # agent's commit is discarded with the runner, but the
                  # feedback watermark is preserved, so the retry re-attempts
                  # the same feedback rather than skipping it.
                  CAUSE="hit a verification-gate error before reaching a verdict"
                  CAUSE_ZH="在得出结论之前遇到验证门错误"
                  LAST_FIX="a maintainer should check the gate logs, then re-arm"
                  LAST_FIX_ZH="维护者应查看验证门日志，然后重新武装"
                fi
                # Budget retries by CAUSE. A transient 429/5xx self-heals, so
                # it keeps the full round budget. An auth/access error that
                # only a maintainer can fix never self-heals, and every attempt
                # costs an agent run AND a PR comment - on a takeover PR that
                # is ~100 comments over ~17h before the useful message lands.
                CAUSE_MAX="${MAX_ROUNDS}"
                if [[ "${API_ERROR_KIND}" == 'auth' && "${API_AUTH_MAX_ROUNDS}" -lt "${MAX_ROUNDS}" ]]; then
                  CAUSE_MAX="${API_AUTH_MAX_ROUNDS}"
                fi
                # Only promise a retry when one will actually happen: at the
                # cap the next scan's round gate skips the PR, and the
                # cap-reached notice is takeover-only, so the final attempt
                # must say so itself. Clamp the displayed numerator:
                # MARK_ROUND counts ALL rounds in the window, so earlier real
                # attempts can push it past CAUSE_MAX — "attempt 4/3" reads
                # as a bug.
                DISPLAY_ROUND=$(( MARK_ROUND < CAUSE_MAX ? MARK_ROUND : CAUSE_MAX ))
                if [[ "${MARK_ROUND}" -lt "${CAUSE_MAX}" ]]; then
                  HEADLINE="🤖 AutoFix ${CAUSE} (attempt ${DISPLAY_ROUND}/${CAUSE_MAX}) — it will retry on the next scan."
                  HEADLINE_ZH="🤖 AutoFix ${CAUSE_ZH}（第 ${DISPLAY_ROUND}/${CAUSE_MAX} 次尝试）—— 将在下次扫描时重试。"
                else
                  HEADLINE="🤖 AutoFix ${CAUSE} (attempt ${DISPLAY_ROUND}/${CAUSE_MAX}) — this was the last automatic attempt; ${LAST_FIX}."
                  HEADLINE_ZH="🤖 AutoFix ${CAUSE_ZH}（第 ${DISPLAY_ROUND}/${CAUSE_MAX} 次尝试）—— 这是最后一次自动尝试；${LAST_FIX_ZH}。"
                  # Stamp the terminal round so the scan's max-round gate skips
                  # this PR instead of re-handing-off every tick; the sentinel
                  # ts keeps the feedback live for a re-arm once the key is
                  # fixed. (No-op when CAUSE_MAX is already MAX_ROUNDS.)
                  MARK_ROUND="${MAX_ROUNDS}"
                fi
              else
                if [[ "${OUTCOME:-}" == 'handoff' ]]; then
                  # A deliberate stop, not a failed fix: the agent stopped
                  # under instruction and deferred the item to a human.
                  # Full rationale → qwen-autofix.md#af-140
                  HEADLINE="🤖 AutoFix deferred this item to a human under instruction (round ${MARK_ROUND}/${MAX_ROUNDS}) — the agent's handoff note below names the decision and the options. The loop stays engaged and still picks up new feedback and base conflicts, but will not retry this item on its own."
                  HEADLINE_ZH="🤖 AutoFix 已按指示将此项移交人工处理（第 ${MARK_ROUND}/${MAX_ROUNDS} 轮）—— 下方 agent 的 handoff 说明列出了待决决策与各选项。循环保持在线，仍会拾取新反馈与 base 冲突，但不会自行重试此项。"
                elif [[ "${OUTCOME:-}" == 'dirty_handoff' ]]; then
                  # A brake VIOLATION, not a failed fix: the agent stopped
                  # under instruction but left a dirty workspace, so the
                  # gate rejected the round under its own outcome (never
                  # retryable — a repair pass would commit against the
                  # brake).
                  # Full rationale → qwen-autofix.md#af-141
                  HEADLINE="🤖 AutoFix rejected this round — the agent wrote a handoff but left a dirty workspace, violating the brake's commit-nothing stop (round ${MARK_ROUND}/${MAX_ROUNDS}). Nothing was committed; the uncommitted work was discarded with the runner. The handoff note below names what the agent stopped on, and the run log lists the dirty files. The loop stays engaged and still picks up new feedback and base conflicts, but will not retry this item on its own."
                  HEADLINE_ZH="🤖 AutoFix 拒绝了本轮 —— agent 写了 handoff 却留下了脏工作区，违反 brake 的禁止提交停止（第 ${MARK_ROUND}/${MAX_ROUNDS} 轮）。未提交任何内容；未提交的改动已随 runner 丢弃。下方 handoff 说明指出了 agent 停止时的状态，运行日志列出了脏文件。循环保持在线，仍会拾取新反馈与 base 冲突，但不会自行重试此项。"
                elif [[ "${OUTCOME:-}" == 'committed_handoff' ]]; then
                  # The committed sibling of the dirty-handoff violation:
                  # the round HAS a commit beside handoff.md.
                  # Full rationale → qwen-autofix.md#af-142
                  HEADLINE="🤖 AutoFix rejected this round — the agent wrote a handoff but the round HAS a commit, violating the brake's commit-nothing stop (round ${MARK_ROUND}/${MAX_ROUNDS}). The commit was NOT pushed and is discarded with the runner; the run log lists it. The handoff note below names what the agent stopped on. The loop stays engaged and still picks up new feedback and base conflicts, but will not retry this item on its own."
                  HEADLINE_ZH="🤖 AutoFix 拒绝了本轮 —— agent 写了 handoff 但本轮存在提交，违反 brake 的禁止提交停止（第 ${MARK_ROUND}/${MAX_ROUNDS} 轮）。该提交未推送，已随 runner 丢弃；运行日志中有记录。下方 handoff 说明指出了 agent 停止时的状态。循环保持在线，仍会拾取新反馈与 base 冲突，但不会自行重试此项。"
                else
                # The gate ran and rejected the agent's fix (a build/test
                # failure). Before handing to a human, check whether the PR is
                # Full rationale → qwen-autofix.md#af-065
                STALE_BASE_RETRY=false
                # A conflict round must PARK quietly at the human call: its
                # own stale-base merge would re-fire every synchronize-
                # triggered workflow on the new head, and those loop-
                # generated checks complete after the conflict marker this
                # same report posts — waking the very park it establishes.
                # Full rationale → qwen-autofix.md#af-143
                STALE_BASE_DEFERRED=false
                if [[ "${AUDIT_VERDICT:-}" != 'conflict' ]]; then
                  # Convergence-break report guard (#10122): this job cannot
                  # see the scan's CONV_PARKED, and this round's inputs froze
                  # at prepare time — a PR parked AFTER prepare (the scan's
                  # next tick; the same race af-153's mirror covers for
                  # dispatch) would merge main in here when the round fails,
                  # self-lifting the park (af-152's exact hazard). Re-derive
                  # the breaker's reading over fresh fetches — the programs
                  # are the scan gate's verbatim; keep in lockstep — and skip
                  # the merge while it holds.
                  # Full rationale → qwen-autofix.md#af-154
                  CONV_PARKED_R='false'
                  # Each fetch sits in an errexit-exempt condition: under the
                  # step shell's -eo pipefail a transient gh failure (5xx,
                  # secondary rate limit, network reset) would otherwise abort
                  # the whole step before the report comment and eval marker
                  # post, so the watermark never advances and the same feedback
                  # re-dispatches. A failed fetch also leaves the derivation
                  # below reading an empty park state, so the merge gate fails
                  # CLOSED on it — skipping update-branch keeps the park
                  # invariant; the step still posts the report.
                  CONV_FETCH_R='true'
                  if ! gh api "repos/${REPO}/pulls/${PR}/reviews" --paginate \
                    | jq -s 'add // []' > "${WORKDIR}/rv.report.json"; then
                    CONV_FETCH_R='false'
                  fi
                  if ! gh api "repos/${REPO}/pulls/${PR}/comments" --paginate \
                    | jq -s 'add // []' > "${WORKDIR}/rc.report.json"; then
                    CONV_FETCH_R='false'
                  fi
                  if ! gh api "repos/${REPO}/issues/${PR}/comments" --paginate \
                    | jq -s 'add // []' > "${WORKDIR}/ic.report.json"; then
                    CONV_FETCH_R='false'
                  fi
                  CONV_REARM_KEY_R="$(jq -r --arg ab "${AUTOFIX_BOT}" '
                    [ .[] | select((.user.login // "") == $ab)
                      | select(((.body // "") | contains("<!-- takeover-ack engaged -->"))
                            or ((.body // "") | contains("<!-- autofix-rearm -->")))
                      | .created_at ] | max // "none"' "${WORKDIR}/ic.report.json" || echo none)"
                  [[ "${CONVERGENCE_BREAK_ROUNDS}" =~ ^[1-9][0-9]{0,2}$ ]] || CONVERGENCE_BREAK_ROUNDS=3
                  CONV_SINCE_R="${CONV_REARM_KEY_R}"
                  [[ "${CONV_SINCE_R}" == 'none' ]] && CONV_SINCE_R=''
                  CONV_HUMAN_AT_R="$(jq -rs \
                    --arg rb "${REVIEW_BOT}" --arg ab "${AUTOFIX_BOT}" \
                    --argjson trust "${TRUSTED_ASSOC}" '
                    (.[0] | map(select((.user.login // "") != $ab and (.user.login // "") != $rb)
                      | select(((.author_association // "") | IN($trust[])))
                      | select((.state // "") | IN("CHANGES_REQUESTED", "COMMENTED", "APPROVED", "DISMISSED")) | (.submitted_at // "")))
                    + (.[1] | map(select((.user.login // "") != $ab and (.user.login // "") != $rb)
                      | select(((.author_association // "") | IN($trust[]))) | (.created_at // "")))
                    + (.[2] | map(select((.user.login // "") != $ab and (.user.login // "") != $rb)
                      | select(((.author_association // "") | IN($trust[])))
                      | select((.body // "") | test("<!-- (autofix-eval|autofix-rearm|qwen-triage|qwen-review-suggestion-summary|pr-force-push|qwen-review-ack) ") | not)
                      | select((.body // "") | test("^\\s*@qwen-code /") | not) | (.created_at // "")))
                    | max // ""' "${WORKDIR}/rv.report.json" "${WORKDIR}/rc.report.json" "${WORKDIR}/ic.report.json" 2> /dev/null || echo "")"
                  [[ "${CONV_HUMAN_AT_R}" > "${CONV_SINCE_R}" ]] && CONV_SINCE_R="${CONV_HUMAN_AT_R}"
                  CONV_STREAK_R="$(jq -r --arg rb "${REVIEW_BOT}" --arg since "${CONV_SINCE_R}" \
                    --argjson codes "${CONVERGENCE_SIGNAL_CODES}" '
                    [ .[] | select((.user.login // "") == $rb)
                      | select((.submitted_at // "") > $since)
                      | {at: (.submitted_at // ""),
                         marker: ((.body // "") | [ scan("<!-- qwen-review-ledger (.*?) -->") ] | last | .[0] // "")}
                      | select(.marker != "")
                      | .led = (.marker | try fromjson catch null)
                      | select(.led | type == "object")
                      | {at, round: (.led.round // 0),
                         rec: (.led.rec | if type == "array" then . else [] end)} ]
                    | sort_by(.at) | group_by(.round) | map(last) | sort_by(.at)
                    | reduce .[] as $r (0; if ($r.rec | any(. as $c | $codes | index($c))) then . + 1 else 0 end)' \
                    "${WORKDIR}/rv.report.json" 2> /dev/null || echo 0)"
                  [[ "${CONV_STREAK_R}" =~ ^[0-9]+$ ]] || CONV_STREAK_R=0
                  if [[ "${CONV_STREAK_R}" -ge "${CONVERGENCE_BREAK_ROUNDS}" ]]; then
                    CONV_PARKED_R='true'
                  else
                    # The scan gate's park HOLD, kept in lockstep: a posted
                    # notice keeps the park until the boundary itself moves.
                    CONV_NOTICES_R="$(jq -r --arg ab "${AUTOFIX_BOT}" --arg since "${CONV_SINCE_R}" '
                      [ .[] | select((.user.login // "") == $ab)
                        | select((.body // "") | contains("<!-- autofix-convergence-break -->"))
                        | select((.created_at // "") > $since) ] | length' "${WORKDIR}/ic.report.json" 2> /dev/null || echo 0)"
                    [[ "${CONV_NOTICES_R}" =~ ^[0-9]+$ ]] || CONV_NOTICES_R=0
                    [[ "${CONV_NOTICES_R}" != "0" ]] && CONV_PARKED_R='true'
                  fi
                  if [[ "${CONV_PARKED_R}" == 'true' ]]; then
                    echo "🫥 #${PR}: convergence-parked — the stale-base merge would self-lift the park; skipping update-branch"
                  elif [[ "${CONV_FETCH_R}" != 'true' ]]; then
                    echo "🫥 #${PR}: convergence guard fetch failed — the park state is unreadable; skipping the stale-base merge (fail closed)"
                  else
                    MAIN_HEAD_R="$(gh api "repos/${REPO}/commits/${DEFAULT_BRANCH:-main}" --jq '.sha' 2> /dev/null || echo '')"
                    if [[ -n "${MAIN_HEAD_R}" && -n "${REPORT_HEAD}" ]]; then
                      CMP_R="$(gh api "repos/${REPO}/compare/${MAIN_HEAD_R}...${REPORT_HEAD}" --jq '.status' 2> /dev/null || echo '')"
                      if [[ "${CMP_R}" == 'behind' || "${CMP_R}" == 'diverged' ]]; then
                        # Review-in-flight hold for the loop's OWN report-time
                        # head move (#10110): same probe pair as the scan's
                        # dispatch gate (#8888), fail-open on probe errors.
                        # The probe sees LIFECYCLE runs only: a command run
                        # executes against the base branch, so its review-pr
                        # check never shows under this PR's rollup, and the
                        # runs fallback is event-scoped exactly like the scan
                        # gate's (af-099) — an in-flight command review does
                        # NOT hold the refresh (af-155).
                        # Full rationale → qwen-autofix.md#af-155
                        ROLLUP_R="$(gh pr view "${PR}" --repo "${REPO}" --json statusCheckRollup --jq '.statusCheckRollup // []' 2> /dev/null || echo '[]')"
                        REVIEW_LIVE_R="$(jq -r '
                          [ .[]
                            | select((((.status // .state // "") | IN("QUEUED", "IN_PROGRESS", "PENDING", "WAITING", "REQUESTED")) and ((.name // "") == "review-pr") and ((.workflowName // "") == "🧐 Qwen Pull Request Review"))) ]
                          | length > 0
                        ' <<< "${ROLLUP_R}" 2> /dev/null || echo '')"
                        if [[ "${REVIEW_LIVE_R}" != 'true' ]]; then
                          RWID_R="$(gh api "repos/${REPO}/actions/workflows/qwen-code-pr-review.yml" --jq '.id' 2> /dev/null || echo '')"
                          LIVE_HEAD_R="$(jq -r '.headRefOid // ""' <<< "$(gh pr view "${PR}" --repo "${REPO}" --json headRefOid 2> /dev/null || echo '{}')")"
                          if [[ -n "${RWID_R}" ]]; then
                            REVIEW_LIVE_R="$(gh api "repos/${REPO}/actions/workflows/${RWID_R}/runs?per_page=100" 2> /dev/null | jq -r --arg pr "${PR}" --arg head "${LIVE_HEAD_R}" '
                              [ .workflow_runs[]?
                                | select((.event // "") == "pull_request_target")
                                | select((.status // "") | IN("queued", "waiting", "pending", "requested", "in_progress"))
                                | select((($head != "") and ((.head_sha // "") == $head)) or any(.pull_requests[]?; (.number | tostring) == $pr)) ]
                              | length > 0
                            ' 2> /dev/null || echo '')"
                          fi
                        fi
                        # The deferral promises that the NEXT scan's round
                        # performs the refresh, so it is honest only while a
                        # next round exists: at the cap (MARK_ROUND ==
                        # MAX_ROUNDS) the scan's round gate parks the PR
                        # before any report step could run, so the hold
                        # yields to the refresh there and the human inherits
                        # a PR current with main — the pre-#10110 shape
                        # (R32-1).
                        if [[ "${REVIEW_LIVE_R}" == 'true' && "${MARK_ROUND}" -lt "${MAX_ROUNDS}" ]]; then
                          STALE_BASE_DEFERRED=true
                        elif gh api -X PUT "repos/${REPO}/pulls/${PR}/update-branch" -f expected_head_sha="${REPORT_HEAD}" > /dev/null 2>&1; then
                          STALE_BASE_RETRY=true
                        fi
                      fi
                    fi
                  fi
                fi
                if [[ "${STALE_BASE_RETRY}" == 'true' ]]; then
                  # Feedback stays live (sentinel ts) so the retry re-reads it
                  # against the freshly-merged base; the incremented round still
                  # bounds it, and the consecutive-failure exemption below keeps
                  # this not-the-PR's-fault round from counting toward the cap.
                  MARK_TS='9999-12-31T23:59:59Z'
                  HEADLINE="🤖 AutoFix updated a stale base — the fix did not pass verification, but this PR was behind \`${DEFAULT_BRANCH:-main}\`, so it merged current main in via update-branch and will retry on the next scan. A stale base (a dependency or symbol main already changed) can fail the build without being the fix's fault; if it still fails once current, it hands off to a human."
                  HEADLINE_ZH="🤖 AutoFix 更新了一个过期的 base —— 修复未通过验证，但本 PR 落后于 \`${DEFAULT_BRANCH:-main}\`，因此已通过 update-branch 合入当前 main，并将在下次扫描时重试。过期的 base（main 已改动的依赖或符号）可能让构建失败而并非修复本身的错；若 base 更新后仍然失败，将移交人工处理。"
                elif [[ "${STALE_BASE_DEFERRED}" == 'true' ]]; then
                  # Same sentinel semantics as the retry branch: feedback stays
                  # live, the next round re-runs, and ITS report step performs
                  # the base refresh once the review has landed.
                  MARK_TS='9999-12-31T23:59:59Z'
                  HEADLINE="🤖 AutoFix deferred a stale-base refresh — the fix did not pass verification and this PR is behind \`${DEFAULT_BRANCH:-main}\`, but a lifecycle review of this PR is still in flight, and merging main now would cancel it or invalidate its posting (#10110). Command-triggered reviews are invisible to this probe (af-155). The refresh and this item's retry move to the next scan."
                  HEADLINE_ZH="🤖 AutoFix 暂缓了一次过期 base 刷新 —— 修复未通过验证且本 PR 落后于 \`${DEFAULT_BRANCH:-main}\`，但该 PR 上仍有一轮生命周期评审在运行，现在合入 main 会取消该评审或使其发布失效（#10110）。命令触发的评审对本探测不可见（af-155）。base 刷新与此项的重试推迟到下次扫描。"
                else
                  # Say what actually happens next. The old "A human should
                  # take over this PR" read as a full release, but the loop
                  # Full rationale → qwen-autofix.md#af-066
                  GATE_CLAUSE=''
                  GATE_CLAUSE_ZH=''
                  if [[ -s "${WORKDIR}/gate-rejection.md" ]]; then
                    GATE_CLAUSE=' — the verification gate rejected the attempt'
                    GATE_CLAUSE_ZH=' —— 验证门拒绝了该尝试'
                  fi
                  # Pre-existing failures get the honest clause: the rejection
                  # is not the agent's and the repair was deliberately skipped.
                  # Full rationale → qwen-autofix.md#af-067
                  if [[ "${PREEXISTING}" == 'true' ]]; then
                    if [[ "${CMP_R:-}" == 'behind' || "${CMP_R:-}" == 'diverged' ]]; then
                      GATE_CLAUSE=' — the verification gate hit a PRE-EXISTING failure (present without this round'"'"'s commit); the repair pass may only amend the round'"'"'s own fix, so it cannot reach it — the branch needs a base update (merge main)'
                      GATE_CLAUSE_ZH=' —— 验证门遇到的是一个先前已存在的失败（没有本轮提交时就在）；修复环节只能修正本轮自己的修复，触及不到它 —— 该分支需要更新 base（合并 main）'
                    elif [[ -z "${CMP_R:-}" ]]; then
                      GATE_CLAUSE=' — the verification gate hit a PRE-EXISTING failure (present without this round'"'"'s commit); the repair pass may only amend the round'"'"'s own fix, so it cannot reach it — the base state could not be compared (compare API failed), so check whether merging main resolves it before auditing the branch'"'"'s own pre-round code'
                      GATE_CLAUSE_ZH=' —— 验证门遇到的是一个先前已存在的失败（没有本轮提交时就在）；修复环节只能修正本轮自己的修复，触及不到它 —— base 状态未能比较（compare API 失败），请先确认合并 main 能否解决，再审查该分支自身本轮之前的代码'
                    else
                      GATE_CLAUSE=' — the verification gate hit a PRE-EXISTING failure (present without this round'"'"'s commit); the repair pass may only amend the round'"'"'s own fix, so it cannot reach it — the branch'"'"'s own pre-round code needs attention'
                      GATE_CLAUSE_ZH=' —— 验证门遇到的是一个先前已存在的失败（没有本轮提交时就在）；修复环节只能修正本轮自己的修复，触及不到它 —— 该分支自身本轮之前的代码需要处理'
                    fi
                  fi
                  HEADLINE="🤖 Could not produce a passing fix for this feedback (round ${MARK_ROUND}/${MAX_ROUNDS})${GATE_CLAUSE}. This item now needs a human; the loop stays engaged and still picks up new feedback and base conflicts, but will not retry this item on its own."
                  HEADLINE_ZH="🤖 未能为该反馈产生可通过验证的修复（第 ${MARK_ROUND}/${MAX_ROUNDS} 轮）${GATE_CLAUSE_ZH}。此项现在需要人工处理；循环保持在线，仍会拾取新反馈与 base 冲突，但不会自行重试此项。"
                fi
                fi
              fi
            elif [[ "${PREPARE_OUTCOME}" != 'success' && "${PREPARE_OUTCOME}" != 'failure' ]]; then
              # NEWEST is empty because Prepare never RAN TO A VERDICT — an
              # earlier step failed or the job stopped before the agent started:
              # Full rationale → qwen-autofix.md#af-068
              MARK_ROUND="$(( ROUND + 1 ))"
              MARK_TS='9999-12-31T23:59:59Z'
              if [[ "${MARK_ROUND}" -lt "${MAX_ROUNDS}" ]]; then
                HEADLINE="🤖 AutoFix could not start — a setup step failed (or the run was cancelled) before the agent ran, so no fix was attempted. This is normally a transient infra issue, a broken base build, or a cancelled run — not this PR. It will retry on the next scan."
                HEADLINE_ZH="🤖 AutoFix 未能启动 —— 在 agent 运行之前某个准备步骤失败（或本次运行被取消），因此未尝试修复。这通常是瞬时的基础设施问题、损坏的 base 构建或被取消的运行 —— 与本 PR 无关。将在下次扫描时重试。"
              else
                MARK_ROUND="${MAX_ROUNDS}"
                HEADLINE="🤖 AutoFix could not start — reached the round cap (${MAX_ROUNDS}) because a setup step (base install/build) kept failing before the agent ran. This is infra or a persistently broken base, not this PR. Fix the base build, then comment \`${RETRY_COMMAND}\` to re-arm."
                HEADLINE_ZH="🤖 AutoFix 未能启动 —— 由于准备步骤（base 安装/构建）在 agent 运行之前持续失败，已达到轮次上限（${MAX_ROUNDS}）。这是基础设施问题或持续损坏的 base，与本 PR 无关。请修复 base 构建，然后评论 \`${RETRY_COMMAND}\` 重新武装。"
              fi
            else
              # Prepare RAN (outcome success/failure) but produced no feedback to
              # read — prepare itself crashed or timed out before emitting a
              # verdict.
              # Full rationale → qwen-autofix.md#af-144
              MARK_ROUND="${MAX_ROUNDS}"
              HEADLINE="🤖 AutoFix could not start evaluation — it crashed or timed out before reading the feedback, so no fix was attempted. This PR is now marked terminal and future scans (including forced dispatch) will skip it. To recover: delete this bot's terminal \`autofix-eval\` marker comment, then re-trigger if the failure looked transient."
              HEADLINE_ZH="🤖 AutoFix 未能开始评估 —— 在读取反馈之前崩溃或超时，因此未尝试修复。本 PR 已被标记为终止，后续扫描（包括强制 dispatch）都会跳过它。恢复方法：删除本 bot 的终止性 \`autofix-eval\` 标记评论；若失败看起来是瞬时的，再重新触发。"
            fi

            # Consecutive-failure circuit breaker, distinct from the round cap.
            # Full rationale → qwen-autofix.md#af-069
            if [[ "${MARK_ROUND}" != "${MAX_ROUNDS}" ]] && [[ "${PREPARE_OUTCOME}" == 'success' || "${PREPARE_OUTCOME}" == 'failure' ]] && [[ "${STALE_BASE_RETRY:-false}" != 'true' ]] && [[ "${STALE_BASE_DEFERRED:-false}" != 'true' ]] && { [[ -z "${API_ERROR_DETAIL}" ]] || [[ "${API_ERROR_KIND}" == 'auth' ]]; }; then
              CONSEC_FAIL=1
              if [[ -f "${WORKDIR}/ic.json" ]]; then
                COMMENTS_JSON="$(cat "${WORKDIR}/ic.json")"
              else
                # Truncated stream: jq -s fails and || true yields '' where
                # the old code kept partial JSON — PRIOR_HEADS empties and
                # duplicate-report suppression weakens for one round (a
                # redundant comment at worst, never a cap reset); a total gh
                # failure now yields '[]' instead of the old ''.
                COMMENTS_JSON="$(gh api "repos/${REPO}/issues/${PR}/comments" --paginate 2> /dev/null | jq -s 'add // []' || true)"
              fi
              PRIOR_HEADS="$(jq -r --arg ab "${AUTOFIX_BOT}" --arg win "${WINDOW:-none}" '
                [.[] | select((.user.login // "") == $ab)
                     | select((.body // "") | contains("<!-- autofix-eval "))
                     | select(
                         ([ ((.body // "") | scan("<!-- autofix-eval ts=[^ ]+ acted=[^ ]+ round=[0-9]+(?: win=([^ ]+))? -->")) ] | map(.[0] // "none")) as $wins
                         | ($wins | length) > 0 and (($wins | last) == $win))]
                | sort_by(.created_at) | .[]
                | (.body | gsub("\r"; "") | split("\n")[0])' <<< "${COMMENTS_JSON}" 2> /dev/null || true)"
              while IFS= read -r H; do
                [[ -n "${H}" ]] || continue
                if [[ "${H}" == *"Addressed the latest review feedback"* || "${H}" == *"no changes needed"* || "${H}" == *"AutoFix could not start —"* || "${H}" == *"updated a stale base"* || "${H}" == *"deferred a stale-base refresh"* ]]; then
                  CONSEC_FAIL=1
                else
                  CONSEC_FAIL=$(( CONSEC_FAIL + 1 ))
                fi
              done <<< "${PRIOR_HEADS}"
              if [[ "${CONSEC_FAIL}" -ge "${CONSECUTIVE_FAILURE_CAP}" ]]; then
                MARK_ROUND="${MAX_ROUNDS}"
                HEADLINE="🤖 AutoFix stopped after ${CONSEC_FAIL} consecutive rounds that pushed nothing (failed rounds, timeouts, gate rejections, or stops under instruction). Retrying at the same per-round budget is not converging — this usually means the PR is too large or conflicts with a fast-moving \`main\`. A human should rebase, split, or reduce it, then comment \`${RETRY_COMMAND}\` to re-arm. Until then future scans will skip this PR."
                HEADLINE_ZH="🤖 AutoFix 已停止：连续 ${CONSEC_FAIL} 轮未能推送任何内容（失败轮次、超时、验证门拒绝或按指示停止）。以相同的单轮预算重试并不收敛 —— 这通常意味着 PR 过大，或与快速变动的 \`main\` 冲突。应由人工 rebase、拆分或缩减它，然后评论 \`${RETRY_COMMAND}\` 重新武装。在此之前，后续扫描将跳过本 PR。"
              fi
              # CUMULATIVE timeout breaker — the sibling of the consecutive
              # one above, for the failure shape it cannot see: timeouts
              # interleaved with pushed rounds.
              # Full rationale → qwen-autofix.md#af-145
              # Idle (silent-sandbox) timeouts are EXCLUDED from this cap:
              # a wedged runner/docker is not this PR being too big, an idle
              # round dies at QWEN_IDLE_TIMEOUT_MS having produced no byte
              # (a fraction of a real round), and no budget increase cures
              # one — so this breaker's remedy does not apply to them. A
              # persistently wedged sandbox stays bounded by
              # CONSECUTIVE_FAILURE_CAP, which an idle round DOES feed.
              # Full rationale → qwen-autofix.md#af-073
              IDLE_N="$(grep -c 'AutoFix ran out of time before finishing (idle-timeout' <<< "${PRIOR_HEADS}" || true)"
              if [[ "${AGENT_TIMEOUT:-}" == 'idle-timeout'* ]]; then
                IDLE_N=$(( IDLE_N + 1 ))
              fi
              # Excluding idle from the cap must not hide it. The job log is
              # the right surface: it reaches the operator without spending a
              # PR comment on infra noise.
              if [[ "${IDLE_N}" -gt 0 ]]; then
                echo "::warning::#${PR}: ${IDLE_N} silent-sandbox (idle) timeout(s) this counting window — excluded from the ${TIMEOUT_WINDOW_CAP}-timeout cap; check the sandbox image and the runner docker daemon"
              fi
              if [[ "${MARK_ROUND}" != "${MAX_ROUNDS}" ]]; then
                # Needle matches the emitted headline verbatim — first lines
                # can embed provider error text (API_ERROR_DETAIL puts up to
                # 200 bytes of it on the same line), so a loose phrase could
                # count a model error message as a timeout.
                TIMEOUT_N="$(grep -c 'AutoFix ran out of time before finishing' <<< "${PRIOR_HEADS}" || true)"
                if [[ -n "${AGENT_TIMEOUT:-}" ]]; then
                  TIMEOUT_N=$(( TIMEOUT_N + 1 ))
                fi
                # IDLE_N's needle is TIMEOUT_N's needle extended with the
                # idle cause's opening token — every line matching it also
                # matches TIMEOUT_N's, so IDLE_N can never exceed TIMEOUT_N
                # and the subtraction below can never go negative.
                BUDGET_TIMEOUT_N=$(( TIMEOUT_N - IDLE_N ))
                if [[ "${BUDGET_TIMEOUT_N}" -ge "${TIMEOUT_WINDOW_CAP}" ]]; then
                  MARK_ROUND="${MAX_ROUNDS}"
                  # The headline states what the census MEASURED — the
                  # window's cumulative count — not "stopped after N
                  # timeouts": the round that trips this can itself have
                  # failed differently (a gate rejection landing on a window
                  # that already carries the cap — the exact rollout state
                  # of #7929/#7846).
                  # No all-idle branch here: the guard above only fires when
                  # BUDGET_TIMEOUT_N alone reaches the cap, so a tripped
                  # window always holds at least TIMEOUT_WINDOW_CAP genuine
                  # budget timeouts — idle rounds can outnumber budget ones
                  # in it, but the budget remedy is always the right one.
                  IDLE_CLAUSE=''
                  IDLE_CLAUSE_ZH=''
                  if [[ "${IDLE_N}" -gt 0 ]]; then
                    IDLE_CLAUSE=" The window also holds ${IDLE_N} silent-sandbox (idle) timeout(s), which no budget increase can cure and which do NOT count toward this cap — investigate the sandbox image and runner docker daemon separately."
                    IDLE_CLAUSE_ZH="本窗口另有 ${IDLE_N} 次静默 sandbox（idle）超时，提高预算也治不了，且不计入本上限 —— 请另行排查 sandbox 镜像与 runner 的 docker daemon。"
                  fi
                  HEADLINE="🤖 AutoFix stopped: this counting window now contains ${BUDGET_TIMEOUT_N} agent time-budget exhaustions (pushed rounds in between included; this round itself may have failed differently). That is ${BUDGET_TIMEOUT_N} full agent runs that pushed nothing.${IDLE_CLAUSE} A human should split or reduce the PR (or raise the agent time budget AND its step backstop together), then comment \`${RETRY_COMMAND}\` to re-arm. Until then future scans will skip this PR."
                  HEADLINE_ZH="🤖 AutoFix 已停止：当前计数窗口内已累计 ${BUDGET_TIMEOUT_N} 次时间预算耗尽（含其间推送过的轮次；本轮本身可能以别的方式失败）。即 ${BUDGET_TIMEOUT_N} 次完整 agent 运行没有推送任何内容。${IDLE_CLAUSE_ZH}应由人工拆分或缩减该 PR（或同时提高 agent 时间预算与其步骤兜底），然后评论 \`${RETRY_COMMAND}\` 重新武装。在此之前，后续扫描将跳过本 PR。"
                fi
              fi
            fi
            {
              echo "${HEADLINE}"
              echo
              if [[ -n "${DETAIL_FILE}" ]]; then
                if [[ "${COMMITTED}" == "true" ]]; then
                  # The agent committed (verify recorded committed=true before
                  # any gate could fail), but every path that reaches this
                  # handoff skipped "Push and report" — nothing landed on the
                  # branch.
                  # Full rationale → qwen-autofix.md#af-146
                  echo "⚠️ This change was NOT pushed — any commit referenced below was made only in the runner workspace and has been discarded. What the agent reported:"
                else
                  echo "**What I found before stopping:**"
                fi
                # -c drops any partial multi-byte sequence a byte-level head -c may
                # have split, so the comment body stays valid UTF-8. iconv -c still
                # Full rationale → qwen-autofix.md#af-070
                head -c 1500 "${DETAIL_FILE}" | iconv -f utf-8 -t utf-8 -c | sed -e 's/<!--/<!\\-\\-/g' -e 's/<[dD][eE][tT][aA][iI][lL][sS]/＜details/g' -e 's/<\/[dD][eE][tT][aA][iI][lL][sS]/＜\/details/g' -e 's/<[sS][uU][mM][mM][aA][rR][yY]/＜summary/g' || true
              else
                echo "AutoFix failed before producing a verified commit (the run crashed or timed out before it could explain why)."
              fi
              # Carry the gate's own rejection reason. Without it the comment
              # shows only the agent's optimistic summary, so neither a human
              # nor the NEXT round can see why the change was refused - the
              # agent then re-reads the original review feedback and repeats
              # the same mistake. Delimited so `Prepare branch and feedback`
              # can lift it back out on the retry.
              if [[ -s "${WORKDIR}/gate-rejection.md" ]]; then
                echo
                echo '<!-- autofix-gate-rejection-start -->'
                echo "**Why it was not pushed:**"
                echo
                # The rejection text below was written BEFORE the stale-base
                # auto-update: on that path its framing (pre-existing,
                # unreachable, cured by a base update) is already stale — the
                # base HAS been updated, and the next round re-measures and
                # may charge the round. Say so, or the retry agent is steered
                # toward no-action on the one round designed to re-measure.
                if [[ "${STALE_BASE_RETRY:-false}" == 'true' ]]; then
                  echo "_Note: the base has since been auto-updated; the verdict below predates that update, and the next round's re-measurement may charge the round._"
                  echo
                fi
                # reject_fix sizes its own document: the evidence tail is
                # dynamic (budget 3300 minus the preamble, floored at 500),
                # so the finished file tops out ≈3.3 KB. 3900 is headroom,
                # not arithmetic — retune it only together with the
                # script's tail_budget formula, or the closing fence gets
                # silently truncated again.
                head -c 3900 "${WORKDIR}/gate-rejection.md" | iconv -f utf-8 -t utf-8 -c | sed 's/<!--/<!\\-\\-/g' || true
                echo '<!-- autofix-gate-rejection-end -->'
              fi
              # Bilingual companion. Repo convention is English first, Chinese
              # in a collapsed <details>. failure.md itself stays English-only
              # Full rationale → qwen-autofix.md#af-071
              echo
              echo '<details>'
              echo '<summary>中文说明</summary>'
              echo
              echo "${HEADLINE_ZH}"
              if [[ -s "${WORKDIR}/failure.zh.md" ]]; then
                echo
                if [[ "${COMMITTED}" == "true" ]]; then
                  echo "⚠️ 此改动未被推送 —— 下文引用的任何提交都只存在于 runner 工作区，已被丢弃。以下是 agent 的报告："
                else
                  echo "**停止前我了解到的情况：**"
                fi
                # Same byte-budget hygiene as the English excerpt above. 3000
                # bytes ≈ 1000 CJK characters — roughly the information in the
                # 1500-byte English excerpt.
                # Full rationale → qwen-autofix.md#af-147
                head -c 3000 "${WORKDIR}/failure.zh.md" | iconv -f utf-8 -t utf-8 -c | sed -e 's/<!--/<!\\-\\-/g' -e 's/<[dD][eE][tT][aA][iI][lL][sS]/＜details/g' -e 's/<\/[dD][eE][tT][aA][iI][lL][sS]/＜\/details/g' -e 's/<[sS][uU][mM][mM][aA][rR][yY]/＜summary/g' || true
              fi
              if [[ -s "${WORKDIR}/gate-rejection.md" ]]; then
                echo
                echo "验证门的拒绝原因与日志证据见上方英文部分（gate-rejection 不翻译）。"
              fi
              echo
              echo '</details>'
              echo
              echo
              echo "Run log: ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
              echo
              echo "---"
              echo "🧠 Handled by **Qwen Code** · model/模型 \`${MODEL_DISPLAY}\`"
              echo
              echo "<!-- autofix-eval ts=${MARK_TS} acted=false round=${MARK_ROUND} win=${WINDOW:-none} -->"
              # Per-round growth history the census counts — same marker
              # the push/no-op report paths write, so an over-budget
              # round that timed out or was gate-rejected is not a gap. run=
              # (per-workflow-run) is the DEDUP identity; measured= orders.
              echo "<!-- autofix-growth-now src=${GROWTH_SRC:-0} test=${GROWTH_TEST:-0} over=${CRITICAL_ONLY_GROWTH:-false} round=${MARK_ROUND} run=${GITHUB_RUN_ID}${MEASURED_AT:+ measured=${MEASURED_AT}} key=${GROWTH_BASE_WIN:-${WINDOW:-none}} -->"
              # The verdict still rides the failure report (the trail must be
              # complete), but a round that FAILED does not get to re-arm the
              # window — the failure path re-measures under the same window.
              emit_growth_audit_marker false
              # A sentinel ts means the agent evaluated NOTHING (crash, API
              # error, gate crash) and the next scan must retry. Recording a
              # judged head here would make RED_HEAD == LIVE_HEAD, so the
              # retry scan sees N_RED_NOW=0 and goes idle despite the handoff
              # promising a retry.
              if [[ "${MARK_TS}" != '9999-12-31T23:59:59Z' ]]; then
                echo "<!-- autofix-redcheck head=${REPORT_HEAD} -->"
              fi
            } > "${WORKDIR}/report.md"
            gh pr comment "${PR}" --repo "${REPO}" --body-file "${WORKDIR}/report.md" || echo "::warning::Failed to post handoff comment on PR #${PR}"
          fi
          # A failed round must not lose verified deferred findings: the
          # agent's analysis is independent of whether this round's commit
          # survived verification. Guarded like the handoff MINUS its
          # outcome!=fixed/noop condition — deliberately: when the outcome IS
          # fixed/noop but "Push and report" died before its own upsert (the
          # push loop's exit paths), this block is the only persistence
          # route left.
          if [[ "${DRY_RUN}" != "true" && "${STALE:-}" != "true" && -n "${GITHUB_TOKEN:-}" ]]; then
            if [[ -z "${UPSERT_SRC:-}" ]]; then
              # Stage never ran (pre-stage failure): nothing was deferred.
              echo 'deferred-findings upsert skipped: stage step never ran'
            else
              # Same shape as run_deferred_upsert in 'Push and report' — no
              # agent-writable path, content from expression context, child
              # messages on fd 3 — plus the PAT bot-identity check, because
              # POST_HANDOFF's own check is skipped on the fixed/noop-outcome
              # path that also reaches here.
              UPSERT_OUT="$( { LD_PRELOAD= LD_AUDIT= LD_LIBRARY_PATH= \
              LD_PROFILE= LD_PROFILE_OUTPUT= LD_DEBUG= LD_DEBUG_OUTPUT= \
                /usr/bin/env -i \
                PATH="${TRUSTED_PATH}" \
                GITHUB_TOKEN="${GITHUB_TOKEN}" \
                GH_HOST=github.com \
                RUNNER_TEMP="${RUNNER_TEMP}" \
                WORKDIR="${WORKDIR}" \
                PR="${PR}" \
                REPO="${REPO}" \
                AUTOFIX_BOT="${AUTOFIX_BOT}" \
                UPSERT_SRC="${UPSERT_SRC}" \
                bash --norc -c '
                  set -uo pipefail
                  exec >&3
                  printf "%s\n" "__upsert_child_live__"
                  if ! GH_CONFIG_DIR="$(mktemp -d "${RUNNER_TEMP}/autofix-gh-config.XXXXXX")"; then
                    echo "__upsert_trusted__::warning::could not create a gh config dir; deferred findings NOT persisted this round"
                    exit 0
                  fi
                  export GH_CONFIG_DIR
                  UPSERT_ACTOR="$(GH_TOKEN="${GITHUB_TOKEN}" gh api user --jq .login 2> /dev/null || true)"
                  if [[ "${UPSERT_ACTOR}" != "${AUTOFIX_BOT}" ]]; then
                    echo "__upsert_trusted__::warning::CI_DEV_BOT_PAT identity check failed for the deferred-findings upsert (got ${UPSERT_ACTOR:-none}); NOT persisted this round"
                    exit 0
                  fi
                  bash -c "${UPSERT_SRC}" ||
                    echo "__upsert_trusted__::warning::deferred-findings upsert failed; continuing"
                ' > /dev/null 2>&1 ; } 3>&1 )" || true
              # Builtin-only inspection, same rationale as the twin.
              if [[ "${UPSERT_OUT}" != *'__upsert_child_live__'* ]]; then
                echo "::warning::deferred-findings upsert child never started (loader trace mode or exec failure); NOT persisted this round"
              fi
              while IFS= read -r _upsert_line; do
                if [[ "${_upsert_line}" == '__upsert_child_live__' ]]; then
                  :
                elif [[ "${_upsert_line}" == __upsert_trusted__* ]]; then
                  printf '%s\n' "${_upsert_line#__upsert_trusted__}"
                else
                  # The canonical two-expression neutralizer, identical to
                  # every other echo site — one spelling for the whole
                  # family, so a syntax change cannot drift across two
                  # implementations (`##[` parses mid-line too — #9761).
                  printf '%s\n' "${_upsert_line}" | sed -e 's/::/;;/g' -e 's/##\[/##［/g'
                fi
              done <<< "${UPSERT_OUT}"
            fi
          fi

      # Flip the status comment out of "working" so a finished round never
      # leaves a live-looking line behind. PATCH-only on purpose: a round that
      # never posted a status (stale duplicate, dry run) must not gain one here.
      # Full rationale → qwen-autofix.md#af-072
      - name: 'Finalize autofix status comment'
        if: |-
          ${{ always() && steps.prepare.outputs.stale != 'true' && needs.route.outputs.dry_run != 'true' }}
        env:
          # Startup-channel pins, the gate steps' doctrine: BASH_ENV is
          # sourced by bash at process STARTUP, before the launch below,
          # and SHELLOPTS is the sibling option-import channel — a
          # body-side unset is one hop late, so both are pinned empty at
          # step level, which outranks any $GITHUB_ENV plant. The LD_*
          # family is mapped by ld.so at startup the same way. These pins
          # protect the parent shell that executes the launch; the
          # BASH_FUNC_<name>%% channel no env: block can enumerate — the
          # attacker chooses the name, and bash imports the entries as
          # functions at startup even under --norc, ahead of builtins and
          # PATH — is closed by the env -i clean child below (R9-1), the
          # gate's R6-4 pattern.
          # Full rationale → qwen-autofix.md#af-149
          BASH_ENV: ''
          SHELLOPTS: ''
          LD_PRELOAD: ''
          LD_AUDIT: ''
          LD_LIBRARY_PATH: ''
          TRUSTED_PATH: '${{ steps.stage.outputs.trusted_path }}'
          # HOME re-enters the clean child below: a $GITHUB_ENV-planted
          # HOME repoints gh's config search at an attacker-writable dir
          # (af-112's http_unix_socket exfil). Pin it from the stage-time
          # capture, the TRUSTED_PATH doctrine above (R8-3).
          HOME: '${{ steps.stage.outputs.trusted_home }}'
          GITHUB_TOKEN: '${{ secrets.CI_DEV_BOT_PAT }}'
          EFFECTIVE_ROUND: '${{ steps.prepare.outputs.effective_round }}'
          OUTCOME: '${{ steps.final_verify.outputs.outcome }}'
          RUN_URL: '${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}'
          # The id the announcement wrote. Empty means this round never
          # announced (its step was skipped, or the post itself failed) — then
          # no comment claims this round is working, so there is nothing to
          # flip and no reason to scan for one. A previous round's comment is
          # already terminal, and the next round's announcement re-PATCHes it.
          STATUS_ID: '${{ steps.post_status.outputs.comment_id }}'
          # 'Push and report' writes this only after its sentinel-verified
          # gate child exits 0; empty means the step no-oped (an env plant
          # can kill its shell at execve, silent exit 0) — the success text
          # must not assert a report that never posted.
          PUSH_REPORTED: '${{ steps.push_report.outputs.round_reported }}'
          # The heartbeat kill targets ride step env, the STATUS_ID
          # shape and the gate step's doctrine (R16-1): the clean
          # child below re-declares them from these pins exactly
          # like STATUS_ID, so a forged output arrives as data,
          # never as run-body shell syntax.
          HB_PID: '${{ steps.post_status.outputs.heartbeat_pid }}'
          HB_START_TICKS: '${{ steps.post_status.outputs.heartbeat_start_ticks }}'
        run: |-
          # This step holds the PAT, and its shell inherits every
          # $GITHUB_ENV plant earlier steps left — including
          # BASH_FUNC_<name>%% imports, which bash loads as functions at
          # STARTUP even under --norc, ahead of builtins and PATH, under
          # attacker-chosen names no step-level pin can enumerate
          # (probe-verified: plants shadowing set/export/builtin/gh each
          # hijacked this step's former inline body). Close the class,
          # not the enumeration: the whole PAT-touching body runs in the
          # gate's env -i clean-child form, re-declaring only what it
          # needs from the step-level pins above and expression context.
          # PATH reaches the child through the allowlist, so the body
          # carries no in-shell pin of its own; inside the clean child
          # no import survives, so bare and builtin words resolve
          # soundly again. The kill target travels through expression
          # context — a pid read from a WORKDIR file would be an
          # untrusted kill target (WORKDIR is sandbox-writable).
          # Full rationale → qwen-autofix.md#af-149
          LD_PRELOAD= LD_AUDIT= LD_LIBRARY_PATH= \
            /usr/bin/env -i \
            PATH="${TRUSTED_PATH}" \
            HOME="${HOME}" \
            GITHUB_TOKEN="${GITHUB_TOKEN}" \
            GH_HOST=github.com \
            RUNNER_TEMP="${RUNNER_TEMP}" \
            WORKDIR="${WORKDIR}" \
            HB_PID="${HB_PID}" \
            HB_START_TICKS="${HB_START_TICKS}" \
            STATUS_ID="${STATUS_ID}" \
            PUSH_REPORTED="${PUSH_REPORTED}" \
            OUTCOME="${OUTCOME}" \
            EFFECTIVE_ROUND="${EFFECTIVE_ROUND}" \
            ROUND="${ROUND}" \
            RUN_URL="${RUN_URL}" \
            REPO="${REPO}" \
            PR="${PR}" \
            bash --norc -c '
              set -uo pipefail
              # Stop the round heartbeat BEFORE flipping this comment
              # to its terminal text, and DRAIN it first: killing the
              # client cannot cancel a PATCH the server already
              # ACCEPTED, so the terminal PATCH must wait the last
              # in-flight tick request out (the stamp drain below).
              # The kill is lifecycle-confirmed: the gate killed this
              # loop long before this step, and by now the pid
              # recorded at launch can be REUSED — TERMing it would
              # kill an unrelated process, its group and session. A
              # failed check means the loop is already gone, so
              # killing nothing is right; the stop marker ends it on
              # its next self-check even if the kills miss.
              # Full rationale → qwen-autofix.md#af-149
              /usr/bin/touch "${WORKDIR}/heartbeat-stop" 2> /dev/null || true
              HB_FIELDS=()
              if [[ "${HB_PID:-}" =~ ^[0-9]+$ ]]; then
                HB_STAT="$(cat "/proc/${HB_PID}/stat" 2>/dev/null)" || HB_STAT=''
                HB_REST="${HB_STAT##*) }"
                HB_FIELDS=(${HB_REST})
              fi
              if [[ "${#HB_FIELDS[@]}" -gt 19 && -n "${HB_START_TICKS:-}" && "${HB_FIELDS[19]}" == "${HB_START_TICKS}" ]]; then
                builtin kill -- -"${HB_PID}" 2>/dev/null || true
                builtin kill "${HB_PID}" 2>/dev/null || true
                /usr/bin/pkill -TERM -s "${HB_PID}" 2>/dev/null || true
              fi
              # Drain the last in-flight tick before the terminal
              # PATCH: each tick stamps its start epoch around its gh
              # call (bounded to 60s), so a stamp older than 65s
              # proves its request committed or died, and no stamp
              # means nothing is in flight. The loop is dead by here,
              # so no new stamp can appear. The read is bounded in
              # time AND bytes (a planted FIFO must not stall this
              # step, and a symlink to an endless non-NUL target —
              # /dev/urandom or a fed FIFO — would otherwise stream
              # into the substitution buffer GB-scale and kill this
              # clean child before the terminal PATCH, R17-3); a
              # valid stamp is ≤ ~20 bytes, so 64 covers it. A
              # planted fresh stamp costs at most the 65s bound, a
              # planted deletion reopens only the cosmetic overwrite.
              DRAIN_END=$(( $(date +%s) + 65 ))
              while :; do
                NOW_S="$(date +%s)"
                (( NOW_S >= DRAIN_END )) && break
                STAMP="$(timeout 5 head -c 64 -- "${WORKDIR}/heartbeat-tick-inflight" 2> /dev/null)" || STAMP=''
                [[ "${STAMP}" =~ ^[0-9]+$ ]] || break
                (( STAMP + 65 <= NOW_S )) && break
                sleep 1
              done
              # Hermetic gh config, the upsert twin'"'"'s shape: pinning
              # HOME'"'"'s PATH does not sanitize its CONTENTS, which the
              # gate-phase host-side branch code (same UID) can write
              # before finalize — a planted http_unix_socket would capture
              # the PATCH'"'"'s Authorization header. Fail closed (the
              # post_status R8-1 shape): refuse the finalize PATCH rather
              # than run gh with the PAT against an untrusted config.
              if ! GH_CONFIG_DIR="$(mktemp -d "${RUNNER_TEMP}/autofix-gh-config.XXXXXX")"; then
                echo "::error::autofix finalize: could not create hermetic gh config dir; refusing the finalize PATCH with the PAT"
                exit 1
              fi
              export GH_CONFIG_DIR
              MARKER="<!-- autofix-status -->"
              if [[ -z "${STATUS_ID}" ]]; then
                echo "This round posted no status comment on PR #${PR}; nothing to finalize."
                exit 0
              fi
              ROUND_DISPLAY="${EFFECTIVE_ROUND:-${ROUND}}"
              # ROUND counts rounds already DONE; every other message
              # numbers the round being performed (the report posts
              # ROUND + 1). Match it, or the same round carries two
              # different numbers in one thread.
              if [[ "${ROUND_DISPLAY}" =~ ^[0-9]+$ ]]; then
                ROUND_DISPLAY="$((ROUND_DISPLAY + 1))"
              fi
              # "fixed"/"noop"/"handoff"/"dirty_handoff"/
              # "committed_handoff" are the outcomes that published a
              # round report — handoff and the two brake-violation
              # rejections post their note + eval marker from the report
              # step; anything else means the round stopped before
              # publishing one. fixed/noop additionally condition on the
              # success output of the push step: an env plant can kill
              # that shell at execve (silent exit 0, gate body never
              # ran), and the success text would otherwise assert a
              # report that never posted.
              PUBLISHED=true
              if [[ "${OUTCOME:-}" == "fixed" || "${OUTCOME:-}" == "noop" ]] && [[ "${PUSH_REPORTED:-}" != "true" ]]; then
                PUBLISHED=false
              fi
              if [[ "${PUBLISHED}" == "true" && ( "${OUTCOME:-}" == "fixed" || "${OUTCOME:-}" == "noop" || "${OUTCOME:-}" == "handoff" || "${OUTCOME:-}" == "dirty_handoff" || "${OUTCOME:-}" == "committed_handoff" ) ]]; then
                EN="$(printf "✅ **AutoFix round %s finished** — [view run](%s). See this round'"'"'s report below." "${ROUND_DISPLAY}" "${RUN_URL}")"
                ZH="$(printf "✅ **AutoFix 第 %s 轮已完成** —— [查看运行](%s)。本轮报告见下方。" "${ROUND_DISPLAY}" "${RUN_URL}")"
              else
                EN="$(printf "⚠️ **AutoFix round %s ended without publishing a report** — [view run](%s)." "${ROUND_DISPLAY}" "${RUN_URL}")"
                ZH="$(printf "⚠️ **AutoFix 第 %s 轮结束但未发布报告** —— [查看运行](%s)。" "${ROUND_DISPLAY}" "${RUN_URL}")"
              fi
              BODY="$(printf "%s\n\n%s\n\n<details>\n<summary>中文说明</summary>\n\n%s\n\n</details>" \
                "${MARKER}" "${EN}" "${ZH}")"
              gh api --method PATCH "repos/${REPO}/issues/comments/${STATUS_ID}" \
                -f body="${BODY}" > /dev/null ||
                echo "::warning::Failed to finalize the autofix status comment on PR #${PR}; continuing."
            '

      # Nothing else removes the per-target WORKDIR; PR numbers only
      # increase, so on the persistent pool every addressed PR would leave
      # its transcripts and decision files behind forever. Last step, after
      # every reader including the artifact upload. always() covers
      # cancellation too; a dir abandoned by a hard runner kill is reclaimed
      # by the next same-PR run's reset, or by the age sweep in Reset
      # autofix workspace if that PR is never addressed again.
      - name: 'Clean up autofix workdir'
        if: 'always()'
        env:
          # The heartbeat kill targets ride step env, the STATUS_ID
          # shape and the gate step's doctrine (R16-1): the runner
          # sets them as data, never as run-body shell syntax.
          HB_PID: '${{ steps.post_status.outputs.heartbeat_pid }}'
          HB_START_TICKS: '${{ steps.post_status.outputs.heartbeat_start_ticks }}'
        run: |-
          # Last heartbeat kill (the gate and finalize already did this on
          # their paths) from the expression-context pid — never from a
          # WORKDIR file, which is sandbox-writable. Lifecycle-confirmed
          # like its twins and stalest of all: signal only a pid whose
          # /proc/<pid>/stat start time still matches the launch
          # capture, kill nothing otherwise. The session kill covers
          # the mid-tick subtree, same as its twins.
          # ABSOLUTE-PATH-ONLY command words, the gate twin's form:
          # this block runs in the step's OUTER shell, which inherits
          # every $GITHUB_ENV plant earlier steps left — a
          # BASH_FUNC_cat%% import shadows cat at bash startup and
          # forges the stat line (the matching start time sits in
          # this step's own env), passing the lifecycle check on a
          # REUSED pid so the real kill TERMs an unrelated process,
          # its group and session; a planted kill or PATH-planted
          # pkill no-ops the delivery instead, leaving the loop
          # holding the PAT alive to its age cap (R17-4).
          # Full rationale → qwen-autofix.md#af-149
          HB_FIELDS=()
          if [[ "${HB_PID:-}" =~ ^[0-9]+$ ]]; then
            HB_STAT="$(/usr/bin/cat "/proc/${HB_PID}/stat" 2>/dev/null)" || HB_STAT=''
            HB_REST="${HB_STAT##*) }"
            HB_FIELDS=(${HB_REST})
          fi
          if [[ "${#HB_FIELDS[@]}" -gt 19 && -n "${HB_START_TICKS}" && "${HB_FIELDS[19]}" == "${HB_START_TICKS}" ]]; then
            /usr/bin/kill -- -"${HB_PID}" 2> /dev/null || true
            /usr/bin/kill "${HB_PID}" 2> /dev/null || true
            /usr/bin/pkill -TERM -s "${HB_PID}" 2> /dev/null || true
          fi
          rm -rf "${WORKDIR}"
